---
phase: 09-codec-encoders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stub-data.ts
  - src/stores/files.ts
  - src/stores/settings.ts
  - src/stores/runtime.ts
  - src/tests/codec-encoders.spec.ts
  - src/tests/per-file-settings.spec.ts
autonomous: true
requirements: [ENC-06]
must_haves:
  truths:
    - "Each file owns an independent FileSettings object (codec, q, method, lossless, resize, metadata, plugins)"
    - "Editing one file's settings does not mutate another file's settings"
    - "Apply-to-all pushes the current global defaults onto every file in the batch"
    - "The inspector reads the selected file's own settings, not the global atom"
    - "runtimeAtom job counts update atomically without read-modify-write clobber (CR-01)"
  artifacts:
    - path: "src/lib/stub-data.ts"
      provides: "FileSettings interface + FileEntry extension (settings, rawBuffer, encodedBuffer, error)"
      contains: "interface FileSettings"
    - path: "src/stores/files.ts"
      provides: "setFileSettings, setFileError, setFileResult, setFileRawBuffer, initFileSettings actions"
      exports: ["setFileSettings", "setFileError", "setFileResult", "setFileRawBuffer"]
    - path: "src/stores/settings.ts"
      provides: "applyToAll action (global defaults → all FileEntry.settings)"
      exports: ["applyToAll"]
    - path: "src/tests/codec-encoders.spec.ts"
      provides: "Wave 0 ENC-01..06 + D-13 test scaffold (red until adapters land)"
      contains: "Codec Encoders"
    - path: "src/tests/per-file-settings.spec.ts"
      provides: "Wave 0 D-01/D-02/D-03 per-file settings store tests"
      contains: "per-file"
  key_links:
    - from: "src/stores/settings.ts applyToAll"
      to: "src/stores/files.ts filesAtom"
      via: "lazy import('@/stores/files') (circular ESM guard)"
      pattern: "import\\(.@/stores/files.\\)"
    - from: "src/stores/files.ts setFileSettings"
      to: "filesAtom.entries[].settings"
      via: "setKey('entries', map)"
      pattern: "setKey\\('entries'"
---

<objective>
Establish the per-file settings foundation for Phase 9 (D-01/D-02/D-03) and create the Wave 0 test scaffolds that every downstream plan verifies against. Refactor the global `settingsAtom` into a defaults-template + "Apply to all" model, extend `FileEntry` to own its complete settings plus buffer/error fields, and fold in the Phase 8 CR-01 atomic-setKey fix on `runtimeAtom`.

Purpose: Per-file settings is the foundation the worker adapters, hooks, and inspector wiring all depend on. Without it, the inspector cannot edit "the selected file's settings" and "Apply to all" has nothing to write to. The Wave 0 tests must exist (red) before adapters land so sampling is continuous.
Output: Extended `FileEntry`/`FileSettings` types, new `files.ts` per-file actions, `settings.ts applyToAll`, CR-01 fix in `runtime.ts`, and two new Playwright spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-codec-encoders/09-CONTEXT.md
@.planning/phases/09-codec-encoders/09-RESEARCH.md
@.planning/phases/09-codec-encoders/09-PATTERNS.md
@.planning/phases/09-codec-encoders/09-VALIDATION.md

<interfaces>
<!-- Current state of the files this plan refactors. Use these as the contract. -->

From src/stores/settings.ts (SettingsState — becomes the global-defaults template):
```typescript
interface SettingsState {
  codec: Codec; q: number; method: number; lossless: boolean
  resizeOn: boolean; w: string; h: string; alg: string; fit: string
  stripMeta: boolean; keepIcc: boolean; aggressive: boolean; plugins: SvgoPlugin[]
}
```

From src/lib/stub-data.ts (FileEntry — extend, do not break existing fields):
```typescript
export type Codec = 'SVG' | 'PNG' | 'WebP' | 'JPEG' | 'AVIF'
export interface FileEntry { id; name; type; orig; opt; status; target; dim; q; prog? }
export interface SvgoPlugin { id: string; on: boolean; saves: string }
```

From src/stores/runtime.ts (setJobCounts — CR-01 fix target, currently uses full-object spread):
```typescript
export function setJobCounts(running: number, queued: number): void  // currently runtimeAtom.set({...spread})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FileEntry/FileSettings types + initFileSettings helper</name>
  <files>src/lib/stub-data.ts</files>
  <read_first>
    - src/lib/stub-data.ts (the file being modified — current FileEntry, SvgoPlugin, Codec, STUB_FILES)
    - .planning/phases/09-codec-encoders/09-PATTERNS.md ("src/lib/stub-data.ts" section, Pattern 5)
    - .planning/phases/09-codec-encoders/09-RESEARCH.md (Pattern 5: Per-File Settings State Shape)
  </read_first>
  <action>
    Add an exported `FileSettings` interface mirroring `SettingsState` from settings.ts: `codec: Codec`, `q: number`, `method: number`, `lossless: boolean`, `resizeOn: boolean`, `w: string`, `h: string`, `alg: string`, `fit: string`, `stripMeta: boolean`, `keepIcc: boolean`, `aggressive: boolean`, `plugins: SvgoPlugin[]`, and `progressive?: boolean` (JPEG-only, default true per 09-RESEARCH Pitfall 6). Extend `FileEntry` with four OPTIONAL fields so existing STUB_FILES literals still typecheck: `settings?: FileSettings`, `rawBuffer?: ArrayBuffer`, `encodedBuffer?: ArrayBuffer`, `error?: string`. Add an exported `initFileSettings(defaults: FileSettings): FileSettings` helper that returns a shallow copy `{ ...defaults }` (per 09-RESEARCH Pitfall 5 — used when entries are created to assign per-file defaults). Do NOT modify STUB_FILES literals (settings stays absent on stubs; downstream code falls back to defaults). Keep the STORE-08 header comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "stub-data" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/stub-data.ts contains `export interface FileSettings` with exactly the 13 required fields plus optional `progressive`
    - FileEntry contains `settings?: FileSettings`, `rawBuffer?: ArrayBuffer`, `encodedBuffer?: ArrayBuffer`, `error?: string`
    - src/lib/stub-data.ts contains `export function initFileSettings`
    - `npx tsc --noEmit` reports no new errors referencing stub-data.ts
  </acceptance_criteria>
  <done>FileSettings exported, FileEntry extended with optional buffer/error/settings fields, initFileSettings helper present, STUB_FILES literals still typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-file settings + buffer/error actions to files.ts; applyToAll to settings.ts; CR-01 fix in runtime.ts</name>
  <files>src/stores/files.ts, src/stores/settings.ts, src/stores/runtime.ts</files>
  <read_first>
    - src/stores/files.ts (current setKey actions at selectFile/removeFile — copy the pattern)
    - src/stores/settings.ts (CIRCULAR ESM GUARD comment; current actions)
    - src/stores/runtime.ts (setJobCounts at the bottom — the CR-01 fix target)
    - .planning/phases/09-codec-encoders/09-PATTERNS.md (files.ts, settings.ts, runtime.ts sections; "Circular ESM guard" + "nanostores map + setKey" shared patterns)
    - .planning/phases/09-codec-encoders/09-RESEARCH.md (Pattern 4 CR-01 + Pattern 5 actions)
  </read_first>
  <action>
    In src/stores/files.ts add four actions following the existing `setKey('entries', entries.map(...))` pattern: `setFileSettings<K extends keyof FileSettings>(id, key, value)` (per D-01/D-03 — updates `{ ...e, settings: { ...e.settings!, [key]: value } }`), `setFileError(id, error: string | undefined)` (D-13), `setFileResult(id, encodedBuffer: ArrayBuffer, optimizedSize: number)` (sets `encodedBuffer`, `opt: optimizedSize`, clears `error`), and `setFileRawBuffer(id, rawBuffer: ArrayBuffer)`. Import `FileSettings` type from `@/lib/stub-data` and re-export it alongside the existing `FileEntry`/`SortKey` re-export. In src/stores/settings.ts append an `applyToAll()` export (D-02) that lazy-imports `@/stores/files` (circular ESM guard — mirror the worker-pool.ts `import('@/stores/runtime')` pattern) and writes `{ ...settingsAtom.get() }` into every `filesAtom.entries[].settings`. In src/stores/runtime.ts replace the `setJobCounts` body (CR-01) from the full-object `runtimeAtom.set({...spread})` to three atomic calls: `setKey('runningJobs', running)`, `setKey('queuedJobs', queued)`, `setKey('running', running > 0 || queued > 0)`. Do NOT remove or rename existing settings.ts setters (still used in global-defaults mode).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v node_modules | grep -cE "stores/(files|settings|runtime)" | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/files.ts exports `setFileSettings`, `setFileError`, `setFileResult`, `setFileRawBuffer` and re-exports `FileSettings`
    - src/stores/settings.ts exports `applyToAll` and it uses `import('@/stores/files')` (lazy, not a top-level import)
    - src/stores/runtime.ts `setJobCounts` body uses three `runtimeAtom.setKey(...)` calls and no `runtimeAtom.set(`
    - `npx tsc --noEmit` reports no new errors in files.ts/settings.ts/runtime.ts
  </acceptance_criteria>
  <done>Per-file actions present in files.ts, applyToAll in settings.ts via lazy import, setJobCounts uses atomic setKey, no circular import cycle introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds — codec-encoders.spec.ts + per-file-settings.spec.ts</name>
  <files>src/tests/codec-encoders.spec.ts, src/tests/per-file-settings.spec.ts</files>
  <read_first>
    - src/tests/worker-pipeline.spec.ts (analog — page.goto, page.evaluate, page.on('request') patterns)
    - src/tests/inspector-tabs.spec.ts (analog for store/UI assertions in per-file-settings)
    - .planning/phases/09-codec-encoders/09-VALIDATION.md (Per-Task Verification Map — exact -g grep titles; Wave 0 Requirements)
    - .planning/phases/09-codec-encoders/09-PATTERNS.md (test sections — the -g title structure)
    - .planning/phases/09-codec-encoders/09-RESEARCH.md (Validation Architecture — assertion shape: byteLength > 0, optimizedSize < originalSize, SVG valid XML + shorter)
  </read_first>
  <action>
    Create src/tests/codec-encoders.spec.ts with a `test.describe('Codec Encoders — ENC-01..06', ...)` block and one test per requirement using EXACTLY these greppable title substrings so the VALIDATION -g commands match: "PNG" (ENC-01), "WebP" (ENC-02), "JPEG" (ENC-03), "AVIF" (ENC-04), "SVG" (ENC-05), "settings" (ENC-06 — control change measurably changes output), "error" (D-13 — failure → fallback). Each raster test navigates `page.goto('/')`, then `page.evaluate` to import `getPool` / construct an `EncodeJob` with a small real image buffer (generate the smallest valid PNG/JPEG/WebP/SVG inline as a base64 → ArrayBuffer in the test), dispatch through the pool, and assert `result.buffer.byteLength > 0`. For lossless-eligible codecs (PNG via OxiPNG) additionally assert `result.optimizedSize <= result.originalSize`. The SVG test asserts the result string parses as XML and is shorter than input. The "settings" test runs the same codec twice with two different quality values and asserts the two output byteLengths differ. The "error" test dispatches an empty buffer (`new ArrayBuffer(0)`) and asserts the job rejects (WR-02 / D-13). These tests are expected RED until Plan 02/03 land — that is correct Wave 0 behavior; write them to assert FINAL expected behavior, not stubs. Create src/tests/per-file-settings.spec.ts with a `test.describe('per-file settings — D-01/D-02/D-03', ...)` covering: (D-01) setting one file's quality leaves another file's quality unchanged; (D-02) applyToAll copies global defaults into every entry's settings; (D-03) selecting a different file shows that file's own settings in the inspector. Use `page.evaluate` against the exposed store (if no window handle exists, observe via the inspector UI labels per the analog inspector-tabs.spec.ts).
  </action>
  <verify>
    <automated>npx playwright test src/tests/codec-encoders.spec.ts src/tests/per-file-settings.spec.ts --list 2>&1 | grep -qE "ENC-01|Codec Encoders"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list` enumerates tests matching -g "PNG", "WebP", "JPEG", "AVIF", "SVG", "settings", "error" in codec-encoders.spec.ts
    - per-file-settings.spec.ts contains describe block "per-file settings — D-01/D-02/D-03" with at least 3 tests
    - Tests are syntactically valid (Playwright `--list` exits 0)
  </acceptance_criteria>
  <done>Both spec files exist, enumerate via Playwright --list with the VALIDATION-mandated grep titles, and assert final expected encode behavior (red until adapters land).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File drop / inspector control → store | User-controlled settings values cross into FileEntry.settings |
| store action → filesAtom | Per-file mutations must not clobber sibling entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | setFileSettings (files.ts) | mitigate | Generic `<K extends keyof FileSettings>` key type — only known settings keys accepted; value typed to `FileSettings[K]`; no arbitrary key write |
| T-9-CR | Tampering | setJobCounts (runtime.ts) | mitigate | CR-01 fix — atomic setKey per field eliminates read-modify-write race that could surface stale running/queued state under concurrent pushToast |
| T-9-02 | Denial of Service | applyToAll (settings.ts) | accept | Bounded by entries.length (≤ batch size); single synchronous map; no unbounded recursion |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for stub-data.ts, files.ts, settings.ts, runtime.ts
- `npx playwright test --list` enumerates the new specs
- Existing suite stays green: `npx playwright test src/tests/worker-pipeline.spec.ts src/tests/backpressure.spec.ts`
- No new circular import: `applyToAll` uses lazy `import('@/stores/files')`, not a top-level import
</verification>

<success_criteria>
- FileSettings + extended FileEntry types compile; STUB_FILES still typecheck
- files.ts exposes the four per-file actions; settings.ts exposes applyToAll; runtime.ts setJobCounts is atomic (CR-01)
- codec-encoders.spec.ts and per-file-settings.spec.ts exist with the exact VALIDATION grep titles
</success_criteria>

<output>
Create `.planning/phases/09-codec-encoders/09-01-SUMMARY.md` when done
</output>
