---
phase: 08-worker-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/_headers
  - src/tests/worker-pipeline.spec.ts
  - src/tests/backpressure.spec.ts
autonomous: true
requirements: [PIPE-03]
must_haves:
  truths:
    - "crossOriginIsolated === true in the running app (dev + Cloudflare Pages headers present)"
    - "public/_headers serves both COOP and COEP headers for all routes"
    - "A worker-pipeline.spec.ts exists and asserts crossOriginIsolated, UI-responsive-during-encode, and no-AVIF-fetch-until-selected"
    - "backpressure.spec.ts asserts the indicator reflects real runningJobs/queuedJobs counts (not only the boolean)"
  artifacts:
    - path: "public/_headers"
      provides: "COOP/COEP headers for Cloudflare Pages production"
      contains: "Cross-Origin-Embedder-Policy"
    - path: "src/tests/worker-pipeline.spec.ts"
      provides: "PIPE-01/02/03 Playwright coverage (Wave 0 scaffold)"
      contains: "crossOriginIsolated"
    - path: "src/tests/backpressure.spec.ts"
      provides: "Extended backpressure coverage for real job counts"
      contains: "Optimize all"
  key_links:
    - from: "src/tests/worker-pipeline.spec.ts"
      to: "crossOriginIsolated"
      via: "page.evaluate"
      pattern: "page\\.evaluate\\(\\(\\) => crossOriginIsolated\\)"
---

<objective>
Establish PIPE-03 (cross-origin isolation) end-to-end and lay down the Wave 0 test scaffolds the rest of Phase 8 verifies against.

Purpose: COOP/COEP headers are the precondition for SharedArrayBuffer / OxiPNG multi-threading. The headers already exist in `vite.config.ts` (dev) and `public/_headers` (prod) but `public/_headers` looks truncated — this plan confirms integrity and proves `crossOriginIsolated === true` at runtime. It also creates `worker-pipeline.spec.ts` and extends `backpressure.spec.ts` so Plans 02 and 03 have automated gates ready before they run.

Output: Verified/complete `public/_headers`, a new `src/tests/worker-pipeline.spec.ts`, and an extended `src/tests/backpressure.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-worker-pipeline-foundation/08-RESEARCH.md
@.planning/phases/08-worker-pipeline-foundation/08-PATTERNS.md
@.planning/phases/08-worker-pipeline-foundation/08-VALIDATION.md

<interfaces>
<!-- Existing test contract the executor MUST preserve. Extracted from codebase. -->
From src/tests/backpressure.spec.ts (the EXISTING, passing test — do not break):
- describe block: `BackpressureIndicator — SHELL-02`
- test 'is hidden on initial load' → `page.getByTestId('backpressure-indicator')` then `.toHaveClass(/opacity-0/)`
- test 'becomes visible when Optimize is clicked' → `page.getByRole('button', { name: 'Optimize all' }).click()` then `.not.toHaveClass(/opacity-0/)`

From src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx:
- reads `const { running } = useStore(runtimeAtom)` — `running` is a boolean
- testid: `data-testid="backpressure-indicator"`; idle state = class `opacity-0`; active = `bg-[var(--color-accent)] animate-pulse`

From playwright.config.ts:
- baseURL `http://localhost:5174`; testDir `./src/tests`; testMatch `**/*.spec.ts`; chromium only; webServer runs `npm run dev -- --port 5174`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete public/_headers COOP/COEP</name>
  <read_first>
    - public/_headers (the file being modified — read it fully; RESEARCH.md Pitfall 1 reports it may be truncated)
    - vite.config.ts (lines 47-52 — the dev-server `server.headers` block that must match the prod headers)
    - .planning/phases/08-worker-pipeline-foundation/08-RESEARCH.md (COOP/COEP Deep Dive section + Pitfall 1)
  </read_first>
  <action>
    Read public/_headers. Ensure it contains exactly one route block `/*` followed by two indented header lines: `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Embedder-Policy: require-corp`, and that the file ends with a trailing newline (Cloudflare Pages requires the block to be terminated). Header values MUST match vite.config.ts `server.headers` verbatim (same-origin / require-corp). Do NOT add any other headers. If the file is already complete and valid, make the closing-newline fix only — do not rewrite content. Do NOT modify vite.config.ts (its dev headers are already correct per RESEARCH.md).
  </action>
  <verify>
    <automated>test "$(grep -c 'Cross-Origin' public/_headers)" = "2" && tail -c1 public/_headers | od -An -c | grep -q '\\n'</automated>
  </verify>
  <acceptance_criteria>
    - public/_headers contains the line `Cross-Origin-Opener-Policy: same-origin`
    - public/_headers contains the line `Cross-Origin-Embedder-Policy: require-corp`
    - public/_headers begins with a `/*` route block and ends with a trailing newline
    - vite.config.ts is unchanged (its `server.headers` already set both policies)
  </acceptance_criteria>
  <done>public/_headers serves both COOP and COEP headers for all routes and is byte-complete (trailing newline present).</done>
</task>

<task type="auto">
  <name>Task 2: Create worker-pipeline.spec.ts (PIPE-01/02/03 scaffold)</name>
  <read_first>
    - src/tests/backpressure.spec.ts (the exact analog — copy its import + describe structure verbatim)
    - playwright.config.ts (baseURL, testMatch, webServer config)
    - .planning/phases/08-worker-pipeline-foundation/08-PATTERNS.md (worker-pipeline.spec.ts section — PIPE-03 and PIPE-01 test patterns, selector conventions)
    - .planning/phases/08-worker-pipeline-foundation/08-VALIDATION.md (Per-Task Verification Map + "Key test strategy for PIPE-01" note)
    - src/components/shell/Toolbar/Toolbar.tsx (lines 68-76 — the `Optimize all` button is `getByRole('button', { name: 'Optimize all' })`)
  </read_first>
  <action>
    Create src/tests/worker-pipeline.spec.ts with header comment `// Phase 08 — PIPE-01/02/03 spec. Source: 08-01-PLAN.md` and import `{ test, expect } from '@playwright/test'`. Add `test.describe('Worker Pipeline — PIPE-01/02/03', () => { ... })` containing three tests:
    (1) PIPE-03: `await page.goto('/')`; `expect(await page.evaluate(() => crossOriginIsolated)).toBe(true)`.
    (2) PIPE-01: `await page.goto('/')`; click `page.getByRole('button', { name: 'Optimize all' })`; assert `page.getByRole('toolbar')` stays visible AND the page remains interactive — drive interactivity by clicking the toolbar testid `toolbar` again and asserting no timeout. Use the VALIDATION.md "UI stays interactive while worker encodes" strategy.
    (3) PIPE-02: register `page.on('request', ...)` BEFORE `page.goto('/')` to collect request URLs; after navigation, assert no request URL matches `/avif/i` (AVIF WASM must not be fetched on initial route). Add a follow-up note in a comment that selecting AVIF (Phase 9) would trigger the fetch — this test guards the initial-route budget.
    Match selector conventions from backpressure.spec.ts exactly (getByTestId, getByRole). These tests are the Wave 0 scaffold for PIPE-01/02; PIPE-01's UI-responsive assertion will pass once the pool wiring lands in Plan 03 — keep the assertions written against the final expected behavior.
  </action>
  <verify>
    <automated>npx playwright test src/tests/worker-pipeline.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/worker-pipeline.spec.ts exists with describe block `Worker Pipeline — PIPE-01/02/03`
    - File contains `page.evaluate(() => crossOriginIsolated)` and asserts `.toBe(true)`
    - File contains a `page.on('request'` listener and an assertion that no AVIF WASM is fetched on initial load (regex `/avif/i`)
    - PIPE-03 test passes: `npx playwright test src/tests/worker-pipeline.spec.ts -g "PIPE-03"` exits 0
  </acceptance_criteria>
  <done>worker-pipeline.spec.ts exists; the PIPE-03 crossOriginIsolated test is green; PIPE-01/02 tests are written against final behavior (PIPE-01 may stay red until Plan 03 wires the pool — documented in the SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 3: Extend backpressure.spec.ts for real job counts</name>
  <read_first>
    - src/tests/backpressure.spec.ts (the file being modified — preserve both existing tests verbatim)
    - src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx (the DOM contract: testid `backpressure-indicator`, class `opacity-0` idle)
    - .planning/phases/08-worker-pipeline-foundation/08-PATTERNS.md (runtime.ts extension — `runningJobs`/`queuedJobs` field names that Plan 03 adds)
    - .planning/phases/08-worker-pipeline-foundation/08-VALIDATION.md (Wave 0 Requirements — "assert real runningJobs / queuedJobs counts drive the indicator")
  </read_first>
  <action>
    Append a new test (do NOT modify the two existing tests) to the existing `BackpressureIndicator — SHELL-02` describe block in src/tests/backpressure.spec.ts: `reflects real running job count after Optimize all (PIPE-04)`. The test navigates to `/`, clicks `getByRole('button', { name: 'Optimize all' })`, then reads the runtime store via `page.evaluate` against the window-exposed store OR asserts the indicator class transitions to active (`not.toHaveClass(/opacity-0/)`) while a job is running. Because Plan 03 adds `runningJobs`/`queuedJobs` to runtimeAtom and derives `running` from them, this test must assert behavior that holds AFTER Plan 03 wiring (indicator active while runningJobs > 0). If the store is not window-exposed, assert via the visible indicator class only — do NOT introduce a window global solely for testing without checking whether Plan 03 exposes one (note the dependency in the SUMMARY). Keep the existing boolean-contract tests untouched so the running suite stays green.
  </action>
  <verify>
    <automated>npx playwright test src/tests/backpressure.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/backpressure.spec.ts still contains both original tests (`is hidden on initial load`, `becomes visible when Optimize is clicked`) unchanged
    - A third test referencing PIPE-04 and real job counts is added to the same describe block
    - The two original tests still pass: `npx playwright test src/tests/backpressure.spec.ts -g "becomes visible"` exits 0
  </acceptance_criteria>
  <done>backpressure.spec.ts has a PIPE-04 job-count test added; the two original boolean-contract tests are unchanged and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → response headers | COOP/COEP set by server; misconfiguration silently disables SharedArrayBuffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | COEP `require-corp` blocking cross-origin resources | accept | App is 100% self-hosted (fonts via `@fontsource-variable/*`, user files from disk) — no cross-origin resources exist, so COEP has no DoS side effect (RESEARCH.md COOP/COEP Deep Dive) |
| T-08-02 | Information Disclosure | COOP `same-origin` window reference leakage | mitigate | `Cross-Origin-Opener-Policy: same-origin` in both vite.config.ts and public/_headers severs cross-origin window references (process isolation) |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH.md Package Legitimacy Audit: all deps pre-existing) — no install step, no slopcheck gate required |
</threat_model>

<verification>
- `grep -c 'Cross-Origin' public/_headers` returns 2
- `npx playwright test src/tests/worker-pipeline.spec.ts -g "PIPE-03"` exits 0 (crossOriginIsolated === true)
- `npx playwright test src/tests/backpressure.spec.ts -g "becomes visible"` exits 0 (existing contract intact)
</verification>

<success_criteria>
- public/_headers is byte-complete with both COOP and COEP headers (PIPE-03 production path)
- crossOriginIsolated === true verified at runtime via Playwright (PIPE-03 dev path)
- worker-pipeline.spec.ts exists covering PIPE-01/02/03; backpressure.spec.ts extended for PIPE-04 — Wave 0 scaffolds ready for Plans 02/03
</success_criteria>

<output>
Create `.planning/phases/08-worker-pipeline-foundation/08-01-SUMMARY.md` when done. Note in the SUMMARY which PIPE-01/02/04 assertions are expected to stay red until Plans 02/03 land.
</output>
