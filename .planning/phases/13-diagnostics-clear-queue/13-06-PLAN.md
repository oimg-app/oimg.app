---
phase: 13
plan: 06
type: execute
wave: 2
depends_on: [13-04]
files_modified:
  - src/components/panels/FilesPane.tsx
  - src/tests/filespane-clear.spec.ts
autonomous: true
requirements: [CLR-01]
user_setup: []
tags: [phase-13, wave-2, filespane, clear-icon, disable-then-explain, CLR-01]

must_haves:
  truths:
    - "D-15: FilesPane header gets a small `XCircle` (phosphor-icons) ghost icon button — same w-[22px] h-[22px] grid-place-items-center pattern as the existing Sort + Add buttons (FilesPane.tsx:65-110 analog)"
    - "D-15 placement (Claude's Discretion in CONTEXT.md): place LEFT of the Sort funnel button — leaves the rightmost positions for the existing Add button (preserves muscle memory; new button is the destructive action and benefits from being visually separated from Add)"
    - "D-15 disable-then-explain triple: `disabled={queueEmpty}` + `aria-disabled={queueEmpty}` + `title={queueEmpty ? 'No files to clear' : 'Clear all files'}` (note: title has TWO values — explanation when disabled, action description when enabled — per PATTERNS line 427)"
    - "`aria-label='Clear all files'` (screen-reader voice — explicit destructive action)"
    - "T-13-03 mitigation: same `handleClearAll` shape as Plan 05 Toolbar — warning toast when `runtimeAtom.runningJobs > 0`, direct call when 0. Plan 05 and Plan 06 each implement their own local handler (no shared hook) since the affordances are simple two-button-tree-shaking is irrelevant — PATTERNS line 467-482 shape is local to each call site."
    - "XCircle import added to the existing `@phosphor-icons/react` named-import group (FilesPane.tsx line 5 imports Funnel + Plus today)"
    - "Existing FilesPane state: dropzone, useIngest, sort popover, Add button, file list, totals bar — all preserved verbatim"
  artifacts:
    - path: "src/components/panels/FilesPane.tsx"
      provides: "Header × (XCircle) icon button with D-15 disable-then-explain triple + T-13-03 warning toast"
    - path: "src/tests/filespane-clear.spec.ts"
      provides: "Playwright e2e: (1) aria-label/title when disabled; (2) idle clear path; (3) warning toast when runningJobs>0 + Clear anyway action"
  key_links:
    - from: "src/components/panels/FilesPane.tsx"
      to: "src/stores/files.ts (clearFiles + $queueEmpty)"
      via: "useStore($queueEmpty) + clearFiles() call"
      pattern: "\\$queueEmpty|clearFiles\\(\\)"
    - from: "src/components/panels/FilesPane.tsx"
      to: "src/stores/runtime.ts (runtimeAtom.runningJobs)"
      via: "runtimeAtom.get().runningJobs check inside handleClearAll"
      pattern: "runningJobs"
---

<objective>
Add a small `XCircle` icon button to the FilesPane header with `aria-label='Clear all files'`, the D-15 disable-then-explain triple, and the T-13-03 in-flight warning-toast wrapper — independent of (but identical in behavior to) Plan 05's Toolbar Clear all.

Purpose: Two affordances per CLR-01 — one in the Toolbar Settings (Plan 05), one in the FilesPane header (this plan). Users reach for whichever is closer to their current focus; both must behave identically (same store call, same in-flight safety net) so the user can't tell them apart by outcome.

Output: FilesPane.tsx header gets one new button + Playwright e2e covering disable, fire-when-idle, warning-when-in-flight.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-diagnostics-clear-queue/13-CONTEXT.md
@.planning/phases/13-diagnostics-clear-queue/13-PATTERNS.md
@.planning/phases/13-diagnostics-clear-queue/13-VALIDATION.md
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@./CLAUDE.md
@src/components/panels/FilesPane.tsx

<interfaces>
<!-- From Plan 04 -->
import { clearFiles, $queueEmpty } from '@/stores/files'

<!-- From Plan 03 (runtime atom) -->
import { runtimeAtom } from '@/stores/runtime'

<!-- Existing FilesPane header (line 65-110) — Sort button + Add button side-by-side -->
<div className="h-8 flex items-center justify-between px-3 border-b border-[var(--line)] shrink-0">
  <span className="font-mono ...">Queue · {files.length} files</span>
  <div className="flex items-center gap-1">
    <Popover>...Sort popover with Funnel icon...</Popover>
    <button ... onClick={() => openPicker(...)} aria-label="Add files"><Plus size={13} /></button>
    <input ... type="file" multiple accept={ACCEPT} hidden />
  </div>
</div>

<!-- Target: insert a new button at the LEFT of the Sort Popover (BEFORE the Sort button in the right-aligned <div>) -->
<button
  className={cn(
    "w-[22px] h-[22px] grid place-items-center rounded text-[var(--fg-2)] hover:bg-[var(--bg-3)] hover:text-[var(--fg-0)]",
    queueEmpty && 'opacity-50 cursor-not-allowed'
  )}
  aria-label="Clear all files"
  title={queueEmpty ? 'No files to clear' : 'Clear all files'}
  onClick={() => handleClearAll()}
  disabled={queueEmpty}
  aria-disabled={queueEmpty}
>
  <XCircle size={13} />
</button>

<!-- handleClearAll shape (same as Plan 05 — local to FilesPane) -->
function handleClearAll() {
  const { runningJobs } = runtimeAtom.get()
  if (runningJobs > 0) {
    toast.warning(`Cancel ${runningJobs} in-flight jobs?`, {
      action: { label: 'Clear anyway', onClick: () => clearFiles() },
    })
    return
  }
  clearFiles()
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add XCircle × button to FilesPane header with disable-then-explain + warning toast (D-15)</name>
  <files>src/components/panels/FilesPane.tsx</files>
  <read_first>
    - src/components/panels/FilesPane.tsx (entire — lines 1-158; identify line 5 `@phosphor-icons/react` named imports `Funnel, Plus` — extend with `XCircle`; identify lines 65-110 header block with the Sort Popover + Add button pair; the existing className pattern `w-[22px] h-[22px] grid place-items-center rounded text-[var(--fg-2)] hover:bg-[var(--bg-3)] hover:text-[var(--fg-0)]` is the exact ghost-icon shape per PATTERNS line 392)
    - src/components/panels/files/FileRow.tsx (lines 121-127 — second ghost-icon analog per PATTERNS line 395)
    - src/stores/files.ts (Plan 04 output)
    - src/stores/runtime.ts (Plan 03 output)
    - .planning/phases/13-diagnostics-clear-queue/13-PATTERNS.md § "`src/components/panels/FilesPane.tsx` (modify — D-15)" (lines 389-427 — verbatim shape; the `cn` className pattern + the aria-label/title pair + the disabled/aria-disabled triple)
    - .planning/phases/13-diagnostics-clear-queue/13-CONTEXT.md D-13, D-15
  </read_first>
  <behavior>
    - Imports: extend `@phosphor-icons/react` named-imports to include `XCircle`; add `import { useStore } from '@nanostores/react'` if not already present (line 4 already has it via `$filteredFiles, $totals` usage — verify and skip the import addition if so); add `import { clearFiles, $queueEmpty } from '@/stores/files'` (verify the `@/stores` barrel works for these — if line 6 currently uses `@/stores` barrel for `$filteredFiles, $totals, setSortBy`, extend it to include `clearFiles, $queueEmpty` instead of importing from `@/stores/files` directly); add `import { runtimeAtom } from '@/stores/runtime'`; add `import { toast } from 'sonner'`
    - Inside the FilesPane function component body, after the existing `useStore` calls: `const queueEmpty = useStore($queueEmpty)`
    - Define `function handleClearAll() { const { runningJobs } = runtimeAtom.get(); if (runningJobs > 0) { toast.warning(\`Cancel \${runningJobs} in-flight jobs?\`, { action: { label: 'Clear anyway', onClick: () => clearFiles() } }); return; } clearFiles(); }` — same shape as Plan 05's Toolbar handler (no shared hook by design — small enough to duplicate; PATTERNS line 467-482 documents that the affordances own their handler locally)
    - In the header `<div className="flex items-center gap-1">` (currently lines 69-110), INSERT the new × button as the FIRST child of that flex container (LEFT of the Sort Popover per the Claude's Discretion choice). The button uses the exact className from PATTERNS lines 414-417, `aria-label="Clear all files"`, `title={queueEmpty ? 'No files to clear' : 'Clear all files'}`, `onClick={() => handleClearAll()}`, `disabled={queueEmpty}`, `aria-disabled={queueEmpty}`. Inside: `<XCircle size={13} />` (matches the Sort funnel size and the Add plus size per FilesPane.tsx lines 76 + 98)
    - All existing FilesPane behavior preserved verbatim: drag/drop, dropzone, useIngest, sort popover, Add button + hidden file input, file list ul, totals bar
  </behavior>
  <action>
    Modify `src/components/panels/FilesPane.tsx`. At line 5, extend the `@phosphor-icons/react` import list: change `import { Funnel, Plus } from '@phosphor-icons/react'` to `import { Funnel, Plus, XCircle } from '@phosphor-icons/react'`. At line 6, extend the `@/stores` barrel import to include `clearFiles, $queueEmpty` alongside `$filteredFiles, $totals, setSortBy` (verify the barrel re-exports them — Plan 04 confirmed via `src/stores/index.ts` line 2 `export * from './files'`). Add `import { runtimeAtom } from '@/stores/runtime'` and `import { toast } from 'sonner'` to the import block. Inside the FilesPane function component body, after the existing `const totals = useStore($totals)` line, add: `const queueEmpty = useStore($queueEmpty)`. Below the existing `function handleDrop(e: React.DragEvent) { ... }` (around line 46), add: `function handleClearAll() { const { runningJobs } = runtimeAtom.get(); if (runningJobs > 0) { toast.warning(\`Cancel \${runningJobs} in-flight jobs?\`, { action: { label: 'Clear anyway', onClick: () => clearFiles() } }); return } clearFiles() }` — matching the Plan 05 handler shape character-for-character so the e2e contract is identical. Now locate the header at lines 65-110. INSIDE the `<div className="flex items-center gap-1">` (line 69), INSERT a new button as the FIRST child (before the `<Popover>` Sort wrapper at line 70): `<button className={cn("w-[22px] h-[22px] grid place-items-center rounded text-[var(--fg-2)] hover:bg-[var(--bg-3)] hover:text-[var(--fg-0)]", queueEmpty && 'opacity-50 cursor-not-allowed')} aria-label="Clear all files" title={queueEmpty ? 'No files to clear' : 'Clear all files'} onClick={() => handleClearAll()} disabled={queueEmpty} aria-disabled={queueEmpty}><XCircle size={13} /></button>`. Do NOT touch any other element in FilesPane.
  </action>
  <acceptance_criteria>
    - `src/components/panels/FilesPane.tsx` imports `XCircle` from `@phosphor-icons/react`
    - File imports `clearFiles` AND `$queueEmpty` (via `@/stores` barrel or direct `@/stores/files`)
    - File imports `runtimeAtom` from `@/stores/runtime` and `toast` from `'sonner'`
    - File contains `aria-label="Clear all files"` literal substring
    - File contains `'No files to clear'` AND `'Clear all files'` (the two title states per D-15)
    - File contains `useStore($queueEmpty)` (React subscription)
    - File contains `runtimeAtom.get()` (the snapshot read inside handleClearAll)
    - File contains `Clear anyway` literal (T-13-03 toast action)
    - File contains `<XCircle size={13} />` (matches the existing 13-size convention)
    - Existing buttons preserved: grep `aria-label="Add files"` returns 1, `aria-label="Sort files"` returns 1
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "XCircle" src/components/panels/FilesPane.tsx && grep -q "Clear all files" src/components/panels/FilesPane.tsx && grep -q "No files to clear" src/components/panels/FilesPane.tsx && grep -q "Clear anyway" src/components/panels/FilesPane.tsx && grep -q "useStore(\\$queueEmpty)" src/components/panels/FilesPane.tsx && grep -q "runtimeAtom.get()" src/components/panels/FilesPane.tsx && [ $(grep -c 'aria-label="Add files"' src/components/panels/FilesPane.tsx) -eq 1 ] && npx tsc -b</automated>
  </verify>
  <done>× button shipped in FilesPane header with D-15 triple + T-13-03 confirmation; behavior parity with Plan 05 Toolbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/tests/filespane-clear.spec.ts — Playwright e2e covering D-15 disable + clearFiles + warning (CLR-01)</name>
  <files>src/tests/filespane-clear.spec.ts</files>
  <read_first>
    - src/tests/status-bar.spec.ts (Playwright analog — `ingestFixtureFiles` helper at line 8)
    - src/tests/file-row-menu.spec.ts (injectEntries at lines 34-72 + aria-disabled/title assertion at lines 154-167)
    - src/components/panels/FilesPane.tsx (Task 1 output — confirm the button's `aria-label="Clear all files"` selector works)
    - src/tests/toolbar-clear.spec.ts (sibling Plan 05 spec — mirror the test shape for consistency; the warning-toast pattern + `setJobCounts(2, 0)` injection should be IDENTICAL across both spec files)
    - .planning/phases/13-diagnostics-clear-queue/13-VALIDATION.md task 13-07-filespane-x row
  </read_first>
  <behavior>
    - Test 1 (D-15 disable when empty queue): `await page.goto('/')`; `const xBtn = page.getByRole('button', { name: 'Clear all files' })`; assert `await expect(xBtn).toHaveAttribute('aria-disabled', 'true')` AND `await expect(xBtn).toHaveAttribute('title', 'No files to clear')`
    - Test 2 (idle path — clear fires): inject 3 entries via injectEntries; verify `await expect(page.getByTestId('status-filecount')).toHaveText('3 files')`; click the × button (`await page.getByRole('button', { name: 'Clear all files' }).click()`); assert `await expect(page.getByTestId('status-filecount')).toHaveText('0 files')`
    - Test 3 (warning path — runningJobs>0): inject 3 entries; `await page.evaluate(async () => { const m = await import('/src/stores/runtime.ts'); m.setJobCounts(2, 0); })`; click the × button; `await expect(page.getByText(/Cancel 2 in-flight jobs/)).toBeVisible()`; verify file count still 3 (not cleared yet); click `Clear anyway` toast action; verify file count drops to 0
    - Test 4 (enabled state title — D-15 has TWO title values): inject 1 entry; assert `await expect(page.getByRole('button', { name: 'Clear all files' })).toHaveAttribute('title', 'Clear all files')` (when queueEmpty===false, title becomes the action description)
  </behavior>
  <action>
    Create `src/tests/filespane-clear.spec.ts`. Header: "Phase 13 — CLR-01 / D-15 / T-13-03: FilesPane header × icon disable + warning-toast e2e". Imports: `import { test, expect, type Page } from '@playwright/test'`. Inline copy `injectEntries` from `file-row-menu.spec.ts:34-72`. Single `test.describe('FilesPane header — Clear all (D-15 + T-13-03)', () => { ... })`. Test 1 body: `await page.goto('/')`; `const xBtn = page.getByRole('button', { name: 'Clear all files' })`; `await expect(xBtn).toHaveAttribute('aria-disabled', 'true')`; `await expect(xBtn).toHaveAttribute('title', 'No files to clear')`. Test 2 body: `await page.goto('/')`; `await injectEntries(page, [...3 specs...])`; `await expect(page.getByTestId('status-filecount')).toHaveText('3 files')`; `await page.getByRole('button', { name: 'Clear all files' }).click()`; `await expect(page.getByTestId('status-filecount')).toHaveText('0 files')`. Test 3 body: same injection; `await page.evaluate(async () => { const m = await import('/src/stores/runtime.ts'); m.setJobCounts(2, 0); })`; click ×; `await expect(page.getByText(/Cancel 2 in-flight jobs/)).toBeVisible()`; assert file count still 3; click `await page.getByRole('button', { name: /^Clear anyway$/ }).click()`; assert file count drops to 0. Test 4 body: `await page.goto('/')`; `await injectEntries(page, [{ id:'a', name:'a.png', target:'webp' }])`; `await expect(page.getByRole('button', { name: 'Clear all files' })).toHaveAttribute('title', 'Clear all files')`.
  </action>
  <acceptance_criteria>
    - `src/tests/filespane-clear.spec.ts` exists
    - `npx playwright test src/tests/filespane-clear.spec.ts --reporter=dot` exits 0
    - File contains `aria-disabled` assertion AND both title states (`'No files to clear'` and `'Clear all files'` enabled)
    - File contains `Cancel 2 in-flight jobs` assertion (T-13-03)
    - File contains `Clear anyway` toast action selector
    - All four tests pass
  </acceptance_criteria>
  <verify>
    <automated>test -f src/tests/filespane-clear.spec.ts && grep -q "Clear all files" src/tests/filespane-clear.spec.ts && grep -q "Clear anyway" src/tests/filespane-clear.spec.ts && grep -q "setJobCounts" src/tests/filespane-clear.spec.ts && npx playwright test src/tests/filespane-clear.spec.ts --reporter=dot</automated>
  </verify>
  <done>Four-test e2e proves D-15 dual-title + idle clear + in-flight warning; 13-07 row in VALIDATION.md flips green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM click → `handleClearAll` | Trusted UI event; queueEmpty disable triple gates at the DOM layer |
| `runtimeAtom.get().runningJobs` → confirmation branch | Trusted internal state read; not user-controlled |
| `toast.warning(...).action.onClick` → `clearFiles()` | User confirms via the toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Accidental queue wipe via the × icon mid-batch | FilesPane header × onClick | mitigate | Task 1 `handleClearAll` mirrors Plan 05 exactly — `runtimeAtom.get().runningJobs > 0` triggers a sonner warning with an explicit `Clear anyway` action. Test 3 asserts the path. |
| (DOM-tampering bypass of disabled state) | Tampering | D-15 disable triple | mitigate | Defense-in-depth: `clearFiles()` is idempotent on empty entries; setKey('entries', []) is a no-op when entries is already empty. No data leak. |
| (icon-button accessibility regression) | Repudiation (a11y) | aria-label vs title divergence | mitigate | `aria-label="Clear all files"` (screen-reader voice) + `title` (visual tooltip — two values per state). Plan 06 acceptance asserts both attributes. PATTERNS line 427 documents the divergence. |
| T-13-01 / T-13-02 / T-13-04 | — | (no diagnostic surface, no Vite define, no version-string JSX in this plan) | n/a | — |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx playwright test src/tests/filespane-clear.spec.ts --reporter=dot` exits 0
- `grep -c "aria-label=\"Clear all files\"" src/components/panels/FilesPane.tsx` returns 1
- `grep -c "XCircle" src/components/panels/FilesPane.tsx` returns ≥ 2 (one import + one JSX usage)
- Existing FilesPane buttons preserved: grep `aria-label="Add files"` returns 1, `aria-label="Sort files"` returns 1
</verification>

<success_criteria>
- D-15 met (× icon with disable-then-explain dual-title triple)
- T-13-03 mitigation in place (mirrors Plan 05)
- VALIDATION.md row `13-07-filespane-x` flips ⬜ → ✅
</success_criteria>

<output>
Create `.planning/phases/13-diagnostics-clear-queue/13-06-SUMMARY.md` when done.
</output>
