---
phase: 13
plan: 05
type: execute
wave: 2
depends_on: [13-03, 13-04]
files_modified:
  - src/components/shell/Toolbar.tsx
  - src/tests/toolbar-clear.spec.ts
autonomous: true
requirements: [CLR-01]
user_setup: []
tags: [phase-13, wave-2, toolbar, clear-all, disable-then-explain, CLR-01]

must_haves:
  truths:
    - "D-14: Toolbar 'Clear all' menu item lands in the EXISTING Settings Popover (Toolbar.tsx:249-268), NOT a new kebab overflow — Settings popover content becomes tabbed in Plan 07, so Clear all lives on the General tab"
    - "Plan 07 adds the Tabs wrapper around the popover content; this plan ships the Clear all button INSIDE the existing single-flat-list popover content. Plan 07 then moves it into TabsContent value='general'. Both plans must agree on the same Clear all button identity (same className + onClick + disable triple) to avoid double-implementation."
    - "Plan-order discipline: this plan runs BEFORE Plan 07; the Clear all button sits in the current `<div className=\"flex flex-col\">` next to `Workers: 4 (auto)`; Plan 07 wraps that container in Tabs"
    - "Disable-then-explain triple (Phase 11 D-13 + Toolbar.tsx:96-159 analog): `disabled={queueEmpty}` + `aria-disabled={queueEmpty}` + `title={queueEmpty ? 'No files to clear' : undefined}` + className `cn(menuItemClass, queueEmpty && 'opacity-50 cursor-not-allowed')`"
    - "T-13-03 mitigation: when `runtimeAtom.runningJobs > 0`, the onClick handler fires a sonner `toast.warning('Cancel ${runningJobs} in-flight jobs?', { action: { label: 'Clear anyway', onClick: () => clearFiles() } })` INSTEAD of calling clearFiles directly; PATTERNS line 467-482 shape"
    - "When `runningJobs === 0`, onClick calls `clearFiles()` directly then `setOpen(null)` to close the popover"
    - "Existing Settings popover open/close state machine (`open === 'tb-settings'` / `setOpen`) is preserved verbatim"
  artifacts:
    - path: "src/components/shell/Toolbar.tsx"
      provides: "Clear all menu item in the Settings popover with D-14 disable-then-explain triple + T-13-03 warning-toast handler"
    - path: "src/tests/toolbar-clear.spec.ts"
      provides: "Playwright e2e: (1) disable+title when no files; (2) calls clearFiles when runningJobs=0 and entries exist; (3) shows warning toast when runningJobs>0 and clicks 'Clear anyway' through chokepoint"
  key_links:
    - from: "src/components/shell/Toolbar.tsx"
      to: "src/stores/files.ts (clearFiles + $queueEmpty)"
      via: "useStore($queueEmpty) + clearFiles() invocation in onClick"
      pattern: "\\$queueEmpty|clearFiles\\(\\)"
    - from: "src/components/shell/Toolbar.tsx"
      to: "src/stores/runtime.ts (runtimeAtom.runningJobs)"
      via: "runtimeAtom.get().runningJobs check in T-13-03 handler"
      pattern: "runningJobs"
    - from: "src/components/shell/Toolbar.tsx"
      to: "sonner toast.warning"
      via: "T-13-03 confirmation pattern (PATTERNS lines 471-481)"
      pattern: "toast\\.warning\\("
---

<objective>
Add the "Clear all" menu item to the Toolbar Settings popover (Toolbar.tsx:249-268) with the canonical Phase 11 D-13 disable-then-explain triple + the T-13-03 in-flight-confirmation toast wrapper. Plan 07 will later wrap this content in Radix Tabs (General + Diagnostics); this plan ships the button inside the current flat popover content so the work composes cleanly.

Purpose: D-14 — the "Clear all" affordance that the user reaches via Settings → General. Plan 04 ships the pure store action; this plan ships the call site + the safety net (sonner warning toast when work is in flight).

Output: Toolbar.tsx patched with a new menu button + Playwright e2e covering disable, fire-when-idle, warning-when-in-flight.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-diagnostics-clear-queue/13-CONTEXT.md
@.planning/phases/13-diagnostics-clear-queue/13-PATTERNS.md
@.planning/phases/13-diagnostics-clear-queue/13-VALIDATION.md
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@./CLAUDE.md
@src/components/shell/Toolbar.tsx

<interfaces>
<!-- From Plan 04 -->
import { clearFiles, $queueEmpty } from '@/stores/files'
export function clearFiles(): void
export const $queueEmpty: ReturnType<typeof computed>  // boolean read via useStore

<!-- From Plan 03 (runtime atom) -->
import { runtimeAtom } from '@/stores/runtime'
// runtimeAtom.get().runningJobs — number, > 0 when work is in flight

<!-- Existing disable-then-explain triple analog (Toolbar.tsx:96-100 — Export button) -->
<button
  className={cn(tbtnClass, 'rounded-r-none border-r-0', !hasDone && 'opacity-50 cursor-not-allowed')}
  onClick={() => { void exportZip(); setOpen(null) }}
  disabled={!hasDone}
  aria-disabled={!hasDone}
  title={disabledTitle}
>...</button>

<!-- Existing Settings popover content (Toolbar.tsx:263-267) -->
<PopoverContent className={popoverContentClass} align="end">
  <div className="flex flex-col">
    <button type="button" className={menuItemClass} onClick={() => { setWorkerCount(4); setOpen(null) }}>Workers: 4 (auto)</button>
  </div>
</PopoverContent>

<!-- T-13-03 confirmation handler (PATTERNS lines 471-481) -->
import { toast } from 'sonner'
function handleClear() {
  const { runningJobs } = runtimeAtom.get()
  if (runningJobs > 0) {
    toast.warning(`Cancel ${runningJobs} in-flight jobs?`, {
      action: { label: 'Clear anyway', onClick: () => clearFiles() },
    })
    return
  }
  clearFiles()
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Clear all button to Settings popover + T-13-03 warning-toast handler (D-14)</name>
  <files>src/components/shell/Toolbar.tsx</files>
  <read_first>
    - src/components/shell/Toolbar.tsx (entire — lines 1-272; pay attention to line 7 `$hasDone` import, line 33 `useStore($hasDone)`, line 40 `disabledTitle`, lines 96-100 + 122-126 disable-then-explain triple shape, lines 249-268 Settings popover scaffold, line 263 `<PopoverContent>` content)
    - src/stores/files.ts (Plan 04 output — confirm `clearFiles` and `$queueEmpty` exports)
    - src/stores/runtime.ts (Plan 03 output — confirm `runtimeAtom` shape includes `runningJobs`)
    - .planning/phases/13-diagnostics-clear-queue/13-PATTERNS.md § "`src/components/shell/Toolbar.tsx` (modify — D-10 / D-11 / D-14)" (lines 319-385 — the Plan 07 will replace 264-267 with the Tabs wrapper; THIS plan only adds the Clear all button INSIDE the current flat content, between `Workers: 4 (auto)` and the closing div) + § "Disable-then-explain (D-14 / D-15)" (lines 488-494) + § "T-13-03 in-flight confirmation toast" (lines 467-482)
    - .planning/phases/13-diagnostics-clear-queue/13-CONTEXT.md D-13, D-14
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-13 (disable-then-explain Phase 11 origin)
  </read_first>
  <behavior>
    - Toolbar imports: `import { clearFiles, $queueEmpty } from '@/stores/files'` (additive — keep existing `filesAtom`, `$hasDone`, `setFilter`, `addFromUrl` imports verbatim); `import { runtimeAtom } from '@/stores/runtime'` (likely already present — verify); `import { toast } from 'sonner'`
    - Inside the Toolbar component body, near the existing `useStore` calls: `const queueEmpty = useStore($queueEmpty)` and (only if not already destructured for Plan 03's StatusBar — verify) `const { runningJobs } = useStore(runtimeAtom)` — OR, since Toolbar may not need runtimeAtom subscription, read it via `runtimeAtom.get().runningJobs` inside the handler ONLY (no useStore subscription — avoids unnecessary re-renders). Choose the runtimeAtom.get() approach inside the handler per the PATTERNS line 472-481 shape.
    - Inside the Toolbar component body: define `const handleClearAll = () => { const { runningJobs } = runtimeAtom.get(); if (runningJobs > 0) { toast.warning(\`Cancel \${runningJobs} in-flight jobs?\`, { action: { label: 'Clear anyway', onClick: () => clearFiles() } }); return; } clearFiles(); }`. Define `const clearDisabledTitle = queueEmpty ? 'No files to clear' : undefined`.
    - Inside `<PopoverContent>` content (Toolbar.tsx:263-267 — the Settings popover, NOT the Add files or Export popovers), add a NEW button AFTER the `Workers: 4 (auto)` button, INSIDE the same `<div className="flex flex-col">`: `<button type="button" className={cn(menuItemClass, queueEmpty && 'opacity-50 cursor-not-allowed')} onClick={() => { handleClearAll(); setOpen(null) }} disabled={queueEmpty} aria-disabled={queueEmpty} title={clearDisabledTitle}>Clear all</button>`
    - The button label text is exactly `Clear all` (matches D-14 spec + future-proofs the e2e selector `getByRole('button', { name: 'Clear all' })`)
    - `setOpen(null)` ALWAYS runs (closes the popover after click); when `runningJobs > 0` and the warning toast appears, clicking the toast's "Clear anyway" action calls `clearFiles()` (already documented in handleClearAll closure)
    - All existing Settings popover behavior preserved: trigger button, GearSix icon, `open === 'tb-settings'` state, the `Workers: 4 (auto)` button — untouched
    - No new imports beyond those listed above; no removal of existing imports
  </behavior>
  <action>
    Modify `src/components/shell/Toolbar.tsx`. Imports (additive): at line 7, extend the `@/stores/files` named-import list to include `clearFiles, $queueEmpty` (keep existing identifiers). At the appropriate import block, add `import { toast } from 'sonner'`. Verify `runtimeAtom` is already imported from `@/stores/runtime` (line 8 — yes, `setWorkerCount` is imported from it; extend the import to include `runtimeAtom`). Inside the Toolbar function component body, near line 33 (`const hasDone = useStore($hasDone)`), add: `const queueEmpty = useStore($queueEmpty)` and below it the `clearDisabledTitle` const. Define the `handleClearAll` callback per the behavior block — INSIDE the component body (closure over the React hook environment), but read `runtimeAtom.get()` INSIDE the body (NOT via `useStore` — avoid Toolbar re-renders on every job-count change). The toast.warning call uses backtick template literal per PATTERNS line 475-477. Now locate the existing Settings popover content at lines 263-267. INSIDE the `<div className="flex flex-col">`, ADD a second button AFTER the `Workers: 4 (auto)` button per the behavior spec — exact className expression `cn(menuItemClass, queueEmpty && 'opacity-50 cursor-not-allowed')`, onClick handler `() => { handleClearAll(); setOpen(null) }`, disabled + aria-disabled + title triple. Do NOT touch the existing Workers button. Do NOT touch the Add files / Export / Auto popovers. Do NOT introduce a new Popover or PopoverContent — Clear all sits inside the SAME Settings popover content per D-14.
  </action>
  <acceptance_criteria>
    - `src/components/shell/Toolbar.tsx` imports `clearFiles` AND `$queueEmpty` from `@/stores/files`
    - File imports `toast` from `'sonner'`
    - File imports `runtimeAtom` from `@/stores/runtime`
    - File contains literal substring `'No files to clear'` (D-14 disabled title)
    - File contains literal substring `'Cancel '` and `'in-flight jobs?'` (T-13-03 toast message — template literal)
    - File contains literal substring `'Clear anyway'` (toast action label)
    - File contains literal substring `'Clear all'` (button label)
    - File contains `useStore($queueEmpty)` (the React subscription)
    - File contains `runtimeAtom.get()` (the snapshot read inside the handler — proves no extra useStore subscription)
    - File contains `disabled={queueEmpty}` (grep -c returns ≥ 1 — the Clear all button)
    - The Workers: 4 (auto) button at line 265 is UNCHANGED (grep `Workers: 4 \\(auto\\)` returns 1 — single occurrence)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Clear all" src/components/shell/Toolbar.tsx && grep -q "Clear anyway" src/components/shell/Toolbar.tsx && grep -q "'No files to clear'" src/components/shell/Toolbar.tsx && grep -q "useStore(\\$queueEmpty)" src/components/shell/Toolbar.tsx && grep -q "runtimeAtom.get()" src/components/shell/Toolbar.tsx && grep -q "from 'sonner'" src/components/shell/Toolbar.tsx && [ $(grep -c "Workers: 4 (auto)" src/components/shell/Toolbar.tsx) -eq 1 ] && npx tsc -b</automated>
  </verify>
  <done>Clear all wired with D-14 disable triple + T-13-03 confirmation; Plan 07 can wrap this content in Tabs without conflict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/tests/toolbar-clear.spec.ts — Playwright e2e covering D-14 disable + T-13-03 warning + clearFiles fire (CLR-01)</name>
  <files>src/tests/toolbar-clear.spec.ts</files>
  <read_first>
    - src/tests/status-bar.spec.ts (entire — closest analog; uses `await page.goto('/')` + `ingestFixtureFiles(page, N)` helper at line 8)
    - src/tests/file-row-menu.spec.ts (entire — injectEntries helper at lines 34-72 + aria-disabled+title assertion shape at lines 154-167; mirror this for the Clear all disable assertion)
    - src/tests/fixtures/ingest-helper.ts (verify it exists — `ingestFixtureFiles` helper used by status-bar.spec.ts:8)
    - src/components/shell/Toolbar.tsx (Task 1 output — confirm the Clear all button is reachable via `page.getByRole('button', { name: 'Clear all' })` and the Settings popover trigger is reachable via `page.getByRole('button', { name: 'Open settings' })` per line 257)
    - .planning/phases/13-diagnostics-clear-queue/13-VALIDATION.md task 13-06-toolbar-clear row
  </read_first>
  <behavior>
    - Test file uses `import { test, expect } from '@playwright/test'`
    - Test 1 (D-14 disable when empty queue): no entries injected; open Settings popover via `page.getByRole('button', { name: 'Open settings' }).click()`; `const clearBtn = page.getByRole('button', { name: 'Clear all' })`; assert `await expect(clearBtn).toHaveAttribute('aria-disabled', 'true')` AND `await expect(clearBtn).toHaveAttribute('title', 'No files to clear')`
    - Test 2 (idle path — runningJobs===0, entries exist): inject 3 entries via injectEntries (status='queued', runningJobs stays 0); open Settings; click Clear all; assert that after the click, the file count in StatusBar (or the FilesPane file count via `page.getByTestId('status-filecount')` per StatusBar.tsx line 61) is `0 files`. The popover closes (setOpen(null)) — assert the popover trigger no longer has `aria-expanded='true'`.
    - Test 3 (warning path — runningJobs > 0): inject 3 entries; AFTER injecting entries, use `page.evaluate(() => { window.__setRunningJobs?.(2) })` OR (more robust) inject via window-side helper that calls `runtimeAtom.setKey('runningJobs', 2)`. If no such helper exists, use page.evaluate with a direct import: `await page.evaluate(async () => { const m = await import('/src/stores/runtime.ts'); m.setJobCounts(2, 0); })` — the existing `setJobCounts` setter is exported from runtime.ts. Open Settings; click Clear all; assert sonner toast appears containing `Cancel 2 in-flight jobs?` (use `await expect(page.getByText(/Cancel 2 in-flight jobs/)).toBeVisible()`); assert the file count is still 3 (Clear all did NOT fire yet); click the `Clear anyway` action button in the toast (`await page.getByRole('button', { name: 'Clear anyway' }).click()`); assert file count drops to 0
    - Test infrastructure: reuse `injectEntries` helper (extract or inline copy from file-row-menu.spec.ts:34-72); reuse `ingestFixtureFiles` if simpler for Test 2's "entries exist" setup
    - Latching: for Test 3 use `await expect(...).toBeVisible()` not `page.waitForTimeout` (latch on transient states per CLAUDE.md MEMORY)
  </behavior>
  <action>
    Create `src/tests/toolbar-clear.spec.ts`. Header: "Phase 13 — CLR-01 / D-14 / T-13-03: Toolbar Clear all disable + warning-toast e2e". Imports: `import { test, expect, type Page } from '@playwright/test'`. Either inline copy `injectEntries` from `file-row-menu.spec.ts:34-72` (cleaner) or import the existing `ingestFixtureFiles` helper. Single `test.describe('Toolbar — Clear all (D-14 + T-13-03)', () => { ... })`. Test 1 body: `await page.goto('/')`; click Settings trigger; `const clearBtn = page.getByRole('button', { name: /^Clear all$/ })`; `await expect(clearBtn).toHaveAttribute('aria-disabled', 'true')`; `await expect(clearBtn).toHaveAttribute('title', 'No files to clear')`. Test 2 body: `await page.goto('/')`; `await injectEntries(page, [{ id:'a', name:'a.png', target:'webp' }, { id:'b', name:'b.png', target:'webp' }, { id:'c', name:'c.png', target:'webp' }])`; verify StatusBar shows `3 files` via `await expect(page.getByTestId('status-filecount')).toHaveText('3 files')`; click Settings; click Clear all (`await page.getByRole('button', { name: /^Clear all$/ }).click()`); `await expect(page.getByTestId('status-filecount')).toHaveText('0 files')`. Test 3 body: same injectEntries setup; then `await page.evaluate(async () => { const m = await import('/src/stores/runtime.ts'); m.setJobCounts(2, 0); })` — sets runningJobs to 2 via the existing setter; click Settings; click Clear all; `await expect(page.getByText(/Cancel 2 in-flight jobs/)).toBeVisible()`; `await expect(page.getByTestId('status-filecount')).toHaveText('3 files')` (NOT cleared yet); `await page.getByRole('button', { name: /^Clear anyway$/ }).click()`; `await expect(page.getByTestId('status-filecount')).toHaveText('0 files')` (now cleared via the toast action). Per CLAUDE.md MEMORY, the `/src/...` page.evaluate dynamic-import pattern is an accepted Vite pattern.
  </action>
  <acceptance_criteria>
    - `src/tests/toolbar-clear.spec.ts` exists
    - `npx playwright test src/tests/toolbar-clear.spec.ts --reporter=dot` exits 0
    - File contains assertions on `aria-disabled='true'` AND `title='No files to clear'` (Test 1 — D-14)
    - File contains `Clear anyway` literal (Test 3 — T-13-03 toast action button selector)
    - File contains `/Cancel 2 in-flight jobs/` or `Cancel 2 in-flight jobs?` literal (Test 3 — toast message assertion)
    - File contains `setJobCounts(2, 0)` literal (Test 3 — forces runningJobs>0 via existing exported setter)
    - All three tests pass (D-14 disable, idle-path fire, in-flight warning)
  </acceptance_criteria>
  <verify>
    <automated>test -f src/tests/toolbar-clear.spec.ts && grep -q "Clear anyway" src/tests/toolbar-clear.spec.ts && grep -q "setJobCounts" src/tests/toolbar-clear.spec.ts && npx playwright test src/tests/toolbar-clear.spec.ts --reporter=dot</automated>
  </verify>
  <done>Three-test e2e proves D-14 disable + T-13-03 warning + clearFiles fire; 13-06 row in VALIDATION.md flips green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM click → `handleClearAll` | Trusted UI event; queueEmpty disable triple gates at the DOM layer |
| `runtimeAtom.get().runningJobs` → confirmation branch | Trusted internal state read; not user-controlled |
| `toast.warning(...).action.onClick` → `clearFiles()` | User confirms via the toast; deferred call respects user intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Accidental queue wipe via Clear all mid-batch | Toolbar Clear all onClick | mitigate | Task 1 `handleClearAll` checks `runtimeAtom.get().runningJobs > 0` and routes to a `toast.warning` with explicit `Clear anyway` action — single-click confirmation. Test 3 asserts the warning path. PATTERNS lines 467-482 verbatim shape. |
| (DOM-tampering bypass of disabled state) | Tampering | Disabled-then-explain triple | mitigate | Defense-in-depth: even if a user re-enables the button via DevTools, `clearFiles()` is idempotent on empty entries (`setKey('entries', [])` is a no-op when already empty). No data leak. |
| T-13-01 | Information Disclosure | (no diagnostic surface in this plan) | n/a | Plan 07 owns Diagnostics tab disclosure |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx playwright test src/tests/toolbar-clear.spec.ts --reporter=dot` exits 0
- `grep -c "Workers: 4 (auto)" src/components/shell/Toolbar.tsx` returns 1 (existing button preserved)
- `grep -c "Clear all" src/components/shell/Toolbar.tsx` returns ≥ 1 (new button label present)
- `grep -q "toast.warning" src/components/shell/Toolbar.tsx` (T-13-03 mitigation present)
</verification>

<success_criteria>
- D-14 met (Clear all in Settings popover with disable-then-explain triple)
- T-13-03 mitigation in place
- VALIDATION.md row `13-06-toolbar-clear` flips ⬜ → ✅
- Plan 07 (Settings Tabs) can wrap the popover content without re-implementing Clear all
</success_criteria>

<output>
Create `.planning/phases/13-diagnostics-clear-queue/13-05-SUMMARY.md` when done.
</output>
