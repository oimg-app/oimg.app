---
phase: 04-inspector-codec-svgo
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/panels/InspectorPane.tsx
autonomous: true
requirements: [INSP-01]
must_haves:
  truths:
    - "InspectorPane renders a pane header with 'INSPECTOR' label and tab bar"
    - "Clicking Codec/SVGO/Output/Report tabs calls setTab and updates uiAtom.tab"
    - "When a SVG file is selected, tab auto-switches to 'svgo' via useEffect"
    - "When a non-SVG file is selected while tab='svgo', tab auto-switches to 'codec'"
    - "Output and Report tabs render stub placeholders without crashing"
    - "Empty state renders 'Select a file to adjust settings' when no file is selected"
  artifacts:
    - path: "src/components/panels/InspectorPane.tsx"
      provides: "InspectorPane shell (INSP-01): header + tab bar + auto-switch + CodecPanel/SvgoPanel routing"
      contains: "export function InspectorPane"
      min_lines: 50
  key_links:
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/stores/ui.ts"
      via: "useStore(uiAtom) + setTab"
      pattern: "setTab"
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/stores/files.ts"
      via: "useStore($selectedFile)"
      pattern: "selectedFile"
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/components/panels/inspector/CodecPanel"
      via: "TabsContent value='codec'"
      pattern: "CodecPanel"
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/components/panels/inspector/SvgoPanel"
      via: "TabsContent value='svgo'"
      pattern: "SvgoPanel"
---

<objective>
Implement InspectorPane shell (INSP-01): the 32px pane header, the 4-tab bar (Codec/SVGO/Output/Report), and the tab auto-switch useEffect. Tab state lives in uiAtom.tab. CodecPanel and SvgoPanel stubs from Plan 01 make the imports resolve; Plan 03 and 04 fill in their content.

Purpose: INSP-01 is the outer shell that gates all inspector interactions. Without it, CodecPanel and SvgoPanel have no mounting point.
Output: Updated `src/components/panels/InspectorPane.tsx` replacing the Phase 1 stub (13-line placeholder) with the full INSP-01 shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inspector-codec-svgo/04-RESEARCH.md
@.planning/phases/04-inspector-codec-svgo/04-PATTERNS.md
@.planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase and Plan 01. -->

From src/stores/ui.ts (already exists):
```typescript
export type Tab = 'codec' | 'svgo' | 'output' | 'report'
export const uiAtom = map<UiState>({ tab: 'codec', ... })
export function setTab(t: Tab): void { uiAtom.setKey('tab', t) }
```

From src/stores/files.ts (already exists):
```typescript
export const $selectedFile = computed(filesAtom, (s) => s.entries.find(f => f.id === s.selectedId) ?? null)
// FileEntry.type is 'svg' | 'png' | 'webp' | 'jpeg' | 'avif' (lowercase)
```

From src/components/ui/tabs.tsx (Shadcn — already generated):
- Controlled via value + onValueChange props
- Use: Tabs, TabsList, TabsTrigger, TabsContent
- TabsTrigger value must match Tab type values

From Plan 01 outputs (now exist at these paths):
- src/components/panels/inspector/CodecPanel.tsx — stub export (filled in Plan 03)
- src/components/panels/inspector/SvgoPanel.tsx — stub export (filled in Plan 04)

From src/components/shell/Toolbar/Toolbar.tsx (analog — useStore pattern):
```typescript
import { useStore } from '@nanostores/react'
const ui = useStore(uiAtom)
```

Tab auto-switch dep array rule (from RESEARCH.md Pattern 2):
- deps: [selectedFile?.id, selectedFile?.type]
- Do NOT include `tab` in deps — causes infinite re-render loop
- Guard: if (!selectedFile) return early
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InspectorPane shell — header + tab bar + auto-switch (INSP-01)</name>
  <files>src/components/panels/InspectorPane.tsx</files>
  <read_first>
    - src/components/panels/InspectorPane.tsx (read current stub — replace entirely)
    - src/components/shell/Toolbar/Toolbar.tsx (useStore pattern + import structure to mirror)
    - src/stores/ui.ts (Tab type + setTab action + uiAtom shape)
    - src/stores/files.ts ($selectedFile computed export)
    - src/components/ui/tabs.tsx (Shadcn Tabs API — value/onValueChange/TabsList/TabsTrigger/TabsContent)
    - .planning/phases/04-inspector-codec-svgo/04-PATTERNS.md (InspectorPane pattern section — imports, useEffect, Tabs wiring)
    - .planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md (Layout Contract: pane header 32px h-8, tab bar bg-1 border-b px-2, tab trigger py-2.5 px-2.5 mono 11px/400 all-caps, active = fg-0 font-semibold border-b-2 accent)
  </read_first>
  <behavior>
    - Clicking "CODEC" tab → setTab('codec') called, tab bar shows CODEC as active
    - Clicking "SVGO" tab → setTab('svgo') called
    - Clicking "OUTPUT" tab → setTab('output') called
    - Clicking "REPORT" tab → setTab('report') called
    - FileEntry with type='svg' selected → useEffect fires → setTab('svgo')
    - FileEntry with type='png' selected while tab='svgo' → useEffect fires → setTab('codec')
    - FileEntry with type='png' selected while tab='output' → tab stays 'output' (no switch)
    - No file selected → renders "Select a file to adjust settings" in pane body
  </behavior>
  <action>
    Replace the existing stub in `src/components/panels/InspectorPane.tsx` entirely. Attribution header on line 1: `// Phase 04 — INSP-01: InspectorPane shell + tab bar + auto-switch. Source: 04-02-PLAN.md`.

    Imports: useEffect from 'react'; useStore from '@nanostores/react'; uiAtom + setTab from '@/stores/ui'; $selectedFile from '@/stores/files'; Tabs + TabsList + TabsTrigger + TabsContent from '@/components/ui/tabs'; CodecPanel from './inspector/CodecPanel'; SvgoPanel from './inspector/SvgoPanel'. Import Tab type from '@/stores/ui'.

    Component body: const { tab } = useStore(uiAtom); const selectedFile = useStore($selectedFile).

    useEffect with dep array [selectedFile?.id, selectedFile?.type]: if (!selectedFile) return; if (selectedFile.type === 'svg') setTab('svgo'); else if (tab === 'svgo') setTab('codec'). Do NOT include tab in the dep array — this causes an infinite loop.

    Pane header JSX: outer div with classes "flex flex-col h-full bg-[var(--color-bg-0)]". Inside, a header div "flex items-center justify-between h-8 px-3.5 border-b border-[var(--color-line)] shrink-0 bg-[var(--color-bg-1)]" containing a span "font-mono text-[11px] font-semibold text-[var(--color-fg-2)] uppercase tracking-wider" with text "INSPECTOR".

    Tab bar via Shadcn Tabs controlled: value={tab} onValueChange={(v) => setTab(v as Tab)}. TabsList uses className "w-full rounded-none bg-[var(--color-bg-1)] border-b border-[var(--color-line)] px-2 h-auto justify-start gap-0". Each TabsTrigger uses className "rounded-none py-2.5 px-2.5 font-mono text-[11px] tracking-wider uppercase data-[state=active]:font-semibold data-[state=active]:text-[var(--color-fg-0)] data-[state=active]:border-b-2 data-[state=active]:border-[var(--color-accent)] text-[var(--color-fg-2)] data-[state=active]:bg-transparent".

    Pane body: div with classes "flex-1 overflow-y-auto pb-20". TabsContent for 'codec': renders CodecPanel. TabsContent for 'svgo': renders SvgoPanel. TabsContent for 'output': div "p-3 text-xs text-[var(--color-fg-3)]" with text "Output — coming in Phase 6". TabsContent for 'report': same pattern with "Report — coming in Phase 6". When no selectedFile: render div "flex items-center justify-center h-full text-[11px] font-mono text-[var(--color-fg-3)]" with text "Select a file to adjust settings" inside the pane body instead of the Tabs tree (conditional render: if (!selectedFile) show empty state, else show Tabs).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "InspectorPane" || echo "no type errors in InspectorPane"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function InspectorPane" src/components/panels/InspectorPane.tsx` returns a match.
    - `grep -n "useEffect" src/components/panels/InspectorPane.tsx` returns a match.
    - `grep -n "selectedFile?.id, selectedFile?.type" src/components/panels/InspectorPane.tsx` returns a match (correct dep array — no tab).
    - `grep -n "setTab('svgo')" src/components/panels/InspectorPane.tsx` returns a match (auto-switch SVG branch).
    - `grep -n "setTab('codec')" src/components/panels/InspectorPane.tsx` returns a match (auto-switch non-SVG branch).
    - `grep -n "CodecPanel" src/components/panels/InspectorPane.tsx` returns a match (tab content wiring).
    - `grep -n "SvgoPanel" src/components/panels/InspectorPane.tsx` returns a match.
    - `grep -n "Select a file to adjust settings" src/components/panels/InspectorPane.tsx` returns a match (empty state).
    - `npx tsc --noEmit -p tsconfig.app.json` reports zero errors referencing InspectorPane.tsx.
    - File contains `// Phase 04` attribution header on line 1.
  </acceptance_criteria>
  <done>InspectorPane renders header + 4-tab bar; clicking tabs calls setTab; auto-switch useEffect present with correct dep array; CodecPanel/SvgoPanel wired into tab content; empty state renders when no file selected; no TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uiAtom.tab → TabsTrigger | Tab value cast from string to Tab type — cast is safe because Shadcn Tabs only emits values provided to TabsTrigger |
| $selectedFile.type → setTab | FileEntry.type from stub-data.ts is a known union; no user-controlled input crosses this boundary this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | useEffect dep array | mitigate | Dep array is [selectedFile?.id, selectedFile?.type] — explicitly excludes `tab` to prevent infinite re-render loop; code review acceptance criterion enforces this |
| T-04-05 | Denial of Service | Tab auto-switch loop | mitigate | useEffect guard `if (!selectedFile) return` prevents null-deref; dep array exclusion of `tab` prevents loop |
| T-04-06 | Tampering | npm/pip/cargo installs | accept | No new packages this plan — all dependencies already present |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` → zero errors
- `grep -n "selectedFile?.id, selectedFile?.type" src/components/panels/InspectorPane.tsx` → match (correct dep array)
- `grep -n "setTab('svgo')" src/components/panels/InspectorPane.tsx` → match
- `grep -n "CodecPanel\|SvgoPanel" src/components/panels/InspectorPane.tsx` → both match
- `node --experimental-strip-types src/tests/settings.test.ts` → still green (no regression)
</verification>

<success_criteria>
- INSP-01 satisfied: InspectorPane header + tab bar render; tab clicks call setTab; auto-switch fires on selectedFile.type changes; empty state shows when no file.
- CodecPanel and SvgoPanel stubs from Plan 01 mount inside their TabsContent without errors.
- Zero TypeScript errors in InspectorPane.tsx.
- Circular ESM guard intact: InspectorPane does NOT import settingsAtom directly (it uses CodecPanel/SvgoPanel as children).
</success_criteria>

<output>
Create `.planning/phases/04-inspector-codec-svgo/04-02-SUMMARY.md` when done
</output>
