---
phase: 04-inspector-codec-svgo
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/panels/inspector/SvgoPanel.tsx
autonomous: true
requirements: [INSP-06]
must_haves:
  truths:
    - "SvgoPanel renders a 'SVGO preset' Section with accent badge 'preset-default'"
    - "Aggressive mode Switch reads settingsAtom.aggressive and calls setAggressive"
    - "Aggressive subtext renders below the toggle"
    - "Plugins Section title shows dynamic count 'Plugins · N / 22'"
    - "All 22 plugins render as clickable rows; clicking calls togglePlugin(p.id)"
    - "Plugin row shows on/off visual state: accent styles when on, fg-3/strikethrough when off"
    - "Plugin saves percentage shows in accent color when on, fg-3 when off"
  artifacts:
    - path: "src/components/panels/inspector/SvgoPanel.tsx"
      provides: "Full SvgoPanel (INSP-06) — replaces Plan 01 stub"
      contains: "export function SvgoPanel"
      min_lines: 60
  key_links:
    - from: "src/components/panels/inspector/SvgoPanel.tsx"
      to: "src/stores/settings.ts"
      via: "useStore(settingsAtom) + setAggressive + togglePlugin"
      pattern: "settingsAtom"
    - from: "src/components/panels/inspector/SvgoPanel.tsx"
      to: "src/components/panels/inspector/Section"
      via: "Section with acc badge for SVGO preset + dynamic title for Plugins"
      pattern: "Section"
---

<objective>
Implement SvgoPanel (INSP-06): the two-section SVGO configuration panel. Replaces the Plan 01 stub with the full implementation. Runs in Wave 2 parallel with Plans 02 and 03 because it depends only on Plan 01 outputs and modifies only SvgoPanel.tsx.

Purpose: SvgoPanel is the primary tool for SVG-workflow users. An SVG file auto-switches the tab to 'svgo', so this panel is the first thing SVG users see.
Output: `src/components/panels/inspector/SvgoPanel.tsx` with aggressive toggle + 22-plugin list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inspector-codec-svgo/04-RESEARCH.md
@.planning/phases/04-inspector-codec-svgo/04-PATTERNS.md
@.planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from Plan 01 outputs and codebase. -->

From src/stores/settings.ts (created by Plan 01):
```typescript
export const settingsAtom = map<SettingsState>({ aggressive: false, plugins: SvgoPlugin[], ... })
export type SvgoPlugin = { id: string; on: boolean; saves: string }
export function setAggressive(v: boolean): void
export function togglePlugin(id: string): void
// togglePlugin flips only the matching plugin's `on` flag immutably
```

From src/lib/stub-data.ts (SVGO_PLUGINS seeded into settingsAtom.plugins — 22 entries):
- Each plugin has: id (e.g. 'removeDoctype'), on: boolean, saves: string (e.g. '~3%')

From src/components/panels/inspector/Section.tsx (Plan 01):
```typescript
export function Section({ title, badge, children }: {
  title: string; badge?: { text: string; acc?: boolean }; children: React.ReactNode
})
// badge.acc=true → accent-dim bg + accent text (per UI-SPEC)
```

From src/components/ui/switch.tsx (Shadcn):
- Props: checked: boolean, onCheckedChange: (checked: boolean) => void

Plugin list layout (from UI-SPEC INSP-06):
- Vertical list, NOT 2-column grid
- Each row: grid grid-cols-[16px_1fr_auto] gap-2 items-center py-1.5 px-1 rounded hover:bg-[var(--color-bg-1)]
- Check box: 13×13px square, border, rounded-[3px]. When on: bg-accent + accent border. When off: bg-[var(--color-bg-1)] + color-line-strong border.
- Plugin name: font-mono text-[11px]. When on: text-[var(--color-fg-0)]. When off: text-[var(--color-fg-3)] line-through.
- Saves: font-mono text-[11px] font-semibold tabular-nums. When on: text-[var(--color-accent)]. When off: text-[var(--color-fg-3)].
- Full row is the click target (not just the checkbox). cursor-default.

INSP-06 copywriting (from UI-SPEC):
- Section 1 title: "SVGO preset"
- Section 1 badge: { text: 'preset-default', acc: true }
- Aggressive toggle label: "Aggressive mode"
- Aggressive subtext: "warns when fidelity drops >10% (butteraugli)"
- Section 2 title: dynamic "Plugins · {onCount} / {plugins.length}"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SvgoPanel — aggressive toggle + plugin list (INSP-06)</name>
  <files>src/components/panels/inspector/SvgoPanel.tsx</files>
  <read_first>
    - src/components/panels/inspector/SvgoPanel.tsx (read current stub from Plan 01 — replace entirely)
    - src/components/panels/FilesPane.tsx (list render + useStore pattern analog)
    - src/stores/settings.ts (verify settingsAtom.plugins shape + togglePlugin + setAggressive exports)
    - src/lib/stub-data.ts (verify SVGO_PLUGINS entries — id, on, saves fields)
    - src/components/ui/switch.tsx (checked + onCheckedChange API)
    - .planning/phases/04-inspector-codec-svgo/04-PATTERNS.md (SvgoPanel imports pattern, store read + list map pattern, plugin grid layout — NOTE: UI-SPEC overrides PATTERNS.md: use vertical list not 2-column grid)
    - .planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md (INSP-06 interaction contract — plugin row layout, check box visual spec, name/saves color rules, aggressive subtext copy)
  </read_first>
  <behavior>
    - settingsAtom.aggressive=false → Switch unchecked; toggle → setAggressive(true) called
    - Plugin list renders 22 rows (all from settings.plugins)
    - Plugin 'removeDoctype' has on=true by default → row shows accent styles (check bg-accent, name text-fg-0, saves text-accent)
    - Clicking plugin row → togglePlugin(p.id) called → that plugin's on flips; others unchanged
    - Section 2 title shows "Plugins · N / 22" where N is count of plugins with on=true
    - Plugin with on=false → check box has bg-[var(--color-bg-1)], name has line-through class and text-fg-3, saves text-fg-3
    - Aggressive subtext "warns when fidelity drops >10% (butteraugli)" renders below the toggle row
  </behavior>
  <action>
    Replace `src/components/panels/inspector/SvgoPanel.tsx` entirely. Attribution header line 1: `// Phase 04 — INSP-06: SvgoPanel aggressive toggle + plugin list. Source: 04-04-PLAN.md`.

    Imports: useStore from '@nanostores/react'; settingsAtom + setAggressive + togglePlugin from '@/stores/settings'; Switch from '@/components/ui/switch'; cn from '@/lib/utils'; Section from './Section'.

    Export function SvgoPanel(): reads const settings = useStore(settingsAtom). Compute: const onCount = settings.plugins.filter(p => p.on).length.

    Section 1 — SVGO preset: title="SVGO preset" badge={{ text: 'preset-default', acc: true }}.
    Inside Section 1:
    Aggressive toggle row: div "flex items-center justify-between" containing span "text-[12px] text-[var(--color-fg-2)]" "Aggressive mode" and Switch checked={settings.aggressive} onCheckedChange={setAggressive}.
    Aggressive subtext: p "text-[10px] font-mono text-[var(--color-fg-3)] mt-1 leading-[1.5]" with text "warns when fidelity drops >10% (butteraugli)".

    Section 2 — Plugins: title={`Plugins · ${onCount} / ${settings.plugins.length}`} (no badge).
    Inside Section 2: div "flex flex-col" mapping settings.plugins to plugin rows.
    Each plugin row (button element): type="button", key={p.id}, onClick={() => togglePlugin(p.id)}, className "grid grid-cols-[16px_1fr_auto] gap-2 items-center py-1.5 px-1 rounded cursor-default hover:bg-[var(--color-bg-1)] w-full text-left transition-colors".

    Check box div (col 1): size 13×13px via className "w-[13px] h-[13px] rounded-[3px] flex items-center justify-center shrink-0 border transition-colors". When p.on: "bg-[var(--color-accent)] border-[var(--color-accent)]"; when off: "bg-[var(--color-bg-1)] border-[var(--color-line-strong)]". Checkmark when on: small svg (viewBox "0 0 10 10", path d="M2 5l2.5 2.5L8 3", stroke="var(--color-accent-fg)" strokeWidth="1.5" fill="none" strokeLinecap="round" strokeLinejoin="round").

    Plugin name span (col 2): className with cn "font-mono text-[11px] truncate" + (p.on ? "text-[var(--color-fg-0)]" : "text-[var(--color-fg-3)] line-through"). Text: {p.id}.

    Saves span (col 3): className with cn "font-mono text-[11px] font-semibold tabular-nums shrink-0" + (p.on ? "text-[var(--color-accent)]" : "text-[var(--color-fg-3)]"). Text: {p.saves}.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "SvgoPanel" || echo "no type errors in SvgoPanel"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function SvgoPanel" src/components/panels/inspector/SvgoPanel.tsx` returns a match and the function body is NOT the stub (more than 5 lines).
    - `grep -n "togglePlugin" src/components/panels/inspector/SvgoPanel.tsx` returns a match (plugin rows call togglePlugin).
    - `grep -n "setAggressive" src/components/panels/inspector/SvgoPanel.tsx` returns a match.
    - `grep -n "onCount" src/components/panels/inspector/SvgoPanel.tsx` returns a match (dynamic count computed).
    - `grep -n "preset-default" src/components/panels/inspector/SvgoPanel.tsx` returns a match (Section 1 badge text).
    - `grep -n "line-through" src/components/panels/inspector/SvgoPanel.tsx` returns a match (off-plugin name style).
    - `grep -n "butteraugli" src/components/panels/inspector/SvgoPanel.tsx` returns a match (aggressive subtext copy).
    - `grep -n "color-accent" src/components/panels/inspector/SvgoPanel.tsx` returns at least 3 matches (check bg, saves, name).
    - `wc -l src/components/panels/inspector/SvgoPanel.tsx | awk '{print $1}'` returns at least 60.
    - `npx tsc --noEmit -p tsconfig.app.json` reports zero errors referencing SvgoPanel.tsx.
    - File does NOT contain "SVGO — pending" (the Plan 01 stub text is gone).
  </acceptance_criteria>
  <done>SvgoPanel renders aggressive Switch with subtext and 22 plugin rows; clicking a plugin row calls togglePlugin; on/off visual states are applied per UI-SPEC; Section 1 badge is accent-styled; Section 2 title shows dynamic on-count; no TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settingsAtom.plugins → render | Plugin list originates from SVGO_PLUGINS in stub-data.ts (static, in-bundle); no user or network input crosses this boundary this phase |
| togglePlugin(p.id) click handler | p.id is from the same static constant — no user-controlled string is passed to togglePlugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | Plugin id injection via click | accept | p.id comes from static SVGO_PLUGINS constant seeded at app init; togglePlugin does a pure .map() compare — no eval, no DOM, no network |
| T-04-11 | Information Disclosure | Plugin saves/id exposure | accept | Plugin metadata is public (SVGO docs); no PII involved |
| T-04-12 | Tampering | settingsAtom.plugins mutation | mitigate | togglePlugin uses immutable map (spread pattern); SVGO_PLUGINS constant in stub-data is never mutated — Plan 01 acceptance criterion enforces this (T-04-01 in Plan 01 threat model) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` → zero errors
- `grep -n "SVGO — pending" src/components/panels/inspector/SvgoPanel.tsx` → empty (stub removed)
- `grep -c "togglePlugin\|setAggressive" src/components/panels/inspector/SvgoPanel.tsx` → at least 2
- `node --experimental-strip-types src/tests/settings.test.ts` → still green (no regression)
</verification>

<success_criteria>
- INSP-06 satisfied: SvgoPanel renders "SVGO preset" Section with accent badge; aggressive Switch wired; Plugins Section title is dynamic; all 22 plugin rows render with on/off visual states; clicking calls togglePlugin.
- The Plan 01 stub text "SVGO — pending" is replaced with the full implementation.
- Zero TypeScript errors in SvgoPanel.tsx.
</success_criteria>

<output>
Create `.planning/phases/04-inspector-codec-svgo/04-04-SUMMARY.md` when done
</output>
