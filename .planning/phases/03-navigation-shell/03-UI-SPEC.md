---
phase: 3
slug: navigation-shell
status: draft
shadcn_initialized: true
preset: radix-lyra
created: 2026-05-17
---

# Phase 3 — Navigation Shell UI Design Contract

> Visual and interaction contract for TitleBar, Toolbar, StatusBar, and CommandPalette.
> Generated by gsd-ui-researcher. All values pre-populated from locked upstream artifacts.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn | components.json — FOUND |
| Preset | radix-lyra | components.json `.style` |
| Component library | Radix UI (via Shadcn) | components.json |
| Icon library | @phosphor-icons/react | components.json `.iconLibrary: "phosphor"` + ICON-01 |
| Font — sans | Inter Variable | src/index.css `:root --font-sans` |
| Font — mono | JetBrains Mono Variable | src/index.css `:root --font-mono` |
| CSS variables | enabled | components.json `.tailwind.cssVariables: true` |
| Registry | shadcn official only | components.json `.registries: {}` |

**shadcn gate result:** `components.json` exists with `style: radix-lyra`. No initialization needed. No third-party registries declared. Registry safety gate: not applicable.

---

## Layout Grid — Chrome Heights

Source: `example-ui/OIMG.html` `.app { grid-template-rows: 36px 44px 1fr 22px }`

| Zone | Height | Component |
|------|--------|-----------|
| TitleBar | 36px | `src/components/shell/TitleBar/TitleBar.tsx` |
| Toolbar | 44px | `src/components/shell/Toolbar/Toolbar.tsx` |
| Work area | `1fr` (flex-grow) | ResizablePanelGroup (Phase 1+2) |
| StatusBar | 22px | `src/components/shell/StatusBar/StatusBar.tsx` |
| CommandPalette | modal overlay | always mounted, visibility from `uiAtom.cmdkOpen` |

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs | 4px | Pane header action gaps; dot gaps in brand mark |
| sm | 8px | TitleBar menu button padding (4px 8px); Toolbar gap |
| md | 12px | TitleBar horizontal padding; popover internal padding |
| lg | 16px | Section spacing inside popovers |
| xl | 24px | N/A — not used in chrome components |
| 2xl | 48px | N/A |
| 3xl | 64px | N/A |

Exceptions:
- TitleBar padding: `0 12px` (12px is 3×4, within the scale)
- Toolbar padding: `0 10px` — use `px-2.5` (10px, acceptable exception for dense toolbar)
- Toolbar button height: 28px — use `h-7`
- Toolbar gap: `gap-2` (8px)
- TitleBar menu button: `px-2 py-1` (8px / 4px)
- StatusBar height: 22px — fixed; use `h-[22px]`
- Brand mark accent square: 12×12px — use `w-3 h-3`
- Worker pip dot: 8×8px — use `w-2 h-2`
- Icon button touch target (pane header): 22×22px — use `w-[22px] h-[22px]`

---

## Typography

Source: `example-ui/OIMG.html` `html { font: 13px/1.45 var(--sans) }`

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body / default | 13px (`text-[13px]`) | 400 (regular) | 1.45 | Inter | StatusBar text, popover body |
| Label / UI | 12px (`text-xs`) | 500 (medium) | 1.4 | Inter | TitleBar menu items, Toolbar button labels, filter input |
| Label / mono | 11–12px (`text-[11px]`) | 600 (semibold) | 1.2 | JetBrains Mono | Pane headers (uppercase, `letter-spacing: 0.08em`); brand mark; Kbd hints |
| Small / meta | 11px (`text-[11px]`) | 400 (regular) | 1.4 | Inter | StatusBar version strings; CommandPalette group headers |

**Active weights: 400 (regular) + 500 (medium) + 600 (semibold)**

Note: the reference uses three weights. The executor may use 500 only where Inter's 400 and 500 are visually indistinct in this component (toolbar buttons). Do not add weight 700.

---

## Color

Source: `src/index.css` — OIMG oklch tokens, both themes. **All values are CSS custom properties; do not hardcode hex.**

| Role | CSS Token | Dark value | Light value | Usage |
|------|-----------|------------|-------------|-------|
| Dominant (60%) | `--color-bg-0` | `oklch(0.165 0.008 250)` | `oklch(0.985 0.003 250)` | Page background, pane bodies |
| Secondary chrome (30%) | `--color-bg-1` | `oklch(0.205 0.009 250)` | `oklch(0.965 0.004 250)` | TitleBar bg, Toolbar bg, StatusBar bg, pane headers |
| Raised surface | `--color-bg-2` | `oklch(0.235 0.009 250)` | `oklch(0.945 0.005 250)` | Toolbar button bg, seg control bg, search input bg, popovers |
| Hover surface | `--color-bg-3` | `oklch(0.275 0.010 250)` | `oklch(0.915 0.006 250)` | Button hover bg, active seg bg |
| Border | `--color-line` | `oklch(0.32 0.010 250)` | `oklch(0.86 0.008 250)` | All 1px separators, toolbar dividers |
| Border strong | `--color-line-strong` | `oklch(0.40 0.012 250)` | `oklch(0.74 0.010 250)` | Button hover borders |
| Foreground primary | `--color-fg-0` | `oklch(0.96 0.005 250)` | `oklch(0.18 0.010 250)` | Brand mark text, file name, active menu items |
| Foreground secondary | `--color-fg-1` | `oklch(0.78 0.008 250)` | `oklch(0.32 0.010 250)` | TitleBar menu labels, default icon color |
| Foreground muted | `--color-fg-2` | `oklch(0.58 0.010 250)` | `oklch(0.50 0.010 250)` | StatusBar text, pane header labels, search placeholder |
| Foreground faint | `--color-fg-3` | `oklch(0.42 0.012 250)` | `oklch(0.66 0.010 250)` | Seg labels unselected, format pill text |
| Accent (10%) | `--color-accent` | `oklch(0.80 0.17 145)` | `oklch(0.62 0.18 145)` | **Reserved — see below** |
| Accent dim | `--color-accent-dim` | `oklch(0.80 0.17 145 / 0.16)` | `oklch(0.62 0.18 145 / 0.14)` | Accent tint backgrounds |
| Accent foreground | `--color-accent-fg` | `oklch(0.18 0.05 145)` | `oklch(0.99 0 0)` | Text on accent-bg buttons |
| Warn | `--color-warn` | `oklch(0.82 0.15 75)` | `oklch(0.62 0.16 65)` | Savings badge <30%; do not use in Phase 3 components |
| Error / destructive | `--color-err` | `oklch(0.72 0.19 25)` | `oklch(0.58 0.20 25)` | Destructive action confirmation text |
| Info / running | `--color-info` | `oklch(0.78 0.13 235)` | `oklch(0.55 0.15 235)` | Worker pip "running" state dot color |

**Accent reserved for (explicit list — Phase 3 only):**
1. "Optimize all" primary Toolbar button background (`--color-accent` bg, `--color-accent-fg` text)
2. Brand mark accent square (`clip-path` polygon, `bg: var(--color-accent)`)
3. Worker pip idle state dot (accent = healthy/ready)
4. CommandPalette selected-item highlight row background (`--color-accent-dim`)
5. Focus ring on interactive elements (`ring: var(--color-accent)`)

**Theme switching:** All color tokens are CSS custom properties on `:root` (light) and `[data-theme="dark"]` (dark). The `<html>` `data-theme` attribute is set from `uiAtom.theme` via `useEffect` in `AppShell`. TitleBar and Toolbar theme toggle buttons only call `setTheme()` — they do not manipulate the DOM directly.

---

## Component Specifications

### TitleBar (NAV-01)

Height: 36px. Background: `--color-bg-1`. Bottom border: 1px `--color-line`.

**Brand mark:**
- Accent square: 12×12px, `clip-path: polygon(0 0, 100% 0, 100% 60%, 60% 100%, 0 100%)`, background `--color-accent`
- Wordmark: "OIMG · image optimizer" — font JetBrains Mono, weight 600, 11.5px, `letter-spacing: 0.04em`, color `--color-fg-0`

**Menu buttons (Codec / View / Help):**
- Font: Inter 12px weight 400
- Padding: `px-2 py-1`
- Border-radius: 4px
- Default color: `--color-fg-1`
- Hover: background `--color-bg-2`, color `--color-fg-0`
- Active (popover open for this key): background `--color-bg-2`, color `--color-fg-0`
- Menu open state driven by `uiAtom.open` — value is `"menu-codec"`, `"menu-view"`, or `"menu-help"`

**Menu popovers (Shadcn `Popover`):**
- Background: `--color-bg-2`
- Border: 1px `--color-line`
- Border-radius: 6px
- Box-shadow: `--shadow-elev`
- Internal padding: 8px
- Menu item: full-width, `px-3 py-1.5`, text 12px, color `--color-fg-0`; hover: background `--color-bg-3`
- Kbd hints inside items: JetBrains Mono 10px, padding `px-1.5 py-0.5`, border 1px `--color-line`, border-radius 3px, color `--color-fg-1`, bg `--color-bg-0`

**Right section (pills + search):**
- "100% local" + "Offline-ready" pills: 11px Inter, color `--color-fg-2`, no background (text only with separator)
- Search/⌘K button: ghost style (no border, transparent bg), icon `MagnifyingGlass` 14px, shortcut hint `⌘K` in Kbd

### Toolbar (NAV-02)

Height: 44px. Background: `--color-bg-1`. Bottom border: 1px `--color-line`. Padding: `px-2.5`. Gap: `gap-2`.

**Toolbar button (`.tbtn` equivalent):**
- Height: 28px (`h-7`)
- Padding: `px-3`
- Border: 1px `--color-line`
- Background: `--color-bg-2`
- Border-radius: 5px
- Font: Inter 500 12px
- Color: `--color-fg-0`
- Icon size: 13px (w-[13px] h-[13px])
- Hover: background `--color-bg-3`, border-color `--color-line-strong`

**Primary button (Optimize all):**
- Background: `--color-accent`
- Color: `--color-accent-fg`
- Border: none (`border-transparent`)
- Font-weight: 600
- Hover: `filter: brightness(1.05)` — use Tailwind `hover:brightness-105`

**Split buttons (Add / Export / Auto):**
- Main action + chevron-down icon in same visual group
- Separator between action and dropdown: 1px vertical `--color-line`
- Popover opens on chevron click (`setOpen("tb-add")` / `"tb-export"` / `"tb-auto"`)

**Ghost button (theme toggle, settings):**
- Background: transparent
- Border: none
- Hover: background `--color-bg-2`

**Segmented control (Batch / Compare / Report):**
- Height: 28px
- Border: 1px `--color-line`
- Border-radius: 5px
- Background: `--color-bg-2`
- Segment padding: `px-2.5`
- Font: Inter 500 11.5px
- Default color: `--color-fg-1`
- Active segment: background `--color-bg-3`, color `--color-fg-0`
- Segments separated by 1px `--color-line` vertical borders

**Toolbar divider:**
- Width: 1px, height: 18px, background: `--color-line`
- Margin: `mx-0.5`

**Filter search input:**
- Height: 28px (`h-7`)
- Min-width: 220px
- Background: `--color-bg-2`
- Border: 1px `--color-line`
- Border-radius: 5px
- Padding: `px-2 gap-[7px]`
- Icon: `MagnifyingGlass` 12px, color `--color-fg-2`
- Input text: 12px, color `--color-fg-0`, placeholder color `--color-fg-2`
- Focus: border-color `--color-accent`, ring `--color-accent` 1px

### StatusBar (NAV-03)

Height: 22px. Background: `--color-bg-1`. Top border: 1px `--color-line`. Padding: `px-3`.

Font: Inter 11px, color `--color-fg-2`. Mono values use JetBrains Mono.

Items (left-to-right, separated by `·` divider character or 1px vertical line):

1. **Worker pip:** 8×8px dot (`w-2 h-2`), border-radius 50%
   - Idle: background `--color-accent`
   - Running: background `--color-info`, `animate-pulse` (1.4s ease-in-out infinite)
   - Label: "Idle" or "Running"
2. **SVGO version:** `"SVGO 4.0.1"` — static string, JetBrains Mono
3. **Codec version:** `"@squoosh-kit/core 0.6.0"` — static string, JetBrains Mono
4. **WASM status:** `"WASM ready · 312 KB"` — static string
5. **File count:** N files from `$totals` — format `"{n} files"`
6. **Size summary:** orig → opt from `$totals` — format `"{fmtBytes(orig)} → {fmtBytes(opt)}"` using `fmtBytes` from `src/lib/format.ts`

### CommandPalette (NAV-04)

Implemented as Shadcn `Dialog` with full-screen backdrop. Always mounted; visibility driven by `uiAtom.cmdkOpen`.

**Modal backdrop:** `Dialog` component — background `oklch(0 0 0 / 0.50)`, `backdrop-blur-sm`

**Palette container:**
- Width: 560px (`w-[560px]`)
- Max-height: 400px
- Background: `--color-bg-1`
- Border: 1px `--color-line`
- Border-radius: 8px
- Box-shadow: `--shadow-elev`
- Position: centered (Dialog default)

**Search input:**
- Height: 48px (`h-12`)
- Padding: `px-4`
- Border-bottom: 1px `--color-line`
- Font: Inter 14px
- Color: `--color-fg-0`
- Placeholder: "Search commands…"
- Icon: `MagnifyingGlass` 16px, color `--color-fg-2`, left-padded

**Command list:**
- Max-height: 320px, overflow-y auto
- Group header: 10px Inter 600, uppercase, `letter-spacing: 0.06em`, color `--color-fg-3`, padding `px-4 py-2`
- Command item: `px-4 py-2`, 13px Inter, color `--color-fg-0`
  - Meta shortcut: 11px JetBrains Mono, color `--color-fg-2`, right-aligned
  - Selected state: background `--color-accent-dim`, left border 2px `--color-accent`
  - Hover: background `--color-bg-2`

**Footer:**
- Height: 32px
- Background: `--color-bg-0`
- Top border: 1px `--color-line`
- Padding: `px-4`
- Font: 11px Inter, color `--color-fg-3`
- Content: `↑↓ navigate · Enter select · Esc close` — use Kbd component for key hints

**Keyboard interactions:**
- `↑` / `↓`: calls `setCmdkSel(Math.max(0, sel - 1))` / `setCmdkSel(Math.min(len - 1, sel + 1))`
- `Enter`: fires `$cmdFlat[cmdkSel]?.do?.()` then `closeCmdk()`
- `Escape`: calls `closeCmdk()`
- `⌘K` / `Ctrl+K` (global, in `AppShell` `useEffect`): calls `openCmdk()`
- Event listener cleanup: `return () => window.removeEventListener('keydown', handler)` — mandatory

**Accessibility:**
- Dialog has `role="dialog"` + `aria-modal="true"` (Shadcn Dialog provides this)
- Search input has `aria-label="Search commands"`
- Command list has `role="listbox"`; each item `role="option"`, `aria-selected` reflects `cmdkSel`
- `aria-activedescendant` on list points to selected item id

---

## Theme Switching Contract

| Component | Responsibility |
|-----------|---------------|
| `AppShell` | `useEffect` subscribes to `uiAtom.theme`; sets `document.documentElement.dataset.theme` |
| `TitleBar` View menu | Renders theme toggle item; calls `setTheme('light' | 'dark')` |
| `Toolbar` theme toggle | Ghost icon button (Sun/Moon); calls `setTheme(...)` |
| CSS | All tokens respond to `[data-theme="dark"]` on `<html>` — already wired in `src/index.css` |

Do NOT use `next-themes` ThemeProvider. Do NOT hardcode `className="dark"` on AppShell after SHELL-03 is implemented.

---

## Interaction States — All Interactive Elements

| State | Visual treatment |
|-------|-----------------|
| Default | Base color as specified per component |
| Hover | background `--color-bg-3` (buttons) / `--color-bg-2` (ghost) |
| Active / pressed | same as hover (no separate press animation in reference) |
| Focus visible | `outline: 2px solid var(--color-accent)` at 2px offset (WCAG AA) |
| Disabled | `opacity-40`, `cursor-not-allowed` — not applicable in Phase 3 (all actions are stubs) |
| Loading / running | worker pip uses `animate-pulse`; Optimize button text unchanged (stub) |

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA | "Optimize all" | NAV-02 spec + `app.jsx` |
| Add files split-button | "Add files" | NAV-02 spec |
| Export split-button | "Export" | NAV-02 spec |
| Auto split-button | "Auto" | NAV-02 spec |
| Add submenu item 1 | "From device" | `app.jsx` |
| Add submenu item 2 | "Watch folder" | `app.jsx` |
| Add submenu item 3 | "From URL or paste" | `app.jsx` |
| Export submenu item 1 | "All as ZIP" | `app.jsx` |
| Export submenu item 2 | "Save individually" | `app.jsx` |
| Export submenu item 3 | "Copy `<picture>` HTML" | `app.jsx` |
| Export submenu item 4 | "Copy as data URIs" | `app.jsx` |
| Export submenu item 5 | "Manifest JSON" | `app.jsx` |
| TitleBar menu labels | "Codec" / "View" / "Help" | NAV-01 spec |
| View menu theme item | "Light theme" / "Dark theme" (toggle label) | `app.jsx` |
| Help submenu item 1 | "Documentation" | `app.jsx` |
| Help submenu item 2 | "Keyboard shortcuts" | `app.jsx` |
| Help submenu item 3 | "What's new" | `app.jsx` |
| Brand wordmark | "OIMG · image optimizer" | NAV-01 spec |
| Pill 1 | "100% local" | NAV-01 spec |
| Pill 2 | "Offline-ready" | NAV-01 spec |
| Search/⌘K button label (screen reader) | "Open command palette" | accessibility |
| CommandPalette placeholder | "Search commands…" | `app.jsx` |
| CommandPalette footer | "↑↓ navigate · Enter select · Esc close" | `app.jsx` |
| CommandPalette empty state | "No commands match" | default |
| StatusBar idle label | "Idle" | `app.jsx` |
| StatusBar running label | "Running" | `app.jsx` |
| Worker pip aria-label | "Worker status: Idle" / "Worker status: Running" | accessibility |
| Seg control labels | "Batch" / "Compare" / "Report" | NAV-02 spec |
| View seg aria-label | "Switch view" | accessibility |
| Filter input placeholder | "Filter files…" | `app.jsx` |
| Destructive actions | None in Phase 3 | — |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Popover, Dialog, Input, Kbd, Button, Separator, Tooltip | not required |
| third-party | none declared | not applicable |

`components.json` `.registries: {}` — no third-party registries. Vetting gate: not executed (nothing to vet).

---

## WCAG AA Compliance — Phase 3 Checklist

| Requirement | Implementation |
|-------------|---------------|
| Contrast 4.5:1 body text | `--color-fg-0` on `--color-bg-1` dark: ~13:1. Light: ~13:1. Passes. |
| Contrast 3:1 large text / UI | `--color-fg-2` on `--color-bg-1` dark: ~5:1. Passes. |
| Keyboard navigation | All interactive elements reachable by Tab; CommandPalette traps focus while open (Shadcn Dialog) |
| Focus visible | `outline: 2px solid var(--color-accent)` with 2px offset — not suppressed |
| ARIA | Dialog: `aria-modal`; listbox: `role="listbox"` + `aria-activedescendant`; seg control: `role="group"` + each button `role="radio"` |
| Motion | `animate-pulse` on worker pip — respects `prefers-reduced-motion` via Tailwind's `motion-safe:` prefix |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
