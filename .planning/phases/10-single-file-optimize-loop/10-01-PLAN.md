---
phase: 10-single-file-optimize-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/fixtures/ingest-helper.ts
  - src/tests/ingest.spec.ts
  - src/tests/inspector-tabs.spec.ts
  - src/tests/output-panel.spec.ts
  - src/tests/per-file-settings.spec.ts
  - src/tests/navigation.spec.ts
  - src/tests/backpressure.spec.ts
autonomous: true
requirements: [OPT-01]
must_haves:
  truths:
    - "App starts with zero entries on load (D-04 empty-start is asserted by an automated spec)"
    - "A shared Playwright helper injects real FileEntry objects with real bytes into filesAtom (D-05)"
    - "ingest.spec.ts greppable titles exist for OPT-01 SC-1/SC-2/SC-3, D-04 empty, and D-06/D-07 silent-skip"
    - "The 5 demo-dependent specs no longer rely on the removed seeded list (D-05)"
  artifacts:
    - path: "src/tests/fixtures/ingest-helper.ts"
      provides: "ingestFixtureFiles(page, n) store-injection helper (D-05)"
      contains: "ingestFixtureFiles"
    - path: "src/tests/ingest.spec.ts"
      provides: "OPT-01 SC-1/2/3 + D-04 + D-06/D-07 spec (greppable titles)"
      contains: "test.describe"
  key_links:
    - from: "src/tests/inspector-tabs.spec.ts"
      to: "src/tests/fixtures/ingest-helper.ts"
      via: "ingestFixtureFiles import + call before file selection"
      pattern: "ingestFixtureFiles"
---

<objective>
Wave 0 test scaffolding for Phase 10. Create the shared `ingestFixtureFiles(page, n)` Playwright helper, the new `ingest.spec.ts` covering OPT-01 SC-1/2/3 + D-04 empty-start + D-06/D-07 silent-skip, and migrate the 5 specs that relied on the seeded demo list off it (D-05).

These tests are written FIRST (Nyquist) so the implementation plans (02/03/04) have automated verification targets. Because the implementation that empties the seed (Plan 02) has not run yet, the new D-04 "empty-start" assertion and the SC-1/2/3 behavior tests are EXPECTED to fail at the end of this plan — that is the RED state. The 5 migrated specs must remain green (they inject their own fixtures and no longer depend on the seed, so they pass whether or not the seed is still present).

Purpose: Establish the validation contract before implementation; prevent the demo-removal (D-04) from silently breaking the suite.
Output: 1 new fixture helper, 1 new spec, 5 migrated specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-single-file-optimize-loop/10-VALIDATION.md
@.planning/phases/10-single-file-optimize-loop/10-RESEARCH.md

<interfaces>
<!-- Store + model contracts the fixture helper injects against. From codebase. -->
From src/stores/files.ts:
  export const filesAtom            // nanostores map<FilesState> { entries, selectedId, filterQuery, sortBy }
  export function selectFile(id: string): void
  export function setFileRawBuffer(id: string, rawBuffer: ArrayBuffer): void

From src/lib/stub-data.ts:
  export interface FileEntry { id; name; type; orig; opt; status; target; dim; q; createdAt?; settings?; rawBuffer?; encodedBuffer?; error? }
  export type FileStatus = 'done' | 'processing' | 'queued' | 'error'
  export function defaultFileSettings(type: string, q: number | null): FileSettings
  // TINY_PNG_B64 base64 constant (1x1 PNG) lives in stub-data.ts — reuse the same string in the helper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ingestFixtureFiles helper</name>
  <read_first>
    - src/tests/per-file-settings.spec.ts (the existing page.evaluate store-injection pattern — exact analog)
    - 10-PATTERNS.md § "src/tests/fixtures/ingest-helper.ts" (store injection pattern, lines 362-404)
    - 10-RESEARCH.md § "Test Fixture Strategy (D-05)" (recommended page.evaluate approach + TINY_PNG_B64)
    - src/lib/stub-data.ts (confirm TINY_PNG_B64 constant + defaultFileSettings signature)
  </read_first>
  <files>src/tests/fixtures/ingest-helper.ts</files>
  <action>
    Create `src/tests/fixtures/ingest-helper.ts` exporting `async function ingestFixtureFiles(page: Page, count = 1): Promise<void>`. Inside, call `page.evaluate(async (n) => {...}, count)`. In the evaluated browser context: dynamic-`import('/src/stores/files.ts')` to get `filesAtom` + `setFileRawBuffer`, and `import('/src/lib/stub-data.ts')` for `defaultFileSettings`. Reuse the SAME 1x1 `TINY_PNG_B64` base64 string used in stub-data.ts; decode it to an ArrayBuffer with an `atob` → `Uint8Array` loop. Build `n` entries with deterministic ids `fixture-${i}`, `name: \`fixture-${i}.png\``, `type: 'png'`, `orig`/`opt` = the decoded byteLength, `status: 'done'`, `target: 'png'`, `dim: '1×1'`, `q: 82`, `createdAt: Date.now() + i` (monotonic so queue-order sort is deterministic), `settings: defaultFileSettings('png', 82)`, and the rawBuffer. Set `filesAtom.setKey('entries', entries)`, `filesAtom.setKey('selectedId', entries[0].id)`, and loop `setFileRawBuffer(e.id, e.rawBuffer)` for each. Add the standard phase/plan attribution header comment. Do NOT inline any production logic here — this is a test fixture only.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "ingest-helper.ts" | grep -q "^0$" && echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>`ingest-helper.ts` exists, exports `ingestFixtureFiles`, typechecks clean with no errors referencing the file.</done>
</task>

<task type="auto">
  <name>Task 2: Create ingest.spec.ts (OPT-01 SC-1/2/3 + D-04 + D-06/D-07)</name>
  <read_first>
    - src/tests/inspector-tabs.spec.ts (goto + page.evaluate + getByTestId structure — analog)
    - 10-PATTERNS.md § "src/tests/ingest.spec.ts" (spec structure + greppable describe/test titles)
    - 10-VALIDATION.md § "Per-Task Verification Map" (the exact -g grep tokens: "drop", "Report", "re-optimize", "empty", "skip")
    - 10-RESEARCH.md § "Validation Architecture" (Req → Test map)
  </read_first>
  <files>src/tests/ingest.spec.ts</files>
  <action>
    Create `src/tests/ingest.spec.ts` with `test.describe('ingest — OPT-01 SC-1/2/3 + D-04 + D-06/D-07', ...)`. Write these tests with greppable titles matching the VALIDATION grep tokens exactly:
    (a) title containing "empty" — `page.goto('/')`, then page.evaluate read `filesAtom.get().entries.length`, `expect(count).toBe(0)` (D-04). This is EXPECTED-RED until Plan 02 empties the seed.
    (b) title containing "drop" — OPT-01 SC-1: use `page.setInputFiles` on the hidden file input (testid/selector to be added by Plan 04) OR drive `ingestFixtureFiles` as a stand-in; assert an entry appears in the queue and the selected file shows real optimized output (encodedBuffer present / opt < orig is NOT required at fixture-level — assert entry visible + selected). Mark the real-drop path with a TODO referencing Plan 04's hidden input testid so the executor wires the true selector once it exists.
    (c) title containing "Report" — OPT-01 SC-2: after ingest of a fixture, navigate to the Report panel and assert Before/After byte labels render from real `entry.orig`/`entry.opt` (assert the Report panel shows numeric byte values, not a stub placeholder).
    (d) title containing "re-optimize" — OPT-01 SC-3: ingest a fixture, change a setting via page.evaluate `setFileSettings`, assert the entry re-encodes (covered by Phase 9 useLiveEncode) — assert opt/encodedBuffer updates.
    (e) title containing "skip" — D-06/D-07: drive ingest with one accepted + one unsupported file (e.g. a `.txt`/`.gif`); assert only the accepted file is added and NO sonner toast appears (silent skip). Use `page.setInputFiles` with mixed files against the hidden input (TODO-marked until Plan 04 adds it) — at minimum assert the entries count reflects only accepted files.
    Import `ingestFixtureFiles` from `./fixtures/ingest-helper`. Add the phase/plan attribution header comment. It is acceptable for (a)-(e) behavior assertions to be RED at end of this plan where they depend on Plan 02/03/04 — the titles and structure must exist so later plans turn them green.
  </action>
  <verify>
    <automated>npx playwright test src/tests/ingest.spec.ts --list 2>&1 | grep -Eic "empty|drop|Report|re-optimize|skip" | awk '$1>=5{print "TITLES_PRESENT"; exit} {exit 1}'</automated>
  </verify>
  <done>`ingest.spec.ts` exists; `--list` shows all 5 greppable titles (empty, drop, Report, re-optimize, skip). Behavior assertions may be RED pending Plans 02-04.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the 5 demo-dependent specs off the seeded list (D-05)</name>
  <read_first>
    - src/tests/inspector-tabs.spec.ts, src/tests/output-panel.spec.ts, src/tests/per-file-settings.spec.ts, src/tests/navigation.spec.ts, src/tests/backpressure.spec.ts (the specs to migrate)
    - 10-PATTERNS.md § "Existing tests — D-05 migration pattern" (replace getByText('hero-banner@2x.png') → ingestFixtureFiles + getByText('fixture-0.png'); replace entries.length<2 guard with ingestFixtureFiles(page, 2))
    - 10-RESEARCH.md § "Specs to Update" table + Pitfall 7 (per-file-settings needs ≥2 entries)
  </read_first>
  <files>src/tests/inspector-tabs.spec.ts, src/tests/output-panel.spec.ts, src/tests/per-file-settings.spec.ts, src/tests/navigation.spec.ts, src/tests/backpressure.spec.ts</files>
  <action>
    Import `ingestFixtureFiles` from `./fixtures/ingest-helper` in each spec. In `inspector-tabs.spec.ts` and `output-panel.spec.ts`: after each `page.goto('/')` that precedes a `getByText('hero-banner@2x.png').click()`, insert `await ingestFixtureFiles(page, 1)` and replace the stub-name selector with `page.getByText('fixture-0.png')`. In `per-file-settings.spec.ts`: insert `await ingestFixtureFiles(page, 2)` before the page.evaluate that guards on `entries.length < 2` (Pitfall 7); keep the guard as a safety assertion. In `navigation.spec.ts` and `backpressure.spec.ts`: insert `await ingestFixtureFiles(page, 1)` after `page.goto('/')` before any "Optimize all"/running-state assertion so the running-state flip has ≥1 file to process (RESEARCH A3 notes these may pass empty, but the fixture makes the assertion meaningful and seed-independent). Do not change assertion logic beyond the selector/fixture swaps. Preserve each file's attribution header.
  </action>
  <verify>
    <automated>npx playwright test src/tests/inspector-tabs.spec.ts src/tests/output-panel.spec.ts src/tests/per-file-settings.spec.ts src/tests/navigation.spec.ts src/tests/backpressure.spec.ts --project=chromium 2>&1 | tail -5 | grep -Eq "passed" && echo "MIGRATED_GREEN"</automated>
  </verify>
  <done>All 5 migrated specs pass against the current (still-seeded) app — they now inject their own fixtures and are seed-independent, so they will stay green after Plan 02 empties the seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app store | Fixtures inject FileEntry objects directly via page.evaluate; not a production trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Test fixture injects synthetic entries | accept | Test-only code path; never shipped in production bundle (src/tests excluded from build). No runtime exposure. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase — Web Platform + existing deps only (per 10-RESEARCH § "No New Packages Required"). |
</threat_model>

<verification>
- `ingest-helper.ts` typechecks clean.
- `ingest.spec.ts --list` shows the 5 greppable titles.
- The 5 migrated specs pass (`--project=chromium`).
- Full suite (`npx playwright test`) has NO NEW failures beyond the expected-RED ingest.spec behavior assertions that depend on Plans 02-04.
</verification>

<success_criteria>
- Wave 0 fixture + spec exist; D-05 migrations complete and green.
- The validation contract for OPT-01 SC-1/2/3, D-04, and D-06/D-07 is encoded as greppable Playwright titles.
</success_criteria>

<output>
Create `.planning/phases/10-single-file-optimize-loop/10-01-SUMMARY.md` when done
</output>
