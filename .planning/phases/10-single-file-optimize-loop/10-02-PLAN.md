---
phase: 10-single-file-optimize-loop
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/lib/stub-data.ts
  - src/stores/files.ts
autonomous: true
requirements: [OPT-01]
must_haves:
  truths:
    - "filesAtom initializes with an empty entries array — the app starts on the dropzone first-run view (D-04)"
    - "FileEntry carries an optional createdAt timestamp for deterministic queue ordering (D-04 sort fix)"
    - "queue-order sort uses createdAt, not STUB_FILES.findIndex (D-04 / Pitfall 2)"
    - "STUB_FILES remains exported for test fixtures but no longer seeds the production list (D-04/D-05)"
  artifacts:
    - path: "src/stores/files.ts"
      provides: "Empty filesAtom seed + createdAt-based queue-order sort"
      contains: "entries: []"
    - path: "src/lib/stub-data.ts"
      provides: "FileEntry.createdAt field"
      contains: "createdAt"
  key_links:
    - from: "src/stores/files.ts"
      to: "FileEntry.createdAt"
      via: "queue-order comparator"
      pattern: "createdAt"
---

<objective>
Remove the seeded demo files (D-04) and fix the queue-order sort that assumed them. Empty `filesAtom`'s initial `entries`, add an optional `createdAt: number` field to the `FileEntry` interface, and switch the `$filteredFiles` 'queue order' comparator from `STUB_FILES.findIndex` (returns -1 for real entries — Pitfall 2) to a `createdAt` comparison. Keep `STUB_FILES` exported for the test fixtures (Plan 01) but stop using it as the production seed.

This is the model/store foundation (the contract) that the ingest hook (Plan 03) writes against. It runs after Wave 0 so the D-04 "empty" spec assertion flips to GREEN here.

Purpose: Honest first-run state + a stable sort key for real ingested entries.
Output: Modified `stub-data.ts` (FileEntry.createdAt) + `files.ts` (empty seed, createdAt sort).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-single-file-optimize-loop/10-RESEARCH.md
@.planning/phases/10-single-file-optimize-loop/10-PATTERNS.md

<interfaces>
<!-- Current store/model shape being modified. From codebase. -->
From src/lib/stub-data.ts:
  export interface FileEntry { id; name; type; orig; opt; status; target; dim; q: number|null; prog?; settings?; rawBuffer?; encodedBuffer?; error? }
  export const STUB_FILES: FileEntry[]   // 12 demo entries — keep export, drop as seed
  export function defaultFileSettings(type: string, q: number | null): FileSettings

From src/stores/files.ts:
  export const filesAtom = map<FilesState>({ entries: STUB_FILES, selectedId: null, filterQuery: '', sortBy: 'queue order' })
  export const $filteredFiles = computed(...)   // 'queue order' currently sorts by STUB_FILES.findIndex
  function updateEntry(id, patch)   // WR-02 single write funnel
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add createdAt to FileEntry</name>
  <behavior>
    - FileEntry type accepts an optional `createdAt: number` without breaking existing entries (STUB_FILES still typechecks).
    - Existing per-file settings + buffer fields are untouched.
  </behavior>
  <read_first>
    - src/lib/stub-data.ts lines 10-25 (FileEntry interface)
    - 10-PATTERNS.md § "src/lib/stub-data.ts" (FileEntry interface — add createdAt field)
    - 10-RESEARCH.md § "Open Questions" #1 (createdAt rationale) + Pitfall 2
  </read_first>
  <files>src/lib/stub-data.ts</files>
  <action>
    Add `createdAt?: number` to the `FileEntry` interface (optional, placed near `q`). Document inline that it is set to `Date.now()` at ingest and is the queue-order sort key after the D-04 seed removal. Do NOT add `createdAt` values to the existing `STUB_FILES` entries (the optional field keeps them valid; fixtures set their own `createdAt`). Keep `STUB_FILES`, `defaultFileSettings`, `TINY_PNG_B64`, and all other exports intact — they are reused by the test fixture (Plan 01) and by `useIngest` (Plan 03). Update the phase attribution header to note the Phase 10 change.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "stub-data.ts" | grep -q "^0$" && echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>`FileEntry.createdAt?: number` exists; project typechecks clean; STUB_FILES export unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Empty the seed + fix queue-order sort</name>
  <behavior>
    - On a fresh load, `filesAtom.get().entries.length === 0` (D-04 — the "empty" ingest.spec assertion turns GREEN).
    - With multiple entries having distinct createdAt values, 'queue order' returns them ascending by createdAt (insertion order).
    - Other sort modes (file size, savings %, name, format) are unchanged.
  </behavior>
  <read_first>
    - src/stores/files.ts lines 17-56 (filesAtom seed + $filteredFiles 'queue order' case)
    - 10-PATTERNS.md § "src/stores/files.ts" (seed removal + queue-order createdAt comparator)
    - 10-RESEARCH.md § "Pattern 6: Emptying the Seed (D-04)" + Pitfall 2
  </read_first>
  <files>src/stores/files.ts</files>
  <action>
    Change the `filesAtom` initializer's `entries: STUB_FILES` to `entries: []` (D-04). In the `$filteredFiles` computed atom's `'queue order'` case, replace the `STUB_FILES.findIndex(...)` comparator with `(a, b) => (a.createdAt ?? 0) - (b.createdAt ?? 0)` (Pitfall 2 — findIndex returns -1 for all real entries). Remove the now-unused `STUB_FILES` import IF it is no longer referenced anywhere in `files.ts` after this change (keep `defaultFileSettings` import — still used by `setFileSettings`/CR-01). Leave all other sort cases, computed atoms (`$selectedFile`, `$totals`), and the `updateEntry`/per-file actions exactly as-is. Update the phase attribution header to note the Phase 10 D-04 changes.
  </action>
  <verify>
    <automated>npx playwright test src/tests/ingest.spec.ts -g "empty" --project=chromium 2>&1 | tail -4 | grep -q "passed" && echo "D04_EMPTY_GREEN"</automated>
  </verify>
  <done>App starts empty (the "empty" ingest.spec test passes); queue-order sorts by createdAt; existing store unit tests (`stores.test.ts`) still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory store/model change; no input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | filesAtom queue-order comparator | accept | `createdAt ?? 0` fallback prevents NaN sort instability for legacy entries lacking the field; no untrusted input reaches the comparator. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Web Platform + existing deps only (10-RESEARCH § "No New Packages Required"). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `ingest.spec.ts -g "empty"` GREEN (D-04).
- `stores.test.ts` + `stub-data.test.ts` still pass.
- Full suite has no regressions in the 5 migrated specs (they inject their own fixtures, seed-independent).
</verification>

<success_criteria>
- App starts empty (D-04). Inspector shows "Select a file", Report empty.
- queue-order sort works for real entries via createdAt.
</success_criteria>

<output>
Create `.planning/phases/10-single-file-optimize-loop/10-02-SUMMARY.md` when done
</output>
