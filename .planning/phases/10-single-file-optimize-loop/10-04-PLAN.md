---
phase: 10-single-file-optimize-loop
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/components/panels/FilesPane.tsx
  - src/components/shell/Toolbar.tsx
autonomous: true
requirements: [OPT-01]
must_haves:
  truths:
    - "Dropping files onto FilesPane ingests them via useIngest, with a drag-active visual state (D-01)"
    - "The FilesPane 'Add files' button and Toolbar 'From device' both open the picker via useIngest (D-01)"
    - "A hidden <input type=file multiple accept> funnels picker-fallback selections into ingest (D-01)"
    - "Components contain only DOM-event wiring — all ingestion logic stays in useIngest (project rule)"
    - "After ingest, DeltaStrip/ReportPanel show real before/after bytes from File.size + encoded byteLength (D-08, OPT-01 SC-2)"
  artifacts:
    - path: "src/components/panels/FilesPane.tsx"
      provides: "Dropzone (dragover/drop) + Add files button + hidden file input wired to useIngest"
      contains: "useIngest"
    - path: "src/components/shell/Toolbar.tsx"
      provides: "Add files / From device buttons calling openPicker"
      contains: "openPicker"
  key_links:
    - from: "src/components/panels/FilesPane.tsx"
      to: "src/hooks/useIngest.ts"
      via: "ingest(Array.from(dataTransfer.files)) on drop + openPicker on Add files"
      pattern: "ingest|openPicker"
    - from: "src/components/shell/Toolbar.tsx"
      to: "src/hooks/useIngest.ts"
      via: "openPicker() on Add files / From device"
      pattern: "openPicker"
---

<objective>
Wire the DOM event seams to `useIngest` (Plan 03). FilesPane gets the dropzone handlers (`dragover`/`dragleave`/`drop` with the relatedTarget flicker guard + drag-active visual), the "Add files" button → `openPicker`, and the hidden `<input type="file" multiple accept>` fallback. Toolbar's "Add files" and "From device" buttons call `openPicker` instead of the empty `addFromDevice` stub.

Components carry ONLY event wiring — no ingestion logic (project rule). This is the final wave; once wired, OPT-01 SC-1/2/3 + D-06/D-07 ingest.spec assertions and the full suite turn GREEN.

Purpose: Make both entry points (drop + pick) real for device ingestion (D-01).
Output: Modified FilesPane.tsx + Toolbar.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-single-file-optimize-loop/10-RESEARCH.md
@.planning/phases/10-single-file-optimize-loop/10-PATTERNS.md

<interfaces>
<!-- Hook contract these components wire to. From Plan 03. -->
From src/hooks/useIngest.ts:
  export function useIngest(): { ingest: (files: File[]) => Promise<void>; openPicker: () => Promise<void>; /* + input ref/trigger contract per Plan 03 */ }

From src/stores/files.ts (current Toolbar imports):
  addFromDevice (stub — replace call sites with openPicker), addWatchFolder/addFromUrl (DEFERRED — leave wired to their stubs)

DeltaStrip.tsx / ReportPanel.tsx: already read entry.orig + encodedBuffer.byteLength — NO changes needed (10-RESEARCH § "Truthful Sizes").
accept string (D-06): ".png,.jpg,.jpeg,.webp,.svg,.avif,image/png,image/jpeg,image/webp,image/svg+xml,image/avif"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FilesPane dropzone + Add files button + hidden input</name>
  <behavior>
    - dragover preventDefault()s (so drop fires — Pitfall 3) and sets drag-active true; dragleave clears drag-active ONLY when the cursor leaves the root (relatedTarget guard — Pitfall 1); drop preventDefault()s, clears drag-active, and calls ingest(Array.from(dataTransfer.files)).
    - 'Add files' button (the existing aria-label="Add files" button, line ~58) calls openPicker; its current @TODO no-op is removed.
    - A hidden <input type="file" multiple accept=...> exists; its onChange calls ingest(Array.from(target.files)) — this is the picker fallback target.
    - Empty-state copy is shown when there are no files (first-run dropzone view — D-04 already empties the list).
  </behavior>
  <read_first>
    - src/components/panels/FilesPane.tsx (root div line ~24-26, Add files button line ~58-60, dropzone markup line ~68-71, files list line ~75)
    - 10-PATTERNS.md § "src/components/panels/FilesPane.tsx" (drag-drop wiring, root div drag-active class, Add files button, hidden input, empty state)
    - 10-RESEARCH.md § "Pattern 1: Drag-Drop Wiring" + "Pattern 2: File Picker" + Pitfalls 1 & 3
    - .agents/skills/building-components (component conventions)
  </read_first>
  <files>src/components/panels/FilesPane.tsx</files>
  <action>
    At the top of the FilesPane component, call `const { ingest, openPicker } = useIngest()` and add a local `const inputRef = useRef<HTMLInputElement>(null)` plus `const [dragActive, setDragActive] = useState(false)` (dragActive/inputRef are ephemeral UI state — allowed under STORE-08). Add `handleDragOver` (preventDefault + stopPropagation + setDragActive(true)), `handleDragLeave` (clear only if `!e.currentTarget.contains(e.relatedTarget as Node)` — Pitfall 1), and `handleDrop` (preventDefault + stopPropagation + setDragActive(false) + `ingest(Array.from(e.dataTransfer.files))`). Attach `onDragOver`/`onDragLeave`/`onDrop` to the existing `data-testid="files-pane"` root div and append the drag-active classes (e.g. accent border + bg) conditionally via the existing `cn(...)` helper. Replace the "Add files" button's `@TODO` onClick with `() => openPicker()`. Add a hidden `<input ref={inputRef} type="file" multiple accept="...the D-06 accept string..." className="hidden" onChange={e => e.target.files && ingest(Array.from(e.target.files))} />` near the button; if Plan 03's openPicker fallback needs the ref, wire it per Plan 03's contract (pass ref or trigger). Keep the existing dropzone copy ("Drop images to optimize"). Ensure NO ingestion/mapping/gate logic is added here — only event-to-hook wiring. Update the attribution header.
  </action>
  <verify>
    <automated>npx playwright test src/tests/ingest.spec.ts -g "drop|Report" --project=chromium 2>&1 | tail -4 | grep -q "passed" && echo "SC1_SC2_GREEN"</automated>
  </verify>
  <done>FilesPane drop + Add files ingest via useIngest; hidden input present with D-06 accept string; drag-active visual toggles; SC-1 ("drop") and SC-2 ("Report") ingest.spec tests pass; no ingestion logic inline (`grep -c "crypto.randomUUID\|createImageBitmap\|defaultFileSettings" src/components/panels/FilesPane.tsx` returns 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Toolbar Add files / From device → openPicker</name>
  <behavior>
    - The Toolbar primary "Add files" button and the "From device" menu item call openPicker() (then setOpen(null)), replacing the addFromDevice() no-op calls.
    - "Watch folder" and "From URL or paste" remain wired to their existing deferred stubs (NOT implemented this phase).
    - "Optimize all" button is unchanged (still calls runOptimize — Phase 11 owns its progress UI).
  </behavior>
  <read_first>
    - src/components/shell/Toolbar.tsx (import line 6, Add files button line ~42, From device line ~62, Watch folder line ~63, From URL line ~64, Optimize all line ~73)
    - 10-PATTERNS.md § "src/components/shell/Toolbar.tsx" ("From device" + "Add files" rewiring)
    - 10-CONTEXT.md § Deferred Ideas (Watch folder + From URL stay deferred)
  </read_first>
  <files>src/components/shell/Toolbar.tsx</files>
  <action>
    Add `const { openPicker } = useIngest()` near the top of the Toolbar component. Replace the `addFromDevice()` call in BOTH the primary "Add files" button (line ~42) and the "From device" menu item (line ~62) onClick handlers with `openPicker()` (keep the trailing `setOpen(null)`). Remove `addFromDevice` from the `@/stores/files` import if it is no longer referenced after this change (leave `addWatchFolder`, `addFromUrl`, and the export-related imports intact — those menu items stay wired to their deferred stubs per CONTEXT Deferred Ideas). Do NOT touch the "Optimize all" button (it already calls runOptimize; Phase 11 owns its progress reporting) or the export menu. No ingestion logic in the component. Update the attribution header.
  </action>
  <verify>
    <automated>npx playwright test src/tests/ingest.spec.ts --project=chromium 2>&1 | tail -5 | grep -q "passed" && echo "INGEST_SUITE_GREEN"</automated>
  </verify>
  <done>Toolbar "Add files" + "From device" call openPicker; Watch folder/From URL untouched (deferred); Optimize all unchanged; full ingest.spec.ts passes; SC-3 ("re-optimize") green via Phase 9 useLiveEncode on the now-real selected entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user (drag/click) → DOM → useIngest | UI events carry untrusted DataTransfer/FileList into the hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-V5b | Tampering | FilesPane drop / file input | mitigate | ASVS V5: raw FileList/DataTransfer is passed to useIngest, which applies the ext+MIME gate (D-06) — the component performs no trust decisions; validation is centralized in the hook. preventDefault on dragover/drop prevents the browser from navigating to a dropped file URL (Pitfall 3). |
| T-10-DRAG | DoS | dragenter/dragleave flicker | accept | relatedTarget guard (Pitfall 1) prevents state thrash; worst case is a cosmetic flicker, no security impact. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Web Platform APIs only (10-RESEARCH § "No New Packages Required"). |
</threat_model>

<verification>
- Full `ingest.spec.ts` GREEN (SC-1/2/3 + D-04 + D-06/D-07).
- Full suite `npx playwright test` GREEN (D-05 migrations hold; no regressions).
- No ingestion logic inline in either component (grep gate in task done-criteria).
- Manual (per 10-VALIDATION Manual-Only): drag-active highlight appears/clears on hover (awkward to drive in Playwright).
</verification>

<success_criteria>
- A developer drops/picks a single file → real optimized output + truthful before/after sizes in the Report (OPT-01).
- Both entry points (dropzone + picker) funnel into the one useIngest seam.
- Re-adjusting a setting re-optimizes and updates sizes (SC-3, via Phase 9 useLiveEncode).
</success_criteria>

<output>
Create `.planning/phases/10-single-file-optimize-loop/10-04-SUMMARY.md` when done
</output>
