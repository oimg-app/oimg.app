---
phase: 10-single-file-optimize-loop
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/hooks/useIngest.ts
autonomous: true
requirements: [OPT-01]
must_haves:
  truths:
    - "A single hook (useIngest) is the only home for drop/pick/gate/map/auto-optimize logic (D-01, project rule)"
    - "ingest() accepts only png/jpg/jpeg/webp/svg/avif by extension OR MIME and silently skips the rest (D-06/D-07 — no toast)"
    - "Each accepted File maps to a FileEntry with orig = File.size and createdAt = Date.now() (D-08, D-04 sort)"
    - "All ingested files are appended, the newest is auto-selected, and runOptimize() auto-optimizes them immediately (D-02, D-03)"
    - "openPicker() uses showOpenFilePicker with an <input> fallback and swallows AbortError on cancel (D-01, Pitfall 4)"
    - "Between ingest and first encode the entry shows orig=File.size with opt pending (D-10)"
  artifacts:
    - path: "src/hooks/useIngest.ts"
      provides: "ingest(files) + openPicker() — format gate, File→FileEntry mapping, append, auto-select, auto-optimize"
      exports: ["useIngest"]
      min_lines: 60
  key_links:
    - from: "src/hooks/useIngest.ts"
      to: "src/hooks/useOptimize.ts"
      via: "runOptimize() after append (auto-optimize, D-03)"
      pattern: "runOptimize"
    - from: "src/hooks/useIngest.ts"
      to: "src/stores/files.ts"
      via: "filesAtom.setKey('entries', ...) + selectFile + setFileRawBuffer"
      pattern: "filesAtom\\.setKey|selectFile|setFileRawBuffer"
---

<objective>
Build `src/hooks/useIngest.ts` — the single ingestion entry point that the components (Plan 04) wire DOM events into. It is the home for ALL drop/pick/format-gate/File→FileEntry-mapping/auto-optimize logic (project rule: never inline in components). It reuses Phase 9's `runOptimize` from `useOptimize` for auto-optimize-on-drop (D-03) — NOT a new dispatch loop.

This is the core business logic of the phase. It depends on Plan 02 (FileEntry.createdAt + empty seed). With this hook in place plus Plan 04's wiring, the OPT-01 SC-1/2/3 + D-06/D-07 ingest.spec assertions turn GREEN.

Purpose: One deterministic File → FileEntry → optimize seam (the path Phase 9 left stubbed).
Output: `src/hooks/useIngest.ts` exporting `useIngest()` returning `{ ingest, openPicker }`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-single-file-optimize-loop/10-RESEARCH.md
@.planning/phases/10-single-file-optimize-loop/10-PATTERNS.md

<interfaces>
<!-- Contracts useIngest consumes. From codebase (Phase 9) + Plan 02. -->
From src/hooks/useOptimize.ts:
  export function useOptimize(): { runOptimize: () => Promise<void> }
  // runOptimize iterates filesAtom.get().entries, slices rawBuffer before transfer,
  // sets results via setFileResult / errors via setFileError + toast (D-13). Reuse as-is.

From src/stores/files.ts:
  export const filesAtom        // map<FilesState>{ entries, selectedId, ... }
  export function selectFile(id: string): void
  export function setFileRawBuffer(id: string, rawBuffer: ArrayBuffer): void

From src/lib/stub-data.ts:
  export interface FileEntry { id; name; type; orig; opt; status; target; dim; q; createdAt?; settings?; rawBuffer? ... }
  export function defaultFileSettings(type: string, q: number | null): FileSettings

Accepted set (D-06): exts {png,jpg,jpeg,webp,svg,avif}; mimes {image/png,image/jpeg,image/webp,image/svg+xml,image/avif}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Format gate + File→FileEntry mapping + dimension read</name>
  <behavior>
    - isAccepted(file) returns true for png/jpg/jpeg/webp/svg/avif by extension OR MIME, false otherwise (D-06).
    - fileToEntry(file) returns a FileEntry with id=crypto.randomUUID(), type normalized from extension, orig=File.size, opt=File.size (pending), status='processing', target=type, q=82, createdAt=Date.now(), settings=defaultFileSettings(type,82), rawBuffer=await file.arrayBuffer() (D-08, D-10).
    - readDimensions returns '—' for SVG without calling createImageBitmap (Pitfall 6); for raster, returns `${w}×${h}` via createImageBitmap (closed after) and '—' on any throw (never blocks ingest).
  </behavior>
  <read_first>
    - src/hooks/useOptimize.ts (toCodec/toSourceFormat guard shape — analog for format normalization)
    - 10-PATTERNS.md § "src/hooks/useIngest.ts" (imports, format gate, fileToEntry, readDimensions patterns)
    - 10-RESEARCH.md § "Pattern 3: File → FileEntry Mapping" + "Pattern 4: Reading Image Dimensions" + Pitfall 6
  </read_first>
  <files>src/hooks/useIngest.ts</files>
  <action>
    Create `src/hooks/useIngest.ts` with the attribution header (Phase 10 — OPT-01). Define module-level `ACCEPTED_EXTS` and `ACCEPTED_MIMES` sets per the accepted set above. Add `getExt(name)` (lowercased last segment after '.'), `isAccepted(file)` (ext OR mime membership — D-06), `readDimensions(file, type)` (SVG → '—' immediately; raster → createImageBitmap try/catch, `bitmap.close()`, '—' on error — Pitfall 6), and `async fileToEntry(file)` building the FileEntry per the behavior block (normalize `jpg`/`jpeg` types consistently; set `status: 'processing'` so the DeltaStrip shimmer shows during the pending window — D-10; set `q: 82` and `settings: defaultFileSettings(type, 82)`; `createdAt: Date.now()`; `rawBuffer: await file.arrayBuffer()`; `orig`/`opt = file.size`). Do NOT import any codec/jSquash module (ingestion is main-thread; 200KB budget). No fenced code in production beyond what the file requires.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "useIngest.ts" | grep -q "^0$" && echo "TYPECHECK_CLEAN"</automated>
  </verify>
  <done>`useIngest.ts` exists with isAccepted/readDimensions/fileToEntry; typechecks clean; no codec imports present (`grep -v '^//' src/hooks/useIngest.ts | grep -c "@jsquash"` returns 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useIngest hook — ingest() + openPicker() with auto-optimize dispatch</name>
  <behavior>
    - ingest(files): filters via isAccepted, returns early (no toast) if none accepted (D-07); maps accepted to entries; appends to filesAtom via setKey preserving insertion order; selectFile(newest) (D-02); setFileRawBuffer per entry; then awaits runOptimize() (D-03).
    - openPicker(): if 'showOpenFilePicker' in window, opens it (multiple:true), maps handles→files, calls ingest; wraps in try/catch ignoring only AbortError (Pitfall 4); else triggers the hidden <input> click via a returned ref or callback the component owns.
    - No toast fires on format rejection; encode-failure toasts remain owned by runOptimize (D-07 boundary).
  </behavior>
  <read_first>
    - src/hooks/useOptimize.ts (runOptimize signature + that it reads filesAtom.get().entries — confirms newly appended entries are picked up)
    - 10-PATTERNS.md § "Core ingest dispatch pattern" + "openPicker" (lines 104-143)
    - 10-RESEARCH.md § "Pattern 5: Auto-Optimize Dispatch (D-03)" + "Pattern 2: File Picker" + Pitfall 4 + Pitfall 5 (rawBuffer slice ownership)
  </read_first>
  <files>src/hooks/useIngest.ts</files>
  <action>
    In the same file, export `function useIngest()` that calls `const { runOptimize } = useOptimize()` and returns `{ ingest, openPicker }`. Implement `async ingest(files: File[])`: `const accepted = files.filter(isAccepted)`; if `accepted.length === 0` return immediately with NO toast (D-07 silent skip); `const entries = await Promise.all(accepted.map(fileToEntry))`; append with `filesAtom.setKey('entries', [...filesAtom.get().entries, ...entries])`; `selectFile(entries[entries.length - 1].id)` (D-02); loop `setFileRawBuffer(e.id, e.rawBuffer)` for each entry that has a rawBuffer (runOptimize reads + slices it — Pitfall 5); then `await runOptimize()` (D-03 — do NOT write a new dispatch loop or use useLiveEncode). Implement `async openPicker()` per the behavior block: feature-detect `showOpenFilePicker`, map handles via `h.getFile()`, call `ingest`, catch and re-throw non-AbortError (Pitfall 4); for the fallback path, expose a way for the component to trigger the hidden `<input>` (return an `inputRef` from the hook OR have openPicker accept/return a trigger — choose the approach that keeps the input element in the component per Plan 04; document the contract in a comment). Ensure the hook itself contains no JSX.
  </action>
  <verify>
    <automated>npx playwright test src/tests/ingest.spec.ts -g "skip" --project=chromium 2>&1 | tail -4 | grep -q "passed" && echo "SILENT_SKIP_GREEN"</automated>
  </verify>
  <done>`useIngest()` exports `{ ingest, openPicker }`; the D-06/D-07 "skip" ingest.spec test passes (once Plan 04 wires the input the full SC-1/2/3 set goes green); no format-rejection toast; auto-optimize calls runOptimize.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user filesystem → app (drop/pick) | Untrusted file bytes + filenames enter the app at ingest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-V5 | Tampering / DoS | useIngest format gate + createImageBitmap + downstream codec worker | mitigate | ASVS V5: ext+MIME gate filters non-image inputs at ingest (D-06/D-07); createImageBitmap wrapped in try/catch returns '—' on malformed input (never throws into ingest); malformed-but-accepted files fail safely in the Phase 9 codec worker (worker isolation) and surface via the D-13 per-file error path. The gate is a UX filter, not the security boundary — createImageBitmap + jSquash decoders are the real malformed-input barrier. |
| T-10-ID | Spoofing | crypto.randomUUID() entry ids | accept | Cryptographically random, collision-free; no security decision keyed on id values. |
| T-10-NET | Info Disclosure | ingestion data flow | accept | Zero-server: no bytes leave the browser; ingestion adds no network calls (URL/paste ingestion explicitly deferred). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Web Platform APIs only (10-RESEARCH § "No New Packages Required"). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `ingest.spec.ts -g "skip"` GREEN (D-06/D-07).
- No `@jsquash` import in `useIngest.ts` (200KB budget preserved).
- No `toast` call on the format-rejection path.
</verification>

<success_criteria>
- One hook owns all ingestion logic; components only wire events to it (Plan 04).
- Accepted files map to truthful FileEntry objects and auto-optimize via runOptimize.
- Unsupported files silently skipped; picker cancel does not throw.
</success_criteria>

<output>
Create `.planning/phases/10-single-file-optimize-loop/10-03-SUMMARY.md` when done
</output>
