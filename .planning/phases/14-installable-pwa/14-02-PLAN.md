---
phase: 14-installable-pwa
plan: 02
type: execute
wave: 2
depends_on: ["14-00", "14-01"]
files_modified:
  - src/sw.ts
autonomous: true
requirements: [PWA-02]
must_haves:
  truths:
    - "src/sw.ts precaches the app shell (index.html, JS, CSS, SVG logo) via Workbox"
    - "Codec wasm + script chunks are runtime-cached CacheFirst, never precached"
    - "AVIF's wasm enters cache only on first AVIF use, not on first load"
    - "Cached responses retain their original COOP/COEP headers (crossOriginIsolated survives offline)"
    - "Plan 14-02 owns the full src/sw.ts file (Plan 14-01 no longer creates a placeholder)"
  artifacts:
    - path: "src/sw.ts"
      provides: "Hand-rolled service worker: precacheAndRoute + CacheFirst wasm + skipWaiting/clientsClaim"
      contains: "precacheAndRoute(self.__WB_MANIFEST)"
      min_lines: 30
  key_links:
    - from: "src/sw.ts CacheFirst route"
      to: "codec wasm fetches"
      via: "request.destination === 'script' || url.endsWith('.wasm')"
      pattern: "CacheFirst"
    - from: "src/sw.ts"
      to: "app shell precache"
      via: "self.__WB_MANIFEST"
      pattern: "__WB_MANIFEST"
---

<objective>
PWA-02: hand-roll `src/sw.ts` using Workbox primitives. Precache the app shell
(injected at build time as `self.__WB_MANIFEST` by Plan 01's injectManifest
config). Runtime-cache codec wasm + script chunks via `CacheFirst` so AVIF's
~3.4MB wasm is fetched only on first AVIF use and served from cache thereafter.
Cache `/squoosh-kit/` worker assets and fonts. Wire skipWaiting (install) +
clientsClaim (activate) so PWA-05's update flow can take over.

This plan moved to Wave 2 (depends_on `["14-00", "14-01"]`) so the VitePWA
plugin is already wired before src/sw.ts is created. Plan 14-01 no longer writes
a placeholder src/sw.ts — this plan owns the file outright.

Purpose: satisfies ROADMAP SC#2 — app shell precached, wasm runtime-cached, AVIF never precached.
Output: a complete src/sw.ts compiled by vite-plugin-pwa into dist/sw.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-installable-pwa/14-RESEARCH.md

<read_first>
- .planning/phases/14-installable-pwa/14-RESEARCH.md §Pattern 2 (full sw.ts source), §Critical Integration Risk: COOP/COEP, §Critical Integration Risk: Module Workers + SW WASM
- src/tests/pwa.spec.ts (Wave 0) — PWA-02 offline + crossOriginIsolated assertions this must satisfy
- public/squoosh-kit/ — static worker assets served at stable /squoosh-kit/ paths
- vite.config.ts (after Plan 14-01) — VitePWA injectManifest config consumes src/sw.ts via srcDir:'src', filename:'sw.ts'
</read_first>

<interfaces>
<!-- Full sw.ts structure (14-RESEARCH.md §Pattern 2). Workbox 7.4.1 transitive deps. -->
src/sw.ts imports:
  workbox-precaching: precacheAndRoute, cleanupOutdatedCaches
  workbox-routing: registerRoute
  workbox-strategies: CacheFirst, StaleWhileRevalidate
  workbox-expiration: ExpirationPlugin
  workbox-cacheable-response: CacheableResponsePlugin

Required top-of-file:
  /// <reference lib="webworker" />
  declare const self: ServiceWorkerGlobalScope

Routes:
  1. cleanupOutdatedCaches(); precacheAndRoute(self.__WB_MANIFEST)
  2. CacheFirst 'oimg-codecs-v1' on (request.destination==='script' || url.endsWith('.wasm'))
     plugins: CacheableResponsePlugin({statuses:[0,200]}),
              ExpirationPlugin({maxEntries:30, maxAgeSeconds:60*60*24*90})
  3. CacheFirst 'oimg-squooshkit-v1' on url.pathname.startsWith('/squoosh-kit/')
  4. StaleWhileRevalidate 'oimg-fonts-v1' on request.destination==='font'
  5. install → self.skipWaiting(); activate → e.waitUntil(self.clients.claim())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement hand-rolled src/sw.ts</name>
  <files>src/sw.ts</files>
  <read_first>.planning/phases/14-installable-pwa/14-RESEARCH.md §Pattern 2</read_first>
  <action>
    Create `src/sw.ts` exactly as structured in the interfaces block + 14-RESEARCH.md
    §Pattern 2. This plan owns the file outright — Plan 14-01 no longer creates a
    placeholder (sequencing fix: 14-02 now runs in Wave 2 after 14-01 wires the
    plugin). Include the phase/plan attribution header, the
    `/// <reference lib="webworker" />` directive and
    `declare const self: ServiceWorkerGlobalScope` (without it TypeScript resolves
    self as Window and errors). Precache app shell via
    `precacheAndRoute(self.__WB_MANIFEST)` after `cleanupOutdatedCaches()`.
    Register the CacheFirst wasm/script route, the /squoosh-kit/ route, and the
    font SWR route. Add install→skipWaiting and activate→clientsClaim listeners.
    Do NOT add any COOP/COEP header injection — 14-RESEARCH.md §COOP/COEP confirms
    cached Response objects already carry the headers (T-14-COEP).
  </action>
  <verify>
    <automated>grep -q "precacheAndRoute(self.__WB_MANIFEST)" src/sw.ts &amp;&amp; grep -q "CacheFirst" src/sw.ts &amp;&amp; grep -q "skipWaiting" src/sw.ts &amp;&amp; grep -q "clients.claim" src/sw.ts &amp;&amp; grep -q "ServiceWorkerGlobalScope" src/sw.ts &amp;&amp; npx tsc -b</automated>
  </verify>
  <done>src/sw.ts has precache + CacheFirst wasm route + SW lifecycle; tsc -b green.</done>
</task>

<task type="auto">
  <name>Task 2: Build-verify wasm exclusion + COOP/COEP survival</name>
  <files>src/sw.ts</files>
  <read_first>public/_headers, .planning/phases/14-installable-pwa/14-RESEARCH.md §Critical Integration Risk: COOP/COEP</read_first>
  <action>
    Run `npm run build` (this is the first plan in the phase where the full build
    is expected to succeed end-to-end, because src/sw.ts now exists). Confirm
    dist/sw.js compiles from src/sw.ts and the injected `__WB_MANIFEST` precache
    list contains NO `.wasm` entries (Plan 01's globIgnores enforces this; this
    task is the assertion gate). Then run the Wave-0 e2e PWA-02 case: it loads
    the preview build, waits for the SW to control the page, and asserts
    `crossOriginIsolated === true` after SW takeover — proving cached responses
    retained COOP/COEP (T-14-COEP). The SW must NOT re-inject headers; if
    crossOriginIsolated is false post-SW, the regression is in _headers
    (Plan 05), not here.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/sw.js &amp;&amp; ! grep -q '\.wasm' dist/sw.js &amp;&amp; npx playwright test src/tests/pwa.spec.ts --grep "PWA-02" --reporter=line</automated>
  </verify>
  <done>dist/sw.js emitted with no .wasm precache entries; PWA-02 e2e (offline + crossOriginIsolated) GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → SW cache | SW intercepts fetches and stores Response objects |
| SW cache → page | cached responses served on subsequent visits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-COEP | Tampering | crossOriginIsolated via cached responses | mitigate | SW does NOT re-inject COOP/COEP; cached Response objects retain original headers (14-RESEARCH.md §COOP/COEP). PWA-02 e2e asserts crossOriginIsolated===true post-SW. Regression guard: do not narrow _headers /* block (Plan 05) |
| T-14-POISON | Tampering | SW cache poisoning / stale-content serving | mitigate | CacheFirst only on content-hashed wasm/script (URL is content-addressed); CacheableResponsePlugin({statuses:[0,200]}) gates cacheable responses; cleanupOutdatedCaches() evicts stale precache on activate |
| T-14-AVIF | DoS | AVIF 3.4MB wasm in precache | mitigate | globIgnores '**/*.wasm' (Plan 01) + dist/sw.js grep assertion; wasm enters oimg-codecs-v1 only on first AVIF fetch |
</threat_model>

<verification>
- src/sw.ts: precacheAndRoute + CacheFirst wasm + skipWaiting + clientsClaim; tsc -b green
- `npm run build`: dist/sw.js emitted, no .wasm precache entries
- PWA-02 e2e GREEN (offline second-load + crossOriginIsolated survives SW)
- `npx playwright test src/tests/foundation.spec.ts` still green
</verification>

<success_criteria>
- ROADMAP SC#2 satisfied: app shell precached; wasm CacheFirst; AVIF not precached
- crossOriginIsolated preserved with SW active (MT codecs keep working offline)
</success_criteria>

<output>
Create `.planning/phases/14-installable-pwa/14-02-SUMMARY.md` when done
</output>
