---
phase: 14-installable-pwa
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - public/_headers
autonomous: false
requirements: [PWA-04]
must_haves:
  truths:
    - "_headers keeps the existing COOP/COEP on the /* wildcard (SharedArrayBuffer / MT codecs survive)"
    - "/sw.js is served with literal Cache-Control: no-cache (PWA-04 verbatim) so SW updates are detected"
    - "/manifest.webmanifest is served with public, max-age=86400"
  artifacts:
    - path: "public/_headers"
      provides: "Cloudflare Pages header rules: COOP/COEP preserved + sw.js no-cache + manifest max-age"
      contains: "Cross-Origin-Embedder-Policy"
  key_links:
    - from: "public/_headers /* block"
      to: "crossOriginIsolated"
      via: "COOP same-origin + COEP require-corp"
      pattern: "Cross-Origin-Embedder-Policy: require-corp"
    - from: "public/_headers /sw.js block"
      to: "SW update detection"
      via: "Cache-Control: no-cache"
      pattern: "/sw.js"
---

<objective>
PWA-04: update Cloudflare Pages `public/_headers` so `/sw.js` is served fresh
with the literal `Cache-Control: no-cache` PWA-04 mandates (required for SW
update detection) and `/manifest.webmanifest` is cached for a day
(`Cache-Control: public, max-age=86400`), WITHOUT
weakening the existing COOP/COEP wildcard that keeps `crossOriginIsolated` true
(SharedArrayBuffer / MT codecs depend on it). The `/*` block must stay broad so
cached Response objects retain COOP/COEP (T-14-COEP regression guard). Because
the headers are edge-served and only verifiable post-deploy, this plan ends with
a blocking human checkpoint to curl the preview URL.

Purpose: satisfies ROADMAP SC#4 — _headers updated, COOP/COEP preserved.
Output: public/_headers with three rule blocks + verified preview headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-installable-pwa/14-RESEARCH.md

<read_first>
- public/_headers — current file: only the /* COOP/COEP block; preserve it verbatim
- .planning/phases/14-installable-pwa/14-RESEARCH.md §Pattern 6 (_headers target) + §Critical Integration Risk: COOP/COEP
- .planning/phases/14-installable-pwa/14-VALIDATION.md §Manual-Only Verifications (PWA-04 curl checks)
</read_first>

<interfaces>
<!-- 14-RESEARCH.md §Pattern 6 + PWA-04 verbatim. The /* block MUST remain (T-14-COEP). -->
public/_headers target:
  /*
    Cross-Origin-Opener-Policy: same-origin
    Cross-Origin-Embedder-Policy: require-corp

  /sw.js
    Cache-Control: no-cache

  /manifest.webmanifest
    Cache-Control: public, max-age=86400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _headers with sw.js + manifest rules</name>
  <files>public/_headers</files>
  <read_first>public/_headers, .planning/phases/14-installable-pwa/14-RESEARCH.md §Pattern 6</read_first>
  <action>
    Edit `public/_headers` to the target in the interfaces block. KEEP the
    existing `/*` COOP/COEP block exactly as-is (do NOT narrow it or exclude
    /sw.js — that would break crossOriginIsolated on cached serving, T-14-COEP).
    Append a `/sw.js` block with the LITERAL `Cache-Control: no-cache` (PWA-04
    verbatim — NOT `max-age=0, must-revalidate`; the requirement is exact) and a
    `/manifest.webmanifest` block with `Cache-Control: public, max-age=86400`.
    Cloudflare `_headers` is whitespace-sensitive: two-space indent under each
    path; blank line between blocks.
  </action>
  <verify>
    <automated>grep -q "Cross-Origin-Embedder-Policy: require-corp" public/_headers &amp;&amp; grep -q "/sw.js" public/_headers &amp;&amp; grep -q "Cache-Control: no-cache" public/_headers &amp;&amp; grep -q "Cache-Control: public, max-age=86400" public/_headers</automated>
  </verify>
  <done>_headers retains COOP/COEP /* block; adds /sw.js Cache-Control: no-cache (PWA-04 verbatim) + /manifest.webmanifest max-age=86400.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    public/_headers updated: COOP/COEP preserved on /*, /sw.js Cache-Control: no-cache (PWA-04 verbatim),
    /manifest.webmanifest max-age=86400. Edge-served headers are only
    verifiable on a deployed/preview build (Cloudflare Pages applies _headers).
  </what-built>
  <how-to-verify>
    1. Build + preview locally: `npm run build && npm run preview` (note: local
       preview may not apply _headers the same way Cloudflare does — the
       authoritative check is the Cloudflare preview deploy).
    2. On the Cloudflare Pages preview URL, run:
       - `curl -I <preview-url>/sw.js` → expect literal `Cache-Control: no-cache` (PWA-04 verbatim)
       - `curl -I <preview-url>/manifest.webmanifest` → expect `Cache-Control: public, max-age=86400`
       - `curl -I <preview-url>/` → expect both COOP `same-origin` and COEP `require-corp`
    3. In the deployed app DevTools console, after the SW controls the page,
       confirm `crossOriginIsolated === true` and run one OxiPNG MT optimize
       (proves SharedArrayBuffer / MT codecs still work with the SW active).
  </how-to-verify>
  <resume-signal>Type "approved" once curl shows the three header rules and crossOriginIsolated is true, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare edge → browser | _headers sets COOP/COEP + cache policy at the HTTP layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-HDR | Tampering | COOP/COEP weakening via _headers edit | mitigate | /* block preserved verbatim; blocking human checkpoint curls preview to confirm COEP require-corp + COOP same-origin still present; crossOriginIsolated manually verified |
| T-14-STALESW | Spoofing / Tampering | stale SW served from cache | mitigate | /sw.js `Cache-Control: no-cache` (PWA-04 verbatim) forces revalidation so update detection works (PWA-05 depends on this) |
| T-14-COEP | Tampering | crossOriginIsolated regression | mitigate | Do NOT narrow /* to exclude /sw.js; cached responses retain COOP/COEP only if the /* wildcard applies on original fetch (14-RESEARCH.md §COOP/COEP) |
</threat_model>

<verification>
- _headers: /* COOP/COEP preserved; /sw.js `Cache-Control: no-cache` literal; /manifest.webmanifest max-age=86400
- Human checkpoint: curl preview confirms all three rules + crossOriginIsolated true with SW active
- MT OxiPNG optimize succeeds post-SW (SharedArrayBuffer intact)
</verification>

<success_criteria>
- ROADMAP SC#4 satisfied: _headers updated with `/sw.js Cache-Control: no-cache` (PWA-04 verbatim) + manifest max-age, COOP/COEP preserved
- crossOriginIsolated verified true on deployed build with SW controlling the page
</success_criteria>

<output>
Create `.planning/phases/14-installable-pwa/14-05-SUMMARY.md` when done
</output>
