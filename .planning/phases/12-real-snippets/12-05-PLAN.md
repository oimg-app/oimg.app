---
phase: 12
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-04]
files_modified:
  - src/components/panels/files/FileRow.tsx
  - src/tests/file-row-snippets.spec.ts
autonomous: true
requirements: [SNIP-01]
user_setup: []
tags: [phase-12, wave-2, file-row, context-menu, SNIP-01]

must_haves:
  truths:
    - "FileRow ContextMenu gains exactly two new items — Copy <picture> and Copy data-URI — as flat siblings to the existing Save as… (D-12; sub-menu collapse deferred per CONTEXT.md Claude's Discretion)"
    - "Both items disabled when file.status !== 'done' with title='Optimize this file first' (D-13 — Phase 11 row-level Save as… pattern reuse)"
    - "Copy <picture> binds to useSnippets().copyPictureOne(file) (D-12 + Plan 04 export)"
    - "Copy data-URI binds to useSnippets().copyDataUriOne(file) (D-12 + Plan 04 export)"
    - "Copy data-URI emits ONLY the data URI string with the data:image/…; prefix included — ready for `<img src=\"…\">` paste (D-12 lock in Claude's Discretion)"
    - "WCAG-AA keyboard semantics inherit from Radix ContextMenu primitives (Phase 11 D-04 already verified; no new keyboard tests needed)"
    - "Existing FileRow placeholder items at lines 146-153 (the two @TODO Phase 3 stubs) are REPLACED — no zombie placeholders remain"
  artifacts:
    - path: "src/components/panels/files/FileRow.tsx"
      provides: "ContextMenu with three items — Save as… (Phase 11) + Copy <picture> + Copy data-URI"
    - path: "src/tests/file-row-snippets.spec.ts"
      provides: "e2e covering both new items + D-13 disabled state + D-15 chokepoint capture"
  key_links:
    - from: "src/components/panels/files/FileRow.tsx"
      to: "src/hooks/useSnippets.ts (Plan 04)"
      via: "const { copyPictureOne, copyDataUriOne } = useSnippets()"
      pattern: "useSnippets\\(\\)"
    - from: "src/components/panels/files/FileRow.tsx ContextMenuItem.onSelect"
      to: "Plan 01 chokepoint via useSnippets"
      via: "void copyPictureOne(file) / void copyDataUriOne(file)"
      pattern: "copy(Picture|DataUri)One"
---

<objective>
Replace the two FileRow ContextMenu placeholder items (lines 146-153, marked `@TODO Phase 3`) with real wiring to `useSnippets().copyPictureOne` and `useSnippets().copyDataUriOne`, matching the Phase 11 `Save as…` disable-then-explain shape.

Purpose: Today's FileRow ContextMenu has two `@TODO` stubs that call `pushToast('Copy data URI')` and `pushToast('Copy <picture>')` — non-functional placeholders shipped in Phase 11. Phase 12's Plan 04 ships the per-file methods `copyPictureOne(file)` and `copyDataUriOne(file)` in `useSnippets`; this plan wires them into the row menu and covers them with an e2e.

Output: real per-row clipboard items + e2e covering both items + D-13 disabled assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-real-snippets/12-CONTEXT.md
@.planning/phases/12-real-snippets/12-PATTERNS.md
@.planning/phases/12-real-snippets/12-VALIDATION.md
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 04 -->
import { useSnippets } from '@/hooks/useSnippets'
const { copyPictureOne, copyDataUriOne } = useSnippets()
copyPictureOne(file: FileEntry): Promise<void>     // guards status==='done' && encodedBuffer != null; routes via chokepoint
copyDataUriOne(file: FileEntry): Promise<void>     // same guards; emits URI alone (no <img> wrapper)

<!-- From Plan 01 Wave 0 -->
import { installClipboardMocks } from './setup/clipboard-mocks'

<!-- Phase 11 D-04 Save as… analog (FileRow.tsx:138-145) -->
<ContextMenuItem
  disabled={file.status !== 'done'}
  title={file.status !== 'done' ? 'Optimize this file first' : undefined}
  onSelect={() => { void exportOne(file) }}
>
  <DownloadSimple size={14} />
  Save as…
</ContextMenuItem>

<!-- Current placeholder items being REPLACED (FileRow.tsx:146-153) -->
<ContextMenuItem onSelect={() => { /* @TODO Phase 3 — pushToast('Copy data URI') */ }}>
  <Copy size={14} />
  Copy data URI
</ContextMenuItem>
<ContextMenuItem onSelect={() => { /* @TODO Phase 3 — pushToast('Copy <picture>') */ }}>
  <Code size={14} />
  {'Copy <picture>'}
</ContextMenuItem>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire FileRow ContextMenu — replace two @TODO placeholders with useSnippets bindings + D-13 disable triple</name>
  <files>src/components/panels/files/FileRow.tsx</files>
  <read_first>
    - src/components/panels/files/FileRow.tsx (entire file — locate the icon import block at lines 4-13 to confirm Copy + Code icons are already imported; locate the existing `Save as…` ContextMenuItem at lines 138-145 as the verbatim analog; locate the two @TODO placeholder items at lines 146-153 being replaced)
    - src/hooks/useSnippets.ts (from Plan 04 — confirm copyPictureOne, copyDataUriOne signatures)
    - .planning/phases/12-real-snippets/12-PATTERNS.md § "src/components/panels/files/FileRow.tsx (modify) — D-12 / D-13" (lines 451-499 — verbatim replacement pattern)
    - .planning/phases/12-real-snippets/12-CONTEXT.md D-12 + D-13 (locked decisions)
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-04 + D-13 (Save as… pattern, disable-then-explain row-level reuse)
  </read_first>
  <behavior>
    - FileRow imports `useSnippets` from `@/hooks/useSnippets`
    - Inside the FileRow component body (near other hooks if any), `const { copyPictureOne, copyDataUriOne } = useSnippets()` is destructured
    - The two ContextMenuItems at lines 146-153 are REPLACED with real wiring per 12-PATTERNS.md lines 480-497 — `Copy data-URI` first, `Copy <picture>` second (or in the order specified in 12-PATTERNS.md — siblings to Save as…, flat menu per CONTEXT.md Claude's Discretion)
    - Each new item carries `disabled={file.status !== 'done'}` AND `title={file.status !== 'done' ? 'Optimize this file first' : undefined}` (matching Save as… verbatim — D-13)
    - Each item's `onSelect` is a single one-statement arrow body: `() => { void copyPictureOne(file) }` and `() => { void copyDataUriOne(file) }` (project rule: no inline logic in components — useSnippets owns the orchestration)
    - Icons remain `<Copy size={14} />` for data-URI item and `<Code size={14} />` for picture item (matches current placeholder icons + 12-PATTERNS.md verbatim)
    - Label text MUST read `Copy data-URI` and `Copy <picture>` (D-12 — note current placeholder has `Copy data URI` without hyphen; Phase 12 standardizes to `Copy data-URI` per 12-PATTERNS.md line 490 to match CONTEXT.md D-12 wording)
    - No remaining `@TODO` or `pushToast('Copy …')` references in FileRow.tsx after this task
  </behavior>
  <action>
    Modify `src/components/panels/files/FileRow.tsx`. Add import: `import { useSnippets } from '@/hooks/useSnippets'`. Inside the FileRow component body, before the return statement, near the existing `const { exportOne } = useExport()` destructure (or equivalent — read the file to find the right insertion point), add: `const { copyPictureOne, copyDataUriOne } = useSnippets()`. Locate the two `ContextMenuItem` blocks at lines 146-153 (the `@TODO Phase 3` placeholders). Replace them verbatim per 12-PATTERNS.md lines 481-497: a `Copy data-URI` item with `<Copy size={14} />` icon, disabled triple, and `onSelect={() => { void copyDataUriOne(file) }}`; followed by a `Copy <picture>` item with `<Code size={14} />` icon, same disabled triple, and `onSelect={() => { void copyPictureOne(file) }}`. Use the JSX label form `{'Copy <picture>'}` to escape the angle brackets (matches current placeholder syntax). Confirm the icon imports `Copy` and `Code` are already present at FileRow.tsx:4-13 — if not, add them (per 12-PATTERNS.md line 499 they should already be there). Confirm no remaining `@TODO Phase 3` or `pushToast('Copy …')` references remain in the file. Verify the existing `Save as…` item (line 138-145) is UNTOUCHED — Plan 11's wiring stays. Per D-12 user-decision: siblings (flat menu) per CONTEXT.md Claude's Discretion lock (research/discussion considered sub-menu Copy ▸ <picture> / data-URI; user locked siblings — sub-menu refactor deferred to polish phase).
  </action>
  <acceptance_criteria>
    - `src/components/panels/files/FileRow.tsx` imports `useSnippets` from `@/hooks/useSnippets`
    - File contains `const { copyPictureOne, copyDataUriOne } = useSnippets()`
    - File contains the literal `void copyPictureOne(file)` AND `void copyDataUriOne(file)` invocation patterns
    - File contains the literal label `Copy data-URI` (note hyphen — D-12 standardization)
    - File contains the literal `{'Copy <picture>'}` JSX label
    - File does NOT contain `@TODO Phase 3` strings (grep -c "@TODO Phase 3" returns 0)
    - File does NOT contain `pushToast('Copy ` strings (grep -c "pushToast\\('Copy" returns 0)
    - Both new ContextMenuItems contain `disabled={file.status !== 'done'}` AND `title={file.status !== 'done' ? 'Optimize this file first' : undefined}` (grep -c "file.status !== 'done'" returns ≥ 3 — Save as… + 2 new items)
    - `tsc -b` exits 0
    - Existing `Save as…` ContextMenuItem block is UNTOUCHED (its `exportOne(file)` onSelect remains as in Phase 11)
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && [ $(grep -c "@TODO Phase 3" src/components/panels/files/FileRow.tsx) -eq 0 ] && grep -c "useSnippets" src/components/panels/files/FileRow.tsx</automated>
  </verify>
  <done>FileRow ContextMenu ships Copy data-URI + Copy <picture> siblings with D-13 disabled triple; placeholders purged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/tests/file-row-snippets.spec.ts — e2e for both new ContextMenu items + D-13 disabled</name>
  <files>src/tests/file-row-snippets.spec.ts</files>
  <read_first>
    - src/tests/file-row-menu.spec.ts (entire — verbatim shape per 12-PATTERNS.md; reuse `injectEntries` + `rightClickRow` helpers)
    - src/tests/setup/clipboard-mocks.ts (from Plan 01 — confirm installClipboardMocks signature)
    - .planning/phases/12-real-snippets/12-PATTERNS.md § "src/tests/file-row-snippets.spec.ts (new — e2e)" (lines 714-744 — verbatim pattern with both test bodies)
  </read_first>
  <behavior>
    - Test 1 (D-12 Copy <picture> invokes useSnippets.copyPictureOne): installs clipboard mocks in 'native' mode; injects one entry with `target: 'webp'`, `status: 'done'`, deterministic encodedBuffer; right-clicks the row; opens the ContextMenu; asserts `Copy <picture>` menuitem is visible; clicks it; latches on `window.__clipboardWrites.length === 1`; asserts captured text contains `<picture>` AND `<source srcset="` (D-12)
    - Test 2 (D-13 Copy data-URI disabled when not done): inject entry with `status: 'queued'`; right-clicks the row; asserts `Copy data-URI` menuitem has `aria-disabled="true"` AND `title="Optimize this file first"` (D-13)
    - Test 3 (D-12 Copy data-URI emits URI alone — webp): inject done file with target webp + tiny encodedBuffer; right-clicks; clicks `Copy data-URI`; latches; asserts captured text starts with `data:image/webp;base64,` AND does NOT contain `<img` (URI-only per CONTEXT.md Claude's Discretion lock)
    - Test 4 (D-12 SVG dispatch — Copy data-URI for SVG): inject done file with target svg + UTF-8 encoded `<svg/>` buffer; right-clicks; clicks `Copy data-URI`; asserts captured text starts with `data:image/svg+xml;charset=utf-8,` (D-01 SVG dispatch reached through the per-file path)
  </behavior>
  <action>
    Create `src/tests/file-row-snippets.spec.ts`. Header: `// Phase 12 Plan 05 — D-12 + D-13 + D-15 chokepoint capture for FileRow per-row items`. Imports: `import { test, expect } from '@playwright/test'`, `import { installClipboardMocks } from './setup/clipboard-mocks'`. Reuse `injectEntries` and `rightClickRow` helpers verbatim — extract from `src/tests/file-row-menu.spec.ts` or inline-copy (project precedent for spec files copying helpers). Each test follows the install-mocks → goto → inject → right-click → assert flow per 12-PATTERNS.md lines 719-743. Test 1 captured-text assertion: `const text = await page.evaluate(() => (window as any).__clipboardWrites[0]); expect(text).toContain('<picture>'); expect(text).toContain('<source srcset="')`. Test 2 disabled assertion: `const item = page.getByRole('menuitem', { name: /^Copy data-URI$/ }); await expect(item).toHaveAttribute('aria-disabled', 'true'); await expect(item).toHaveAttribute('title', 'Optimize this file first')`. Test 3 URI-alone assertion: `expect(text).toMatch(/^data:image\/webp;base64,/); expect(text).not.toContain('<img')`. Test 4 SVG dispatch: inject with `encodedBuffer: <utf-8 bytes of '<svg/>'>` (use `new TextEncoder().encode('<svg/>').buffer` — but since `page.evaluate` is the injection layer, encode inside the eval: `await page.evaluate(() => { … const buf = new TextEncoder().encode('<svg/>').buffer; … })`). Test 4 assertion: `expect(text).toMatch(/^data:image\/svg\+xml;charset=utf-8,/)`. All four tests inside `test.describe('FileRow — D-12/D-13 per-row snippet items', () => { … })`. For the right-click trigger, match the existing `rightClickRow(page, filename)` helper signature — read file-row-menu.spec.ts to confirm.
  </action>
  <acceptance_criteria>
    - `src/tests/file-row-snippets.spec.ts` exists
    - `pnpm exec playwright test src/tests/file-row-snippets.spec.ts --reporter=dot` exits 0
    - All four tests pass (D-12 picture, D-13 disabled, D-12 data-URI raster URI-alone, D-12 SVG dispatch)
    - File imports `installClipboardMocks` from `./setup/clipboard-mocks`
    - File contains `getByRole('menuitem', { name: /^Copy <picture>$/ })` AND `getByRole('menuitem', { name: /^Copy data-URI$/ })` (matching the D-12 standardized labels)
    - File contains assertions on `aria-disabled='true'` AND `title='Optimize this file first'` (D-13)
    - File contains the regex `/^data:image\/webp;base64,/` (Test 3) AND `/^data:image\/svg\+xml;charset=utf-8,/` (Test 4)
    - File contains `expect(text).not.toContain('<img')` (Test 3 — URI-alone lock per Claude's Discretion D-12)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test src/tests/file-row-snippets.spec.ts --reporter=dot</automated>
  </verify>
  <done>e2e proves D-12 + D-13 per-row items + SVG dispatch reaches the per-file path; 12-05-filerow row in VALIDATION.md flips green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FileRow disabled gate (D-13) → useSnippets per-file guard → chokepoint | Defense-in-depth: row item disabled via ContextMenuItem `disabled` prop AND useSnippets.copyPictureOne/copyDataUriOne guards `if (file.status !== 'done' || !file.encodedBuffer) return` |
| FileRow ContextMenu DOM → Radix ContextMenu primitive keyboard/screen-reader semantics | Inherited from Radix (Phase 11 D-04 verified); no new accessibility surface to test |
| Per-row file.name → label string `Copy <picture> for ${file.name}` → chokepoint toast | Plan 04's useSnippets builds the label inside the hook; chokepoint toast is project-controlled message format; no XSS risk in toast text (sonner renders as text node) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-ZOMBIE-2 | Tampering | @TODO Phase 3 placeholders shipping behaviour stubs in production | mitigate | Task 1 asserts `grep -c "@TODO Phase 3" src/components/panels/files/FileRow.tsx` returns 0 AND `grep -c "pushToast\\('Copy" returns 0; zombie placeholder behaviour removed |
| T-12-A11Y | Information Disclosure | Disabled ContextMenuItem missing aria-disabled would announce as enabled to screen readers despite being non-interactive | mitigate | Task 1 uses Radix ContextMenuItem `disabled` prop which sets both `data-disabled` and `aria-disabled` per Radix conventions; Task 2 Test 2 asserts `aria-disabled='true'` is present in the DOM |
| T-12-SVG-PATH | Tampering / Elevation | SVG bytes reaching per-row Copy data-URI through the URL-encoded path (T-12-01 chain) | mitigate | Plan 04 copyDataUriOne calls `buildDataUri(file)` which dispatches on file.target === 'svg' (Plan 02 D-01); Task 2 Test 4 asserts the SVG dispatch reaches the per-file path with the `image/svg+xml;charset=utf-8,` prefix (NOT base64) |
</threat_model>

<verification>
- `tsc -b` exits 0
- `pnpm exec playwright test src/tests/file-row-snippets.spec.ts --reporter=dot` exits 0
- `grep -c "@TODO Phase 3" src/components/panels/files/FileRow.tsx` returns 0 (zombie placeholders purged)
- `grep -c "useSnippets" src/components/panels/files/FileRow.tsx` returns ≥ 2 (import + invocation)
- `grep -c "Copy data-URI" src/components/panels/files/FileRow.tsx` returns ≥ 1 (D-12 standardized label)
- `grep -c "Copy <picture>" src/components/panels/files/FileRow.tsx` returns ≥ 1 (D-12 standardized label, possibly inside JSX `{'…'}` form)
</verification>

<success_criteria>
- D-12 / D-13 met
- SVG dispatch covered end-to-end through the per-row path (T-12-01 mitigation chain verified)
- 12-05-filerow row in `.planning/phases/12-real-snippets/12-VALIDATION.md` flips ⬜ → ✅
- Phase 12 SNIP-01 requirement satisfied across all 5 surfaces (Output panel × 3 sections + Toolbar × 3 bulk + FileRow × 2 per-row)
</success_criteria>

<output>
Create `.planning/phases/12-real-snippets/12-05-SUMMARY.md` when done.
</output>
