---
phase: 12
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - src/components/panels/inspector/OutputPanel.tsx
  - src/tests/output-panel-live.spec.ts
autonomous: true
requirements: [SNIP-01]
user_setup: []
tags: [phase-12, wave-2, output-panel, live-refresh, SNIP-01]

must_haves:
  truths:
    - "OutputPanel useEffect dep array includes file?.id, file?.encodedBuffer, and file?.target — re-renders mid-encode when useLiveEncode pushes new bytes (D-05)"
    - "Per-status presentation per D-06: status==='done' + encodedBuffer != null → real snippet; status==='processing' → thin skeleton/loading row in each section with copy disabled and tooltip 'Encoding in progress'; status==='queued' → 'Optimize this file first' placeholder; status==='error' → disabled section with file.error message"
    - "Snippets are derived live in the component — no snippetsAtom, no caching (D-07)"
    - "Existing direct navigator.clipboard.writeText call at src/components/panels/inspector/OutputPanel.tsx:103 is REROUTED through copyToClipboard from Plan 01 (D-15 chokepoint)"
    - "The section title (e.g. 'Data URI · Base64') is passed as the label argument to copyToClipboard so the toast reads `${title} copied`"
    - "Live-encode e2e: drop file → optimize → mutate encodedBuffer in filesAtom → snippet text reflects new bytes WITHOUT re-selecting the file (D-05 SC-3)"
  artifacts:
    - path: "src/components/panels/inspector/OutputPanel.tsx"
      provides: "Output panel with per-status states + live encodedBuffer refresh + chokepoint copy routing"
    - path: "src/tests/output-panel-live.spec.ts"
      provides: "e2e asserting D-05 live refresh + D-06 per-status states + D-15 chokepoint write capture"
  key_links:
    - from: "src/components/panels/inspector/OutputPanel.tsx"
      to: "src/lib/clipboard.ts (Plan 01)"
      via: "copyToClipboard(text, 'snippet', label)"
      pattern: "copyToClipboard\\("
    - from: "src/components/panels/inspector/OutputPanel.tsx"
      to: "src/lib/snippets.ts (Plan 02)"
      via: "buildBase64Snippet / buildUrlEncodedSnippet / buildPictureSnippet"
      pattern: "build(Base64|UrlEncoded|Picture)Snippet"
    - from: "src/components/panels/inspector/OutputPanel.tsx useEffect"
      to: "file.encodedBuffer reactive driver"
      via: "[file?.id, file?.encodedBuffer, file?.target] dep array"
      pattern: "file\\?\\.encodedBuffer"
---

<objective>
Wire the Output panel to the real bytes pipeline (D-05) and per-status states (D-06), and replace the direct `navigator.clipboard.writeText` call with the Plan 01 chokepoint (D-15).

Purpose: Today's OutputPanel useEffect at lines 47-50 depends only on `file` identity — it misses `encodedBuffer` changes pushed by Phase 9's `useLiveEncode`. Today's `handleCopy` at lines 101-109 calls `navigator.clipboard.writeText` directly (D-15 violation; misses Safari quirks and the toast surface). Per-status presentation today is binary (done vs not-done); D-06 requires four distinct states.

Output: live-refreshing OutputPanel + per-status sections + chokepoint copy routing + e2e asserting D-05 SC-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-real-snippets/12-CONTEXT.md
@.planning/phases/12-real-snippets/12-PATTERNS.md
@.planning/phases/12-real-snippets/12-VALIDATION.md
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- From Plan 01 -->
import { copyToClipboard } from '@/lib/clipboard'
export type CopyKind = 'snippet' | 'manifest' | 'data-uri'
export interface CopyResult { ok: boolean; method: 'native' | 'execCommand' | 'failed' }
export async function copyToClipboard(text: string, kind: CopyKind, label: string): Promise<CopyResult>

<!-- From Plan 02 -->
export async function buildDataUri(file: FileEntry): Promise<string>
export async function buildBase64Snippet(file: FileEntry): Promise<string>
export async function buildUrlEncodedSnippet(file: FileEntry): Promise<string>
export function buildPictureSnippet(file: FileEntry): string

<!-- From Plan 01 Wave 0 -->
import { installClipboardMocks, type ClipboardMockMode } from '@/tests/setup/clipboard-mocks'
// page.evaluate access: window.__clipboardWrites: string[]

<!-- FileRow STATUS_DOT lookup analog for D-06 per-status table (per 12-PATTERNS.md lines 338-344) -->
const STATUS_DOT: Record<string, string> = {
  queued:     'bg-[var(--fg-3)]',
  processing: 'bg-[var(--info)] animate-pulse',
  done:       'bg-[var(--primary)]',
  error:      'bg-[var(--err)]',
}

<!-- Existing handleCopy shape (OutputPanel.tsx:101-109 — direct navigator.clipboard call, D-15 violation) -->
async function handleCopy(sectionId: string, text: string) {
  try {
    await navigator.clipboard.writeText(text)
    setCopied(sectionId)
    setTimeout(() => setCopied(null), 1500)
  } catch {
    pushToast('Clipboard unavailable — check browser permissions')
  }
}

<!-- Existing useEffect at OutputPanel.tsx:47-50 (D-05 violation) -->
useEffect(() => {
  builder(file).then(setText)
}, [file])  // BUG: misses encodedBuffer change
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire OutputPanel.tsx — useEffect dep fix (D-05) + per-status states (D-06) + chokepoint copy (D-15)</name>
  <files>src/components/panels/inspector/OutputPanel.tsx</files>
  <read_first>
    - src/components/panels/inspector/OutputPanel.tsx (entire current file — locate useEffect at ~line 47, handleCopy at ~line 101, the existing Section/Snippet render shape, and confirm the import list at the top)
    - src/components/panels/files/FileRow.tsx lines 67-72 (STATUS_DOT lookup table — analog for the D-06 per-status state map)
    - src/components/panels/files/FileRow.tsx lines 130-150 (the existing per-status branch shape showing how queued/processing/done/error are rendered for the row — Sanity check the colour token names against `var(--color-fg-3)` / `var(--color-info)` / `var(--color-primary)` / `var(--color-err)` patterns)
    - src/lib/snippets.ts (from Plan 02 — confirm exported signatures, note buildPictureSnippet is now sync)
    - src/lib/clipboard.ts (from Plan 01 — confirm copyToClipboard signature)
    - .planning/phases/12-real-snippets/12-PATTERNS.md § "src/components/panels/inspector/OutputPanel.tsx (modify) — D-05 / D-06 / D-07" (lines 320-392 — verbatim replacement patterns for the three analog blocks)
    - .planning/phases/12-real-snippets/12-CONTEXT.md D-05 + D-06 + D-07 + D-15
  </read_first>
  <behavior>
    - The useEffect that derives snippet text from a builder now depends on `[file?.id, file?.encodedBuffer, file?.target]` — re-runs when bytes mutate (useLiveEncode push) or target swap, NOT just when the file identity changes
    - When `file.status === 'queued'` each section renders `<p className="text-[12px] text-[var(--color-fg-3)]">Optimize this file first</p>` and the section's Copy button is disabled with title 'Optimize this file first'
    - When `file.status === 'processing'` OR `file.encodedBuffer == null` (mid-encode race) each section renders a thin skeleton: `<div className="h-[60px] rounded-md bg-[var(--color-bg-2)] animate-pulse" aria-label="Encoding in progress" />` and Copy is disabled with title 'Encoding in progress'
    - When `file.status === 'error'` each section renders `<p className="text-[12px] text-[var(--color-err)]">{file.error ?? 'Encoding failed'}</p>` and Copy is disabled
    - When `file.status === 'done' && file.encodedBuffer != null` each section renders the real snippet (existing `<pre>` block) and Copy is enabled
    - `handleCopy(sectionId, text, label)` is rewritten: calls `await copyToClipboard(text, 'snippet', label)`; if `result.ok === true` it sets the local `copied` indicator state and clears it after 1500ms; if `result.ok === false` it does NOT toast (the chokepoint already raised the error toast — double-toast forbidden per D-14 last sentence)
    - The section's `title` (e.g. "Data URI · Base64", "Data URI · URL-encoded", "<picture>") is threaded through as the `label` argument so the toast reads `${title} copied`
    - Existing `pushToast('Clipboard unavailable …')` call site is REMOVED — chokepoint owns the failure surface
    - When no file is selected (`file == null` / `file === undefined`) the existing empty-state panel renders verbatim (no regression)
  </behavior>
  <action>
    Modify `src/components/panels/inspector/OutputPanel.tsx`. Add import: `import { copyToClipboard } from '@/lib/clipboard'`. Locate the snippet-derive useEffect at ~line 47 and replace its dep array from `[file]` to `[file?.id, file?.encodedBuffer, file?.target]` (D-05). Add a `// D-05: re-run when bytes change (live-encode push) or target swap.` comment inline above the closing `]`. Locate the handleCopy at ~line 101. Replace its body per 12-PATTERNS.md lines 379-389: the new signature is `async function handleCopy(sectionId: string, text: string, label: string)`. Body: `const { ok } = await copyToClipboard(text, 'snippet', label); if (ok) { setCopied(sectionId); setTimeout(() => setCopied(null), 1500) }`. Locate the SECTIONS map (the source of the three section titles) and confirm each Copy-button invocation site passes the section's title as the third arg (e.g. `onClick={() => handleCopy(s.id, text, s.title)}`). Remove the existing `pushToast('Clipboard unavailable …')` line entirely AND remove its import if `pushToast` is unused after the change (grep the file for other usages first). Extract a small inline helper component or inline-render block per D-06 — naming up to executor, but the four-branch logic MUST follow this order: status === 'queued' → empty placeholder; status === 'error' → error message in `var(--color-err)`; status === 'processing' || !file.encodedBuffer → skeleton with `animate-pulse`; else (done + encodedBuffer present) → existing `<pre>` snippet block. Per-section Copy button MUST: be disabled when status !== 'done' OR encodedBuffer == null; carry `title={status === 'queued' ? 'Optimize this file first' : status === 'processing' ? 'Encoding in progress' : status === 'error' ? 'Encoding failed' : undefined}`; carry matching `aria-disabled={status !== 'done' || encodedBuffer == null}` (disable-then-explain pattern reused from Phase 11 D-13 — see 12-PATTERNS.md "Disable-then-explain" shared pattern at lines 750-758). Per D-07: no `snippetsAtom`, no caching — the derivation stays in the existing useEffect path; do NOT introduce a useMemo over encodedBuffer (rebuild cost is fine). Per D-15: this file MUST NOT contain any remaining `navigator.clipboard.writeText` call (acceptance criteria asserts `grep -c "navigator.clipboard" src/components/panels/inspector/OutputPanel.tsx` returns 0).
  </action>
  <acceptance_criteria>
    - `src/components/panels/inspector/OutputPanel.tsx` imports `copyToClipboard` from `@/lib/clipboard`
    - `tsc -b` exits 0
    - File contains the substring `file?.encodedBuffer` inside a useEffect dep array context (D-05; grep -c "file\\?\\.encodedBuffer" returns ≥ 1)
    - File does NOT contain the substring `navigator.clipboard` (D-15 chokepoint enforcement; grep -c returns 0)
    - File contains `animate-pulse` (D-06 processing skeleton)
    - File contains the literal string `'Optimize this file first'` (D-06 queued placeholder text)
    - File contains the literal string `'Encoding in progress'` (D-06 processing tooltip)
    - File contains a per-section branch on `status === 'queued'`, `status === 'processing'`, `status === 'error'`, `status === 'done'` (grep -c "status === 'queued'" returns ≥ 1, similar for processing/error/done — at least 4 unique status branches)
    - The Copy button gains `disabled={…}` and `aria-disabled={…}` attributes (Phase 11 D-13 disable-then-explain triple)
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && [ $(grep -c "navigator.clipboard" src/components/panels/inspector/OutputPanel.tsx) -eq 0 ] && grep -c "copyToClipboard" src/components/panels/inspector/OutputPanel.tsx</automated>
  </verify>
  <done>OutputPanel ships D-05 dep fix + D-06 per-status states + D-15 chokepoint; no direct clipboard call remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/tests/output-panel-live.spec.ts — D-05 live refresh e2e</name>
  <files>src/tests/output-panel-live.spec.ts</files>
  <read_first>
    - src/tests/file-row-menu.spec.ts lines 34-72 (injectEntries helper — verbatim pattern per 12-PATTERNS.md; `/src/...` page.evaluate import IS the accepted Vite pattern per project MEMORY)
    - src/tests/export-single.spec.ts lines 86-90 (page.waitForFunction latch pattern — D-05 e2e latches on the snippet text containing the new base64 substring)
    - src/tests/setup/clipboard-mocks.ts (from Plan 01 Task 2 — confirm installClipboardMocks signature)
    - src/components/panels/inspector/OutputPanel.tsx (from Task 1 — confirm the data-testid attribute for the output-panel container; if missing, the executor adds it during Task 1 — see action below)
    - src/stores/files.ts (filesAtom + FileEntry — confirm shape of `setKey('entries', ...)` mutation path)
    - .planning/phases/12-real-snippets/12-PATTERNS.md § "src/tests/output-panel-live.spec.ts (new — e2e)" (lines 651-686 — verbatim pattern)
  </read_first>
  <behavior>
    - Test 1 (D-05 live refresh): drops a queued file via injectEntries with `target: 'webp'`, status: 'done', and a known `encodedBuffer` (e.g. `new Uint8Array([1,2,3]).buffer`). Opens the Output panel. Reads initial snippet text — asserts it contains `data:image/webp;base64,AQID` (base64 of [1,2,3]). Then mutates `filesAtom` via `page.evaluate` to swap the encodedBuffer to `new Uint8Array([89,89,89]).buffer`. Latches with `page.waitForFunction` on the snippet `<pre>` text containing `WVlZ` (base64 of [89,89,89]) — must succeed WITHOUT re-selecting the file
    - Test 2 (D-06 processing skeleton): injects entries with `status: 'processing'`, opens Output panel, asserts the skeleton div is present with `aria-label="Encoding in progress"` AND that the Copy button is disabled with `aria-disabled="true"`
    - Test 3 (D-06 queued placeholder): injects entries with `status: 'queued'`, asserts the placeholder text `Optimize this file first` is present in EACH of the three sections
    - Test 4 (D-15 chokepoint): drops a done file, installs clipboard mocks in 'native' mode, clicks the Base64 section's Copy button, then `page.waitForFunction(() => (window as any).__clipboardWrites?.length === 1)` — asserts the captured text starts with `<img src="data:image/webp;base64,` (confirms the chokepoint write fired with the snippet text)
  </behavior>
  <action>
    Create `src/tests/output-panel-live.spec.ts`. Header: `// Phase 12 Plan 03 — D-05 live refresh + D-06 per-status + D-15 chokepoint capture`. Imports: `import { test, expect } from '@playwright/test'`, `import { installClipboardMocks } from './setup/clipboard-mocks'`. Reuse the `injectEntries` helper verbatim from `src/tests/file-row-menu.spec.ts:34-72` — either by extracting it into a shared util or by copying inline (project precedent: spec files copy helpers; no shared helper file). The `page.evaluate` import path MUST be `'/src/stores/files.ts'` (project Vite-dev pattern per MEMORY). For Test 1's buffer mutation, write `await page.evaluate(async () => { const { filesAtom } = await import(/* @vite-ignore */ '/src/stores/files.ts'); const newBuf = new Uint8Array([89, 89, 89]).buffer; const entries = filesAtom.get().entries.map(e => e.id === 'f1' ? { ...e, encodedBuffer: newBuf } : e); filesAtom.setKey('entries', entries) })`. Latches: `await page.waitForFunction(() => { const txt = document.querySelector('[data-testid="output-panel"] pre')?.textContent ?? ''; return txt.includes('WVlZ') }, undefined, { timeout: 3000 })`. **If** the OutputPanel container does not currently have `data-testid="output-panel"`, the executor MUST add it during Task 1 (one line attribute on the root <section> or <div> of the panel). Test 4 captured-text assertion: `const written = await page.evaluate(() => (window as any).__clipboardWrites[0]); expect(written).toContain('data:image/webp;base64,')`. Each test routes through `await installClipboardMocks(page, { mode: 'native' })` before `page.goto('/')` so the chokepoint native path is the one exercised (Plan 04/05 cover fallback). All four tests run under a single `test.describe('OutputPanel — D-05 live refresh + D-06 per-status', () => { … })`.
  </action>
  <acceptance_criteria>
    - `src/tests/output-panel-live.spec.ts` exists
    - `pnpm exec playwright test src/tests/output-panel-live.spec.ts --reporter=dot` exits 0
    - All four tests pass (D-05 live refresh, D-06 processing skeleton, D-06 queued placeholder, D-15 chokepoint capture)
    - File imports `installClipboardMocks` from `./setup/clipboard-mocks`
    - File contains `page.evaluate` with `import(/* @vite-ignore */ '/src/stores/files.ts')` (accepted Vite-dev pattern per MEMORY)
    - File contains the `WVlZ` (base64 of [89,89,89]) latch substring AND `AQID` (base64 of [1,2,3]) initial assertion substring
    - File contains `aria-label="Encoding in progress"` selector (D-06 processing)
    - File contains `Optimize this file first` literal (D-06 queued)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test src/tests/output-panel-live.spec.ts --reporter=dot</automated>
  </verify>
  <done>e2e proves D-05 live refresh + D-06 per-status + D-15 chokepoint capture; 12-03-output row in VALIDATION.md flips green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `file.encodedBuffer` mutation (worker → store → component) → snippet text re-derive | Reactive subscription via Plan 02 builders; D-05 dep array fix prevents stale-render reads of old bytes |
| OutputPanel section title → `copyToClipboard` `label` arg → user-facing toast text | Section titles are project-controlled strings ("Data URI · Base64" etc.); no user input — no escape needed |
| Section snippet text → clipboard via Plan 01 chokepoint | All escaping already done at Plan 02 buildBase64Snippet level (T-12-02 escapeAttr); this layer just routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | navigator.clipboard.writeText throws in non-secure context — caller surfaces error | mitigate | Task 1 reroutes through Plan 01 chokepoint which handles non-secure context fall-through to textarea+execCommand silently |
| T-12-STALE | Information Disclosure | Stale snippet text rendered after re-optimize (D-05 bug) — developer pastes old bytes thinking they're new | mitigate | Task 1 useEffect dep array literally lists `file?.encodedBuffer` (grep enforceable); Task 2 Test 1 asserts the snippet text refreshes mid-flight without file re-selection |
| T-12-RACE | Information Disclosure | OutputPanel renders the previous file's snippet bytes during the brief window between target swap and re-encode | mitigate | Task 1 D-06 processing branch shows skeleton when `status === 'processing' || !file.encodedBuffer` — the previous bytes are never displayed during the re-encode window |
| T-12-DOUBLE | Denial of Service / Confusion | Double-toast on clipboard failure (one from local handler, one from chokepoint) | mitigate | Task 1 handleCopy only sets local `copied` state on `ok === true`; on `ok === false` it returns silently — chokepoint owns the failure toast (D-14 last sentence) |
</threat_model>

<verification>
- `tsc -b` exits 0
- `pnpm exec playwright test src/tests/output-panel-live.spec.ts --reporter=dot` exits 0
- `grep -c "navigator.clipboard" src/components/panels/inspector/OutputPanel.tsx` returns 0 (D-15 chokepoint enforcement)
- `grep -c "file\\?\\.encodedBuffer" src/components/panels/inspector/OutputPanel.tsx` returns ≥ 1 (D-05 dep array)
- `grep -c "copyToClipboard" src/components/panels/inspector/OutputPanel.tsx` returns ≥ 1 (D-15 chokepoint usage)
- `grep -c "animate-pulse" src/components/panels/inspector/OutputPanel.tsx` returns ≥ 1 (D-06 processing skeleton)
</verification>

<success_criteria>
- D-05 / D-06 / D-07 / D-15 all met
- SNIP-01 SC-3 (Selecting a different file or re-optimizing refreshes the snippets) covered by Test 1 + Test 4
- 12-03-output row in `.planning/phases/12-real-snippets/12-VALIDATION.md` flips ⬜ → ✅
</success_criteria>

<output>
Create `.planning/phases/12-real-snippets/12-03-SUMMARY.md` when done.
</output>
