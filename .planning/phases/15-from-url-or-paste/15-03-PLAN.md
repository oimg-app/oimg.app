---
phase: 15
plan: 03
title: Document-level paste hook (useClipboardIngest)
type: execute
wave: 2
depends_on: [15-01, 15-02]
requirements: [ING-02]
threats: [T-15-03]
files_modified:
  - path: src/hooks/useClipboardIngest.ts
    action: create
  - path: src/App.tsx
    action: modify
  - path: src/tests/paste-ingest.spec.ts
    action: create
autonomous: true
test_type: e2e
test_command: npx playwright test src/tests/paste-ingest.spec.ts

must_haves:
  truths:
    - "Pasting an image anywhere outside <input>/<textarea>/contenteditable ingests it via useIngest.ingest (SC-4)"
    - "Pasting an image URL string outside text inputs routes through pickFromUrl + ingests (SC-4)"
    - "Cmd/Ctrl+V inside the FilesPane filter input does NOT ingest (D-11 guard, SC-4 + Manual row 3)"
    - "preventDefault is called ONLY when the event was consumed (D-12) so unrelated text pastes never break browser semantics"
  artifacts:
    - path: src/hooks/useClipboardIngest.ts
      provides: "useClipboardIngest(): void — mounts document.paste listener mirroring useWatchFolder shape"
      exports: ["useClipboardIngest"]
    - path: src/App.tsx
      provides: "useClipboardIngest() invocation at component root"
      contains: "useClipboardIngest()"
    - path: src/tests/paste-ingest.spec.ts
      provides: "Playwright e2e: paste image, paste URL string, input-guard regression"
  key_links:
    - from: "src/hooks/useClipboardIngest.ts"
      to: "src/lib/clipboard-ingest.ts (processClipboardEvent)"
      via: "named import"
      pattern: "import\\s*\\{\\s*processClipboardEvent\\s*\\}\\s*from\\s*'@/lib/clipboard-ingest'"
    - from: "src/hooks/useClipboardIngest.ts"
      to: "src/hooks/useIngest.ts"
      via: "useIngest() destructure { ingest }"
      pattern: "useIngest\\(\\)"
    - from: "src/App.tsx"
      to: "src/hooks/useClipboardIngest.ts"
      via: "useClipboardIngest() invocation in App component body"
      pattern: "useClipboardIngest\\(\\)"
---

<objective>
Wire ING-02: a single document-level `paste` listener mounted at app root that routes through the shared `processClipboardEvent` dispatcher from plan 15-02. Mirrors the `useWatchFolder` analog (Quick task 260603-s2x).

Purpose: SC-4 (document-level Cmd/Ctrl+V handler on `<App />` ingests pasted images). Threat T-15-03 (per-paste cost) mitigated by the single tagName early-return guard.
Output: `src/hooks/useClipboardIngest.ts` + `App.tsx` invocation + Playwright e2e covering image, URL, and input-guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/15-from-url-or-paste/15-CONTEXT.md
@.planning/phases/15-from-url-or-paste/15-RESEARCH.md
@.planning/phases/15-from-url-or-paste/15-VALIDATION.md
@.planning/phases/15-from-url-or-paste/15-01-SUMMARY.md
@.planning/phases/15-from-url-or-paste/15-02-SUMMARY.md
@src/hooks/useIngest.ts
@src/hooks/useWatchFolder.ts
@src/App.tsx
@src/lib/clipboard-ingest.ts

<interfaces>
From plan 15-02 (`src/lib/clipboard-ingest.ts`):
```
export interface ClipboardDispatcher { ingest(files: File[]): Promise<void> }
export function processClipboardEvent(e: ClipboardEvent, d: ClipboardDispatcher): Promise<boolean>
```

From `src/hooks/useIngest.ts` (Phase 10 single seam):
```
export function useIngest(): { ingest(files: File[]): Promise<void>, isAccepted: ... }
```

D-11 input-elements guard (verified against the codebase in RESEARCH §3 — no Shadow DOM):
```
const target = e.target as HTMLElement | null
const tag = target?.tagName?.toLowerCase()
if (tag === 'input' || tag === 'textarea' || target?.isContentEditable) return
```

D-12 preventDefault rule: only when processClipboardEvent returns true.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/hooks/useClipboardIngest.ts</name>
  <files>src/hooks/useClipboardIngest.ts</files>
  <read_first>
    - src/hooks/useWatchFolder.ts (analog hook shape — Quick task 260603-s2x; mirror useEffect + cleanup pattern)
    - src/hooks/useIngest.ts (verify exported `ingest` signature)
    - src/lib/clipboard-ingest.ts (Wave 1 plan 15-02 output — processClipboardEvent signature)
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§1.3, §3)
    - .planning/phases/15-from-url-or-paste/15-CONTEXT.md (D-10..D-13, T-15-03)
  </read_first>
  <behavior>
    - Hook signature: `useClipboardIngest(): void`
    - Mounts `document.addEventListener('paste', onPaste)` inside a useEffect
    - Returns the cleanup `removeEventListener` from useEffect
    - Effect dependency array includes `ingest` (closes over the ingest fn from useIngest())
    - `onPaste` checks D-11 early-return on input / textarea / contenteditable, then `await processClipboardEvent(e, { ingest })`, then `if (consumed) e.preventDefault()`
  </behavior>
  <action>
    Create `src/hooks/useClipboardIngest.ts` per RESEARCH §1.3. Include the provenance comment `// Phase 15 — ING-02: document-level Cmd/Ctrl+V handler. Mirrors useWatchFolder shape.` Import `useEffect` from `react`, `useIngest` from `@/hooks/useIngest`, `processClipboardEvent` from `@/lib/clipboard-ingest`. Inside the effect, declare an inner `async function onPaste(e: ClipboardEvent)`. Apply D-11 guard exactly as written in RESEARCH §3. Call `processClipboardEvent(e, { ingest })` and `e.preventDefault()` only on `consumed === true`. Cleanup returns `removeEventListener('paste', onPaste)`. Do NOT pass `{ capture: true }` (D-12 demands the default bubbling phase).
  </action>
  <verify>
    <automated>node --experimental-strip-types --check src/hooks/useClipboardIngest.ts; grep -c "addEventListener('paste'\|removeEventListener('paste'" src/hooks/useClipboardIngest.ts | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/useClipboardIngest.ts` exists
    - `grep -E "^export function useClipboardIngest" src/hooks/useClipboardIngest.ts` returns 1 match
    - `grep -c "useEffect" src/hooks/useClipboardIngest.ts` returns ≥ 1
    - `grep -c "document.addEventListener\('paste'" src/hooks/useClipboardIngest.ts` returns 1
    - `grep -c "document.removeEventListener\('paste'" src/hooks/useClipboardIngest.ts` returns 1
    - D-11 guard present: `grep -c "isContentEditable" src/hooks/useClipboardIngest.ts` returns 1
    - D-12 conditional preventDefault present: `grep -E "if \(consumed\)" src/hooks/useClipboardIngest.ts` returns 1
  </acceptance_criteria>
  <done>Hook mounts/unmounts cleanly; D-11 guard prevents input-elements from being captured; D-12 conditional preventDefault preserves browser semantics for unrelated pastes.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke useClipboardIngest() inside App.tsx component body</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current component body and existing imports / SW useEffect)
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§1.4)
    - .planning/phases/15-from-url-or-paste/15-CONTEXT.md (D-13)
  </read_first>
  <behavior>
    - `useClipboardIngest()` is called inside the App functional component body — NOT inside the existing SW-bootstrap useEffect
    - Import added: `import { useClipboardIngest } from '@/hooks/useClipboardIngest'`
    - Provenance comment `// Phase 15 — ING-02: document-level Cmd/Ctrl+V handler.` next to the invocation
  </behavior>
  <action>
    Edit `src/App.tsx`. Add the import alphabetically near other `@/hooks/*` imports. In the `App()` component body, add `useClipboardIngest()` BEFORE the existing SW-bootstrap `useEffect` (so it's at the top of the body, immediately after any existing hooks like `useTheme()` if present). Add an inline comment on the same line: `useClipboardIngest() // Phase 15 — ING-02: document-level Cmd/Ctrl+V handler.` Do NOT move or modify any existing code.
  </action>
  <verify>
    <automated>grep -c "useClipboardIngest" src/App.tsx | grep -q "^2$" && grep -c "from '@/hooks/useClipboardIngest'" src/App.tsx | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useClipboardIngest()" src/App.tsx` returns 1 (invocation)
    - `grep -c "from '@/hooks/useClipboardIngest'" src/App.tsx` returns 1 (import)
    - `grep -c "Phase 15 — ING-02" src/App.tsx` returns 1 (provenance comment)
    - `npm run build` (or `tsc -b`) does not regress vs. baseline (baseline tsc is currently red; compare against pre-edit baseline per CLAUDE.md)
  </acceptance_criteria>
  <done>Hook is mounted at app root; document.paste listener is live for the lifetime of the SPA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playwright e2e for paste-ingest (image, URL, input-guard)</name>
  <files>src/tests/paste-ingest.spec.ts</files>
  <read_first>
    - src/hooks/useClipboardIngest.ts
    - src/lib/clipboard-ingest.ts
    - src/lib/url-ingest.ts
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§9 task 15-03)
    - Any existing e2e that dispatches a `ClipboardEvent` via `page.evaluate` — mirror the synthetic event shape
  </read_first>
  <behavior>
    - Case A — image paste: dispatch a `ClipboardEvent('paste', { clipboardData: dt })` on `document.body` where `dt.items` carries a tiny PNG File. Assert `filesAtom.entries.length > 0` afterward AND `page.getByText('Pasted from clipboard:')` is visible
    - Case B — URL paste: `page.route` returns a tiny PNG for the test URL; dispatch paste with `text/plain` payload of that URL onto `document.body`. Assert ingest fires + `page.getByText('Imported from URL:')` visible
    - Case C — input-guard regression: click the FilesPane filter input first, then dispatch the same image paste with `e.target` resolving to the input. Assert NO entries were added AND no success toast surfaced
  </behavior>
  <action>
    Create `src/tests/paste-ingest.spec.ts`. Use the existing Playwright config (port 5174 dev server auto-started). For each case, navigate to `/`, await any boot. Build a helper `dispatchPaste(page, { type, payload, targetSelector? })` that injects via `page.evaluate` a freshly-constructed `ClipboardEvent('paste')` using `new DataTransfer()` (Chromium supports it) populated with either a File (image) or string (text/plain URL). For Case C, `page.locator(filterInputSelector).click()` first so `e.target` ends up inside the input; use `dispatchEvent` on that locator. Read `filesAtom.entries.length` via `page.evaluate('window.__getFilesLen?.()')` — or, if no debug hook exists, count rows via the FilesPane row selector. Use the constants from RESEARCH §5 for visible toast text assertions.
  </action>
  <verify>
    <automated>npx playwright test src/tests/paste-ingest.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/paste-ingest.spec.ts` exists
    - `npx playwright test src/tests/paste-ingest.spec.ts` exits 0
    - Spec contains all three cases (`test(` count ≥ 3)
    - Case A asserts the `Pasted from clipboard:` toast text
    - Case B asserts the `Imported from URL:` toast text
    - Case C asserts ZERO entries are added (negative assertion on row count OR `filesAtom.entries.length === 0`)
  </acceptance_criteria>
  <done>Three real-browser scenarios pass; D-11 input-guard is regression-locked; D-12 preventDefault semantics confirmed by Case C's silent passthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document keystrokes → paste handler | Every Cmd/Ctrl+V outside text inputs flows through this hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Denial of Service | Per-paste cost of the document-level handler | accept | Single string-comparison early-return path (D-11). Negligible cost confirmed (CONTEXT T-15-03, RESEARCH §10 + §6 P-12). |
| T-15-02 (reuse) | Tampering | Pasted SVG bytes carrying script payloads | mitigate | DOMPurify in the existing useIngest pipeline (Phase 3 carry-forward). This hook adds no new attack surface — it dispatches through `useIngest.ingest`. |
</threat_model>

<verification>
- `npx playwright test src/tests/paste-ingest.spec.ts` exits 0
- `grep -c "useClipboardIngest()" src/App.tsx` returns 1
- `grep -c "addEventListener\('paste'" src/hooks/useClipboardIngest.ts` returns 1; matching cleanup returns 1
- Phase 14's existing e2e suite still passes (no regression of unrelated `paste` semantics)
</verification>

<success_criteria>
- Cmd/Ctrl+V outside text inputs ingests pasted images / URL strings
- Cmd/Ctrl+V inside the FilesPane filter input is passed through to the browser unchanged
- The handler unmounts cleanly on App unmount (no listener leaks under hot reload)
</success_criteria>

<output>
Create `.planning/phases/15-from-url-or-paste/15-03-SUMMARY.md` when done. Note the App.tsx delta is a single hook invocation + import (no functional regression to SW bootstrap).
</output>
