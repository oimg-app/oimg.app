---
phase: 15
plan: 04
title: Toolbar wire-up + addFromUrl stub deletion
type: execute
wave: 2
depends_on: [15-01, 15-02]
requirements: [ING-01]
threats: []
files_modified:
  - path: src/components/shell/Toolbar.tsx
    action: modify
  - path: src/stores/files.ts
    action: modify
  - path: src/tests/toolbar-paste.spec.ts
    action: create
autonomous: true
test_type: e2e
test_command: npx playwright test src/tests/toolbar-paste.spec.ts && npm run test:bundle

must_haves:
  truths:
    - "Toolbar 'From URL or paste' onClick calls pickFromClipboard({ ingest }) and closes the popover (SC-1)"
    - "Toolbar success path: clipboard image bytes appear in filesAtom.entries + 'Pasted from clipboard:' toast visible (SC-1)"
    - "Toolbar negative path: empty clipboard surfaces 'Clipboard has no image or image URL' toast (SC-3)"
    - "addFromUrl(): void {} stub deleted from src/stores/files.ts (SC-5)"
    - "addFromUrl import removed from src/components/shell/Toolbar.tsx (SC-5)"
    - "Initial-JS gzip bundle remains ≤ 200 KB (PIPE-02; pre-phase baseline 197.97 KB)"
  artifacts:
    - path: src/components/shell/Toolbar.tsx
      provides: "From URL or paste menu item wired to pickFromClipboard"
      contains: "pickFromClipboard"
    - path: src/stores/files.ts
      provides: "addFromUrl stub removed"
      excludes: "addFromUrl"
    - path: src/tests/toolbar-paste.spec.ts
      provides: "Playwright e2e: image happy path + empty clipboard negative + stub-deleted source assertions"
  key_links:
    - from: "src/components/shell/Toolbar.tsx"
      to: "src/lib/clipboard-ingest.ts (pickFromClipboard)"
      via: "named import + onClick dispatch"
      pattern: "import\\s*\\{\\s*pickFromClipboard\\s*\\}\\s*from\\s*'@/lib/clipboard-ingest'"
    - from: "src/components/shell/Toolbar.tsx onClick"
      to: "useIngest().ingest"
      via: "void pickFromClipboard({ ingest })"
      pattern: "void pickFromClipboard\\(\\{ ingest"
---

<objective>
Wire the Toolbar "From URL or paste" menu item to `pickFromClipboard` from plan 15-02 AND retire the empty `addFromUrl(): void {}` stub. This closes ING-01's Toolbar surface and completes SC-5 (stub deletion).

Purpose: SC-1 (Toolbar From URL or paste → clipboard read → ingest with success toast), SC-3 (negative empty-clipboard toast), SC-5 (stub deletion). No new threats (reuses 15-01 + 15-02 mitigations).
Output: Toolbar.tsx wire-up, files.ts stub deletion, Playwright e2e covering both happy + negative + source-grep verification, bundle-budget gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/15-from-url-or-paste/15-CONTEXT.md
@.planning/phases/15-from-url-or-paste/15-RESEARCH.md
@.planning/phases/15-from-url-or-paste/15-VALIDATION.md
@.planning/phases/15-from-url-or-paste/15-01-SUMMARY.md
@.planning/phases/15-from-url-or-paste/15-02-SUMMARY.md
@src/components/shell/Toolbar.tsx
@src/stores/files.ts
@src/hooks/useIngest.ts
@src/lib/clipboard-ingest.ts

<interfaces>
Current Toolbar wire (RESEARCH §1.4):
- `src/components/shell/Toolbar.tsx:22` — `addFromUrl` imported from `@/stores/files`
- `src/components/shell/Toolbar.tsx:28` — `const { ingest } = useIngest()` already available in component body
- `src/components/shell/Toolbar.tsx:153-156` — "From URL or paste" menu item onClick currently calls `addFromUrl()`

Target wire:
- Add import: `import { pickFromClipboard } from '@/lib/clipboard-ingest'`
- Remove import: `addFromUrl,` from `@/stores/files` line 22
- New onClick body:
  - `void pickFromClipboard({ ingest })`
  - `setOpen(null)` (existing pattern that closes the popover)

`src/stores/files.ts:100`:
- Delete: `export function addFromUrl(): void {}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Toolbar onClick to pickFromClipboard + remove addFromUrl import</name>
  <files>src/components/shell/Toolbar.tsx</files>
  <read_first>
    - src/components/shell/Toolbar.tsx (lines 1-40 for imports, 140-170 for the menu item — exact line numbers are RESEARCH §1.4 reference points)
    - src/hooks/useIngest.ts (confirm `useIngest()` already destructured as line 28 expects)
    - src/lib/clipboard-ingest.ts (Wave 1 plan 15-02 output — pickFromClipboard signature + ClipboardDispatcher shape)
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§1.4 wire-up block)
    - .planning/phases/15-from-url-or-paste/15-CONTEXT.md (D-14, D-15)
  </read_first>
  <behavior>
    - The "From URL or paste" button's onClick fires `void pickFromClipboard({ ingest })` and `setOpen(null)`
    - No `await` (fire-and-forget; pickFromClipboard handles all toasts internally — matches `useExport.exportOne` precedent per RESEARCH §6 P-13)
    - `addFromUrl` import deleted; no other usage remains in the file
  </behavior>
  <action>
    Edit `src/components/shell/Toolbar.tsx`. (1) On the import line currently importing `addFromUrl` from `@/stores/files`, remove `addFromUrl` from the import list (preserve the rest of the named imports). (2) Add a new import: `import { pickFromClipboard } from '@/lib/clipboard-ingest'` near the other `@/lib/*` imports. (3) Locate the "From URL or paste" menu item onClick (currently invoking `addFromUrl()`). Replace the onClick body with `() => { void pickFromClipboard({ ingest }); setOpen(null) }`. Confirm `const { ingest } = useIngest()` (line ~28) is in scope. Add a trailing inline comment on the onClick line: `// Phase 15 — ING-01: clipboard → ingest dispatcher.` Do NOT change any other Toolbar markup or popover behavior.
  </action>
  <verify>
    <automated>grep -c "addFromUrl" src/components/shell/Toolbar.tsx | grep -q "^0$" && grep -c "pickFromClipboard" src/components/shell/Toolbar.tsx | grep -q "^[12]$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addFromUrl" src/components/shell/Toolbar.tsx` returns 0
    - `grep -c "from '@/lib/clipboard-ingest'" src/components/shell/Toolbar.tsx` returns 1
    - `grep -E "void pickFromClipboard\\(\\{ ingest" src/components/shell/Toolbar.tsx` returns 1 match
    - `grep -c "Phase 15 — ING-01" src/components/shell/Toolbar.tsx` returns 1
    - Popover-close call `setOpen(null)` present in the new onClick
  </acceptance_criteria>
  <done>Toolbar surface lights up: clicking the menu item triggers the real clipboard dispatcher.</done>
</task>

<task type="auto">
  <name>Task 2: Delete addFromUrl stub from src/stores/files.ts</name>
  <files>src/stores/files.ts</files>
  <read_first>
    - src/stores/files.ts (line 100 area — confirm the exact `export function addFromUrl(): void {}` form)
    - .planning/phases/15-from-url-or-paste/15-CONTEXT.md (D-14)
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§1.4)
  </read_first>
  <behavior>
    - `export function addFromUrl(): void {}` deletion is complete and atomic
    - No other store function regressed
  </behavior>
  <action>
    Edit `src/stores/files.ts`. Delete the single line `export function addFromUrl(): void {}` (and an adjacent blank line if it leaves a double-blank). Confirm via grep that `addFromUrl` no longer appears in the file. If the surrounding block has a `// Phase NN — …` provenance comment specifically tied to that stub, delete it too (it was placed only to mark this stub for retirement).
  </action>
  <verify>
    <automated>grep -c "addFromUrl" src/stores/files.ts | grep -q "^0$" && ! grep -r "addFromUrl" src/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addFromUrl" src/stores/files.ts` returns 0
    - `grep -r "addFromUrl" src/` returns 0 matches across the whole src tree
    - No other store exports were touched (diff bounded to the stub line and optional provenance comment)
  </acceptance_criteria>
  <done>Stub retirement complete per D-14 / SC-5; consumers cannot accidentally re-wire to an empty function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playwright e2e — Toolbar From URL or paste + bundle budget gate</name>
  <files>src/tests/toolbar-paste.spec.ts</files>
  <read_first>
    - src/components/shell/Toolbar.tsx (current selector / data-testid for the menu item)
    - src/lib/clipboard-ingest.ts
    - .planning/phases/15-from-url-or-paste/15-RESEARCH.md (§9 task 15-04 — happy + negative + source grep)
    - .planning/phases/15-from-url-or-paste/15-CONTEXT.md (T-15-04 zero-regression on stub)
    - Existing toolbar e2e for the Add files ▾ popover selector pattern
  </read_first>
  <behavior>
    - Happy path: `page.addInitScript` stubs `navigator.clipboard.read` to return a one-image ClipboardItem (tiny PNG). Click `Add files ▾` → click `From URL or paste`. Assert file row count incremented + `Pasted from clipboard:` toast visible + popover closed
    - Negative path: stub `navigator.clipboard.read` to throw NotAllowedError AND `readText` to return `''`. Repeat the click flow. Assert: NO file added, `Clipboard has no image or image URL` toast visible
    - Source-level check: read `src/stores/files.ts` and `src/components/shell/Toolbar.tsx` via Node `fs.readFileSync` inside the spec OR as a separate `test()` that uses `require('node:fs')`. Assert: no `addFromUrl` token in either file. (Playwright tests run under Node, so this is valid.)
  </behavior>
  <action>
    Create `src/tests/toolbar-paste.spec.ts`. Use `page.addInitScript` to inject a `navigator.clipboard` shim BEFORE navigation. Synthesize a tiny PNG (base64 → Uint8Array → Blob). For the happy path, the shim's `read()` returns `[{ types: ['image/png'], getType: async () => blob }]`. For the negative path, it throws `new DOMException('denied', 'NotAllowedError')` and `readText: async () => ''`. Use the Toolbar's existing `Add files ▾` selector pattern (mirror an existing toolbar e2e). After click, assert files-pane row count via the same row selector used by Phase 11 / 14 e2e. Use `page.getByText('Pasted from clipboard:')` and `page.getByText('Clipboard has no image or image URL')`. For the source-grep check, add a final `test('addFromUrl stub fully retired', () => { ... })` that uses `fs.readFileSync` on both files and asserts the substring is absent.
  </action>
  <verify>
    <automated>npx playwright test src/tests/toolbar-paste.spec.ts --reporter=line && npm run test:bundle</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/toolbar-paste.spec.ts` exists
    - `npx playwright test src/tests/toolbar-paste.spec.ts` exits 0
    - `npm run test:bundle` exits 0 (bundle ≤ 200 KB gzip — pre-phase baseline 197.97 KB; Phase 15 adds ~1.5 KB per RESEARCH §8)
    - Spec contains 3 tests: happy, negative, stub-retired
    - Spec uses `page.addInitScript` to install the clipboard shim
    - Stub-retired test asserts `addFromUrl` is absent from both source files via fs.readFileSync
  </acceptance_criteria>
  <done>Toolbar happy + negative paths green; source grep proves the stub is retired; bundle budget gate confirms the 200 KB ceiling holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundaries. It wires the Toolbar onClick into the dispatcher (15-02) and deletes the empty stub (15-14).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 (reuse) | Information Disclosure | URL fetch inherited via pickFromClipboard → pickFromUrl | accept | Mitigated by plan 15-01 (`credentials: 'omit'`); RESEARCH §10 |
| T-15-02 (reuse) | Tampering | Pasted SVG bytes | mitigate | DOMPurify in existing useIngest pipeline (Phase 3) |
| T-15-04 (reuse) | Tampering | URL-derived filename in ZIP export | mitigate | sanitizeBaseName chokepoint (plan 15-01) |
</threat_model>

<verification>
- `npx playwright test src/tests/toolbar-paste.spec.ts` exits 0
- `npm run test:bundle` exits 0
- `grep -r "addFromUrl" src/` returns 0 matches
- `grep -c "pickFromClipboard" src/components/shell/Toolbar.tsx` returns ≥ 1
- All four Phase 15 plans' tests pass together:
  - `node --experimental-strip-types --import ./src/tests/_alias-loader.mjs src/tests/url-ingest.test.ts`
  - `node --experimental-strip-types --import ./src/tests/_alias-loader.mjs src/tests/clipboard-ingest.test.ts`
  - `npx playwright test src/tests/url-ingest.spec.ts src/tests/paste-ingest.spec.ts src/tests/toolbar-paste.spec.ts`
</verification>

<success_criteria>
- Toolbar From URL or paste → clipboard image → file ingested + success toast (SC-1)
- Toolbar From URL or paste → empty clipboard → 'Clipboard has no image or image URL' toast (SC-3)
- `addFromUrl` stub deleted (SC-5)
- Bundle ≤ 200 KB gzip
</success_criteria>

<output>
Create `.planning/phases/15-from-url-or-paste/15-04-SUMMARY.md` when done. Record final bundle size delta from baseline 197.97 KB and confirm all five phase Success Criteria are now backed by passing tests (SC-1 → 15-04 + 15-02, SC-2 → 15-01, SC-3 → 15-02 + 15-04, SC-4 → 15-03, SC-5 → 15-04).
</output>
