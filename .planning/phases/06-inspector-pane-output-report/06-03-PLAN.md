---
phase: 06-inspector-pane-output-report
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/components/panels/InspectorPane.tsx
  - src/tests/inspector-tabs.spec.ts
autonomous: false
requirements: [INSP-07, INSP-08]
must_haves:
  truths:
    - "Clicking the Output tab renders the OutputPanel (snippet sections + copy buttons) in the running app"
    - "Clicking the Report tab renders the ReportPanel (stats grid + bar chart + format breakdown) in the running app"
    - "Copying a snippet from the Output tab writes to the clipboard and flashes 'Copied!'"
    - "The two placeholder 'coming in Phase 6' divs no longer exist in InspectorPane"
  artifacts:
    - path: "src/components/panels/InspectorPane.tsx"
      provides: "Output/Report tab slots mounting OutputPanel and ReportPanel"
      contains: "OutputPanel"
  key_links:
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/components/panels/inspector/OutputPanel.tsx"
      via: "import + render when tab==='output'"
      pattern: "OutputPanel"
    - from: "src/components/panels/InspectorPane.tsx"
      to: "src/components/panels/inspector/ReportPanel.tsx"
      via: "import + render when tab==='report'"
      pattern: "ReportPanel"
---

<objective>
Wire OutputPanel (06-01) and ReportPanel (06-02) into the visible InspectorPane, replacing the two "coming in Phase 6" placeholder divs, then verify the full vertical slice in the browser. After this plan the developer can actually click the Output and Report tabs and use the snippets + report — completing the Phase 6 user story end-to-end.

Purpose: Closes the MVP slice — the panels move from standalone modules to live, clickable UI.
Output: Updated `src/components/panels/InspectorPane.tsx`, an end-to-end Playwright spec, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inspector-pane-output-report/06-UI-SPEC.md
@.planning/phases/06-inspector-pane-output-report/06-01-SUMMARY.md
@.planning/phases/06-inspector-pane-output-report/06-02-SUMMARY.md

<interfaces>
<!-- Current InspectorPane content to modify. The two placeholder divs at the output/report branches must be replaced. -->

From src/components/panels/InspectorPane.tsx (current — lines ~52-60):
```tsx
<div className="flex-1 overflow-y-auto pb-20">
  {tab === 'codec' && <CodecPanel />}
  {tab === 'output' && (
    <div className="p-3 text-xs text-[var(--color-fg-3)]">Output — coming in Phase 6</div>
  )}
  {tab === 'report' && (
    <div className="p-3 text-xs text-[var(--color-fg-3)]">Report — coming in Phase 6</div>
  )}
</div>
```

New panel exports to mount:
```typescript
// src/components/panels/inspector/OutputPanel.tsx
export function OutputPanel(): JSX.Element   // data-testid="output-panel" / "output-empty"
// src/components/panels/inspector/ReportPanel.tsx
export function ReportPanel(): JSX.Element   // data-testid="report-panel" / "report-empty"
```

Note: InspectorPane's TABS array is `['codec', 'output', 'report']` and the pane only renders when a file is selected. ReportPanel reads filesAtom.entries (not $selectedFile) so it always has data, but a file must be selected for the tab bar to show. Do not alter the tab-switch or selection logic (that is INSP-01 / Phase 4 territory).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount OutputPanel + ReportPanel in InspectorPane</name>
  <files>src/components/panels/InspectorPane.tsx, src/tests/inspector-tabs.spec.ts</files>
  <read_first>
    - src/components/panels/InspectorPane.tsx (the exact placeholder divs to replace + import block + TABS array)
    - src/components/panels/inspector/OutputPanel.tsx (export name + testids, from 06-01)
    - src/components/panels/inspector/ReportPanel.tsx (export name + testids, from 06-02)
    - src/tests/navigation.spec.ts (Playwright conventions: selecting a file, clicking tabs, clipboard grant)
    - .planning/phases/06-inspector-pane-output-report/06-UI-SPEC.md (Tab integration note: panels are slot-mounted, visibility via uiAtom.tab)
  </read_first>
  <action>Edit src/components/panels/InspectorPane.tsx: add `import { OutputPanel } from './inspector/OutputPanel'` and `import { ReportPanel } from './inspector/ReportPanel'` alongside the existing CodecPanel import. Replace the `tab === 'output'` placeholder div with `{tab === 'output' && <OutputPanel />}` and the `tab === 'report'` placeholder div with `{tab === 'report' && <ReportPanel />}`. Do NOT change the TABS array, the selection guard, the tab-bar markup, or the `pb-20` scroll container. Leave the CodecPanel branch untouched. Then create src/tests/inspector-tabs.spec.ts (Playwright) that exercises the full slice end-to-end: navigate to '/', grant clipboard permissions via `context.grantPermissions(['clipboard-read', 'clipboard-write'])` (use `test.use` or `browser.newContext` per navigation.spec.ts conventions), click the first file row to select a file, then (a) click the Output tab and assert `getByTestId('output-panel')` is visible and `getByRole('button', { name: 'Copy Base64 snippet' })` is visible; click it and assert the visible label becomes "Copied!"; (b) click the Report tab and assert `getByTestId('report-panel')` is visible, that `getByTestId('report-bar')` resolves to at least one element, and that `getByTestId('format-row')` resolves to at least one element. Use locator selectors that match how rows expose their click target (FileRow uses a ContextMenuTrigger with onClick selectFile — select via the file name text, e.g. `getByText('hero-banner@2x.png')`).</action>
  <verify>
    <automated>npx tsc -b && npx playwright test src/tests/inspector-tabs.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` completes with no type errors
    - InspectorPane.tsx imports both panels (grep -c 'OutputPanel' >= 2 and grep -c 'ReportPanel' >= 2: import + usage each)
    - The placeholder strings are gone (grep -c 'coming in Phase 6' returns 0)
    - The CodecPanel branch is unchanged (grep -c 'tab === .codec. && <CodecPanel' returns 1)
    - `npx playwright test src/tests/inspector-tabs.spec.ts` exits 0 — Output tab shows panel + copy flashes "Copied!"; Report tab shows panel + >=1 bar + >=1 format row
  </acceptance_criteria>
  <done>Both panels are live in InspectorPane; placeholder divs removed; end-to-end spec passes for copy + report rendering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Output + Report tabs render and copy works</name>
  <what-built>OutputPanel (Output tab) and ReportPanel (Report tab) are now wired into the running InspectorPane. The Output tab shows three copy-ready snippet sections (Base64 data URI, URL-encoded CSS background, Responsive <picture>) each with a working copy button. The Report tab shows a Total savings 2×2 stats grid, a per-file savings bar chart (orange/warn bars for <30% savings), and a per-format breakdown list.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Click any file in the left Files Pane queue (e.g. "hero-banner@2x.png") to select it
    3. In the right Inspector pane, click the "output" tab
       - Expect three sections: "Data URI · Base64", "Data URI · URL-encoded", "Responsive `<picture>`"
       - Each shows a mono code block + a "Copy snippet" button
       - Click a copy button → label flashes "Copied!" for ~1.5s, then reverts; paste into a text editor to confirm a real snippet string landed on the clipboard
    4. Click the "report" tab
       - Expect a "Total savings" section with 4 stat cells (Before / After / Saved / Files)
       - Expect a row of bars below the stats — at least one bar should be orange (warn) for a file with <30% savings (e.g. menu-card.webp)
       - Expect a "Format breakdown" section listing PNG / JPG / SVG / WebP rows with file counts and bytes saved
    5. Confirm dark theme tokens look correct and there is no horizontal scroll except inside code blocks
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tab state (uiAtom.tab) → mounted panel | Visibility is store-driven; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | InspectorPane wiring | accept | Pure slot-mount of two already-audited components; no new data sink introduced |
</threat_model>

<verification>
- `npx tsc -b` clean
- `npx playwright test src/tests/inspector-tabs.spec.ts` passes
- Human checkpoint confirms both tabs render and the copy flow works in the browser
</verification>

<success_criteria>
- Output and Report tabs render their panels in the running app; placeholder divs removed
- End-to-end spec passes; copy writes to clipboard and flashes "Copied!"; report shows bars + format rows
- Human verifies the visual + interaction slice
</success_criteria>

<output>
Create `.planning/phases/06-inspector-pane-output-report/06-03-SUMMARY.md` when done
</output>
