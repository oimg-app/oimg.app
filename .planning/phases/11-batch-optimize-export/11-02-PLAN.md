---
phase: 11-batch-optimize-export
plan: 02
type: execute
wave: 1
depends_on: [11-00]
files_modified:
  - src/components/shell/StatusBar.tsx
  - src/tests/status-bar.spec.ts
autonomous: true
requirements: [OPT-02]
user_setup: []
tags: [phase-11, wave-1, statusbar, aria-live, OPT-02]

must_haves:
  truths:
    - "StatusBar shows `X/Y optimized` derived live from runtimeAtom + filesAtom (D-01)"
    - "The counter region announces politely (aria-live='polite' + aria-atomic='true') without flooding the screen-reader queue (Pitfall 4)"
    - "When the queue is empty, the counter renders empty string (no `0/0 optimized` clutter)"
    - "Per-file `encoding` indicator stays indeterminate — no synthetic determinate progress bar fill on FileRow (D-02); codec encodes are atomic, so any intra-file percentage would be dishonest"
  artifacts:
    - path: "src/components/shell/StatusBar.tsx"
      provides: "Aggregate counter element with data-testid='agg-counter'"
      contains: "data-testid=\"agg-counter\""
    - path: "src/tests/status-bar.spec.ts"
      provides: "OPT-02 SC-1 e2e — counter advances during a batch"
  key_links:
    - from: "src/components/shell/StatusBar.tsx"
      to: "src/stores/runtime.ts + src/stores/files.ts"
      via: "useStore(runtimeAtom) + useStore(filesAtom) live derivation"
      pattern: "useStore\\(runtimeAtom\\)"
---

<objective>
Add the aggregate `X/Y optimized` counter span to `StatusBar.tsx` with WCAG-AA-compliant aria-live announcements. Derives state from existing atoms — no new `batchProgressAtom`.

Purpose: D-01 mandates progress surfaces simultaneously per-row AND in the StatusBar. Plan 01 delivers per-row; this plan delivers the aggregate. Both depend on D-03's live streaming write-back (Plan 01).

Output: A `data-testid="agg-counter"` `<span>` in StatusBar that renders `${done}/${total} optimized` while a batch is active and empty string otherwise; pollite aria-live region with `aria-atomic="true"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@.planning/phases/11-batch-optimize-export/11-RESEARCH.md
@.planning/phases/11-batch-optimize-export/11-PATTERNS.md

<interfaces>
<!-- From src/stores/runtime.ts -->
runtimeAtom: { running: boolean; runningJobs: number; queuedJobs: number; encodingFileId: string | null; ... }

<!-- From src/stores/files.ts -->
filesAtom: { entries: FileEntry[]; ... }
type FileEntry = { id: string; name: string; status: 'queued' | 'processing' | 'done' | 'error'; ... }

<!-- From src/components/shell/StatusBar.tsx (current — lines 8-46) -->
// already imports useStore + filesAtom + runtimeAtom
// container <div role="status" aria-live="polite" ...>
// renders worker-pip + BackpressureIndicator + version

<!-- From src/components/shell/BackpressureIndicator.tsx (a11y idiom analog — lines 6-20) -->
<span role="status" aria-live="polite" aria-label={running ? 'Optimization running' : undefined} ... />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aggregate X/Y counter span to StatusBar.tsx</name>
  <files>src/components/shell/StatusBar.tsx</files>
  <read_first>
    - src/components/shell/StatusBar.tsx (entire file — confirm imports and where to insert the new span)
    - src/components/shell/BackpressureIndicator.tsx (a11y idiom — `role="status" aria-live="polite"` + conditional `aria-label`)
    - .planning/phases/11-batch-optimize-export/11-PATTERNS.md § "src/components/shell/StatusBar.tsx (MODIFY — add aggregate counter + aria-live)" — verbatim insertion shape + Pitfall 4 mitigation note
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Pattern 4: Aggregate X/Y optimized counter" + § "Pitfall 4: aria-live announcement spam during fast batches"
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-01 + D-02
  </read_first>
  <behavior>
    - Counter derives live (no new atom): `done = entries.filter(e => e.status === 'done').length`; `total = entries.length`; `inFlight = runningJobs + queuedJobs`
    - Render: `total > 0 ? `${done}/${total} optimized` : ''` (no clutter when queue is empty)
    - Region: `role="status" aria-live="polite" aria-atomic="true" data-testid="agg-counter"`
    - Pitfall 4 mitigation: If the StatusBar container already wraps with `aria-live="polite"`, the new span should NOT introduce a nested live region. Either (a) drop the outer container's `aria-live` and rely on per-element regions, OR (b) keep the outer and add `aria-atomic="true"` ONLY on the counter (the existing approach in BackpressureIndicator). Pick option (b) — minimal diff
    - During a fast batch the announcement queue does not visibly lag — the deferred-value throttle is OPTIONAL for Phase 11; only required if manual screen-reader test (VALIDATION.md manual block) reveals spam
  </behavior>
  <action>
    Modify `src/components/shell/StatusBar.tsx`. Add (or reuse existing) imports of `useStore`, `filesAtom`, `runtimeAtom`. Inside the component, compute three locals: `const { runningJobs, queuedJobs } = useStore(runtimeAtom); const { entries } = useStore(filesAtom); const done = entries.filter(e => e.status === 'done').length; const total = entries.length;`. Insert a new `<span data-testid="agg-counter" role="status" aria-live="polite" aria-atomic="true" className="font-mono text-[11px]">{ total > 0 ? `${done}/${total} optimized` : '' }</span>` between the existing worker-pip block and the version block (consult the file's current JSX order during the Read step). Per 11-PATTERNS.md Pitfall 4 note: if the outer container has `aria-live="polite"`, keep it as-is and rely on `aria-atomic="true"` to ensure the full string is announced. Per D-02: do NOT add a synthetic determinate progress bar; the counter text is sufficient. Use the `className` tailwind shape already present in adjacent spans (match the existing typography).
  </action>
  <acceptance_criteria>
    - `src/components/shell/StatusBar.tsx` contains the substrings `data-testid="agg-counter"`, `aria-live="polite"`, `aria-atomic="true"`, and `optimized`
    - `tsc -b` exits 0
    - No new atom imported (NO `batchProgressAtom` substring anywhere in src/)
    - When `filesAtom.entries.length === 0`, the rendered span has empty text content (no `0/0 optimized`)
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && grep -c "agg-counter" src/components/shell/StatusBar.tsx</automated>
  </verify>
  <done>StatusBar renders the data-testid='agg-counter' span with the correct ARIA and derivation; no new atom; type-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E test — aggregate counter advances mid-batch</name>
  <files>src/tests/status-bar.spec.ts</files>
  <read_first>
    - src/tests/backpressure.spec.ts (Playwright skeleton + ingest helper usage)
    - src/components/shell/StatusBar.tsx (after Task 1 — confirm the data-testid is wired)
    - .planning/phases/11-batch-optimize-export/11-VALIDATION.md row 11-02-counter
  </read_first>
  <behavior>
    - Ingest 6 files, click Optimize all
    - Counter renders `0/6 optimized` immediately after click (or any `K/6 optimized` form where K starts at 0 and advances monotonically)
    - During the batch, counter passes through at least one intermediate value (e.g. some K with `0 < K < 6`)
    - On completion, counter reads `6/6 optimized` and remains stable
    - Counter element has `aria-live="polite"` and `aria-atomic="true"` attributes
  </behavior>
  <action>
    Create `src/tests/status-bar.spec.ts`. Use Playwright. Skeleton: `import { test, expect } from '@playwright/test'; import { ingestFixtureFiles } from './fixtures/ingest-helper'`. Three tests under `test.describe('OPT-02 — Aggregate Counter', ...)`. Test 1: assert ARIA attrs — after page load + ingesting 1 file, `await expect(page.getByTestId('agg-counter')).toHaveAttribute('aria-live', 'polite')`, same for `aria-atomic`. Test 2: assert advancement — ingest 6 files, click Optimize all, then `await page.waitForFunction(() => { const el = document.querySelector('[data-testid=\"agg-counter\"]'); if (!el) return false; const m = (el.textContent ?? '').match(/^(\\d+)\\/6 optimized$/); return m && parseInt(m[1], 10) > 0 && parseInt(m[1], 10) < 6 })` — proves intermediate state visible. Then `await expect(page.getByTestId('agg-counter')).toHaveText('6/6 optimized', { timeout: 30_000 })`. Test 3: assert empty state — fresh page, no ingest, `await expect(page.getByTestId('agg-counter')).toHaveText('')`. Reuse fixture helper from Plan 00 only if `ingestFixtureFiles` doesn't accept N=6 (it should — verify in Read step).
  </action>
  <acceptance_criteria>
    - `src/tests/status-bar.spec.ts` exists with three tests under `test.describe('OPT-02 — Aggregate Counter')`
    - `npx playwright test src/tests/status-bar.spec.ts` exits 0
    - Test 2 fails if Task 1 is reverted (regression guard)
    - Test 3 fails if the empty-state guard is missing (would show `0/0 optimized`)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test src/tests/status-bar.spec.ts --reporter=dot</automated>
  </verify>
  <done>status-bar.spec.ts exists with three green tests asserting D-01 aggregate counter behavior + ARIA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen reader live region | aria-live=polite + aria-atomic=true is the standard non-urgent status pattern (WAI-ARIA APG); no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-A11Y | Denial of Service (assistive tech) | aria-live announcement queue saturation during fast batches | accept | Pitfall 4 documents the throttle mitigation as OPTIONAL; for Phase 11 batches (≤200 files in expected use) `polite` is sufficient. Manual screen-reader verification deferred to VALIDATION.md manual block |
| T-11-LR | Information Disclosure | Two competing live regions in the same container | mitigate | Task 1 explicitly keeps the outer container's existing aria-live (option b in PATTERNS.md note) — aria-atomic on the inner span ensures the full string is announced without nesting |
</threat_model>

<verification>
- `tsc -b` exits 0
- `npx playwright test src/tests/status-bar.spec.ts --reporter=dot` exits 0
- `grep -c "batchProgressAtom" src/` returns 0 (no new atom)
</verification>

<success_criteria>
- D-01: StatusBar shows `X/Y optimized` derived live; counter advances mid-batch
- WCAG-AA: live region uses `polite` + `aria-atomic="true"`
- No new state added — derivation only
</success_criteria>

<output>
Create `.planning/phases/11-batch-optimize-export/11-02-SUMMARY.md` when done.
</output>
