---
phase: 11-batch-optimize-export
plan: 07
type: execute
wave: 2
depends_on: [11-00, 11-04, 11-05, 11-06]
files_modified:
  - src/stores/files.ts
  - src/components/shell/Toolbar.tsx
  - src/components/panels/inspector/ReportPanel.tsx
  - src/tests/export-disabled.spec.ts
autonomous: true
requirements: [OPT-02, EXP-01, EXP-02]
user_setup: []
tags: [phase-11, wave-2, disable-then-explain, computed-atom, D-13]

must_haves:
  truths:
    - "Toolbar 'All as ZIP' and 'Save individually' buttons are disabled with an explanatory tooltip until ≥1 file in the queue has status='done' (D-13)"
    - "Inspector Download button is hidden when no done file is selected (already covered by Plan 04 conditional render)"
    - "FileRow ContextMenu 'Save as…' disabled per-row (already covered by Plan 06)"
    - "Derived atom $hasDone uses nanostores computed() — matches existing $filteredFiles/$selectedFile/$totals shape in files.ts"
  artifacts:
    - path: "src/stores/files.ts"
      provides: "$hasDone computed atom (new)"
      exports: ["$hasDone"]
    - path: "src/components/shell/Toolbar.tsx"
      provides: "Export buttons bound to $hasDone for disable + aria-disabled + title"
    - path: "src/tests/export-disabled.spec.ts"
      provides: "D-13 e2e — disabled states + tooltip + enabling on first done"
  key_links:
    - from: "src/components/shell/Toolbar.tsx"
      to: "src/stores/files.ts"
      via: "useStore($hasDone)"
      pattern: "\\$hasDone"
---

<objective>
Implement D-13 "disable-then-explain" for all three export entry points: Toolbar "All as ZIP", Toolbar "Save individually", Inspector Download. Add a single derived atom (`$hasDone`) and bind disabled + aria-disabled + title to it.

Purpose: Prevents shipping a "batch" that's actually empty or originals-only, and removes the need for a separate "optimize first?" prompt (per CONTEXT.md D-13). The Inspector button's conditional render in Plan 04 already covers its case; Plan 06's per-row gate covers FileRow. This plan covers the Toolbar.

Output: One new computed atom + Toolbar button attribute updates + an e2e spec that asserts disabled states pre-optimize and enabling post-optimize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@.planning/phases/11-batch-optimize-export/11-RESEARCH.md
@.planning/phases/11-batch-optimize-export/11-PATTERNS.md

<interfaces>
<!-- Existing computed atoms in src/stores/files.ts (lines ~25-63) -->
import { computed } from 'nanostores'
export const $filteredFiles = computed(filesAtom, (s) => ...)
export const $selectedFile = computed(filesAtom, (s) => ...)
export const $totals = computed(filesAtom, (s) => ...)
// Add: $hasDone (this plan)

<!-- Toolbar.tsx current button shape (lines ~88-114 per 11-PATTERNS.md) -->
<button type="button" className={cn(tbtnClass, ...)} onClick={...}>Export</button>
// Plan 05 wired onClick → useExport().exportZip; this plan adds disable + aria-disabled + title
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add $hasDone computed atom to src/stores/files.ts</name>
  <files>src/stores/files.ts</files>
  <read_first>
    - src/stores/files.ts lines 25-63 (existing computed atoms — copy the shape verbatim)
    - .planning/phases/11-batch-optimize-export/11-PATTERNS.md § "src/stores/files.ts (MODIFY — wire export stubs)" — Add a derived atom for D-13 disable selector
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Pattern 5: Disable-then-explain selector (D-13)"
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-13
  </read_first>
  <behavior>
    - `$hasDone` is a `computed` nanostore derived from `filesAtom`
    - Returns `true` iff `s.entries.some(e => e.status === 'done')`
    - Re-evaluates on every `filesAtom` change (handled by nanostores computed)
    - Matches the shape of the three existing computed atoms in this file (`$filteredFiles`, `$selectedFile`, `$totals`) — verifies consistency
  </behavior>
  <action>
    Modify `src/stores/files.ts`. Confirm `import { computed } from 'nanostores'` is already at the top (it should be — three existing `computed` calls exist). Add the export immediately after the last existing computed atom (find via grep `computed(filesAtom`): `export const $hasDone = computed(filesAtom, (s) => s.entries.some(e => e.status === 'done'))`. One line. Add a JSDoc above: `/** D-13: any file in the queue has status='done' — gates export controls. */`. Do NOT introduce a `batchProgressAtom` or any other new state (RESEARCH.md anti-pattern explicit rejection). Verify `tsc -b` still passes.
  </action>
  <acceptance_criteria>
    - `src/stores/files.ts` exports `$hasDone`
    - The exported value is a `computed(filesAtom, (s) => ...)` call — same shape as `$filteredFiles`/`$selectedFile`/`$totals`
    - `tsc -b` exits 0
    - `grep -c '^export const \\$hasDone' src/stores/files.ts` returns 1
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && grep -c '^export const \$hasDone' src/stores/files.ts</automated>
  </verify>
  <done>$hasDone computed atom added in the existing computed-atom shape; no new state introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind Toolbar Export buttons to $hasDone (disabled + aria-disabled + title)</name>
  <files>src/components/shell/Toolbar.tsx, src/components/panels/inspector/ReportPanel.tsx</files>
  <read_first>
    - src/components/shell/Toolbar.tsx (current — find the "Export" / "All as ZIP" / "Save individually" buttons; Plan 05 just wired the onClick handlers, this plan adds the disable gate)
    - src/stores/files.ts (verify $hasDone exists from Task 1)
    - src/components/panels/inspector/ReportPanel.tsx (verify the Plan 04 conditional render `selected && selected.status === 'done'` is still the gate — no change needed here unless the Download button was made always-visible)
    - .planning/phases/11-batch-optimize-export/11-PATTERNS.md § "src/components/shell/Toolbar.tsx (MODIFY — wire export + D-13 disable)" — verbatim disable shape
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Pattern 5: Disable-then-explain selector"
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-13
  </read_first>
  <behavior>
    - When `$hasDone === false`: Toolbar "Export" / "All as ZIP" button has `disabled` AND `aria-disabled="true"` AND `title="Optimize at least one file first"` AND visual indication (`opacity-50 cursor-not-allowed` matching existing disabled-button style if present in the codebase, else just opacity)
    - When `$hasDone === false`: Toolbar "Save individually" menu item has the same disable + title treatment
    - When `$hasDone === true`: all three controls are fully interactive (no disabled, no aria-disabled, no title)
    - Inspector Download button: stays under Plan 04's conditional render (`selected && selected.status === 'done'`); this plan does NOT modify it (verify on Read)
  </behavior>
  <action>
    Modify `src/components/shell/Toolbar.tsx`. Add `import { $hasDone } from '@/stores/files'` (verify the import path matches the file's existing aliases). Near other `useStore` calls: `const hasDone = useStore($hasDone)`. Locate the "Export" / "All as ZIP" `<button>` (per 11-PATTERNS.md, around lines 88-114). Add three attributes: `disabled={!hasDone}`, `aria-disabled={!hasDone}`, `title={!hasDone ? 'Optimize at least one file first' : undefined}`. Append `!hasDone && 'opacity-50 cursor-not-allowed'` to the existing `cn(...)` className (use the existing `cn` helper). Repeat for the "Save individually" `<button>` / `MenuItem` (locate via the existing `exportIndividually` onClick binding from Plan 05). DO NOT modify the "Optimize all" button (that's not an export control). Verify `src/components/panels/inspector/ReportPanel.tsx`: the Plan 04 conditional `selected && selected.status === 'done' && (...)` already gates the Download button — no change needed; if a deviation is found (e.g. button was made always-visible during Plan 04 revisions), restore the conditional render so the button is hidden when not applicable (consistent with D-13's intent for the Inspector which only renders when a file is selected).
  </action>
  <acceptance_criteria>
    - `src/components/shell/Toolbar.tsx` imports `$hasDone` from `@/stores/files`
    - The Export/`All as ZIP` button has `disabled={!hasDone}` AND `aria-disabled={!hasDone}` AND `title={!hasDone ? 'Optimize at least one file first' : undefined}`
    - The "Save individually" control has the same three attributes
    - `tsc -b` exits 0
    - Inspector ReportPanel still uses the Plan 04 conditional render (no `disabled` style on the Download button — it's render-gated, not attribute-gated, because Inspector content is selection-driven)
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && grep -c "hasDone" src/components/shell/Toolbar.tsx</automated>
  </verify>
  <done>Toolbar buttons bound to $hasDone; disabled + aria-disabled + title applied; Inspector path verified unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E spec — D-13 disabled states + enabling on first done</name>
  <files>src/tests/export-disabled.spec.ts</files>
  <read_first>
    - src/tests/backpressure.spec.ts (Playwright skeleton)
    - src/components/shell/Toolbar.tsx (after Task 2)
    - .planning/phases/11-batch-optimize-export/11-VALIDATION.md row 11-07-disable
  </read_first>
  <behavior>
    - Test 1 (initial state): page load with no files → Toolbar "Export" / "All as ZIP" has `disabled` AND `aria-disabled="true"` AND `title` contains "Optimize at least one file first"
    - Test 2 (queued only): ingest 1 file but do NOT optimize → button still disabled (status='queued' ≠ 'done')
    - Test 3 (enabling on first done): ingest 1 file, optimize, wait for done → button becomes `not.toBeDisabled()` AND title attribute is absent (or set to `undefined`)
    - Test 4 (Save individually parallel): same flow as Test 3 but assert against the "Save individually" control — disabled before optimize, enabled after
    - Test 5 (Inspector Download invisible before done): ingest 1 file, do not optimize, select the file → Download button is `not.toBeVisible()` (Plan 04 conditional render gating)
  </behavior>
  <action>
    Create `src/tests/export-disabled.spec.ts`. Use Playwright. Five tests under `test.describe('D-13 — Export disable-then-explain')`. Test 1: `await page.goto('/')`; locate the All-as-ZIP button by accessible name (`page.getByRole('button', { name: /All as ZIP/i })` — verify exact name from Toolbar.tsx during Read step); assert `toBeDisabled()`, `toHaveAttribute('aria-disabled', 'true')`, `toHaveAttribute('title', /Optimize at least one file first/)`. Test 2: `await ingestFixtureFiles(page, 1)`; same assertions. Test 3: ingest + optimize + latch `[aria-label="Status: done"]`; then `await expect(page.getByRole('button', { name: /All as ZIP/i })).not.toBeDisabled()`. Test 4: same flow targeting "Save individually" control. Test 5: ingest 1 fixture without optimizing; click the row to select it; `await expect(page.getByRole('button', { name: 'Download optimized file' })).not.toBeVisible()`.
  </action>
  <acceptance_criteria>
    - `src/tests/export-disabled.spec.ts` exists with 5 tests under `test.describe('D-13 — Export disable-then-explain')`
    - `npx playwright test src/tests/export-disabled.spec.ts` exits 0
    - Test 3 fails if `$hasDone` is removed or Task 2 disable binding is reverted
    - Test 5 fails if Plan 04's conditional render is removed
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test src/tests/export-disabled.spec.ts --reporter=dot</automated>
  </verify>
  <done>Five green tests cover D-13 disable + tooltip + enable-on-first-done for all three control surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$hasDone` derivation → UI state | Single derived computed; consumed in three places. Nanostores computed re-runs on `filesAtom` change — no manual sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-D13 | Tampering | User triggers export on an empty/queued-only batch (would yield empty ZIP or no-op) | mitigate | Task 2 disables all three control surfaces; Plan 05's `buildZip` empty-input guard is defense-in-depth |
| T-11-A11Y2 | Denial of Service (a11y) | `disabled` attribute without `aria-disabled` confuses some assistive tech | mitigate | Task 2 applies BOTH `disabled` and `aria-disabled` per WAI-ARIA recommendation; Tests 1+2 assert both |
| T-11-TIP | Information Disclosure | `title` tooltip text revealing internal state names | accept | Tooltip text is user-facing copy ("Optimize at least one file first") — no internal state leaked |
</threat_model>

<verification>
- `tsc -b` exits 0
- `npx playwright test src/tests/export-disabled.spec.ts --reporter=dot` exits 0
- `grep -c '\\$hasDone' src/stores/files.ts` returns 1 (the export declaration)
- `grep -c "hasDone" src/components/shell/Toolbar.tsx` returns ≥ 1
</verification>

<success_criteria>
- D-13 fully implemented across all three export control surfaces
- Single derived atom (no new state machine)
- WCAG-AA: disabled + aria-disabled + explanatory title
</success_criteria>

<output>
Create `.planning/phases/11-batch-optimize-export/11-07-SUMMARY.md` when done.
</output>
