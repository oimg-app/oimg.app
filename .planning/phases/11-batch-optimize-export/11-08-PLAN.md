---
phase: 11-batch-optimize-export
plan: 08
type: execute
wave: 3
depends_on: [11-00, 11-01]
files_modified:
  - src/tests/backpressure.spec.ts
autonomous: true
requirements: [OPT-02]
user_setup: []
tags: [phase-11, wave-3, backpressure, SC-4, e2e]

must_haves:
  truths:
    - "During a ≥20-file batch, WorkerPool peak runningJobs is bounded by the dynamic cap `Math.min(navigator.hardwareConcurrency || 4, 4)` derived in-page (Phase 11 ROADMAP SC-4) — CI runners with hwConc=2 must cap at 2"
    - "Peak runningJobs reaches ≥2 during the batch (proves the pool actually parallelizes — guards against Pitfall 1 regression)"
    - "Test reads window.__peakRunning written by the Plan 00 main.tsx bridge (only available in import.meta.env.MODE === 'test')"
  artifacts:
    - path: "src/tests/backpressure.spec.ts"
      provides: "Extended SC-4 assertion on ≥20-file batch using window.__peakRunning bridge"
  key_links:
    - from: "src/tests/backpressure.spec.ts"
      to: "src/main.tsx (test-mode bridge from Plan 00)"
      via: "window.__peakRunning + window.__runningJobs"
      pattern: "__peakRunning"
---

<objective>
Add (or extend) the SC-4 backpressure e2e assertion: during a ≥20-file batch, the WorkerPool's concurrency cap (≤ min(hwConc, 4)) must hold AND parallelism must actually be exercised (peak ≥ 2). The test consumes the test-mode `window.__peakRunning` bridge from Plan 00.

Purpose: This is the SC-4 success criterion from ROADMAP. It guards against Pitfall 1 (RESEARCH.md) — the most likely regression from Plan 01's streaming refactor. Wave 3 (this plan) runs LAST because it depends on Plan 01's streaming write-back AND Plan 00's bridge.

Output: An extended `src/tests/backpressure.spec.ts` with the SC-4 ≥20-file assertion using the peak-tracking bridge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@.planning/phases/11-batch-optimize-export/11-RESEARCH.md
@.planning/phases/11-batch-optimize-export/11-PATTERNS.md

<interfaces>
<!-- Plan 00 main.tsx bridge (test mode only) -->
window.__runningJobs: number   // mirrors runtimeAtom.runningJobs
window.__peakRunning: number   // monotonic max of __runningJobs over the session

<!-- src/tests/backpressure.spec.ts (existing — Phase 8 SC-4 partial) -->
// Currently tests pool cap under a smaller batch. This plan adds the ≥20-file SC-4 case.

<!-- src/tests/fixtures/batch-fixtures.ts (Plan 00) -->
batchFixtures: Array of 20 fixture files

<!-- WorkerPool cap (src/lib/worker-pool.ts) -->
// cap = Math.min(navigator.hardwareConcurrency || 4, 4)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend src/tests/backpressure.spec.ts with SC-4 ≥20-file assertion</name>
  <files>src/tests/backpressure.spec.ts</files>
  <read_first>
    - src/tests/backpressure.spec.ts (entire file — find existing tests and the imports pattern)
    - src/main.tsx (Plan 00 bridge — confirm `__peakRunning` is written under `MODE === 'test'`)
    - src/tests/fixtures/batch-fixtures.ts (Plan 00 — 20 fixtures)
    - src/tests/fixtures/ingest-helper.ts (existing helper — verify it can ingest N=20 files; if not, this task includes adding an `ingestNFixtureFiles(page, n)` shim)
    - src/lib/worker-pool.ts (cap derivation: `Math.min(navigator.hardwareConcurrency, 4)` — the assertion must mirror this expression)
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Code Examples → SC-4 backpressure assertion (Playwright)" — full implementation
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Pitfall 1: Backpressure broken by streaming refactor" — the bug this test guards against
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md § "Claude's Discretion" → "Backpressure preservation under load" bullet
    - .planning/ROADMAP.md Phase 11 Success Criterion 4
  </read_first>
  <behavior>
    - Test ingests 20 fixtures (from `batchFixtures` via the helper)
    - Clicks "Optimize all"
    - Polls `window.__peakRunning` via `page.waitForFunction` until it's `≥ 2` (proves the pool actually parallelized — the streaming refactor did NOT serialize to cap=1)
    - After batch completes (latch on `await expect(page.locator('[aria-label="Status: done"]')).toHaveCount(20)`), reads the dynamic cap inside the page via `const cap = await page.evaluate(() => Math.min(navigator.hardwareConcurrency || 4, 4))` and asserts `window.__peakRunning ≤ cap` — the bounded pool cap mirrors `src/lib/worker-pool.ts` so a CI runner with `hwConc=2` should cap at 2, not 4
    - Combined assertion is `2 ≤ peak ≤ cap` — the lower bound `>= 2` stays (proves the pool is actually parallel, not serialized); the upper bound MUST be the dynamic `cap`, never a hardcoded literal `4`
    - The cap is dynamic on purpose: hardcoding `≤ 4` would mask a regression where `hwConc=2` but the pool over-spawns to 4 (the test would still pass on a 4-core dev box but the production WorkerPool would be broken)
    - Test reads the bridge directly — no `page.addInitScript` rAF polling needed (Plan 00's main.tsx bridge subscribes to runtimeAtom directly, providing a cleaner data source than the RESEARCH.md prototype's rAF loop)
  </behavior>
  <action>
    Modify `src/tests/backpressure.spec.ts`. Add a new test under the existing `test.describe('Backpressure', ...)` block (or create the describe if not present): `test('SC-4: WorkerPool concurrency stays at cap during ≥20-file batch', async ({ page }) => { ... })`. Body: `await page.goto('/')`; `await page.waitForFunction(() => typeof (window as any).__peakRunning === 'number')` (verifies the Plan 00 bridge initialized — peak starts at 0); `await ingestFixtureFiles(page, 20)` (use the existing helper if it accepts N=20; if it doesn't, add `await page.evaluate((fixtures) => { ... ingest via the existing dropzone API ... }, await import('./fixtures/batch-fixtures.ts').then(m => m.batchFixtures.slice(0, 20)))` inside the test); `await page.getByRole('button', { name: 'Optimize all' }).click()`; `await page.waitForFunction(() => ((window as any).__peakRunning ?? 0) >= 2, { timeout: 15_000 })` (proves parallelism — catches Pitfall 1 regression where streaming refactor accidentally serializes); `await expect(page.locator('[aria-label="Status: done"]')).toHaveCount(20, { timeout: 60_000 })` (full batch settles); `const cap = await page.evaluate(() => Math.min(navigator.hardwareConcurrency || 4, 4))` (mirror the WorkerPool's own cap derivation from `src/lib/worker-pool.ts` — this MUST be the exact literal `Math.min(navigator.hardwareConcurrency || 4, 4)` so the assertion adapts to CI runners with `hwConc=2`); `const peak = await page.evaluate(() => (window as any).__peakRunning ?? 0)`; `expect(peak).toBeGreaterThanOrEqual(2)`; `expect(peak).toBeLessThanOrEqual(cap)`. The test file MUST contain the exact substring `Math.min(navigator.hardwareConcurrency || 4, 4)` — do NOT regress to a literal `expect(peak).toBeLessThanOrEqual(4)`; that would mask hwConc=2 cap-bypass regressions. Add a brief inline comment above the `cap` line explaining the dynamic-cap rationale (mirror of WorkerPool, adapts to CI runners with hwConc=2). DO NOT add a separate "kill" path — if the test times out, that itself is the failure signal. DO NOT use `--ui` or `--watch` flags. Per VALIDATION.md, the helper extension (if needed) is part of Plan 00's scope but if `ingestFixtureFiles(page, 20)` fails in practice (helper hard-coded for smaller N), add the `batchFixtures`-driven path inline in THIS test only — do NOT modify the helper file (out of scope for Wave 3).
  </action>
  <acceptance_criteria>
    - `src/tests/backpressure.spec.ts` contains a new test whose name contains the literal substring `SC-4` and `20-file batch`
    - `npx playwright test src/tests/backpressure.spec.ts` exits 0 (existing tests + new SC-4 test all pass)
    - `tests/e2e/backpressure.spec.ts` (i.e. `src/tests/backpressure.spec.ts` in this repo layout) contains `Math.min(navigator.hardwareConcurrency || 4, 4)` to derive the cap so the executor doesn't regress to a literal `4` — verifiable via `grep -F 'Math.min(navigator.hardwareConcurrency || 4, 4)' src/tests/backpressure.spec.ts` returning ≥ 1
    - Test asserts `peak >= 2` AND `peak <= cap` (where `cap` is captured from the page via `page.evaluate`) — NOT a hardcoded literal `4` in the `toBeLessThanOrEqual` call
    - Test fails if Plan 01's streaming refactor is reverted to an `await pool.run` inside a for loop (the regression Pitfall 1 documents)
    - Test fails if Plan 00's main.tsx bridge is removed (peak would stay 0 forever)
    - Test fails on a CI runner with `navigator.hardwareConcurrency === 2` if the WorkerPool over-spawns to 4 (the dynamic cap catches what a hardcoded `≤ 4` would miss)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test src/tests/backpressure.spec.ts --reporter=dot</automated>
  </verify>
  <done>backpressure.spec.ts has the SC-4 ≥20-file assertion; combined `2 ≤ peak ≤ cap` enforced with dynamic in-page cap; consumes Plan 00 bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-mode `window.__peakRunning` bridge | Only present when `import.meta.env.MODE === 'test'` (Plan 00 gate); cannot be observed in prod bundle |
| Playwright timing assumptions | The polling latch (`waitForFunction` peak ≥ 2) is the established Phase 10 NAV-02 pattern — handles slow CI machines without flakiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CAP | Denial of Service | WorkerPool cap bypassed (concurrency > min(hwConc, 4) saturates CPU) | mitigate | Task 1's `peak ≤ cap` assertion (cap derived in-page via `Math.min(navigator.hardwareConcurrency || 4, 4)`) fails if any regression in Plan 01's streaming refactor or the WorkerPool's `_drain` loop bypasses the cap on any hardware profile, not just 4-core dev boxes |
| T-11-SER | Denial of Service | Streaming refactor accidentally serializes (peak = 1) | mitigate | Task 1's `peak ≥ 2` assertion catches the Pitfall 1 regression (RESEARCH.md) |
| T-11-FLK | Denial of Service (CI flakiness) | Latch-based polling timing out on slow CI | accept | Generous timeouts (15s for peak ≥ 2, 60s for full batch settle); existing Phase 10 NAV-02 pattern proven across CI machines |
</threat_model>

<verification>
- `npx playwright test src/tests/backpressure.spec.ts --reporter=dot` exits 0
- `grep -c "SC-4" src/tests/backpressure.spec.ts` returns ≥ 1
- `grep -F "Math.min(navigator.hardwareConcurrency || 4, 4)" src/tests/backpressure.spec.ts` returns ≥ 1
- The test demonstrably fails if `Promise.allSettled` is reintroduced or if the bridge is removed
</verification>

<success_criteria>
- ROADMAP Phase 11 SC-4 covered: backpressure holds during ≥20-file batch
- Pitfall 1 regression guard: peak ≥ 2 prevents accidental serialization
- Cap assertion is dynamic (mirrors `Math.min(navigator.hardwareConcurrency || 4, 4)`) — covers CI runners with hwConc=2 as well as 4-core dev boxes
- Consumes Plan 00's test-mode bridge — production bundle unaffected
</success_criteria>

<output>
Create `.planning/phases/11-batch-optimize-export/11-08-SUMMARY.md` when done.
</output>
