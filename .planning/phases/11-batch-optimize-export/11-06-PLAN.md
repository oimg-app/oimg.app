---
phase: 11-batch-optimize-export
plan: 06
type: execute
wave: 2
depends_on: [11-00, 11-04]
files_modified:
  - src/components/panels/files/FileRow.tsx
  - src/tests/file-row-menu.spec.ts
autonomous: true
requirements: [EXP-01]
user_setup: []
tags: [phase-11, wave-2, file-row, context-menu, EXP-01]

must_haves:
  truths:
    - "FileRow ContextMenu 'Save as…' item calls useExport().exportOne(file) when the row's file has status='done' (D-04)"
    - "When file.status !== 'done', the menu item is disabled with title 'Optimize this file first' (RESEARCH.md Open Question 1 recommendation)"
    - "The menu opens via right-click AND via clicking the existing ctxbtn (synthesized contextmenu event); both paths invoke the wired action"
    - "Menu is keyboard accessible (ESC closes; arrow keys navigate items) — inherited from the existing Radix ContextMenu"
  artifacts:
    - path: "src/components/panels/files/FileRow.tsx"
      provides: "Wired ContextMenuItem 'Save as…' invoking useExport.exportOne"
    - path: "src/tests/file-row-menu.spec.ts"
      provides: "EXP-01 per-row menu wiring + WCAG-AA keyboard openable + ESC close"
  key_links:
    - from: "src/components/panels/files/FileRow.tsx"
      to: "src/hooks/useExport.ts"
      via: "useExport().exportOne(file) wired into ContextMenuItem onSelect"
      pattern: "exportOne\\("
---

<objective>
Wire the existing `FileRow` ContextMenu's `Save as…` stub to `useExport().exportOne`. Per RESEARCH.md and PATTERNS.md, the existing Radix `ContextMenu` already satisfies the D-04 WCAG-AA requirements (keyboard-openable, ESC close, arrow nav inherited from Radix) — this plan does NOT extract a new `FileRowMenu.tsx` component.

Purpose: D-04 mandates per-row download via the per-row "File options" context menu. The menu and the ctxbtn already exist (Phase 2); only the onSelect handler is a stub.

Output: One-line wiring change in FileRow.tsx + an e2e spec covering keyboard openability, ESC close, and the wired action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-batch-optimize-export/11-CONTEXT.md
@.planning/phases/11-batch-optimize-export/11-RESEARCH.md
@.planning/phases/11-batch-optimize-export/11-PATTERNS.md

<interfaces>
<!-- src/components/panels/files/FileRow.tsx existing structure -->
// Lines 73-163: <ContextMenu> wraps the row; <ContextMenuContent> has multiple <ContextMenuItem> entries
// Lines 117-123: ctxbtn synthesizes contextmenu event via dispatchEvent(new MouseEvent('contextmenu', ...))
// Lines 136-139: STUB to wire:
//   <ContextMenuItem onSelect={() => { /* @TODO Phase 3 — pushToast('Save as') */ }}>
//     <DownloadSimple size={14} />
//     Save as…
//   </ContextMenuItem>

<!-- src/hooks/useExport.ts (Plan 04 + Plan 05) -->
useExport(): { exportOne, exportZip, exportIndividually }

<!-- FileEntry status enum (for the disable gate) -->
type FileStatus = 'queued' | 'processing' | 'done' | 'error'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the ContextMenuItem 'Save as…' to useExport.exportOne</name>
  <files>src/components/panels/files/FileRow.tsx</files>
  <read_first>
    - src/components/panels/files/FileRow.tsx (entire file — find the existing `ContextMenuItem` stub for `Save as…` around lines 136-139)
    - src/hooks/useExport.ts (after Plan 04 + 05 — confirm `exportOne` is exported)
    - .planning/phases/11-batch-optimize-export/11-PATTERNS.md § "src/components/panels/files/FileRow.tsx (MODIFY — wire ContextMenu 'Save as…')" — verbatim wiring + Open Question 1 disposition (render disabled, not absent)
    - .planning/phases/11-batch-optimize-export/11-RESEARCH.md § "Open Questions" Q1 (recommendation: disabled when status !== 'done' with title)
    - .planning/phases/11-batch-optimize-export/11-CONTEXT.md D-04
  </read_first>
  <behavior>
    - When `file.status === 'done'`: ContextMenuItem is enabled, clicking invokes `useExport().exportOne(file)` (which dispatches to saveBlob from Plan 04)
    - When `file.status !== 'done'`: ContextMenuItem renders as `disabled` with `title="Optimize this file first"` (RESEARCH.md Open Question 1 recommendation — consistent with D-13 disable-then-explain philosophy)
    - Menu still opens via right-click AND via clicking the existing `ctxbtn` (no change to either path)
    - Keyboard a11y is unchanged — Radix `ContextMenu` handles ESC + arrow nav (verified by Phase 2 + e2e test below)
  </behavior>
  <action>
    Modify `src/components/panels/files/FileRow.tsx`. Add import: `import { useExport } from '@/hooks/useExport'`. Near the existing `useStore` calls at the top of the component (around line 42-43): `const { exportOne } = useExport()`. Locate the stub `ContextMenuItem` for `Save as…` (lines ~136-139 — confirm exact line numbers during Read). Replace with: `<ContextMenuItem disabled={file.status !== 'done'} title={file.status !== 'done' ? 'Optimize this file first' : undefined} onSelect={() => { void exportOne(file) }}><DownloadSimple size={14} />Save as…</ContextMenuItem>`. Per CLAUDE.md project rule, the `onSelect` body is ONE statement (no inline logic). Do NOT extract a new `FileRowMenu.tsx` component — 11-PATTERNS.md and RESEARCH.md both recommend keeping the wire-in-place (matches Phase 10 "do not over-extract" precedent).
  </action>
  <acceptance_criteria>
    - `src/components/panels/files/FileRow.tsx` contains `useExport` import
    - The `Save as…` `ContextMenuItem` no longer contains `@TODO Phase 3`
    - The item has `disabled={file.status !== 'done'}` attribute
    - The item's `onSelect` is exactly `() => { void exportOne(file) }`
    - No new file `src/components/panels/files/FileRowMenu.tsx` is created
    - `tsc -b` exits 0
  </acceptance_criteria>
  <verify>
    <automated>tsc -b && grep -c "exportOne(file)" src/components/panels/files/FileRow.tsx</automated>
  </verify>
  <done>FileRow ContextMenu 'Save as…' is wired to exportOne; disabled gating + title tooltip applied for non-done rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E spec — context menu open + keyboard nav + wired action</name>
  <files>src/tests/file-row-menu.spec.ts</files>
  <read_first>
    - src/tests/backpressure.spec.ts (Playwright skeleton)
    - src/tests/setup/save-file-mocks.ts (Plan 00 — installSaveFileMocks for capturing the saved blob)
    - src/components/panels/files/FileRow.tsx (after Task 1)
    - .planning/phases/11-batch-optimize-export/11-VALIDATION.md row 11-06-menu
  </read_first>
  <behavior>
    - Test 1 (right-click opens menu, Save as… invokes exportOne): ingest + optimize one file; right-click the row; click `Save as…`; assert `window.__savedFiles` captured a blob (saveBlob ran)
    - Test 2 (ctxbtn click opens menu): same setup; click the row's ctxbtn instead of right-click; assert menu opens (item is visible)
    - Test 3 (ESC closes menu): open menu via either path; press Escape; assert menu is no longer visible
    - Test 4 (arrow nav focuses items): open menu; press ArrowDown; assert first item has focus (Radix focuses first item on open; ArrowDown moves to next)
    - Test 5 (Save as… disabled when status !== 'done'): ingest a file but DO NOT optimize (or use a fixture that fails); right-click; the `Save as…` item has `aria-disabled="true"` (Radix maps `disabled` prop to ARIA)
  </behavior>
  <action>
    Create `src/tests/file-row-menu.spec.ts`. Use Playwright. Five tests under `test.describe('EXP-01 — Per-row File Options Menu (D-04)')`. Common setup: install save-file mocks in 'accept' mode; ingest 1 fixture. Test 1: optimize the file (latch on `Status: done`); right-click the row (locate by `[aria-label="Status: done"]` ancestor); click `Save as…`; `await page.waitForFunction(() => (window as any).__savedFiles?.length >= 1)`. Test 2: optimize; click the ctxbtn (the row's overflow button — locate via aria-label or icon); assert the Save-as item is `await expect(page.getByText('Save as…')).toBeVisible()`. Test 3: open menu; `await page.keyboard.press('Escape')`; `await expect(page.getByText('Save as…')).not.toBeVisible()`. Test 4: open menu; `await page.keyboard.press('ArrowDown')`; assert focus is on a menuitem `await expect(page.locator('[role="menuitem"]:focus')).toHaveCount(1)`. Test 5: ingest but skip optimize (file stays in queued/processing). Right-click; assert `await expect(page.getByText('Save as…')).toHaveAttribute('aria-disabled', 'true')` (or check `data-disabled` if Radix uses that — verify by inspecting DOM during dev).
  </action>
  <acceptance_criteria>
    - `src/tests/file-row-menu.spec.ts` exists with 5 tests under `test.describe('EXP-01 — Per-row File Options Menu (D-04)')`
    - `npx playwright test src/tests/file-row-menu.spec.ts` exits 0
    - Test 3 fails if ESC handling is broken (regression guard for WCAG-AA)
    - Test 5 fails if the disabled-when-not-done gate is missing (regression guard for D-04 / Open Question 1)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test src/tests/file-row-menu.spec.ts --reporter=dot</automated>
  </verify>
  <done>Five green tests cover D-04 + WCAG-AA keyboard/ESC behavior + disabled gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Radix ContextMenu a11y guarantees | Trusted: Radix `ContextMenu` has been a11y-audited upstream + Phase 2 verified in this codebase; this plan reuses the primitive |
| `file.status` gate → action enablement | Same status enum used everywhere; truthy `'done'` is the only path that invokes saveBlob |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-DIS | Tampering | User triggers `Save as…` on a `processing` or `error` file (no encodedBuffer) | mitigate | Task 1 applies `disabled={file.status !== 'done'}`; defense-in-depth via Plan 04's `exportOne` guard (`if (!entry.encodedBuffer) return`) |
| T-11-KB | Denial of Service (a11y) | Keyboard-only users unable to access download | mitigate | Radix `ContextMenu` handles arrow + ESC + Enter natively; Tests 3+4 in Task 2 assert behavior |
| T-11-EXT | Tampering | A new `FileRowMenu.tsx` component drifting from the existing menu's a11y guarantees | accept | Task 1 explicitly does NOT extract a new component — keeps the single-source ContextMenu in FileRow.tsx |
</threat_model>

<verification>
- `tsc -b` exits 0
- `npx playwright test src/tests/file-row-menu.spec.ts --reporter=dot` exits 0
- `grep -c "exportOne(file)" src/components/panels/files/FileRow.tsx` returns 1
</verification>

<success_criteria>
- D-04 per-row context menu Download wired
- WCAG-AA: keyboard openable + ESC close + arrow nav (Radix inherited)
- Disabled gating with explanatory title for non-done files (Open Question 1)
</success_criteria>

<output>
Create `.planning/phases/11-batch-optimize-export/11-06-SUMMARY.md` when done.
</output>
