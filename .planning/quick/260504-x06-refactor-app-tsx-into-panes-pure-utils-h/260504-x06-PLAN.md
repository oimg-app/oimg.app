---
phase: 260504-x06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/panes/QueuePane.tsx
  - src/components/panes/ComparePane.tsx
  - src/components/panes/InspectorPane.tsx
  - src/components/file-row/ContextMenu.tsx
  - src/components/panels/ReportPanel.tsx
  - src/hooks/usePreviewUrls.ts
  - src/hooks/useShellFiles.ts
  - src/hooks/useCommandGroups.ts
  - src/hooks/useKeyboardShortcuts.ts
  - src/hooks/useBatchLifecycleAnnouncements.ts
  - src/hooks/useRenameToasts.ts
  - src/hooks/useDevStoreExposure.ts
  - src/hooks/usePluginChangePreview.ts
  - src/hooks/useBatchDispatcher.ts
  - src/lib/ingest.ts
  - src/lib/svgo-savings.ts
autonomous: true
requirements: [REFACTOR-X06]
must_haves:
  truths:
    - "App.tsx is a thin composition root (~150 lines) wiring panes + AppShell"
    - "All file/optimize business logic lives in src/hooks/* or src/stores/*"
    - "Panes consume FileEntry directly via store selectors — no MockFile shim"
    - "Golden-path UX (drop → optimize → cancel → re-optimize → preview swap → SVG plugin toggle → savings) behaves identically to pre-refactor"
    - "Two atomic commits land in order: (1) panes + pure utils, (2) hooks/dispatcher"
  artifacts:
    - path: "src/components/panes/QueuePane.tsx"
      provides: "Queue pane (dropzone + filelist + totals)"
    - path: "src/components/panes/ComparePane.tsx"
      provides: "Compare stage pane"
    - path: "src/components/panes/InspectorPane.tsx"
      provides: "Inspector pane"
    - path: "src/lib/ingest.ts"
      provides: "formatFromFile + ingestDroppedFiles pure utils"
    - path: "src/lib/svgo-savings.ts"
      provides: "computePluginSavings async pure util"
    - path: "src/hooks/useBatchDispatcher.ts"
      provides: "startOptimize, cancelBatch, pool factory"
    - path: "src/hooks/useShellFiles.ts"
      provides: "FileEntry-shaped view models with derived display fields"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/panes/*"
      via: "JSX composition under AppShell"
    - from: "src/hooks/useBatchDispatcher.ts"
      to: "src/stores/*"
      via: "store selectors + actions"
    - from: "src/components/panes/*"
      to: "src/stores/*"
      via: "selector hooks (useFilesStore, useSettingsStore, useRuntimeStore)"
---

<objective>
Refactor src/App.tsx (1382 lines) into a thin composition root by extracting:
1. Three pane components (Queue, Compare, Inspector)
2. Pure utility modules (ingest, svgo-savings)
3. Eight hooks covering preview URLs, shell-file view models, command groups, keyboard shortcuts, batch lifecycle announcements, rename toasts, dev exposure, plugin-change preview, and the batch dispatcher.

Land in TWO atomic commits with no behavior change. Drop the MockFile shim in commit 1; consumers (ContextMenu, ReportPanel, file-row stat block) migrate to FileEntry directly.

Purpose: Enforce the architectural rule that components are presentational and all file/optimize logic lives in hooks/stores. Reduce App.tsx to ~150 lines for maintainability.

Output: Two commits, App.tsx slim, panes/hooks/lib modules created, MockFile gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@src/App.tsx
@src/index.css
@src/types/
@src/stores/
@src/components/file-row/ContextMenu.tsx
@src/components/panels/ReportPanel.tsx
@src/components/panels/SnippetPanel.tsx

<interfaces>
<!-- Executor MUST read these before extracting. The exact line ranges are locked. -->

App.tsx extraction map (from locked_decisions):
- L77–171   : computePluginSavings        → src/lib/svgo-savings.ts            (commit 2)
- L177–215  : formatFromFile + ingestDroppedFiles → src/lib/ingest.ts          (commit 1)
- L309–341  : pool factory                 → src/hooks/useBatchDispatcher.ts   (commit 2)
- L345–375  : keyboard shortcuts effect    → src/hooks/useKeyboardShortcuts.ts (commit 2)
- L380–450  : live-region quartile + completion subscriber → src/hooks/useBatchLifecycleAnnouncements.ts (commit 2)
- L460–479  : rename-toast subscriber      → src/hooks/useRenameToasts.ts      (commit 2)
- L486–494  : dev store exposure           → src/hooks/useDevStoreExposure.ts  (commit 2)
- L502–516  : plugin-change preview        → src/hooks/usePluginChangePreview.ts (commit 2)
- L540–576 + L633–639 : SHELL_FILES + totals + filteredFiles → src/hooks/useShellFiles.ts (commit 1, returns FileEntry-shaped VMs, NO MockFile)
- L596–616  : preview URL effect           → src/hooks/usePreviewUrls.ts       (commit 1)
- L650–809  : startOptimize + cancelBatch  → src/hooks/useBatchDispatcher.ts   (commit 2)
- L830–862  : cmdGroups builder            → src/hooks/useCommandGroups.ts    (commit 2)
- L869–1051 : Queue (dropzone + filelist + totals) → src/components/panes/QueuePane.tsx (commit 1)
- L1053–1174: Compare stage                → src/components/panes/ComparePane.tsx (commit 1)
- L1176–1298: Inspector                    → src/components/panes/InspectorPane.tsx (commit 1)

Display-layer mapping (commit 1):
- 'jpeg' → 'jpg' applied ONLY at thumb className + crumbs label sites. Source data stays canonical 'jpeg'.

Architectural rule (locked):
- Panes import store selectors directly; they receive NO file/optimize callbacks except those produced by hooks (useBatchDispatcher).
- Pure utils in src/lib/* have no React imports.

Comment preservation rule (locked):
- Preserve all D-#, WR-#, plan-ref, race-condition rationale comments verbatim.
- Compress only when identical rationale repeats 3× or more.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 1 — extract pure utils (ingest) + preview/shell hooks</name>
  <files>src/lib/ingest.ts, src/hooks/usePreviewUrls.ts, src/hooks/useShellFiles.ts</files>
  <action>
Create three new modules. NO behavior change. NO App.tsx edits yet.

1. src/lib/ingest.ts — copy formatFromFile + ingestDroppedFiles from App.tsx L177–215. Pure functions only, no React imports. Preserve all WHY comments verbatim. Export both functions.

2. src/hooks/usePreviewUrls.ts — extract the preview-URL effect from App.tsx L596–616 as `usePreviewUrls(selectedEntry: FileEntry | null)`. Returns `{ originalUrl, optimizedUrl }` (or whatever shape the source effect uses). Keep the URL.revokeObjectURL cleanup race-condition rationale comment intact.

3. src/hooks/useShellFiles.ts — replace SHELL_FILES + totals + filteredFiles (L540–576, L633–639). Return real FileEntry-shaped view models with derived display fields (formatted size, status label, etc.). NO MockFile. NO PLACEHOLDER_FILE. Source from useFilesStore selector. Apply 'jpeg'→'jpg' display mapping ONLY in derived display fields, not on the underlying entry.format.

Do NOT modify App.tsx in this task — only create the three files.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Three new files exist, tsc passes, App.tsx unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Commit 1 — extract three pane components</name>
  <files>src/components/panes/QueuePane.tsx, src/components/panes/ComparePane.tsx, src/components/panes/InspectorPane.tsx</files>
  <action>
Create the three pane components by lifting JSX from App.tsx with NO behavior change.

1. QueuePane.tsx — from App.tsx L869–1051 (dropzone + filelist + totals). Consume useShellFiles() + store selectors directly. Accept only callbacks that originate from hooks (none in commit 1 — handlers stay in App.tsx and are passed through props until commit 2). Apply 'jpeg'→'jpg' display mapping at thumb className + crumbs label sites only.

2. ComparePane.tsx — from App.tsx L1053–1174. Consume usePreviewUrls(selectedEntry) where selectedEntry comes from useFilesStore. NO PLACEHOLDER_FILE — if no selection, render the empty state.

3. InspectorPane.tsx — from App.tsx L1176–1298. Consume FileEntry from useFilesStore.

Preserve all D-#, WR-#, plan-ref comments verbatim. Compress only when identical rationale repeats 3× or more.

Do NOT touch App.tsx in this task — verify the panes typecheck in isolation.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Three pane files exist, tsc passes, App.tsx still unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Commit 1 — migrate ContextMenu + ReportPanel + file-row stat block to FileEntry; wire panes into App.tsx; drop MockFile</name>
  <files>src/components/file-row/ContextMenu.tsx, src/components/panels/ReportPanel.tsx, src/App.tsx</files>
  <action>
1. ContextMenu.tsx — change props from MockFile to FileEntry. Update field accesses (use FileEntry shape from src/types/). Apply 'jpeg'→'jpg' display mapping if the menu surfaces a format label.

2. ReportPanel.tsx — change props from MockFile[] to FileEntry[]. Update field accesses. Apply display mapping as needed.

3. file-row stat block (the consumer in QueuePane / file-row component touched by L869–1051) — confirm it consumes FileEntry directly.

4. App.tsx — replace the inlined Queue/Compare/Inspector JSX with `<QueuePane />`, `<ComparePane />`, `<InspectorPane />`. Replace SHELL_FILES + filteredFiles + totals with `useShellFiles()`. Replace the preview-URL effect with `usePreviewUrls(selectedEntry)`. Delete the MockFile type alias and PLACEHOLDER_FILE constant. Keep ALL handlers, the dispatcher, and the other 7 effects in App.tsx for now (commit 2 extracts them).

5. Run golden-path manual click-through (verification gate before commit):
   - drop file → optimize batch → cancel mid-batch → re-optimize → preview swap on selection → SVG plugin toggle → savings panel populates

6. Commit 1 with message: `refactor(x06): extract panes + ingest/preview/shell utils, drop MockFile`
   - Include all files created in Tasks 1–3.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>tsc passes, build succeeds, MockFile + PLACEHOLDER_FILE deleted, App.tsx imports the three panes, golden path verified, commit 1 landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Golden-path verification before commit 1 lands</name>
  <what-built>Commit 1 changes (panes + pure utils, MockFile dropped). Codebase still compiles.</what-built>
  <how-to-verify>
Run `pnpm dev`, then in the browser:
1. Drop an SVG + a PNG into the dropzone — both appear in the queue.
2. Click "Optimize" — both files run through the batch.
3. Cancel mid-batch on a fresh batch — cancellation is clean.
4. Re-optimize after cancel — runs to completion.
5. Select a file — preview swaps to that file's before/after.
6. Toggle an SVGO plugin — preview updates.
7. Open the savings panel — populated with non-zero numbers.

If any step misbehaves, revert and report.
  </how-to-verify>
  <resume-signal>Type "approved" if golden path is clean, otherwise describe the regression.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit 2 — extract pure svgo-savings + 6 effect hooks</name>
  <files>src/lib/svgo-savings.ts, src/hooks/useKeyboardShortcuts.ts, src/hooks/useBatchLifecycleAnnouncements.ts, src/hooks/useRenameToasts.ts, src/hooks/useDevStoreExposure.ts, src/hooks/usePluginChangePreview.ts, src/hooks/useCommandGroups.ts</files>
  <action>
Create the seven modules. NO App.tsx edits yet.

1. src/lib/svgo-savings.ts — copy computePluginSavings (App.tsx L77–171). Pure async, no React imports. Preserve all comments.

2. src/hooks/useKeyboardShortcuts.ts — extract effect from L345–375. Wire to whatever store actions/refs the source effect uses; expose as `useKeyboardShortcuts()` (no args) or with the minimal arg surface the source needs.

3. src/hooks/useBatchLifecycleAnnouncements.ts — extract live-region quartile + completion subscriber from L380–450.

4. src/hooks/useRenameToasts.ts — extract rename-toast subscriber from L460–479.

5. src/hooks/useDevStoreExposure.ts — extract dev store exposure from L486–494. Guard with import.meta.env.DEV as in source.

6. src/hooks/usePluginChangePreview.ts — extract plugin-change preview subscriber from L502–516.

7. src/hooks/useCommandGroups.ts — extract cmdGroups builder from L830–862. Returns the cmdGroups array. Accept the args the source uses (likely store selectors + dispatcher actions — but dispatcher arrives in Task 5; for now accept startOptimize/cancelBatch as parameters).

Preserve all D-#, WR-#, plan-ref, race-condition comments verbatim.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>Seven new files exist, tsc passes, App.tsx unchanged.</done>
</task>

<task type="auto">
  <name>Task 5: Commit 2 — extract useBatchDispatcher (pool + startOptimize + cancelBatch)</name>
  <files>src/hooks/useBatchDispatcher.ts</files>
  <action>
Create useBatchDispatcher. This is the largest extraction.

Source ranges:
- Pool factory: App.tsx L309–341
- startOptimize + cancelBatch: App.tsx L650–809

Hook signature: `useBatchDispatcher() → { startOptimize, cancelBatch, pool }`.

Internals:
- Create the pool via useRef + useEffect cleanup matching the source's lifetime semantics.
- startOptimize closes over store selectors (useFilesStore, useSettingsStore, useRuntimeStore) and the pool ref.
- cancelBatch matches source semantics for in-flight + queued-but-not-started entries.
- Preserve all race-condition rationale comments verbatim — these are load-bearing.

Do NOT modify App.tsx yet.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <done>useBatchDispatcher.ts exists, tsc passes, App.tsx unchanged.</done>
</task>

<task type="auto">
  <name>Task 6: Commit 2 — wire all hooks into App.tsx; reduce to ~150 lines; commit</name>
  <files>src/App.tsx</files>
  <action>
1. Replace inline effect/handler blocks in App.tsx with hook calls:
   - L77–171  : delete (now in src/lib/svgo-savings.ts; importers updated if any)
   - L309–341 + L650–809 : replace with `const { startOptimize, cancelBatch } = useBatchDispatcher()`
   - L345–375 : replace with `useKeyboardShortcuts()`
   - L380–450 : replace with `useBatchLifecycleAnnouncements()`
   - L460–479 : replace with `useRenameToasts()`
   - L486–494 : replace with `useDevStoreExposure()`
   - L502–516 : replace with `usePluginChangePreview()`
   - L830–862 : replace with `const cmdGroups = useCommandGroups({ startOptimize, cancelBatch })` (or whatever arg shape Task 4 settled on)

2. Update any other site importing computePluginSavings to use src/lib/svgo-savings.

3. App.tsx end-state: thin composition root, ~150 lines, only AppShell + panes + the hook calls + cmdGroups wiring.

4. Run golden-path manual click-through (verification gate before commit 2):
   - drop file → optimize batch → cancel mid-batch → re-optimize → preview swap on selection → SVG plugin toggle → savings panel populates

5. Commit 2 with message: `refactor(x06): extract batch dispatcher + lifecycle/keyboard hooks, slim App.tsx to composition root`
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>App.tsx ~150 lines, tsc + build pass, golden path verified, commit 2 landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Golden-path verification before commit 2 lands</name>
  <what-built>Commit 2 changes (hooks + dispatcher). App.tsx is now a thin composition root.</what-built>
  <how-to-verify>
Run `pnpm dev`, repeat the same 7-step golden path used before commit 1:
1. Drop SVG + PNG.
2. Optimize batch.
3. Cancel mid-batch.
4. Re-optimize after cancel.
5. Preview swap on selection.
6. SVGO plugin toggle updates preview.
7. Savings panel populated.

Also verify keyboard shortcuts still fire and rename toasts still appear (these are now hook-driven).
  </how-to-verify>
  <resume-signal>Type "approved" if golden path is clean, otherwise describe the regression.</resume-signal>
</task>

</tasks>

<verification>
- pnpm tsc --noEmit passes after Task 1, 2, 4, 5 (in-progress gates).
- pnpm tsc --noEmit && pnpm build pass after Task 3 and Task 6 (commit gates).
- Golden-path manual click-through approved at both checkpoints.
- App.tsx final line count ~150.
- No MockFile / PLACEHOLDER_FILE references remain anywhere: `grep -rn "MockFile\|PLACEHOLDER_FILE" src/ | grep -v '^#' | wc -l` returns 0.
- All file/optimize logic lives in src/hooks/* or src/stores/*: panes contain no fetch / pool / FileReader / encoder calls.
</verification>

<success_criteria>
- Two atomic commits land in order.
- App.tsx is a thin composition root (~150 lines).
- src/components/panes/{Queue,Compare,Inspector}Pane.tsx exist and consume FileEntry directly.
- src/lib/{ingest,svgo-savings}.ts exist as pure modules.
- src/hooks/use{PreviewUrls,ShellFiles,CommandGroups,KeyboardShortcuts,BatchLifecycleAnnouncements,RenameToasts,DevStoreExposure,PluginChangePreview,BatchDispatcher}.ts exist.
- MockFile + PLACEHOLDER_FILE deleted; ContextMenu + ReportPanel migrated to FileEntry.
- 'jpeg'→'jpg' mapping confined to display layer.
- Golden path passes at both checkpoints.
- All preserved D-#/WR-# comments still present in extracted modules.
</success_criteria>

<output>
After completion, no SUMMARY.md required (quick mode). The two commit messages serve as the record.
</output>
