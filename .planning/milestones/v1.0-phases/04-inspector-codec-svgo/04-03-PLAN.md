---
phase: 04-inspector-codec-svgo
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/panels/inspector/CodecPanel.tsx
autonomous: true
requirements: [INSP-02, INSP-03, INSP-04, INSP-05]
must_haves:
  truths:
    - "Output format section renders codec selector buttons from CODECS constant; clicking calls setCodec"
    - "Lossless Switch reads settingsAtom.lossless and calls setLossless; hidden when codec='SVG'"
    - "Parameters section (INSP-03) is hidden when codec='SVG'; badge shows codec engine name"
    - "Quality Slider (0-100) and Effort Slider (0-6) read/write settingsAtom and call setQuality/setMethod"
    - "PNG Palette seg shows only when codec='PNG'; AVIF Subsample seg shows only when codec='AVIF'"
    - "Resize section (INSP-04) resizeOn Switch gates width/height inputs + Fit/Algorithm segs"
    - "Metadata section (INSP-05) renders stripMeta and keepIcc Switches"
  artifacts:
    - path: "src/components/panels/inspector/CodecPanel.tsx"
      provides: "Full CodecPanel (INSP-02 through INSP-05) — replaces Plan 01 stub"
      contains: "export function CodecPanel"
      min_lines: 100
  key_links:
    - from: "src/components/panels/inspector/CodecPanel.tsx"
      to: "src/stores/settings.ts"
      via: "useStore(settingsAtom) + all set* actions"
      pattern: "settingsAtom"
    - from: "src/components/panels/inspector/CodecPanel.tsx"
      to: "src/components/panels/inspector/Section"
      via: "Section component with title + badge"
      pattern: "Section"
    - from: "src/components/panels/inspector/CodecPanel.tsx"
      to: "src/components/panels/inspector/SegControl"
      via: "SegControl for Fit/Algorithm/Palette/Subsample"
      pattern: "SegControl"
---

<objective>
Implement CodecPanel (INSP-02 through INSP-05): four Section blocks wired directly to settingsAtom. This replaces the CodecPanel stub from Plan 01 (which returned null) with a fully interactive panel. Runs in Wave 2 parallel with Plan 02 (InspectorPane) because both depend only on Plan 01 outputs and modify separate files.

Purpose: The codec, quality, resize, and metadata controls are the primary developer workflow — they must be functional before the inspector is usable.
Output: `src/components/panels/inspector/CodecPanel.tsx` with all four sections implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inspector-codec-svgo/04-RESEARCH.md
@.planning/phases/04-inspector-codec-svgo/04-PATTERNS.md
@.planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from Plan 01 outputs and existing codebase. -->

From src/stores/settings.ts (created by Plan 01):
```typescript
export const settingsAtom = map<SettingsState>({ codec:'WebP', q:82, method:4, lossless:false,
  resizeOn:false, w:'1600', h:'auto', alg:'lanczos3', fit:'contain',
  stripMeta:true, keepIcc:false, aggressive:false, plugins:SvgoPlugin[] })
export const CODECS: readonly string[]      // ['SVG','PNG','WebP','JPEG','AVIF']
export const RESIZE_ALGS: readonly string[] // ['lanczos3','mitchell','catrom','triangle']
export const FIT_MODES: readonly string[]   // ['cover','contain','fill']
export type Codec = 'SVG' | 'PNG' | 'WebP' | 'JPEG' | 'AVIF'
export function setCodec(c: Codec): void
export function setQuality(q: number): void
export function setMethod(m: number): void
export function setLossless(v: boolean): void
export function setResizeOn(v: boolean): void
export function setResizeDimensions(w: string, h: string): void
export function setFit(f: string): void
export function setAlg(a: string): void
export function setStripMeta(v: boolean): void
export function setKeepIcc(v: boolean): void
```

From src/components/panels/inspector/Section.tsx (Plan 01):
```typescript
export function Section({ title, badge, children }: {
  title: string; badge?: { text: string; acc?: boolean }; children: React.ReactNode
})
```

From src/components/panels/inspector/SegControl.tsx (Plan 01):
```typescript
export function SegControl({ options, value, onChange }: {
  options: readonly string[]; value: string; onChange: (v: string) => void
})
```

From src/components/ui/slider.tsx (Shadcn — CRITICAL API):
- value must be number[] (array), not number
- onValueChange receives number[] — destructure: ([v]) => setQuality(v)

From src/components/ui/switch.tsx (Shadcn):
- Props: checked: boolean, onCheckedChange: (checked: boolean) => void

From src/components/ui/input.tsx (Shadcn):
- Standard controlled input: value + onChange

Codec engine badge map (hardcoded — from RESEARCH.md Pattern 7):
AVIF → 'libavif', WebP → 'libwebp', JPEG → 'mozjpeg', PNG → 'oxipng', SVG → 'svgo'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CodecPanel — Output format + Parameters sections (INSP-02, INSP-03)</name>
  <files>src/components/panels/inspector/CodecPanel.tsx</files>
  <read_first>
    - src/components/panels/inspector/CodecPanel.tsx (read current stub from Plan 01 — replace)
    - src/components/shell/Toolbar/Toolbar.tsx (useStore pattern + import structure analog)
    - src/components/ui/slider.tsx (verify value prop is number[] — critical)
    - src/components/ui/switch.tsx (checked + onCheckedChange props)
    - .planning/phases/04-inspector-codec-svgo/04-PATTERNS.md (CodecPanel imports pattern, Slider value pattern, Switch pattern, CODEC_ENGINE map)
    - .planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md (INSP-02 and INSP-03 interaction contracts, row layout grid-cols-[100px_1fr], slider readout grid-cols-[1fr_42px])
  </read_first>
  <behavior>
    - settingsAtom.codec defaults to 'WebP' → WebP button is active in codec selector
    - Clicking 'PNG' codec button → setCodec('PNG') called → PNG is now active
    - settingsAtom.lossless=false + codec='WebP' → Lossless Switch unchecked and visible
    - codec='SVG' → Lossless Switch hidden, Parameters section hidden
    - Quality slider value={[82]} → thumb at 82; drag to 60 → setQuality(60) called
    - Effort slider value={[4]} → thumb at 4; drag to 6 → setMethod(6) called
    - codec='PNG' → Palette SegControl visible with options ['off','auto','PNG-8']
    - codec='AVIF' → Subsample SegControl visible with options ['4:2:0','4:4:4']
    - codec='WebP' → neither Palette nor Subsample visible
    - Parameters section badge text = 'libwebp' when codec='WebP'
  </behavior>
  <action>
    Create `src/components/panels/inspector/CodecPanel.tsx`. Attribution header line 1: `// Phase 04 — INSP-02 through INSP-05: CodecPanel sections. Source: 04-03-PLAN.md`.

    Imports: useStore from '@nanostores/react'; settingsAtom + CODECS + RESIZE_ALGS + FIT_MODES + setCodec + setQuality + setMethod + setLossless + setResizeOn + setResizeDimensions + setFit + setAlg + setStripMeta + setKeepIcc + Codec from '@/stores/settings'; Slider from '@/components/ui/slider'; Switch from '@/components/ui/switch'; Input from '@/components/ui/input'; cn from '@/lib/utils'; Section from './Section'; SegControl from './SegControl'.

    Define inline const: const CODEC_ENGINE: Record<string, string> = { AVIF: 'libavif', WebP: 'libwebp', JPEG: 'mozjpeg', PNG: 'oxipng', SVG: 'svgo' }.

    Export function CodecPanel(): reads const settings = useStore(settingsAtom).

    INSP-02 — "Output format" Section (title="Output format", no badge):
    Codec selector: div with "flex gap-1 mb-2.5". Map CODECS to button elements. Each button: type="button", onClick={() => setCodec(c as Codec)}, className with cn base "flex-1 h-6 rounded text-[11px] font-mono transition-colors border" plus active state (settings.codec === c) "bg-[var(--color-bg-3)] text-[var(--color-fg-0)] border-[var(--color-line-strong)] font-semibold" vs inactive "bg-transparent text-[var(--color-fg-2)] border-[var(--color-line)] hover:text-[var(--color-fg-0)]".
    Lossless toggle: when settings.codec !== 'SVG', render a div "flex items-center justify-between mt-1" containing label span "text-[12px] text-[var(--color-fg-2)]" with text "Lossless" and Switch checked={settings.lossless} onCheckedChange={setLossless}.

    INSP-03 — Parameters Section (hidden when codec='SVG'): use {settings.codec !== 'SVG' && <Section title={`${settings.codec} parameters`} badge={{ text: CODEC_ENGINE[settings.codec] ?? '' }}>...</Section>}.
    Inside Section, two slider rows using grid "grid grid-cols-[100px_1fr] gap-2.5 mb-2 items-center":
    Row 1: label span "text-[12px] text-[var(--color-fg-2)]" "Quality"; control div "grid grid-cols-[1fr_42px] gap-2.5 items-center" containing Slider min={0} max={100} step={1} value={[settings.q]} onValueChange={([v]) => setQuality(v)} className="w-full" and readout span "text-right font-mono text-[12px] font-semibold text-[var(--color-fg-0)] tabular-nums" showing {settings.q}.
    Row 2: label "Effort"; Slider min={0} max={6} step={1} value={[settings.method]} onValueChange={([v]) => setMethod(v)}; readout {settings.method}.
    PNG Palette row: {settings.codec === 'PNG' && <div className="grid grid-cols-[100px_1fr] gap-2.5 mb-2 items-center"><span className="text-[12px] text-[var(--color-fg-2)]">Palette</span><SegControl options={['off','auto','PNG-8']} value="off" onChange={() => {}} /></div>} (no store field for palette yet — onChange is a no-op stub, value fixed 'off', per INSP-03 spec: shown when codec='PNG').
    AVIF Subsample row: same pattern for codec='AVIF', options=['4:2:0','4:4:4'], value='4:2:0', onChange no-op stub.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "CodecPanel" || echo "no type errors in CodecPanel"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function CodecPanel" src/components/panels/inspector/CodecPanel.tsx` returns a match.
    - `grep -n "CODEC_ENGINE" src/components/panels/inspector/CodecPanel.tsx` returns a match (engine badge map present).
    - `grep -n "value={\[settings.q\]}" src/components/panels/inspector/CodecPanel.tsx` returns a match (array syntax for Slider — critical).
    - `grep -n "value={\[settings.method\]}" src/components/panels/inspector/CodecPanel.tsx` returns a match.
    - `grep -n "codec !== 'SVG'" src/components/panels/inspector/CodecPanel.tsx` returns at least 2 matches (Parameters hidden + Lossless hidden).
    - `grep -n "setCodec" src/components/panels/inspector/CodecPanel.tsx` returns a match.
    - `grep -n "setQuality\|setMethod" src/components/panels/inspector/CodecPanel.tsx` returns matches.
    - `grep -n "codec === 'PNG'" src/components/panels/inspector/CodecPanel.tsx` returns a match (Palette conditional).
    - `grep -n "codec === 'AVIF'" src/components/panels/inspector/CodecPanel.tsx` returns a match (Subsample conditional).
    - `npx tsc --noEmit -p tsconfig.app.json` reports zero errors referencing CodecPanel.tsx.
  </acceptance_criteria>
  <done>Output format section renders CODECS buttons with active state; Lossless hidden for SVG; Parameters section hidden for SVG, badge shows engine name, Quality/Effort sliders wired, PNG/AVIF conditional segs present; no TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CodecPanel — Resize + Metadata sections (INSP-04, INSP-05)</name>
  <files>src/components/panels/inspector/CodecPanel.tsx</files>
  <read_first>
    - src/components/panels/inspector/CodecPanel.tsx (read the Task 1 result before appending)
    - src/components/ui/input.tsx (Shadcn Input — standard controlled input API)
    - .planning/phases/04-inspector-codec-svgo/04-UI-SPEC.md (INSP-04: Width/Height inputs font-mono 12px h-6, Fit seg, Algorithm seg; INSP-05: flex rows with mb-1.5)
    - .planning/phases/04-inspector-codec-svgo/04-PATTERNS.md (CodecPanel imports already established in Task 1)
  </read_first>
  <behavior>
    - Resize section "Resize on export" Switch unchecked → width/height inputs and Fit/Algorithm segs are hidden
    - Switch checked → width/height inputs + Fit/Algorithm segs visible
    - Width input value='1600'; typing '800' calls setResizeDimensions('800', settings.h) on blur/change
    - Height input value='auto'; Fit SegControl shows FIT_MODES values, selecting 'cover' calls setFit('cover')
    - Algorithm SegControl shows RESIZE_ALGS, selecting 'mitchell' calls setAlg('mitchell')
    - Metadata section shows two rows: Strip EXIF Switch and Keep ICC Switch; both call their respective store actions
  </behavior>
  <action>
    Add two more Section blocks to the CodecPanel function body (appending after the Parameters Section from Task 1).

    INSP-04 — "Resize" Section (title="Resize", no badge):
    Toggle row: div "flex items-center justify-between mb-2" with label "text-[12px] text-[var(--color-fg-2)]" "Resize on export" and Switch checked={settings.resizeOn} onCheckedChange={setResizeOn}.
    Conditional body: {settings.resizeOn && <div className="space-y-2">...</div>}. Inside:
    Width row: grid-cols-[100px_1fr] gap-2.5 items-center. Label "Width". Input value={settings.w} onChange={(e) => setResizeDimensions(e.target.value, settings.h)} className="h-6 font-mono text-[12px] bg-[var(--color-bg-2)] border-[var(--color-line)]".
    Height row: same grid. Label "Height". Input value={settings.h} onChange={(e) => setResizeDimensions(settings.w, e.target.value)} same className.
    Fit row: grid-cols-[100px_1fr]. Label "Fit". SegControl options={FIT_MODES} value={settings.fit} onChange={setFit}.
    Algorithm row: grid-cols-[100px_1fr]. Label "Algorithm". SegControl options={RESIZE_ALGS} value={settings.alg} onChange={setAlg}.

    INSP-05 — "Metadata" Section (title="Metadata", no badge):
    Two rows each using "flex items-center justify-between mb-1.5":
    Row 1: span "text-[12px] text-[var(--color-fg-2)]" "Strip EXIF / XMP / IPTC" + Switch checked={settings.stripMeta} onCheckedChange={setStripMeta}.
    Row 2: span "text-[12px] text-[var(--color-fg-2)]" "Keep ICC profile" + Switch checked={settings.keepIcc} onCheckedChange={setKeepIcc}.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep "CodecPanel" || echo "no type errors in CodecPanel"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resizeOn" src/components/panels/inspector/CodecPanel.tsx` returns at least 2 matches (Switch binding + conditional body).
    - `grep -n "setResizeDimensions" src/components/panels/inspector/CodecPanel.tsx` returns 2 matches (w input + h input).
    - `grep -n "setFit\|setAlg" src/components/panels/inspector/CodecPanel.tsx` returns matches.
    - `grep -n "FIT_MODES\|RESIZE_ALGS" src/components/panels/inspector/CodecPanel.tsx` returns matches.
    - `grep -n "setStripMeta\|setKeepIcc" src/components/panels/inspector/CodecPanel.tsx` returns matches.
    - `grep -n "Strip EXIF" src/components/panels/inspector/CodecPanel.tsx` returns a match (copywriting per UI-SPEC).
    - `grep -n "Keep ICC profile" src/components/panels/inspector/CodecPanel.tsx` returns a match.
    - `npx tsc --noEmit -p tsconfig.app.json` reports zero errors referencing CodecPanel.tsx.
    - File line count `wc -l src/components/panels/inspector/CodecPanel.tsx` returns at least 100.
  </acceptance_criteria>
  <done>Resize section gates width/height/Fit/Algorithm behind resizeOn Switch; Metadata section has both toggles; setResizeDimensions, setFit, setAlg, setStripMeta, setKeepIcc all wired; no TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Width/Height Input → setResizeDimensions | User-typed strings stored as-is in settingsAtom.w/h; values are strings, not numbers. Validation deferred to Phase 7 per REQUIREMENTS.md. |
| Slider onValueChange | Radix Slider emits number[] — destructured immediately; no string coercion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Width/Height input injection | accept | Values stored as strings in settingsAtom; never executed, never sent to server; no XSS vector in a CSP-enforced client-only app |
| T-04-08 | Tampering | Slider min/max bypass | accept | Radix Slider enforces min/max in the component; store receives clamped values from onValueChange |
| T-04-09 | Tampering | Codec cast (v as Codec) | mitigate | Cast is guarded by CODECS.map() — only values in the constant array produce onClick handlers; TypeScript type covers all union members |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` → zero errors
- `grep -n "value={\[settings" src/components/panels/inspector/CodecPanel.tsx` → matches for q and method (array syntax confirmed)
- `grep -c "Section" src/components/panels/inspector/CodecPanel.tsx` → at least 4 (Output format, Parameters, Resize, Metadata)
- `node --experimental-strip-types src/tests/settings.test.ts` → still green (no regression)
</verification>

<success_criteria>
- INSP-02 satisfied: codec selector from CODECS constant, active state on settingsAtom.codec, setCodec called on click; lossless Switch hidden for SVG.
- INSP-03 satisfied: Parameters section hidden for SVG; quality + effort sliders with correct array syntax; PNG palette and AVIF subsample conditionals.
- INSP-04 satisfied: resizeOn Switch gates conditional body; w/h inputs bound; Fit and Algorithm segs from FIT_MODES/RESIZE_ALGS.
- INSP-05 satisfied: stripMeta and keepIcc Switches present.
- Zero TypeScript errors in CodecPanel.tsx.
</success_criteria>

<output>
Create `.planning/phases/04-inspector-codec-svgo/04-03-SUMMARY.md` when done
</output>
