---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/input.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/context-menu.tsx
  - src/components/ui/menubar.tsx
  - src/components/ui/kbd.tsx
  - src/components/ui/resizable.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/spinner.tsx
autonomous: true
requirements: [SETUP-03]
tags: [foundation, shadcn, ui-primitives]

must_haves:
  truths:
    - "All 17 shadcn UI primitives exist under src/components/ui/ as importable TSX modules"
    - "Each generated component uses cn() from @/lib/utils"
    - "resizable.tsx imports from 'react-resizable-panels' and exports ResizablePanelGroup, ResizablePanel, ResizableHandle"
    - "TypeScript compile (tsc -b) succeeds for the components/ui/ directory"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button primitive with cva variants"
    - path: "src/components/ui/resizable.tsx"
      provides: "Wrapper around react-resizable-panels"
      exports: ["ResizablePanelGroup", "ResizablePanel", "ResizableHandle"]
    - path: "src/components/ui/sonner.tsx"
      provides: "Sonner toast container wrapper"
    - path: "src/components/ui/kbd.tsx"
      provides: "Keyboard shortcut display primitive"
  key_links:
    - from: "src/components/ui/resizable.tsx"
      to: "react-resizable-panels"
      via: "import * as ResizablePrimitive from 'react-resizable-panels'"
      pattern: "from\\s+['\"]react-resizable-panels['\"]"
    - from: "src/components/ui/*.tsx"
      to: "@/lib/utils"
      via: "import { cn } from '@/lib/utils'"
      pattern: "from\\s+['\"]@/lib/utils['\"]"
---

<objective>
Generate all 17 shadcn UI primitive components via the shadcn CLI so subsequent phases (and the AppShell in Plan 05) have a complete component vocabulary.

Purpose: SETUP-03 (Shadcn base components generated). The radix-lyra style + phosphor icon library is already configured in `components.json`. Using the CLI ensures consistent up-to-date radix-lyra output rather than hand-rolling.
Output: 17 .tsx files under `src/components/ui/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@components.json

<interfaces>
<!-- shadcn CLI contract -->
- Command: `npx shadcn@4.7.0 add <name1> <name2> ...`
- Reads components.json: style=radix-lyra, tsx=true, iconLibrary=phosphor, aliases.ui=@/components/ui, aliases.utils=@/lib/utils, css=src/index.css
- Writes to src/components/ui/<name>.tsx
- Auto-imports cn() from @/lib/utils
- May prompt for overwrite; pass `-y` / `--overwrite` to suppress prompts in CI/non-interactive mode
- Each generated file follows pattern: import primitives, wrap with cn() via className prop, export named functions

<!-- The 17 required component names -->
button, separator, tooltip, popover, slider, dialog, tabs, input, checkbox, switch, dropdown-menu, context-menu, menubar, kbd, resizable, sonner, spinner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn CLI to generate all 17 components</name>
  <files>src/components/ui/*.tsx (17 files)</files>
  <read_first>
    - components.json (verify style=radix-lyra, tsx=true, iconLibrary=phosphor, aliases.ui=@/components/ui)
    - src/lib/utils.ts (must exist from Plan 02 — shadcn imports cn from here)
  </read_first>
  <action>From repo root, ensure `src/lib/utils.ts` exists (it should after Plan 02 completes). Then run the single CLI command:

  `npx shadcn@4.7.0 add button separator tooltip popover slider dialog tabs input checkbox switch dropdown-menu context-menu menubar kbd resizable sonner spinner --overwrite --yes`

  If the `--yes`/`--overwrite` flags are unsupported by 4.7.0, fall back to: `yes "" | npx shadcn@4.7.0 add <list>` to auto-accept any prompts. Do NOT hand-edit the generated files. Do NOT skip any of the 17. If the CLI fails for any single component, run that component again individually; do not substitute a hand-rolled replacement.

  After generation, confirm each of the 17 files exists under `src/components/ui/`. Do not modify components.json.</action>
  <verify>
    <automated>for f in button separator tooltip popover slider dialog tabs input checkbox switch dropdown-menu context-menu menubar kbd resizable sonner spinner; do test -f "src/components/ui/$f.tsx" || { echo "MISSING: $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All 17 files exist: `button.tsx`, `separator.tsx`, `tooltip.tsx`, `popover.tsx`, `slider.tsx`, `dialog.tsx`, `tabs.tsx`, `input.tsx`, `checkbox.tsx`, `switch.tsx`, `dropdown-menu.tsx`, `context-menu.tsx`, `menubar.tsx`, `kbd.tsx`, `resizable.tsx`, `sonner.tsx`, `spinner.tsx` under `src/components/ui/`
    - CLI: `ls src/components/ui/*.tsx | wc -l` returns a number `>= 17`
    - Source assertion (resizable.tsx): contains `from "react-resizable-panels"` and exports `ResizablePanelGroup`, `ResizablePanel`, and `ResizableHandle` (grep for `export.*Resizable(PanelGroup|Panel|Handle)`)
    - Source assertion (any sample, e.g. button.tsx): contains `import { cn } from "@/lib/utils"` (the exact alias path)
    - Source assertion (sonner.tsx): contains `from "sonner"`
    - Source assertion (kbd.tsx): file present and non-empty (`test -s src/components/ui/kbd.tsx`)
    - components.json UNCHANGED (no diff vs. HEAD)
  </acceptance_criteria>
  <done>17 shadcn components are generated and importable as `@/components/ui/<name>`; AppShell can import from `@/components/ui/resizable`.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript compile gate for components/ui/</name>
  <files>(no new files; compile-only check)</files>
  <read_first>
    - src/components/ui/resizable.tsx (verify import surface)
    - src/components/ui/button.tsx (verify cn import path)
  </read_first>
  <action>Run `npx tsc -b --noEmit` to check that all 17 generated components type-check against the current `node_modules` (react 19, react-resizable-panels 4.11, radix-ui 1.4, sonner 2.0). Do NOT modify generated files to fix errors — if a generated file fails to type-check, re-run the shadcn CLI for that single component (it indicates an installation hiccup, not a code bug). Only acceptable hand-edit: if a generated file uses `lucide-react` icons (some shadcn templates default to lucide), leave them — phosphor preference is for application-level code, not generated UI primitives.</action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npx tsc -b --noEmit` exits 0. Note: `tsc -b` reads `tsconfig.json` references; if the project lacks `src/App.tsx` at this point, the typecheck for `App.tsx` will fail. Acceptable alternative: `npx tsc --noEmit -p tsconfig.json --excludeFiles 'src/App.tsx'` is NOT supported by tsc; instead run `npx tsc --noEmit src/components/ui/*.tsx --jsx react-jsx --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --paths '{"@/*":["./src/*"]}'` to scope to the ui directory.
    - Behavior: above scoped tsc command exits 0
    - Source assertion: `grep -l 'from "@/lib/utils"' src/components/ui/*.tsx | wc -l` returns a number > 0 (at least some generated files import cn)
  </acceptance_criteria>
  <done>All 17 shadcn components type-check; ready for AppShell consumption.</done>
</task>

</tasks>

<verification>
- 17 component files exist under `src/components/ui/`
- `resizable.tsx` exports the three react-resizable-panels wrappers
- Scoped tsc typecheck of the ui directory exits 0
</verification>

<success_criteria>
- `ls src/components/ui/*.tsx | wc -l` returns >= 17
- `grep -l 'react-resizable-panels' src/components/ui/resizable.tsx` returns the file
- No hand-rolled shadcn components (all generated by CLI)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`.
</output>
