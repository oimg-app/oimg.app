---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - src/App.tsx
  - src/components/shell/AppShell/AppShell.tsx
  - src/components/panels/FilesPane.tsx
  - src/components/panels/CenterPane.tsx
  - src/components/panels/InspectorPane.tsx
autonomous: false
requirements: [SHELL-01]
tags: [foundation, shell, vertical-slice, mvp]

must_haves:
  truths:
    - "`npm run dev` starts Vite and serves the app at http://localhost:5173 with HTTP 200"
    - "Visiting / renders the AppShell with role='application' and class 'dark' on the root container"
    - "Three panes are visible and labeled via data-testid: files-pane, center-pane, inspector-pane"
    - "The viewport is filled by the application root (height = window.innerHeight, width = window.innerWidth)"
    - "Resizing the browser does not introduce horizontal scrollbars; the resize handles are visible between panes"
    - "Inter Variable font is loaded (network resource list contains an inter-*.woff2)"
    - "Default theme is dark (computed --color-bg-0 resolves to the dark oklch value)"
  artifacts:
    - path: "src/App.tsx"
      provides: "Root component, default export, renders <AppShell>"
      exports: ["default"]
    - path: "src/components/shell/AppShell/AppShell.tsx"
      provides: "3-pane resizable layout using react-resizable-panels"
      exports: ["AppShell"]
    - path: "src/components/panels/FilesPane.tsx"
      provides: "Skeleton left pane with data-testid='files-pane'"
      exports: ["FilesPane"]
    - path: "src/components/panels/CenterPane.tsx"
      provides: "Skeleton center pane with data-testid='center-pane'"
      exports: ["CenterPane"]
    - path: "src/components/panels/InspectorPane.tsx"
      provides: "Skeleton right pane with data-testid='inspector-pane'"
      exports: ["InspectorPane"]
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "import App from './App.tsx'"
      pattern: "import App from"
    - from: "src/App.tsx"
      to: "src/components/shell/AppShell/AppShell.tsx"
      via: "import { AppShell } from '@/components/shell/AppShell/AppShell'"
      pattern: "AppShell"
    - from: "src/components/shell/AppShell/AppShell.tsx"
      to: "src/components/ui/resizable.tsx"
      via: "import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from '@/components/ui/resizable'"
      pattern: "ResizablePanelGroup"
    - from: "AppShell root div"
      to: ".dark Tailwind variant"
      via: "className='dark ...'"
      pattern: "className=\"dark"
---

## Phase Goal

**As a** developer evaluating oimg.app, **I want to** run `npm run dev` and see a rendered 3-pane AppShell skeleton with the correct OIMG dark theme and design tokens, **so that** I know the foundation is solid before any feature work and I can verify the visual identity matches example-ui/OIMG.html.

<objective>
Wire the Walking Skeleton: App.tsx → AppShell → 3 skeleton panes — the thinnest end-to-end stack that proves Plans 01–04 work together and gives a visible page on `localhost:5173`.

Purpose: SHELL-01 (3-pane resizable layout). This is the LAST plan of Phase 1 and the first user-visible deliverable of the whole UI port. After this plan, `npm run dev` shows the OIMG dark shell.
Output: 5 component files + a running dev server + a manual visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@src/main.tsx
@src/index.css

<interfaces>
<!-- AppShell contract -->
import type { ReactNode } from 'react'
interface AppShellProps { children?: ReactNode }
export function AppShell(props: AppShellProps): JSX.Element

<!-- react-resizable-panels via shadcn wrapper -->
import {
  ResizablePanelGroup,   // <ResizablePanelGroup direction="horizontal">
  ResizablePanel,         // <ResizablePanel defaultSize={20} minSize={15} maxSize={35}>
  ResizableHandle,        // <ResizableHandle />
} from '@/components/ui/resizable'

<!-- Skeleton pane contracts (zero props in Phase 1) -->
export function FilesPane(): JSX.Element
export function CenterPane(): JSX.Element
export function InspectorPane(): JSX.Element

<!-- Tailwind token usage rule (CLAUDE.md + CONVENTIONS) -->
- Use bg-[var(--color-bg-N)], text-[var(--color-fg-N)], border-[var(--color-line)]
- Do NOT use bg-gray-900, text-white, border-gray-700 etc.
- Use h-[var(--height-pane-header)] for pane headers (32px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton panes (FilesPane, CenterPane, InspectorPane)</name>
  <files>src/components/panels/FilesPane.tsx, src/components/panels/CenterPane.tsx, src/components/panels/InspectorPane.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (sections "FilesPane.tsx", "CenterPane.tsx", "InspectorPane.tsx" — skeleton placeholders with data-testid + Tailwind token classes)
    - src/index.css (verify --color-bg-0/1, --color-line, --color-fg-2, --height-pane-header are defined)
  </read_first>
  <action>Create three pane components — each a named export, each renders a single root div with `data-testid`, Tailwind utility classes referencing CSS vars from `src/index.css`, and a pane header strip:

  `FilesPane.tsx`:
  - Root: `<div data-testid="files-pane" className="h-full flex flex-col border-r border-[var(--color-line)] bg-[var(--color-bg-1)]">`
  - Inside, a pane header: `<div className="h-[var(--height-pane-header)] flex items-center px-3 text-xs text-[var(--color-fg-2)] border-b border-[var(--color-line)]">Files</div>`

  `CenterPane.tsx`:
  - Root: `<div data-testid="center-pane" className="h-full flex flex-col bg-[var(--color-bg-0)]">`
  - Header: same shape, text content `Preview`

  `InspectorPane.tsx`:
  - Root: `<div data-testid="inspector-pane" className="h-full flex flex-col border-l border-[var(--color-line)] bg-[var(--color-bg-1)]">`
  - Header: same shape, text content `Inspector`

  Use named exports only. NO `useState`. NO imports from stores (none exist in Phase 1). NO direct import of `@/lib/stub-data` (STORE-08). NO CSS modules. NO inline `style={{...}}` (Tailwind utilities only).</action>
  <verify>
    <automated>for f in FilesPane CenterPane InspectorPane; do test -f "src/components/panels/$f.tsx" || exit 1; done &amp;&amp; grep -l "data-testid=\"files-pane\"" src/components/panels/FilesPane.tsx &amp;&amp; grep -l "data-testid=\"center-pane\"" src/components/panels/CenterPane.tsx &amp;&amp; grep -l "data-testid=\"inspector-pane\"" src/components/panels/InspectorPane.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `src/components/panels/`
    - Each contains its corresponding `data-testid` literal (`files-pane`, `center-pane`, `inspector-pane`)
    - Each contains `h-full flex flex-col` and a `bg-[var(--color-bg-` token reference
    - Source assertion: `grep -c 'useState' src/components/panels/FilesPane.tsx src/components/panels/CenterPane.tsx src/components/panels/InspectorPane.tsx | grep -v ':0$' | grep -v '^$'` returns no matches (no useState anywhere — STORE-08 baseline)
    - Source assertion: `grep -c "from '@/lib/stub-data'" src/components/panels/*.tsx` returns `0` (STORE-08)
    - Source assertion: each file uses `export function FilesPane`/`CenterPane`/`InspectorPane` (named export)
  </acceptance_criteria>
  <done>Three skeleton panes exist with data-testids; ready to be composed by AppShell.</done>
</task>

<task type="auto">
  <name>Task 2: Create AppShell with react-resizable-panels 3-pane layout</name>
  <files>src/components/shell/AppShell/AppShell.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (section "AppShell.tsx" — Phase 1 pattern with className="dark" on root + 20/55/25 panel splits)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 + Pitfall 3 on dark class placement)
    - src/components/ui/resizable.tsx (verify exported names: ResizablePanelGroup, ResizablePanel, ResizableHandle)
  </read_first>
  <action>Create `src/components/shell/AppShell/AppShell.tsx` (create the `AppShell/` subdirectory if it does not exist) with:

  - Imports: `ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle` from `@/components/ui/resizable`; `FilesPane` from `@/components/panels/FilesPane`; `CenterPane` from `@/components/panels/CenterPane`; `InspectorPane` from `@/components/panels/InspectorPane`; `type { ReactNode } from 'react'`.
  - Named export `function AppShell({ children }: { children?: ReactNode })`.
  - Root element: `<div role="application" aria-label="OIMG Image Optimizer" className="dark h-screen w-screen flex flex-col overflow-hidden bg-[var(--color-bg-0)] text-[var(--color-fg-0)]">`. The `dark` class on this root is what activates the `.dark` Tailwind variant + the `.dark {}` token block in index.css.
  - Inside, render `<ResizablePanelGroup direction="horizontal" className="flex-1 min-h-0">`.
  - Three panels separated by `<ResizableHandle />`:
    1. `<ResizablePanel defaultSize={20} minSize={15} maxSize={35}><FilesPane /></ResizablePanel>`
    2. `<ResizablePanel defaultSize={55}><CenterPane /></ResizablePanel>`
    3. `<ResizablePanel defaultSize={25} minSize={18} maxSize={40}><InspectorPane /></ResizablePanel>`
  - The `children` prop is accepted but unused in Phase 1; do not render `{children}` for now (the pane wiring is internal). Keep the prop in the type signature so Phase 7 (overlays / BackpressureIndicator) can pass `children` later without a breaking change.

  NO CSS modules. NO inline styles. Components reference OIMG tokens via Tailwind arbitrary value syntax `bg-[var(--color-bg-0)]`.</action>
  <verify>
    <automated>test -f src/components/shell/AppShell/AppShell.tsx &amp;&amp; grep -q "ResizablePanelGroup" src/components/shell/AppShell/AppShell.tsx &amp;&amp; grep -q "defaultSize={20}" src/components/shell/AppShell/AppShell.tsx &amp;&amp; grep -q "defaultSize={55}" src/components/shell/AppShell/AppShell.tsx &amp;&amp; grep -q "defaultSize={25}" src/components/shell/AppShell/AppShell.tsx &amp;&amp; grep -q 'className="dark' src/components/shell/AppShell/AppShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/shell/AppShell/AppShell.tsx` exists
    - Source assertion: contains `import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from '@/components/ui/resizable'` (exact import path)
    - Source assertion: contains exactly 3 occurrences of `<ResizablePanel` (regex `<ResizablePanel[\s>]` count = 3)
    - Source assertion: contains exactly 2 occurrences of `<ResizableHandle` (between panels)
    - Source assertion: contains `role="application"` and `aria-label="OIMG Image Optimizer"`
    - Source assertion: contains `className="dark` substring on the root element
    - Source assertion: contains `defaultSize={20}`, `defaultSize={55}`, and `defaultSize={25}` exactly once each
    - Source assertion: `export function AppShell`
  </acceptance_criteria>
  <done>AppShell renders the 3-pane layout; verifiable in isolation by TypeScript compile.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/App.tsx + build + boot dev server</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/main.tsx (confirm `import App from './App.tsx'` + fontsource imports already exist; do not modify main.tsx)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/App.tsx" — minimal Phase 1 shell)
  </read_first>
  <action>Create `src/App.tsx` as the thinnest possible wrapper:

  - Single import: `import { AppShell } from '@/components/shell/AppShell/AppShell'`
  - Single default export: `export default function App() { return <AppShell /> }`
  - No `useStore`, no hooks, no providers. (Phase 2+ adds nanostores; Phase 3+ adds Sonner toast container at App level.)

  Do NOT touch `src/main.tsx` — its imports for `@fontsource-variable/inter`, `@fontsource-variable/jetbrains-mono`, `./index.css`, and `./App.tsx` are correct (per 01-PATTERNS.md).

  After creating App.tsx, run `npm run build` to confirm the full TypeScript + Vite production build succeeds (this is the gate that proves Plans 02–05 compose correctly).</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/App.tsx` exists
    - Source assertion: contains `export default function App` (default export per main.tsx convention)
    - Source assertion: contains `import { AppShell } from '@/components/shell/AppShell/AppShell'`
    - Source assertion: NO `useState` import — `grep -c "useState" src/App.tsx` returns 0
    - Source assertion: NO `useEffect` import — `grep -c "useEffect" src/App.tsx` returns 0
    - Build behavior: `npm run build` exits 0
    - Build behavior: `dist/index.html` exists after build
    - Build behavior: `dist/assets/` contains at least one .js and one .css output (`ls dist/assets/*.js dist/assets/*.css 2>/dev/null | wc -l` ≥ 2)
    - src/main.tsx UNCHANGED vs. HEAD (no diff)
  </acceptance_criteria>
  <done>The full build succeeds end-to-end; `npm run dev` will serve a working page.</done>
</task>

<task type="auto">
  <name>Task 4: Run Playwright smoke + Node unit tests (full Phase 1 validation)</name>
  <files>(no new files; test execution only)</files>
  <read_first>
    - playwright.config.ts (confirm webServer auto-starts `npm run dev` on port 5173)
    - src/tests/foundation.spec.ts (verify the three test names that will now turn green)
  </read_first>
  <action>Run the complete Phase 1 validation suite:

  1. `node --experimental-strip-types src/tests/stub-data.test.ts` — should report `6 passed, 0 failed`.
  2. `node --experimental-strip-types src/tests/format.test.ts` — should report `8 passed, 0 failed`.
  3. `npm test -- --project=chromium` — Playwright will auto-start `npm run dev` via the configured webServer, then run `src/tests/foundation.spec.ts`. All three tests must pass: dark bg applied, 3 panes render, viewport fills.

  If a Playwright test fails, do NOT modify the test to make it pass. Diagnose: which assertion fails? Likely candidates: missing `className="dark"` on root, missing `data-testid` on a pane, missing `--color-bg-0` definition. Fix the SOURCE (AppShell, panes, or index.css) — never the test.</action>
  <verify>
    <automated>node --experimental-strip-types src/tests/stub-data.test.ts &amp;&amp; node --experimental-strip-types src/tests/format.test.ts &amp;&amp; npm test -- --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `node --experimental-strip-types src/tests/stub-data.test.ts` exits 0
    - CLI: `node --experimental-strip-types src/tests/format.test.ts` exits 0
    - CLI: `npm test -- --project=chromium` exits 0
    - Behavior: Playwright output contains `3 passed` (the three foundation tests)
    - Behavior: Dev server logs show `Local: http://localhost:5173` (auto-started by webServer config)
    - No flaky retries: with `retries: 0` (non-CI), the suite passes on the first attempt
  </acceptance_criteria>
  <done>Full Phase 1 validation suite is green; the foundation is verified end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual visual verification — Walking Skeleton</name>
  <what-built>
    A running dev server at `http://localhost:5173` showing the OIMG dark-theme AppShell with three resizable panes (Files, Preview, Inspector) — the Walking Skeleton for the entire UI port.
  </what-built>
  <how-to-verify>
    1. From repo root, run `npm run dev` and visit `http://localhost:5173`.
    2. Confirm the page background is the DARK OIMG color (`oklch(0.165 0.008 250)` — a deep blue-grey, NOT pure black, NOT white).
    3. Confirm THREE distinct vertical panes are visible: left pane labeled "Files" (~20% width), center pane labeled "Preview" (~55%), right pane labeled "Inspector" (~25%).
    4. Confirm the pane headers ("Files", "Preview", "Inspector") render in `--color-fg-2` (muted grey) at the top of each pane.
    5. Drag the resize handle between Files and Preview — the panes should resize smoothly; min size on left should clamp at ~15%.
    6. Open DevTools → Network tab → reload — confirm at least one `inter-*.woff2` font file is loaded (Inter Variable from `@fontsource-variable/inter` imported by main.tsx).
    7. Open DevTools → Elements → click the root `<div role="application">` — confirm `class="dark ..."` is on the root.
    8. Open DevTools → Console — confirm NO red errors. (A warning about `crossOriginIsolated is false` from main.tsx is expected and harmless in Phase 1.)
    9. Resize the browser window — no horizontal scrollbar should appear; the app should always fill the viewport.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe specific visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- All four automated test commands exit 0 (Task 4)
- Human visually confirms dark theme, three panes, resize behavior, Inter font (Task 5)
- `npm run build` succeeds; `dist/` is produced
</verification>

<success_criteria>
- 5 component files exist; all type-check
- `npm test -- --project=chromium` reports 3 passed
- `npm run dev` serves a visible OIMG AppShell at localhost:5173
- Human approval recorded
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`. After human approval, the Walking Skeleton is the foundation for Phase 2 (Files Pane wires the left pane to filesAtom).
</output>
