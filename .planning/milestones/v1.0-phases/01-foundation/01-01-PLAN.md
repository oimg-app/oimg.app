---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - node_modules/
  - src/tests/foundation.spec.ts
  - src/tests/stub-data.test.ts
  - src/tests/format.test.ts
autonomous: true
requirements: []
tags: [foundation, wave-0, setup, tests]

must_haves:
  truths:
    - "node_modules/ exists with shadcn, react-resizable-panels, tailwindcss v4, @phosphor-icons/react installed"
    - "Wave 0 test stubs exist and are executable (failing in expected-Wave-0 state, not parse-erroring)"
    - "npm run build succeeds (TypeScript compile + Vite build) against current src/main.tsx + empty App"
  artifacts:
    - path: "node_modules/"
      provides: "Installed packages"
    - path: "src/tests/foundation.spec.ts"
      provides: "Playwright smoke: dark bg, 3 panes visible, viewport fills"
    - path: "src/tests/stub-data.test.ts"
      provides: "Node unit: STUB_FILES.length === 12, SVGO_PLUGINS.length === 22"
    - path: "src/tests/format.test.ts"
      provides: "Node unit: fmtBytes(1024) === '1.0 KB', fmtPct(100,50) === '−50.0%'"
  key_links:
    - from: "package.json"
      to: "node_modules/"
      via: "npm install"
      pattern: "node_modules/react-resizable-panels"
    - from: "playwright.config.ts"
      to: "src/tests/*.spec.ts"
      via: "testDir: './src/tests', testMatch: '**/*.spec.ts'"
      pattern: "foundation\\.spec\\.ts"
---

<objective>
Establish the executable environment for Phase 1 and create Wave 0 validation stubs.

Purpose: All subsequent plans depend on installed `node_modules/` and on validation tests existing before the code they validate is shipped (Nyquist sampling per 01-VALIDATION.md).
Output: Installed dependencies + three test files in `src/tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@playwright.config.ts
@package.json

<interfaces>
<!-- Test runner conventions -->
- Playwright config: testDir = './src/tests', testMatch = '**/*.spec.ts', baseURL = 'http://localhost:5173', webServer = 'npm run dev'
- Node unit tests: extension '.test.ts', run via 'node --experimental-strip-types src/tests/<file>.test.ts'
- Wave 0 contract: tests must PARSE and EXECUTE; assertions targeting not-yet-shipped modules should catch and log "Wave 0 stub state" rather than throw
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies</name>
  <files>node_modules/, package-lock.json</files>
  <read_first>
    - package.json (verify lockfile present; do not modify package.json)
  </read_first>
  <action>Run `npm install` from repo root. Do not edit package.json or upgrade any package. The `postinstall` script `scripts/ensure-rollup-binding.mjs` will run automatically — let it. After install, verify the four critical packages resolve: `node_modules/react-resizable-panels/package.json`, `node_modules/tailwindcss/package.json`, `node_modules/shadcn/package.json`, `node_modules/@phosphor-icons/react/package.json`.</action>
  <verify>
    <automated>npm install &amp;&amp; test -d node_modules/react-resizable-panels &amp;&amp; test -d node_modules/tailwindcss &amp;&amp; test -d node_modules/shadcn &amp;&amp; test -d node_modules/@phosphor-icons/react</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `test -d node_modules/react-resizable-panels` exit code 0
    - CLI: `node -e "console.log(require('tailwindcss/package.json').version)"` prints a version starting with `4.`
    - CLI: `node -e "console.log(require('react-resizable-panels/package.json').version)"` prints `4.11.0`
    - Source assertion: `package.json` UNCHANGED (no diff vs. HEAD); `package-lock.json` may be touched only by npm if it was stale.
  </acceptance_criteria>
  <done>node_modules/ contains all dependencies; `npm run build` will be able to resolve imports in subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/tests/foundation.spec.ts (SETUP-01/02 + SHELL-01 smoke)</name>
  <files>src/tests/foundation.spec.ts</files>
  <read_first>
    - playwright.config.ts (confirm baseURL = http://localhost:5173, webServer starts npm run dev, testMatch = '**/*.spec.ts')
    - .planning/phases/01-foundation/01-PATTERNS.md (section "src/tests/foundation.spec.ts" — Wave 0 spec template)
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>Create `src/tests/foundation.spec.ts` exporting three Playwright tests using `import { test, expect } from '@playwright/test'`:
  1) `'dark background applied (SETUP-01/02)'` — `page.goto('/')`, then read `getComputedStyle(document.documentElement).getPropertyValue('--color-bg-0').trim()` via `page.evaluate`; assert it is truthy (non-empty string). Locate `[role="application"]` and assert its `classList.contains('dark')` is true via `page.locator(...).evaluate`.
  2) `'3 panes render (SHELL-01)'` — `page.goto('/')`, then `await expect(page.getByTestId('files-pane')).toBeVisible()`, same for `'center-pane'` and `'inspector-pane'`.
  3) `'viewport fills screen'` — `page.goto('/')`, take `boundingBox()` of `[role="application"]`; expect `width > 0` and `height > 0`.
  Tests will be RED in Wave 0 (no app shell yet). Do NOT add try/catch — Playwright spec failures are expected pre-implementation; downstream plans turn them GREEN.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/tests/foundation.spec.ts','utf8');if(!s.includes('files-pane')||!s.includes('center-pane')||!s.includes('inspector-pane')||!s.includes('--color-bg-0')||!s.includes('role=\"application\"')){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/foundation.spec.ts` exists
    - Contains the three test names verbatim: `'dark background applied (SETUP-01/02)'`, `'3 panes render (SHELL-01)'`, `'viewport fills screen'`
    - References `getByTestId('files-pane')`, `getByTestId('center-pane')`, `getByTestId('inspector-pane')`
    - Reads CSS var `--color-bg-0` via `page.evaluate`
    - Uses `import { test, expect } from '@playwright/test'`
    - File is parseable: `node -e "require('typescript').transpileModule(require('fs').readFileSync('src/tests/foundation.spec.ts','utf8'),{compilerOptions:{module:'esnext',target:'es2022',jsx:'preserve'}})"` exits 0
  </acceptance_criteria>
  <done>foundation.spec.ts exists and is syntactically valid TypeScript; Playwright will discover it via testMatch.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/tests/stub-data.test.ts and src/tests/format.test.ts (Node unit stubs)</name>
  <files>src/tests/stub-data.test.ts, src/tests/format.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (sections "src/tests/stub-data.test.ts" and "src/tests/format.test.ts" — Wave 0 test template with passed/failed counters and Wave 0 stub-catch fallback)
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>Create two Node-runnable test files using the pattern shown in 01-PATTERNS.md (top-level await + dynamic `import('../lib/<name>.ts')`, manual `assert(name, cond)` counter, try/catch that treats "module not yet shipped" as expected Wave 0 state and sets `passed++`, `process.exit(failed > 0 ? 1 : 0)`).

  `src/tests/stub-data.test.ts` assertions (when module is shipped):
  - `mod.STUB_FILES.length === 12`
  - `mod.SVGO_PLUGINS.length === 22`
  - `mod.CODECS.length === 5`
  - `mod.RESIZE_ALGS.length === 4`
  - `mod.FIT_MODES.length === 3`
  - `'id' in mod.STUB_FILES[0] && 'orig' in mod.STUB_FILES[0]`

  `src/tests/format.test.ts` assertions (when module is shipped):
  - `fmtBytes(0) === '0 B'`
  - `fmtBytes(1024) === '1.0 KB'`
  - `fmtBytes(1048576) === '1.00 MB'`
  - `fmtBytes(null) === '—'`
  - `fmtPct(100, 50) === '−50.0%'`
  - `fmtPct(100, 150) === '+50.0%'`
  - `fmtPct(0, 0) === '—'`
  - `fmtPct(100, 100) === ''`

  Both files MUST print `${passed} passed, ${failed} failed` and exit 0 in Wave 0 (modules missing) so plan 01 verifies cleanly.</action>
  <verify>
    <automated>node --experimental-strip-types src/tests/stub-data.test.ts &amp;&amp; node --experimental-strip-types src/tests/format.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under `src/tests/`
    - Behavior: `node --experimental-strip-types src/tests/stub-data.test.ts` exits 0 in Wave 0 state (module not yet shipped) AND prints a line matching `^\d+ passed, \d+ failed$`
    - Behavior: same for `format.test.ts`
    - Source assertion: each file contains `await import('../lib/stub-data.ts')` or `'../lib/format.ts'` respectively
    - Source assertion: each file contains `process.exit(failed > 0 ? 1 : 0)`
    - Source assertion (stub-data): contains the exact literals `STUB_FILES.length === 12`, `SVGO_PLUGINS.length === 22`, `CODECS`, `RESIZE_ALGS`, `FIT_MODES`
    - Source assertion (format): contains the exact literals `fmtBytes(1024)`, `'1.0 KB'`, `fmtPct(100, 50)`, `'−50.0%'`
  </acceptance_criteria>
  <done>Both Node test files exit 0 in their Wave 0 stub state; will switch to real assertions once Plan 04 ships the modules.</done>
</task>

</tasks>

<verification>
- `npm install` completed and `node_modules/react-resizable-panels` exists
- `npm run build` succeeds against current `src/main.tsx` + minimal `src/App.tsx` (note: App.tsx may not exist yet — `tsc -b && vite build` may fail until Plan 05 ships App.tsx; this is expected and re-verified after Wave 2)
- All three test files exist; Node tests exit 0 in Wave 0 stub state
</verification>

<success_criteria>
- `test -d node_modules/react-resizable-panels` exits 0
- `node --experimental-strip-types src/tests/stub-data.test.ts` exits 0
- `node --experimental-strip-types src/tests/format.test.ts` exits 0
- `ls src/tests/foundation.spec.ts` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`.
</output>
