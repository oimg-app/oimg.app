---
phase: 05-center-pane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/panels/CenterPane.tsx
  - src/components/panels/center/CenterHeader.tsx
  - src/components/panels/center/CompareStage.tsx
autonomous: true
requirements:
  - CENTER-01
  - CENTER-02
  - CENTER-03
must_haves:
  truths:
    - "CenterPane renders as a flex-col column filling full height with 3 sections: header, stage, delta strip placeholder"
    - "CenterHeader shows breadcrumb with file name, type->target tag, dim tag, q tag (conditionally), and zoom popover"
    - "CompareStage shows two placeholder colored layers clipped by --split CSS var; split handle is draggable and calls setSplit"
  artifacts:
    - path: "src/components/panels/CenterPane.tsx"
      provides: "Root shell composing CenterHeader + CompareStage + DeltaStrip placeholder"
      contains: "flex flex-col h-full"
    - path: "src/components/panels/center/CenterHeader.tsx"
      provides: "Breadcrumb + zoom popover wired to uiAtom"
      exports: ["CenterHeader"]
    - path: "src/components/panels/center/CompareStage.tsx"
      provides: "Compare stage with CSS --split var, layer-orig, layer-opt, split handle drag"
      exports: ["CompareStage"]
  key_links:
    - from: "CenterHeader"
      to: "uiAtom.zoom / setZoom"
      via: "useStore(uiAtom)"
    - from: "CompareStage"
      to: "uiAtom.split / setSplit"
      via: "useStore(uiAtom)"
    - from: "CenterPane"
      to: "$selectedFile"
      via: "useStore($selectedFile) propagated to children"
---

<objective>
Build the CenterPane shell, CenterHeader breadcrumb + zoom popover, and CompareStage with draggable split handle.

Purpose: Deliver the visible skeleton of the center pane — a developer opening the app sees the selected file breadcrumb, type/dim/q tags, zoom control, and the compare stage with split handle draggable across the placeholder image layers.

Output: Three new components (CenterPane, CenterHeader, CompareStage) fully wired to nanostores uiAtom and $selectedFile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-center-pane/05-UI-SPEC.md
</context>

<interfaces>
From src/stores/ui.ts:
  export const uiAtom = map({ split: 50, zoom: 100, ... })
  export function setSplit(pct: number): void
  export function setZoom(z: number): void

From src/stores/files.ts:
  export const $selectedFile: ReadableAtom (resolves to FileEntry | null)

From src/lib/stub-data.ts:
  interface FileEntry { id, name, type, orig, opt, status, target, dim, q: number | null, prog? }

From src/lib/format.ts:
  export function fmtBytes(b: number | null | undefined): string

Shadcn (already generated — src/components/ui/popover.tsx):
  export { Popover, PopoverTrigger, PopoverContent }

Phosphor icons (@phosphor-icons/react):
  Eye, Check, CaretDown — already in package.json
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CenterHeader — breadcrumb + zoom popover wired to stores</name>
  <files>src/components/panels/center/CenterHeader.tsx</files>
  <read_first>
    - src/components/panels/InspectorPane.tsx (useStore pattern, className conventions, null-guard pattern)
    - src/stores/ui.ts (uiAtom shape, setZoom signature)
    - src/stores/files.ts ($selectedFile type)
    - .planning/phases/05-center-pane/05-UI-SPEC.md (CenterHeader layout section, typography table, color table)
  </read_first>
  <behavior>
    - When selectedFile is null: breadcrumb renders only the "Queue" span (text-[var(--color-fg-2)]); "Optimized" pill is absent
    - When selectedFile is { name: 'hero.png', type: 'png', target: 'webp', dim: '2400x1600', q: 82 }: breadcrumb shows Queue / hero.png / PNG->WEBP tag / 2400x1600 tag / q82 tag
    - When selectedFile.q === null: q tag is absent from the DOM entirely (conditional render, not hidden)
    - Zoom popover trigger shows Eye icon + zoom label + CaretDown; zoom=100 shows "100%"; zoom='fit' shows "Fit"
    - Clicking zoom option 50 calls setZoom(50) and closes popover; active option shows Check icon
  </behavior>
  <action>
    Create src/components/panels/center/CenterHeader.tsx with phase attribution comment at top:
    // Phase 05 — CENTER-02: CenterHeader breadcrumb + zoom popover

    Imports: useStore from @nanostores/react; uiAtom + setZoom from @/stores/ui; $selectedFile from @/stores/files; Popover/PopoverTrigger/PopoverContent from @/components/ui/popover; Eye + Check + CaretDown from @phosphor-icons/react.

    Data reads: const { zoom } = useStore(uiAtom); const selectedFile = useStore($selectedFile).
    Ephemeral state: const [open, setOpen] = useState(false) for popover open/close — this is ephemeral UI state, permitted by STORE-08.

    Constant at module level: const FILE_TAG = 'font-mono text-[11px] font-semibold bg-[var(--color-bg-2)] text-[var(--color-fg-1)] px-1.5 py-0.5 rounded-[3px] whitespace-nowrap'

    Root: header element, className `flex items-center justify-between h-9 px-3 border-b border-[var(--color-line)] bg-[var(--color-bg-1)] shrink-0`.

    Left (breadcrumb): nav with `flex items-center gap-1.5 min-w-0`. Children:
      span `text-[13px] text-[var(--color-fg-2)]` -> "Queue"
      span `text-[13px] text-[var(--color-fg-3)]` -> "/"
      When selectedFile non-null:
        span `text-[13px] font-medium text-[var(--color-fg-0)] truncate max-w-[200px]` -> {selectedFile.name}
        span className={FILE_TAG} -> {selectedFile.type.toUpperCase()}->{selectedFile.target.toUpperCase()}
        span className={FILE_TAG} -> {selectedFile.dim}
        {selectedFile.q !== null && span className={FILE_TAG} -> q{selectedFile.q}}

    Right controls: div `flex items-center gap-2 shrink-0`. Children:
      When selectedFile non-null: span `font-mono text-[11px] font-semibold bg-[var(--color-accent-dim)] text-[var(--color-accent)] px-2 py-0.5 rounded-full flex items-center gap-1` with Check size={10} + " Optimized"
      Zoom popover: Popover open={open} onOpenChange={setOpen}.
        PopoverTrigger: button (type="button") `flex items-center gap-1 h-6 px-2 font-mono text-[11px] text-[var(--color-fg-1)] hover:text-[var(--color-fg-0)] hover:bg-[var(--color-bg-2)] rounded transition-colors` containing Eye size={12} + zoom label (zoom === 'fit' ? 'Fit' : `${zoom}%`) + CaretDown size={10}.
        PopoverContent side="bottom" align="end", className `w-32 p-1 bg-[var(--color-bg-1)] border-[var(--color-line)]`.
          Render options const ZOOM_OPTS: Array of [25, 50, 100, 200, 'fit']. For each opt: button (type="button") `w-full flex items-center justify-between h-7 px-2 font-mono text-[11px] rounded hover:bg-[var(--color-bg-2)] text-[var(--color-fg-1)]` showing label (typeof opt === 'number' ? `${opt}%` : 'Fit') + when zoom===opt Check size={10}.
          onClick: calls setZoom(opt as number) then setOpen(false). Note: setZoom accepts number; 'fit' should pass as setZoom('fit' as unknown as number) matching existing uiAtom zoom type (which is typed as number but spec says 'fit' string is valid — use (setZoom as (z: number | string) => void)('fit')).
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/oimg.app && npx tsc --noEmit 2>&1 | grep -E "CenterHeader|error"</automated>
  </verify>
  <done>
    - src/components/panels/center/CenterHeader.tsx exists
    - Phase attribution comment present: "// Phase 05 — CENTER-02"
    - TypeScript reports zero errors for the file
    - FILE_TAG constant defined at module level containing the exact class string
    - q tag render is wrapped in {selectedFile.q !== null && ...} guard
    - ZOOM_OPTS array contains exactly [25, 50, 100, 200, 'fit']
    - useStore(uiAtom) and useStore($selectedFile) used for data (not useState for data)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompareStage — CSS --split var, placeholder layers, draggable split handle</name>
  <files>src/components/panels/center/CompareStage.tsx</files>
  <read_first>
    - src/stores/ui.ts (uiAtom.split, setSplit signature)
    - src/stores/files.ts ($selectedFile)
    - src/lib/format.ts (fmtBytes signature)
    - .planning/phases/05-center-pane/05-UI-SPEC.md (CompareStage layout section, Critical Implementation Notes §1 §4 §7 §8)
  </read_first>
  <behavior>
    - image-frame div carries inline style { '--split': '50%' } when split=50 (cast as React.CSSProperties)
    - layer-orig has clipPath string 'inset(0 calc(100% - var(--split)) 0 0)'
    - layer-opt has clipPath string 'inset(0 0 0 var(--split))'
    - On mousedown on split handle: window.addEventListener('mousemove') and window.addEventListener('mouseup') are added
    - On mousemove during drag: pct = (e.clientX - rect.left) / rect.width * 100 clamped to [2, 98] then setSplit(pct) called
    - On mouseup: both mousemove and mouseup window listeners removed (no listener leak)
    - When selectedFile is null: split labels show fmtBytes(null) = "—"
    - Split label left text: "ORIGINAL · {fmtBytes(file.orig)}"
    - Split label right text: "{file.target.toUpperCase()} · {fmtBytes(file.opt)}"
  </behavior>
  <action>
    Create src/components/panels/center/CompareStage.tsx with phase attribution comment:
    // Phase 05 — CENTER-03: CompareStage compare view with CSS --split var

    Imports: useRef from react; useStore from @nanostores/react; uiAtom + setSplit from @/stores/ui; $selectedFile from @/stores/files; fmtBytes from @/lib/format.

    Data reads: const { split } = useStore(uiAtom); const selectedFile = useStore($selectedFile).
    Refs: const frameRef = useRef<HTMLDivElement>(null) for image-frame; per STORE-08 §8 drag uses useRef not useState — const dragging = useRef(false).

    handleMouseDown: function that captures the current frame rect via frameRef.current.getBoundingClientRect(), defines onMove (computes pct from e.clientX, clamps to [2,98], calls setSplit), defines onUp (calls window.removeEventListener for both 'mousemove' onMove and 'mouseup' onUp), then adds both listeners via window.addEventListener.

    Root div: `flex-1 min-h-0 flex flex-col overflow-hidden bg-[var(--color-bg-0)]`.

    image-frame div: ref={frameRef}, className `relative w-full h-full overflow-hidden`, style={{ '--split': split + '%' } as React.CSSProperties}.

    layer-orig div: `absolute inset-0 bg-[var(--color-bg-2)]`, style={{ clipPath: 'inset(0 calc(100% - var(--split)) 0 0)' }}.

    layer-opt div: `absolute inset-0 bg-[var(--color-bg-3)]`, style={{ clipPath: 'inset(0 0 0 var(--split))' }}.

    split-handle div: `absolute top-0 bottom-0 w-1 cursor-col-resize bg-[var(--color-accent)]`, style={{ left: 'var(--split)', transform: 'translateX(-50%)' }}, onMouseDown={handleMouseDown}.

    Split label left (absolute bottom-3 left-3):
      className `absolute bottom-3 left-3 flex items-center gap-1.5 px-2 py-1 rounded-[4px] font-mono text-[11px] font-semibold text-[var(--color-fg-2)] bg-[var(--color-bg-0)]/70 backdrop-blur-sm pointer-events-none`
      dot span `w-2 h-2 rounded-full bg-[var(--color-fg-3)] shrink-0`
      text: ORIGINAL · {fmtBytes(selectedFile?.orig ?? null)}

    Split label right (absolute bottom-3 right-3):
      same classNames except bottom-3 right-3
      dot span `w-2 h-2 rounded-full bg-[var(--color-accent)] shrink-0`
      text: {selectedFile?.target.toUpperCase() ?? '—'} · {fmtBytes(selectedFile?.opt ?? null)}
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/oimg.app && npx tsc --noEmit 2>&1 | grep -E "CompareStage|error"</automated>
  </verify>
  <done>
    - src/components/panels/center/CompareStage.tsx exists
    - Phase attribution comment present: "// Phase 05 — CENTER-03"
    - TypeScript reports zero errors
    - image-frame inline style uses 'as React.CSSProperties' cast
    - layer-orig style.clipPath contains 'var(--split)'
    - layer-opt style.clipPath contains 'var(--split)'
    - handleMouseDown adds both 'mousemove' and 'mouseup' window listeners; onUp removes both
    - No useState in the file (grep returns zero results) — drag uses useRef
    - fmtBytes imported from @/lib/format
  </done>
</task>

<task type="auto">
  <name>Task 3: CenterPane shell — compose CenterHeader + CompareStage + DeltaStrip placeholder</name>
  <files>src/components/panels/CenterPane.tsx</files>
  <read_first>
    - src/components/panels/InspectorPane.tsx (pane root class pattern, data-testid pattern)
    - src/components/panels/center/CenterHeader.tsx (verify export name CenterHeader)
    - src/components/panels/center/CompareStage.tsx (verify export name CompareStage)
    - .planning/phases/05-center-pane/05-UI-SPEC.md (Layout Contract — CenterPane shell classes)
  </read_first>
  <action>
    Create src/components/panels/CenterPane.tsx with phase attribution comment:
    // Phase 05 — CENTER-01: CenterPane shell composing header / compare stage / delta strip

    Imports: CenterHeader from ./center/CenterHeader; CompareStage from ./center/CompareStage.

    Export function CenterPane(). Root div: data-testid="center-pane", className `flex flex-col h-full min-h-0 bg-[var(--color-bg-0)] overflow-hidden`.

    Children in order:
      1. CenterHeader (provides shrink-0 internally via its header element)
      2. CompareStage (provides flex-1 min-h-0 internally)
      3. Delta strip placeholder: div className `h-[72px] shrink-0 border-t border-[var(--color-line)] bg-[var(--color-bg-1)] flex items-center justify-center` with inner span `text-[11px] font-mono text-[var(--color-fg-3)]` -> "DeltaStrip — Plan 05-02". This placeholder is replaced entirely in Plan 05-02.

    Do NOT import or wire CenterPane into AppShell in this task — AppShell wiring is out of Phase 5 scope.
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/oimg.app && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>
    - src/components/panels/CenterPane.tsx exists
    - Phase attribution comment present: "// Phase 05 — CENTER-01"
    - data-testid="center-pane" on root div
    - Root div contains: flex flex-col h-full min-h-0 bg-[var(--color-bg-0)] overflow-hidden
    - CenterHeader and CompareStage rendered as children
    - DeltaStrip placeholder div present with h-[72px] shrink-0
    - npx tsc --noEmit reports 0 error TS lines across the whole project
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store->component | $selectedFile and uiAtom values flow from nanostores into React render |
| user->drag event | Raw mouse coordinates from window events compute split percentage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CenterHeader filename display | mitigate | React JSX text nodes auto-escape all special characters; filename is inserted as a React text node, never as raw HTML — use {selectedFile.name} not innerHTML or innerHTML-equivalent props |
| T-05-02 | Denial of Service | CompareStage window event listeners | mitigate | onUp removes both mousemove and mouseup listeners synchronously within the same closure; useRef (not useState) prevents listener accumulation across re-renders |
| T-05-03 | Tampering | Zoom value written to uiAtom | accept | setZoom is only called from a closed list [25,50,100,200,'fit']; no free-text user input path exists |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan; @phosphor-icons/react already in package.json |
</threat_model>

<verification>
After all tasks complete:
1. npx tsc --noEmit exits 0 with zero error TS lines
2. grep -r "innerHTML" src/components/panels/center/ returns no results
3. grep -n "useState" src/components/panels/center/CompareStage.tsx returns no results (drag uses useRef)
4. grep -c "useState" src/components/panels/center/CenterHeader.tsx returns 1 (popover open — ephemeral, permitted)
5. grep "data-testid=\"center-pane\"" src/components/panels/CenterPane.tsx finds a match
6. grep "FILE_TAG" src/components/panels/center/CenterHeader.tsx finds the constant containing px-1.5 py-0.5 rounded-\[3px\]
</verification>

<success_criteria>
- CenterPane.tsx, CenterHeader.tsx, CompareStage.tsx all exist and compile cleanly
- CenterHeader shows Queue breadcrumb, file tags from $selectedFile, and zoom popover reading from uiAtom
- CompareStage --split CSS var updates when setSplit is called via drag
- Split handle drag adds and removes window listeners without leaks
- All three components follow STORE-08: data via useStore; only popover open uses useState (ephemeral)
- Phase attribution comments present in all three files
</success_criteria>

<output>
Create `.planning/phases/05-center-pane/05-01-SUMMARY.md` when done
</output>
