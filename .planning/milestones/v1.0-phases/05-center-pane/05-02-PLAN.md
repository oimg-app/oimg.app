---
phase: 05-center-pane
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/panels/center/DeltaStrip.tsx
  - src/components/panels/CenterPane.tsx
autonomous: true
requirements:
  - CENTER-04
must_haves:
  truths:
    - "DeltaStrip renders 6 metric cards in a horizontal row (Original, Optimized, Saved, SSIM, Butteraugli, Decode)"
    - "Original and Optimized cards read live values from $selectedFile; Saved card computes savings"
    - "Saved card value is accent-colored; stub cards (SSIM, Butteraugli, Decode) show hardcoded values"
    - "When no file is selected, all card values show — placeholder"
  artifacts:
    - path: "src/components/panels/center/DeltaStrip.tsx"
      provides: "6 delta metric cards wired to $selectedFile and settingsAtom"
      exports: ["DeltaStrip"]
    - path: "src/components/panels/CenterPane.tsx"
      provides: "Updated shell replacing placeholder div with DeltaStrip"
      contains: "DeltaStrip"
  key_links:
    - from: "DeltaStrip"
      to: "$selectedFile"
      via: "useStore($selectedFile)"
    - from: "DeltaStrip"
      to: "settingsAtom.codec / q / method"
      via: "useStore(settingsAtom)"
    - from: "CenterPane"
      to: "DeltaStrip"
      via: "import and render replacing placeholder"
---

<objective>
Build the DeltaStrip component with 6 metric cards and wire it into CenterPane in place of the placeholder.

Purpose: Complete the center pane vertical slice — a developer sees live file metrics (size, savings, codec settings) in the delta strip, reading from the same stores that drive the rest of the app.

Output: DeltaStrip.tsx (new) + CenterPane.tsx (updated to import DeltaStrip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-center-pane/05-UI-SPEC.md
@.planning/phases/05-center-pane/05-01-SUMMARY.md
</context>

<interfaces>
From src/stores/files.ts:
  export const $selectedFile: ReadableAtom (resolves to FileEntry | null)
  FileEntry: { id, name, type, orig, opt, status, target, dim, q: number | null }

From src/stores/settings.ts:
  export const settingsAtom: MapStore<SettingsState>
  SettingsState includes: codec: Codec, q: number, method: number

From src/lib/format.ts:
  export function fmtBytes(b: number | null | undefined): string
  export function fmtPct(orig, opt): string   // returns "−34.5%" style string

CenterPane (to be updated — Plan 05-01 output):
  src/components/panels/CenterPane.tsx — replace placeholder div with DeltaStrip import
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeltaStrip — 6 metric cards wired to $selectedFile and settingsAtom</name>
  <files>src/components/panels/center/DeltaStrip.tsx</files>
  <read_first>
    - src/stores/files.ts ($selectedFile, FileEntry interface)
    - src/stores/settings.ts (settingsAtom shape: codec, q, method)
    - src/lib/format.ts (fmtBytes and fmtPct signatures)
    - .planning/phases/05-center-pane/05-UI-SPEC.md (DeltaStrip cards table, typography section, color section)
  </read_first>
  <behavior>
    - When selectedFile is { orig: 1842300, opt: 412800 } and settings { codec: 'WebP', q: 82, method: 4 }:
        Original card: label="ORIGINAL", value=fmtBytes(1842300)="1.76 MB", sub="2400x1600 · png"
        Optimized card: label="OPTIMIZED", value=fmtBytes(412800)="403.1 KB", sub="WebP q82 m4"
        Saved card: label="SAVED", value="-{fmtBytes(1429500)}", sub="{fmtPct(1842300, 412800)} smaller"
    - When selectedFile is null:
        All .v spans show "—" (use fmtBytes(null) which returns "—"); sub spans show "—"
    - Saved card .v span has text-[var(--color-accent)] class
    - SSIM card: label="SSIM", value="0.987", sub="visually identical" (hardcoded)
    - Butteraugli card: label="BUTTERAUGLI", value="1.24", sub="target <= 1.40" (hardcoded)
    - Decode card: label="DECODE", value="38ms", sub="est. on 4G" (hardcoded)
    - All 6 cards in a single horizontal flex row; last card has no right border
  </behavior>
  <action>
    Create src/components/panels/center/DeltaStrip.tsx with phase attribution comment:
    // Phase 05 — CENTER-04: DeltaStrip 6 metric cards

    Imports: useStore from @nanostores/react; $selectedFile from @/stores/files; settingsAtom from @/stores/settings; fmtBytes + fmtPct from @/lib/format.

    Data reads: const selectedFile = useStore($selectedFile); const { codec, q, method } = useStore(settingsAtom).

    Derived values (compute in render body):
      const orig = selectedFile?.orig ?? null
      const opt = selectedFile?.opt ?? null
      const saved = orig !== null && opt !== null ? orig - opt : null
      const dim = selectedFile?.dim ?? '—'
      const type = selectedFile?.type ?? '—'

    Root div: className `h-[72px] shrink-0 border-t border-[var(--color-line)] bg-[var(--color-bg-1)] flex` (no padding — cards fill full width via flex-1).

    Define a DeltaCard internal component (or inline JSX helper) accepting props: label: string, value: React.ReactNode, sub: string, accent?: boolean.
    DeltaCard renders a div `flex flex-col items-center justify-center flex-1 gap-0.5 border-r border-[var(--color-line)] last:border-r-0 px-2 min-w-0`:
      span `font-mono text-[11px] font-normal text-[var(--color-fg-2)] uppercase tracking-[0.04em]` -> {label}
      span className for value: base `font-mono text-[13px] font-semibold tabular-nums` + when accent add `text-[var(--color-accent)]` else `text-[var(--color-fg-0)]`
      span `font-mono text-[11px] font-normal text-[var(--color-fg-3)] truncate` -> {sub}

    Render 6 DeltaCard instances:
      1. label="ORIGINAL" value={fmtBytes(orig)} sub={`${dim} · ${type}`}
      2. label="OPTIMIZED" value={fmtBytes(opt)} sub={selectedFile ? `${codec} q${q} m${method}` : '—'}
      3. label="SAVED" value={saved !== null ? `−${fmtBytes(saved)}` : '—'} sub={fmtPct(orig, opt) ? fmtPct(orig, opt) + ' smaller' : '—'} accent={true}
         Note: the minus sign here is a regular ASCII hyphen (the leading "−" in the spec is U+2212 MINUS SIGN; use that character literal in the string).
      4. label="SSIM" value="0.987" sub="visually identical"
      5. label="BUTTERAUGLI" value="1.24" sub="target <= 1.40"
      6. label="DECODE" value="38ms" sub="est. on 4G"
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/oimg.app && npx tsc --noEmit 2>&1 | grep -E "DeltaStrip|error TS"</automated>
  </verify>
  <done>
    - src/components/panels/center/DeltaStrip.tsx exists
    - Phase attribution comment present: "// Phase 05 — CENTER-04"
    - TypeScript reports zero errors
    - Component renders exactly 6 cards (grep for DeltaCard or card div repetitions)
    - Saved card has accent={true} (or equivalent accent class applied to .v span)
    - fmtBytes and fmtPct imported from @/lib/format (not inline)
    - No useState in file — all data via useStore
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire DeltaStrip into CenterPane — replace placeholder</name>
  <files>src/components/panels/CenterPane.tsx</files>
  <read_first>
    - src/components/panels/CenterPane.tsx (current state — contains placeholder div from Plan 05-01)
    - src/components/panels/center/DeltaStrip.tsx (verify export name DeltaStrip)
    - .planning/phases/05-center-pane/05-UI-SPEC.md (Layout Contract — DeltaStrip: h-[72px] shrink-0)
  </read_first>
  <action>
    Edit src/components/panels/CenterPane.tsx:

    1. Add import: import { DeltaStrip } from './center/DeltaStrip'

    2. Replace the placeholder div (the one containing "DeltaStrip — Plan 05-02" text) with: &lt;DeltaStrip /&gt;

    The DeltaStrip component already provides its own h-[72px] shrink-0 border-t classes — do not wrap it or add redundant classes.

    Update the phase attribution header to include both plans:
    // Phase 05 — CENTER-01: CenterPane shell. Updated: CENTER-04 (DeltaStrip wired)

    Do not change any other line in the file.
  </action>
  <verify>
    <automated>cd /Users/jilizart/Projects/oimg.app && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>
    - CenterPane.tsx imports DeltaStrip from ./center/DeltaStrip
    - DeltaStrip rendered as child of root div (not the placeholder div)
    - Placeholder div text "DeltaStrip — Plan 05-02" is absent from file
    - npx tsc --noEmit reports 0 error TS lines across the whole project
    - npm run build exits 0 (or npx vite build if npm run build not defined)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store->component | $selectedFile.name/.type/.dim flow into React text nodes |
| settings->display | settingsAtom.codec/q/method rendered as text in card subtitle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | DeltaStrip filename/codec display | mitigate | All values inserted as React text nodes (JSX expressions); codec is a Codec enum value from a closed type; no free-form rendering path exists |
| T-05-05 | Information Disclosure | DeltaStrip savings display | accept | All data is client-side stub data already in bundle; no PII; zero server involvement |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages added in this plan; all imports resolve to existing dependencies |
</threat_model>

<verification>
After all tasks complete:
1. npx tsc --noEmit exits 0 with zero error TS lines
2. grep -n "useState" src/components/panels/center/DeltaStrip.tsx returns no results
3. grep "DeltaStrip" src/components/panels/CenterPane.tsx finds both the import and JSX usage
4. grep "DeltaStrip — Plan 05-02" src/components/panels/CenterPane.tsx returns no results (placeholder removed)
5. grep -c "DeltaCard\|flex-1.*border-r" src/components/panels/center/DeltaStrip.tsx returns >= 6 (one per card)
6. grep "color-accent" src/components/panels/center/DeltaStrip.tsx finds the Saved card accent class
</verification>

<success_criteria>
- DeltaStrip.tsx exists and compiles without errors
- 6 metric cards render in a horizontal row filling the 72px strip
- Original/Optimized/Saved cards show live values from $selectedFile and settingsAtom
- Saved card value is accent-colored
- SSIM, Butteraugli, Decode show hardcoded stub values
- Empty state (no file): all cards show "—" values
- CenterPane.tsx replaces placeholder with DeltaStrip — full center pane layout visible
- Zero TypeScript errors across the entire project
</success_criteria>

<output>
Create `.planning/phases/05-center-pane/05-02-SUMMARY.md` when done
</output>
