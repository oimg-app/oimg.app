---
phase: 06-inspector-pane-output-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/panels/inspector/ReportPanel.tsx
  - src/tests/report-panel.spec.ts
autonomous: true
requirements: [INSP-08]
must_haves:
  truths:
    - "When the Report tab is active and files exist, a Total savings section shows a 2×2 stats grid (Before / After / Saved / Files) from $totals"
    - "A per-file bar chart renders one bar per filesAtom.entries item; bars with savings < 30% use warn color, >= 30% use accent"
    - "A Format breakdown section shows one row per unique format with type label, file count, and bytes saved"
    - "When filesAtom.entries is empty the panel shows the empty-state heading 'No files in queue'"
  artifacts:
    - path: "src/components/panels/inspector/ReportPanel.tsx"
      provides: "ReportPanel component: Total savings stats + bar chart + Format breakdown"
      exports: ["ReportPanel"]
  key_links:
    - from: "src/components/panels/inspector/ReportPanel.tsx"
      to: "src/stores/files.ts (filesAtom, $totals)"
      via: "useStore(filesAtom) + useStore($totals)"
      pattern: "useStore\\(filesAtom\\)"
    - from: "src/components/panels/inspector/ReportPanel.tsx"
      to: "src/lib/format.ts (fmtBytes, fmtPct)"
      via: "import"
      pattern: "fmtBytes|fmtPct"
---

<objective>
Build the ReportPanel vertical slice (INSP-08): the inspector Report tab showing a Total savings stats grid, a per-file savings bar chart (warn-colored for low savings), and a Format breakdown list — all derived from `filesAtom.entries` and the `$totals` computed atom.

Purpose: Gives the developer an at-a-glance optimization report across the whole queue. After this plan the report renders from real store data (wired into the visible app in Plan 06-03).
Output: `src/components/panels/inspector/ReportPanel.tsx` and a Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inspector-pane-output-report/06-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/lib/stub-data.ts:
```typescript
export interface FileEntry { id; name; type; orig: number; opt: number; status; target; dim; q; prog? }
// type values present in stub data: 'png' | 'jpg' | 'svg' | 'webp' | 'avif'
```

From src/stores/files.ts:
```typescript
interface FilesState { entries: FileEntry[]; selectedId; filterQuery; sortBy }
export const filesAtom: MapStore<FilesState>
export const $totals: ReadableAtom<{ orig: number; opt: number; saved: number; pct: number }>
```

From src/lib/format.ts:
```typescript
export function fmtBytes(b: number | null | undefined): string   // '412.8 KB' style
export function fmtPct(orig, opt): string                         // '−77.6%' (U+2212) or '' on zero
```

From src/components/panels/inspector/Section.tsx — use verbatim:
```typescript
export function Section({ title, badge, children }): JSX.Element
```

Separator: `import { Separator } from '@/components/ui/separator'`.
Format badge color convention (from src/components/file-row/FileRow.tsx BADGE_CLASS):
  svg=purple, png=blue(250 hue), jpg=orange(60/30 hue), webp=cyan(195 hue), avif=rose(320 hue).
Tooltip (for bar hover): `import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/tooltip'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReportPanel — Total savings stats grid + per-file bar chart</name>
  <files>src/components/panels/inspector/ReportPanel.tsx</files>
  <read_first>
    - src/components/panels/center/DeltaStrip.tsx (existing stat-card grid pattern from Phase 5 — replicate the metric-card structure and token classes)
    - src/components/panels/inspector/Section.tsx (Section props + frozen padding)
    - src/stores/files.ts (filesAtom shape, $totals fields orig/opt/saved/pct)
    - src/lib/format.ts (fmtBytes/fmtPct signatures and U+2212 minus behavior)
    - .planning/phases/06-inspector-pane-output-report/06-UI-SPEC.md (Interaction Contract → ReportPanel; Color table for accent vs warn; Copywriting for stat keys)
  </read_first>
  <behavior>
    - When filesAtom.entries is empty: render empty state heading "No files in queue" + body "Drop images into the queue to see savings data." No sections.
    - When entries exist: render a "Total savings" Section containing a 2×2 stats grid: Before = fmtBytes($totals.orig), After = fmtBytes($totals.opt), Saved = '−' + fmtBytes($totals.saved), Files = String(entries.length). Keys are exactly "Before", "After", "Saved", "Files".
    - Below the grid, a per-file bar chart: one bar per entry. Bar height = savingsPct mapped 0–100% → 0–48px, with a 4px minimum floor. Bar color uses CSS var --color-accent when savingsPct >= 30, --color-warn when < 30. Each bar has a tooltip "{file.name} · {fmtPct(orig,opt)}". Bars are non-interactive.
  </behavior>
  <action>Create src/components/panels/inspector/ReportPanel.tsx exporting `ReportPanel` (named export, no props). Use `const { entries } = useStore(filesAtom)` and `const totals = useStore($totals)` (import filesAtom and $totals from '@/stores/files'; import fmtBytes from '@/lib/format'). When `entries.length === 0`, return the empty state: centered container, heading "No files in queue" (12px), body "Drop images into the queue to see savings data.", with `data-testid="report-empty"`. Otherwise render the panel root with `data-testid="report-panel"`. First `<Section title="Total savings">`: a 2×2 grid (`grid grid-cols-2 gap-3`) of four stat cells; each cell shows a mono uppercase key label (`text-[var(--color-fg-2)]`) and a 14px semibold value: "Before"→fmtBytes(totals.orig), "After"→fmtBytes(totals.opt), "Saved"→`'−' + fmtBytes(totals.saved)` rendered with `text-[var(--color-accent)]`, "Files"→String(entries.length). Replicate the stat-card class structure from DeltaStrip.tsx. After the grid, render the bar chart: a flex row (`flex items-end gap-[3px] h-[52px]`) mapping `entries` to bars; for each entry compute `savingsPct = entry.orig > 0 ? ((entry.orig - entry.opt) / entry.orig) * 100 : 0`, `heightPx = Math.max(4, Math.round((savingsPct / 100) * 48))`. Each bar is a `<div data-testid="report-bar">` styled with width flex-1 and a CSS-var background: `savingsPct < 30 ? 'var(--color-warn)' : 'var(--color-accent)'`. Bar height MUST be applied via inline style for the dynamic px value (this is the documented exception — dynamic per-bar height cannot be a static Tailwind class; mirror how DeltaStrip/CompareStage set dynamic `--split`/width). Wrap each bar in a Tooltip (TooltipTrigger asChild around the bar, TooltipContent text `{entry.name} · {fmtPct(entry.orig, entry.opt)}`) imported from '@/components/ui/tooltip'. Add the phase/plan attribution header comment ("Phase 06, Plan 02 — INSP-08 ReportPanel"). Use Tailwind utility classes + oklch tokens only; the per-bar height/background is the sole inline-style exception for dynamic values. Format-breakdown section is added in Task 2.</action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` completes with no type errors
    - ReportPanel.tsx reads the store (grep -c 'useStore(filesAtom)' returns 1 and grep -c 'useStore($totals)' returns 1)
    - Empty-state heading literal "No files in queue" present in source
    - Stat keys "Before", "After", "Saved", "Files" each appear in source
    - Bar color branch present: source contains both `var(--color-warn)` and `var(--color-accent)` and the threshold `30`
    - grep -c 'data-testid="report-bar"' returns 1 (single mapped element)
  </acceptance_criteria>
  <done>ReportPanel renders empty state, Total savings 2×2 grid from $totals, and a warn/accent per-file bar chart; typechecks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Format breakdown section + Playwright spec</name>
  <files>src/components/panels/inspector/ReportPanel.tsx, src/tests/report-panel.spec.ts</files>
  <read_first>
    - src/components/panels/inspector/ReportPanel.tsx (the file from Task 1 — append the breakdown section)
    - src/components/file-row/FileRow.tsx (BADGE_CLASS map — replicate format→color convention for the row labels)
    - src/components/ui/separator.tsx (Separator API)
    - src/lib/format.ts (fmtBytes for bytes-saved column)
    - src/tests/navigation.spec.ts (Playwright spec conventions)
    - .planning/phases/06-inspector-pane-output-report/06-UI-SPEC.md (Format breakdown row spec + label-color convention)
  </read_first>
  <behavior>
    - A "Format breakdown" Section lists one row per unique `entry.type` present in entries.
    - Each row shows: format type label (uppercased, colored per format convention), file count for that type, and total bytes saved for that type (fmtBytes of sum(orig-opt)).
    - Rows are separated by the Separator component.
    - The Playwright spec asserts the app mounts cleanly and authors report-tab assertions that pass once 06-03 wires the panel.
  </behavior>
  <action>Edit src/components/panels/inspector/ReportPanel.tsx to append a `<Section title="Format breakdown">` after the Total savings section (only in the non-empty branch). Compute a breakdown by grouping `entries` by `entry.type`: for each unique type, derive `{ type, count, saved }` where count is the number of entries of that type and saved is `sum(orig - opt)` over those entries. Render one row per group as `data-testid="format-row"`: a grid (`grid grid-cols-[1fr_auto_auto] gap-2 items-center py-1.5`) with (a) the type label uppercased in a small badge whose color follows the FileRow BADGE_CLASS convention (svg=purple, png=blue, jpg=orange, webp=cyan, avif=rose) — define a local `FORMAT_COLOR: Record<string,string>` of token classes mirroring FileRow; (b) `{count} files` in mono fg-2; (c) `fmtBytes(saved)` in mono semibold. Place a `<Separator />` (import from '@/components/ui/separator') between rows (not after the last). Preserve the header comment. Then create src/tests/report-panel.spec.ts following navigation.spec.ts: at minimum navigate to '/' and assert no console errors on load; additionally author assertions targeting `getByTestId('report-panel')`, that bars exist (`getByTestId('report-bar')` count > 0), and that at least one `getByTestId('format-row')` is visible — with a comment "fully exercised after 06-03 wiring" so they pass once the Report tab is reachable.</action>
  <verify>
    <automated>npx tsc -b && npx playwright test src/tests/report-panel.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` completes with no type errors
    - ReportPanel.tsx contains a Section titled "Format breakdown"
    - grep -c 'data-testid="format-row"' returns 1 (single mapped element)
    - A local FORMAT_COLOR map exists covering keys svg, png, jpg, webp, avif (grep finds all five keys)
    - Source uses Separator between rows (grep -c 'Separator' returns >= 2: import + usage)
    - `npx playwright test src/tests/report-panel.spec.ts` exits 0
  </acceptance_criteria>
  <done>Format breakdown section renders grouped per-format rows with colored labels, counts, and saved bytes, separated by Separator; spec passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store data → rendered chart/labels | filesAtom.entries (orig/opt/type/name) flows into computed stats, bar dimensions, and text labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering/XSS | format labels + tooltips | mitigate | All store strings rendered as auto-escaped React text children; no raw-HTML injection props used |
| T-06-05 | Integer/divide-by-zero | savingsPct + breakdown sums | mitigate | Guard `entry.orig > 0` before computing percentage (mirror $totals guard); Math.max(4, …) floors bar height |
| T-06-06 | Information disclosure | rendered savings report | accept | Derived solely from local stub FileEntry data; no network, zero-server constraint holds |
</threat_model>

<verification>
- `npx tsc -b` clean
- `npx playwright test src/tests/report-panel.spec.ts` passes
- Bar heights computed with a 4px floor; percentages guarded against divide-by-zero
</verification>

<success_criteria>
- ReportPanel renders empty state, Total savings 2×2 grid from $totals, per-file warn/accent bar chart, and grouped Format breakdown rows
- All data flows from filesAtom.entries / $totals — zero direct stub-data imports, zero data useState
- All verification commands exit 0
</success_criteria>

<output>
Create `.planning/phases/06-inspector-pane-output-report/06-02-SUMMARY.md` when done
</output>
