---
phase: 07-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx
  - src/components/shell/AppShell/AppShell.tsx
  - src/tests/backpressure.spec.ts
# Note: AppShell edits are consolidated here (Task 2) to avoid parallel write conflict with 07-02.
autonomous: true
requirements:
  - SHELL-02
must_haves:
  truths:
    - "BackpressureIndicator component exists and exports a named function"
    - "AppShell mounts BackpressureIndicator inside the role=application div"
    - "When runtimeAtom.running=true the indicator is visible (not opacity-0)"
    - "When runtimeAtom.running=false the indicator is hidden (opacity-0)"
    - "Playwright test for SHELL-02 passes — clicking Optimize makes indicator visible"
  artifacts:
    - path: "src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx"
      provides: "BackpressureIndicator component — reads runtimeAtom.running, renders top-edge animated bar"
      exports: ["BackpressureIndicator"]
    - path: "src/components/shell/AppShell/AppShell.tsx"
      provides: "AppShell with BackpressureIndicator mounted, pointer-events-none overlay"
    - path: "src/tests/backpressure.spec.ts"
      provides: "Playwright spec for SHELL-02: indicator visible on run, hidden at rest"
  key_links:
    - from: "src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx"
      to: "src/stores/runtime.ts"
      via: "useStore(runtimeAtom)"
      pattern: "useStore.*runtimeAtom"
    - from: "src/components/shell/AppShell/AppShell.tsx"
      to: "src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx"
      via: "import + JSX mount"
      pattern: "BackpressureIndicator"
---

<objective>
Build BackpressureIndicator and mount it in AppShell so the user sees visual feedback during optimization runs.

Purpose: SHELL-02 requires a visible run indicator. Currently nothing renders when runtimeAtom.running=true. This plan delivers the component and a Playwright test to guard the requirement.
Output: BackpressureIndicator.tsx, updated AppShell.tsx, backpressure.spec.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/shell/AppShell/AppShell.tsx
@src/stores/runtime.ts
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration needed. -->

From src/stores/runtime.ts:
```typescript
interface RuntimeState {
  running: boolean
  progress: number  // 0-100
}
export const runtimeAtom: MapStore<RuntimeState>
export function startRun(): void
export function stopRun(): void
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

AppShell structure (append BackpressureIndicator as last child of the role="application" div, after CommandPalette):
```tsx
<div role="application" ...>
  <TitleBar />
  <Toolbar />
  <ResizablePanelGroup>...</ResizablePanelGroup>
  <StatusBar />
  <CommandPalette />
  <BackpressureIndicator />   {/* ← mount here */}
</div>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BackpressureIndicator component</name>
  <files>src/components/shell/BackpressureIndicator/BackpressureIndicator.tsx</files>
  <behavior>
    - When running=false: root div has class "opacity-0"
    - When running=true: root div does NOT have class "opacity-0"; it has "animate-pulse" and bg-[var(--color-accent)]
    - Root div always has: pointer-events-none, absolute, top-0, left-0, right-0, h-0.5, z-50
    - Root div has role="status", aria-live="polite"
    - When running=true: aria-label="Optimization running"
    - When running=false: no aria-label (or undefined)
  </behavior>
  <action>
    Create the directory src/components/shell/BackpressureIndicator/ and write BackpressureIndicator.tsx.

    Add a phase/plan attribution header comment: "// Phase 07-polish / Plan 01 — SHELL-02: BackpressureIndicator. Source: 07-01-PLAN.md"

    Import useStore from @nanostores/react, runtimeAtom from @/stores/runtime, and cn from @/lib/utils.

    The component reads running from useStore(runtimeAtom). Return a single div with:
    - role="status" aria-live="polite"
    - aria-label set to "Optimization running" when running is true, undefined otherwise
    - className built with cn(): always include "pointer-events-none absolute top-0 left-0 right-0 h-0.5 z-50 transition-opacity"; conditionally "bg-[var(--color-accent)] animate-pulse" when running=true and "opacity-0" when running=false

    No children inside the div. The visual effect is the div itself changing from invisible to a colored animated sliver at the top of the shell.

    Do NOT use inline styles. Do NOT add pointer-events-auto anywhere. Do NOT use setTimeout or manual state for the visibility — it is driven purely by runtimeAtom.running.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>BackpressureIndicator.tsx exists, TypeScript check passes, component conditionally applies opacity-0 vs animate-pulse based on runtimeAtom.running</done>
</task>

<task type="auto">
  <name>Task 2: Mount BackpressureIndicator in AppShell + wire data-theme attribute</name>
  <files>src/components/shell/AppShell/AppShell.tsx</files>
  <action>
    Edit AppShell.tsx — two changes in one commit to avoid parallel write conflict with 07-02:

    1. Add import: import { BackpressureIndicator } from '@/components/shell/BackpressureIndicator/BackpressureIndicator'
    2. Inside the role="application" div, append &lt;BackpressureIndicator /&gt; as the last child, after &lt;CommandPalette /&gt;.
    3. Extend the existing theme useEffect to also set data-theme attribute. The effect currently reads:
         document.documentElement.classList.toggle('dark', theme === 'dark')
       Add a second line inside the same effect:
         document.documentElement.setAttribute('data-theme', theme)

    Update the attribution comment at top of file to include "Phase 07-polish — SHELL-02 + data-theme. Source: 07-01-PLAN.md".

    Do NOT change any other part of AppShell (no layout, no FOUC logic, no key bindings).
    The FOUC inline script in index.html is handled by plan 07-02 Task 1 — do NOT add any localStorage reads here.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AppShell.tsx imports and renders BackpressureIndicator; TypeScript check passes</done>
</task>

<task type="auto">
  <name>Task 3: Playwright test for SHELL-02</name>
  <files>src/tests/backpressure.spec.ts</files>
  <action>
    Create src/tests/backpressure.spec.ts with a Playwright spec covering SHELL-02.

    Add a phase/plan attribution header comment: "// Phase 07-polish / Plan 01 — SHELL-02 spec. Source: 07-01-PLAN.md"

    Test structure (two tests in one describe block "BackpressureIndicator — SHELL-02"):

    Test 1 "is hidden on initial load":
    - Navigate to http://localhost:5173 (or use baseURL from playwright config)
    - Find the element with role="status" and aria-label="Optimization running" — use locator('[role="status"]')
    - Assert it has CSS class "opacity-0" via expect(locator).toHaveClass(/opacity-0/)

    Test 2 "becomes visible when Optimize is clicked":
    - Navigate to app
    - Click the button that calls startRun — find it by text "Optimize" or by data-testid if present; fall back to getByText('Optimize').first()
    - Assert the [role="status"] element does NOT have class "opacity-0" via expect(locator).not.toHaveClass(/opacity-0/)

    Import from @playwright/test. Use page.goto('/') with the baseURL configured in playwright.config.ts (check that config first to confirm baseURL setting; if not set, use http://localhost:5173).

    Note: startRun sets running=true but does not automatically set it back to false (stopRun is manual). This means after clicking Optimize the indicator stays visible — the test can rely on that without needing to mock timers.
  </action>
  <verify>
    <automated>npx playwright test src/tests/backpressure.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>backpressure.spec.ts exists; Playwright test reports at least 1 pass for "is hidden on initial load" (the visible-after-click test requires the dev server running)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtimeAtom state | Store is client-side only, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | BackpressureIndicator animate-pulse | accept | CSS animation is GPU-composited; no JS timer; negligible perf cost |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx tsc -b --noEmit passes with no errors
- BackpressureIndicator.tsx exports named function BackpressureIndicator
- AppShell.tsx contains BackpressureIndicator import and JSX usage
- backpressure.spec.ts exists and Playwright can parse it
</verification>

<success_criteria>
SHELL-02 satisfied: BackpressureIndicator is mounted in AppShell, reads runtimeAtom.running, and is visually present (not opacity-0) when running=true. Playwright spec guards this behavior.
</success_criteria>

<output>
Create `.planning/phases/07-polish/07-01-SUMMARY.md` when done
</output>
