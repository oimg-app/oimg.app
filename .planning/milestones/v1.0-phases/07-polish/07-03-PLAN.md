---
phase: 07-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified: []
autonomous: false
requirements:
  - SHELL-02
must_haves:
  truths:
    - "BackpressureIndicator is visible (not opacity-0) when runtimeAtom.running=true"
    - "Theme toggle switches between dark and light with no visible white flash on hard reload"
    - "Tabbing through the UI produces visible focus rings on all interactive elements"
    - "No component file imports types directly from @/lib/stub-data"
    - "No cross-store ESM imports detected by grep audit"
    - "npx tsc -b --noEmit passes with no errors"
  artifacts:
    - path: "src/tests/backpressure.spec.ts"
      provides: "Playwright spec passing for SHELL-02"
  key_links:
    - from: "Playwright backpressure.spec.ts"
      to: "BackpressureIndicator role=status"
      via: "page.locator"
      pattern: "role.*status"
---

<objective>
Final integration checkpoint and audit pass for Phase 7. Verifies all automated work from plans 07-01 and 07-02 functions correctly end-to-end through both automated checks and human visual inspection.

Purpose: Plans 07-01 and 07-02 are autonomous. This wave-2 plan collects the results, runs the full audit (circular ESM grep, STORE-08 grep, TypeScript build, Playwright), and asks the developer to visually confirm BackpressureIndicator visibility, focus rings, and no-FOUC theme toggle.
Output: Human sign-off + phase completion
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish/07-01-SUMMARY.md
@.planning/phases/07-polish/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated audit — TypeScript, circular ESM, STORE-08, Playwright</name>
  <files></files>
  <action>
    Run all automated checks in sequence. Report any failures before proceeding to the human-verify checkpoint.

    Check 1 — TypeScript build:
    ```
    npx tsc -b --noEmit
    ```
    Expected: exits 0, no output. If errors appear, fix them before proceeding.

    Check 2 — Circular ESM guard (per project convention: ui.ts must NOT import from files.ts, runtime.ts, or settings.ts):
    ```
    grep -n "from.*stores/files\|from.*stores/runtime\|from.*stores/settings" src/stores/ui.ts
    ```
    Expected: no matches (empty output). If matches appear, they are violations — fix before proceeding.

    Check 3 — STORE-08 type import audit (no component should import types directly from stub-data):
    ```
    grep -rn "from '@/lib/stub-data'\|from \"@/lib/stub-data\"" src/components/
    ```
    Expected: no matches. If matches appear (excluding type-only acceptable cases), fix before proceeding.

    Check 4 — STORE-08 useState audit (no non-ephemeral useState in components):
    ```
    grep -rn "useState" src/components/ | grep -v "// ephemeral\|copied\|open\|hover\|focus"
    ```
    Review any matches. Ephemeral UI state (popover open/close, "Copied!" feedback) is permitted per STORE-08. Data-holding useState is a violation.

    Check 5 — Playwright SHELL-02:
    ```
    npx playwright test src/tests/backpressure.spec.ts --reporter=line
    ```
    Expected: all tests pass. If dev server is not running, start it first with npm run dev.

    Report the results of all five checks as a numbered list before the human-verify checkpoint proceeds.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>&1 | head -5 && grep -rn "from '@/lib/stub-data'" src/components/ 2>&1 | head -10</automated>
  </verify>
  <done>All five automated checks pass: tsc clean, no circular ESM, no stub-data component imports, no non-ephemeral useState, Playwright SHELL-02 green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 07-01 and 07-02 delivered:
    1. BackpressureIndicator component mounted in AppShell — animated top-edge bar visible when runtimeAtom.running=true
    2. FOUC fix — inline script in index.html sets html.dark class before React mounts
    3. data-theme attribute wired in AppShell useEffect
    4. Global :focus-visible CSS rule added to index.css
    5. Type imports redirected from @/lib/stub-data to @/stores/files in FileRow.tsx and FilesPane.tsx
  </what-built>
  <how-to-verify>
    Run npm run dev and open http://localhost:5173.

    **BackpressureIndicator (SHELL-02):**
    1. Open browser DevTools → Console
    2. Click "Optimize all" button in the Toolbar
    3. Look for a thin green animated bar at the very top of the shell (above TitleBar, below the browser chrome) — it should appear immediately
    4. In DevTools, inspect the element with role="status" — confirm it has class "animate-pulse" and does NOT have class "opacity-0"

    **Theme toggle (no FOUC):**
    5. If in dark mode, click the theme toggle (Sun icon in Toolbar or View menu → "Light theme")
    6. Confirm all panels switch to light theme — background turns light, text turns dark, no components remain in dark style
    7. Hard-reload the page (Cmd+Shift+R / Ctrl+Shift+R) — confirm there is NO white flash before dark theme renders (page should render dark immediately)
    8. In DevTools → Elements, confirm &lt;html&gt; has both class="dark" and data-theme="dark"

    **Focus rings (WCAG AA):**
    9. Click somewhere neutral (e.g., empty space in FilesPane)
    10. Press Tab repeatedly — each focused interactive element (buttons, inputs, sliders) should show a visible green outline ring
    11. Confirm TitleBar menu buttons, Toolbar buttons (Add, Optimize, Export), and file rows all show focus rings

    **STORE-08 visual sanity:**
    12. Confirm the app renders correctly — no blank panels, no console errors about missing imports
  </how-to-verify>
  <verify>
    <human-check>Visually confirm BackpressureIndicator appears, theme toggle works without FOUC, focus rings are visible on Tab, no console errors</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan runs only audit commands and manual verification — no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
All checks from Task 1 must pass before human checkpoint proceeds:
- npx tsc -b exits 0
- grep finds no circular ESM imports in ui.ts
- grep finds no stub-data imports in src/components/
- Playwright backpressure.spec.ts passes
- Human visual confirmation of all four areas
</verification>

<success_criteria>
Phase 7 complete when:
1. All automated checks in Task 1 pass (tsc, ESM grep, STORE-08 grep, Playwright)
2. Developer confirms BackpressureIndicator visible, theme toggle smooth, focus rings present
3. No regressions in any prior phase features
</success_criteria>

<output>
Create `.planning/phases/07-polish/07-03-SUMMARY.md` when done
</output>
