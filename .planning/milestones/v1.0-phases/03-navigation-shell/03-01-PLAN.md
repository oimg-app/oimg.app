---
phase: 03-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/ui.ts
  - src/stores/runtime.ts
  - src/stores/index.ts
  - src/components/shell/AppShell/AppShell.tsx
  - src/components/shell/Toolbar/Toolbar.tsx
  - src/components/shell/StatusBar/StatusBar.tsx
  - src/tests/navigation.spec.ts
  - src/tests/stores.test.ts
autonomous: true
requirements:
  - STORE-03
  - STORE-04
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - Clicking the Toolbar "Optimize all" button sets runtimeAtom.running to true
    - StatusBar worker pip changes from idle (accent) to running (info) when runtimeAtom.running is true
    - All 10 ui.ts @TODO Phase 3 stubs now write to uiAtom via setKey
    - runtimeAtom exists with running and toasts state, plus startRun, stopRun, pushToast, dismissToast actions
  artifacts:
    - path: src/stores/runtime.ts
      provides: runtimeAtom + startRun/stopRun/pushToast/dismissToast
      contains: "export const runtimeAtom"
    - path: src/components/shell/Toolbar/Toolbar.tsx
      provides: Toolbar with Optimize-all primary button (minimal slice)
      contains: "Optimize all"
    - path: src/components/shell/StatusBar/StatusBar.tsx
      provides: StatusBar with worker pip + Idle/Running label
      contains: "data-testid=\"statusbar\""
  key_links:
    - from: Toolbar Optimize button onClick
      to: runtimeAtom
      via: startRun import from @/stores/runtime
      pattern: "onClick=\\{startRun\\}"
    - from: StatusBar
      to: runtimeAtom.running
      via: useStore(runtimeAtom)
      pattern: "useStore\\(runtimeAtom\\)"
    - from: AppShell
      to: Toolbar, StatusBar
      via: JSX mount
      pattern: "<Toolbar"
---

## Phase Goal

**As a** developer running the OIMG UI port, **I want to** see a Toolbar with a primary "Optimize all" button that flips a global runtime state which the StatusBar pip reflects, **so that** the end-to-end store-to-chrome wiring for Phase 3 is proven before adding more breadth.

<objective>
Ship the thinnest end-to-end Phase 3 slice: fill the ui.ts action stubs, create runtimeAtom, and mount a minimal Toolbar+StatusBar pair in AppShell so that clicking "Optimize all" toggles a visible worker-pip color change.

Purpose: Unblock Plan 02 (TitleBar + full Toolbar/StatusBar) and Plan 03 (CommandPalette + theme + commands) with all underlying store actions and the runtime store in place, while still delivering a user-visible capability after Wave 1.

Output: src/stores/runtime.ts (new), filled-in ui.ts action bodies, src/stores/index.ts re-export, minimal Toolbar.tsx + StatusBar.tsx mounted in AppShell, Wave-0 test scaffolds (navigation.spec.ts + stores.test.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-shell/03-RESEARCH.md
@.planning/phases/03-navigation-shell/03-PATTERNS.md
@.planning/phases/03-navigation-shell/03-UI-SPEC.md
@.planning/phases/03-navigation-shell/03-VALIDATION.md
@src/stores/ui.ts
@src/stores/files.ts
@src/stores/index.ts
@src/components/shell/AppShell/AppShell.tsx
@src/components/panels/FilesPane.tsx
@src/lib/format.ts
@src/tests/foundation.spec.ts

<interfaces>
<!-- Existing exports the executor MUST use, not redefine. -->

From src/stores/ui.ts (existing — DO NOT change shape):
- export type View = 'Batch' | 'Compare' | 'Report'
- export type Tab = 'codec' | 'svgo' | 'output' | 'report'
- export const uiAtom: map<UiState> with keys: open, view, tab, split, zoom, cmdkOpen, cmdkQ, cmdkSel, rowMenu, theme
- Existing real action: setRowMenu(id) — already uses uiAtom.setKey('rowMenu', id)
- Stubs to fill (currently no-ops): setOpen, setView, setTab, setSplit, setZoom, openCmdk, closeCmdk, setCmdkQuery, setCmdkSel, setTheme

From src/stores/files.ts (existing — pattern reference for new runtime.ts):
- export const filesAtom = map<FilesState>({...})
- export const $totals = computed(filesAtom, (s) => ({ orig, opt, saved, pct }))
- Action pattern: export function selectFile(id) { filesAtom.setKey('selectedId', id) }

From src/components/shell/AppShell/AppShell.tsx (existing — minimal modification):
- Currently renders only ResizablePanelGroup inside a div with className="dark ..."
- Plan 01 must preserve className="dark" for now (theme effect moves to Plan 03 SHELL-03)

From src/lib/format.ts (existing):
- export function fmtBytes(b: number): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave-0 test scaffolds for navigation + stores</name>
  <files>src/tests/navigation.spec.ts, src/tests/stores.test.ts</files>
  <read_first>
    - src/tests/foundation.spec.ts (Playwright shape and selectors used)
    - src/tests/stub-data.test.ts (Node strip-types unit pattern with assert + process.exit)
    - src/stores/files.ts (filesAtom shape used by future assertions)
    - .planning/phases/03-navigation-shell/03-VALIDATION.md (sampling rate, automated commands)
  </read_first>
  <action>
    Create two test files matching existing Phase 1 patterns (no new test framework).

    File 1 — `src/tests/navigation.spec.ts` (Playwright smoke, mirrors `foundation.spec.ts` import + `test(name, async ({ page }))` shape):
    - Test "Toolbar mounts (NAV-02 slice)" — `page.goto('/')`, expect `page.getByTestId('toolbar')` visible, expect `page.getByRole('button', { name: 'Optimize all' })` visible.
    - Test "StatusBar mounts with worker pip (NAV-03 slice)" — expect `page.getByTestId('statusbar')` visible, expect `page.getByTestId('worker-pip')` visible, expect aria-label of the pip to start with "Worker status: Idle".
    - Test "Clicking Optimize all flips worker pip to Running (NAV-02 wire)" — click the Optimize all button, expect the pip aria-label to be "Worker status: Running".
    - File header comment: `// Phase 03 Plan 01 — Wave 0 Playwright smoke: Toolbar Optimize + StatusBar pip slice.`

    File 2 — `src/tests/stores.test.ts` (Node strip-types unit, mirrors `stub-data.test.ts` assert/passed/failed pattern, NOT vitest — the project's bundle test runner is `node --experimental-strip-types`):
    - Dynamic import `../stores/ui.ts`; reset uiAtom via `mod.uiAtom.set({...full default state})` before each assertion block.
    - Assert STORE-03 actions: setOpen('menu-codec') → uiAtom.get().open === 'menu-codec'; openCmdk() → cmdkOpen === true AND cmdkQ === '' AND cmdkSel === 0; closeCmdk() → cmdkOpen === false; setTheme('light') → theme === 'light'; setView('Compare') → view === 'Compare'; setTab('svgo') → tab === 'svgo'; setSplit(33) → split === 33; setZoom(150) → zoom === 150; setCmdkQuery('img') → cmdkQ === 'img'; setCmdkSel(2) → cmdkSel === 2.
    - Dynamic import `../stores/runtime.ts`; assert STORE-04: startRun() → running === true; stopRun() → running === false; pushToast('hi','meta') → toasts.length === 1 AND toasts[0].msg === 'hi'; dismissToast(id) → toasts.length === 0.
    - Use the Wave-0 ERR_MODULE_NOT_FOUND tolerance pattern from `stub-data.test.ts` only if a module is genuinely missing; once Tasks 1+2 ship, these assertions must pass for real.
    - Add npm script entry expectation only in the file header comment; do NOT modify package.json in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --experimental-strip-types src/tests/stores.test.ts; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/navigation.spec.ts` exists and contains the strings: getByTestId('toolbar'), `Optimize all`, and `Worker status: Running`.
    - File `src/tests/stores.test.ts` exists and contains references to `setOpen`, `openCmdk`, `setTheme`, `startRun`, `pushToast`.
    - `grep -c "Wave 0" src/tests/stores.test.ts` returns at least 1 (header comment).
    - `npx tsc --noEmit` exits 0 with the new test files in place.
  </acceptance_criteria>
  <done>Both test files exist, compile under tsc, and will start passing once Tasks 1 and 2 land. Pre-Task-1 stub failures are acceptable (Wave 0 expectation).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Fill ui.ts action stubs + create runtime.ts + barrel re-export</name>
  <files>src/stores/ui.ts, src/stores/runtime.ts, src/stores/index.ts</files>
  <read_first>
    - src/stores/ui.ts (current stubs to replace — preserve setRowMenu and uiAtom shape exactly)
    - src/stores/files.ts (action body pattern: setKey on map; pushToast-style array immutability for runtime toasts)
    - .planning/phases/03-navigation-shell/03-PATTERNS.md (lines 29-69 ui.ts action bodies; lines 92-120 runtime.ts pattern)
    - .planning/phases/03-navigation-shell/03-RESEARCH.md (Code Examples → runtimeAtom for exact Toast interface)
  </read_first>
  <behavior>
    - Test 1: setOpen('menu-codec') mutates uiAtom.open to 'menu-codec'; setOpen(null) returns it to null.
    - Test 2: openCmdk() sets cmdkOpen=true, cmdkQ='', cmdkSel=0 in one tick.
    - Test 3: closeCmdk() sets cmdkOpen=false but does NOT clear cmdkQ.
    - Test 4: setTheme('light') and setTheme('dark') round-trip through uiAtom.theme.
    - Test 5: runtimeAtom default state is { running: false, toasts: [] }.
    - Test 6: startRun() → running=true; stopRun() → running=false.
    - Test 7: pushToast('msg','meta') appends Toast with unique string id, msg, meta to toasts array (immutable copy).
    - Test 8: dismissToast(id) removes the toast with matching id; non-matching ids are no-ops.
    - Test 9: stores/index.ts re-exports runtimeAtom and at least startRun.
  </behavior>
  <action>
    Modify `src/stores/ui.ts`:
    - Replace each `/* @TODO Phase 3 */` no-op body with a real `uiAtom.setKey(...)` call following the exact pattern in PATTERNS.md lines 37-69. Preserve every existing type, function name, parameter name, and the existing `setRowMenu` function. Do NOT add `$cmdFlat`, `registerCommands`, or any computed atom in this task — those land in Plan 03.
    - `openCmdk` must call setKey three times: cmdkOpen=true, cmdkQ='', cmdkSel=0 (per PATTERNS.md and RESEARCH.md Pattern 1).
    - `closeCmdk` must only set cmdkOpen=false (do not touch cmdkQ / cmdkSel — they should persist if the user reopens).
    - Update the file header comment from "Phase 02 — STORE-03: ... with Phase 3 action stubs" to "Phase 03 — STORE-03 (action bodies filled). Source: 03-01-PLAN.md".
    - Keep the CIRCULAR ESM GUARD comment as-is.

    Create `src/stores/runtime.ts`:
    - File header comment: `// Phase 03 — STORE-04: runtimeAtom + startRun/stopRun/pushToast/dismissToast. Source: 03-01-PLAN.md`.
    - Import `map` from 'nanostores'.
    - Export interface Toast { id: string; msg: string; meta?: string }.
    - Local interface RuntimeState { running: boolean; toasts: Toast[] }.
    - export const runtimeAtom = map<RuntimeState>({ running: false, toasts: [] }).
    - export function startRun(): void → runtimeAtom.setKey('running', true).
    - export function stopRun(): void → runtimeAtom.setKey('running', false).
    - export function pushToast(msg: string, meta?: string): void → generate id as `String(Date.now() + Math.random())`, append new Toast immutably via spread (do NOT mutate the existing array).
    - export function dismissToast(id: string): void → filter out matching id immutably.

    Modify `src/stores/index.ts`:
    - Append `export * from './runtime'` after the existing two lines. Do not remove the existing exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --experimental-strip-types src/tests/stores.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@TODO Phase 3" src/stores/ui.ts` returns 0 (all stubs removed).
    - `grep -c "uiAtom.setKey" src/stores/ui.ts` returns at least 11 (10 new + existing setRowMenu).
    - `grep -c "export const runtimeAtom" src/stores/runtime.ts` returns 1.
    - `grep -c "export function startRun" src/stores/runtime.ts` returns 1.
    - `grep -c "export function pushToast" src/stores/runtime.ts` returns 1.
    - `grep -v '^[[:space:]]*//' src/stores/index.ts | grep -c "from './runtime'" ` returns 1.
    - `node --experimental-strip-types src/tests/stores.test.ts` exits 0 with all assertions passing (no Wave-0 module-not-found path taken).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All ui.ts actions write to uiAtom; runtimeAtom and its 4 actions exist; barrel re-exports runtime; tests pass for real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimal Toolbar + StatusBar slice mounted in AppShell</name>
  <files>src/components/shell/Toolbar/Toolbar.tsx, src/components/shell/StatusBar/StatusBar.tsx, src/components/shell/AppShell/AppShell.tsx</files>
  <read_first>
    - src/components/shell/AppShell/AppShell.tsx (current layout — preserve className="dark", 3-pane ResizablePanelGroup; only add chrome rows)
    - src/components/panels/FilesPane.tsx (useStore + cn + token-var className pattern to copy)
    - src/stores/runtime.ts (just created — import startRun and runtimeAtom)
    - src/stores/files.ts (import $totals)
    - src/lib/format.ts (fmtBytes signature)
    - .planning/phases/03-navigation-shell/03-UI-SPEC.md (Toolbar primary button + StatusBar worker pip specs)
    - .planning/phases/03-navigation-shell/03-PATTERNS.md (Toolbar primary button, StatusBar worker pip, chrome heights)
  </read_first>
  <behavior>
    - Test 1: Rendering AppShell mounts a Toolbar element with data-testid="toolbar" above the ResizablePanelGroup and a StatusBar element with data-testid="statusbar" below it.
    - Test 2: Toolbar renders a button with accessible name "Optimize all" using --color-accent background and --color-accent-fg text.
    - Test 3: StatusBar renders a worker pip element with data-testid="worker-pip" and aria-label "Worker status: Idle" when runtimeAtom.running is false.
    - Test 4: Clicking the Optimize all button updates runtimeAtom.running to true; on next render the worker-pip aria-label becomes "Worker status: Running" and the pip className contains `bg-[var(--color-info)]`.
    - Test 5: StatusBar shows the literal text "Idle" or "Running" matching the running state.
    - Test 6: Layout grid order top-to-bottom: Toolbar (h-11), ResizablePanelGroup (flex-1 min-h-0), StatusBar (h-[22px]).
  </behavior>
  <action>
    Create `src/components/shell/Toolbar/Toolbar.tsx`:
    - File header: `// Phase 03 — NAV-02 (minimal slice: Optimize all primary button only). Source: 03-01-PLAN.md`.
    - Import `Lightning` from '@phosphor-icons/react'; `startRun` from '@/stores/runtime'.
    - Export named function `Toolbar()` returning a `<div data-testid="toolbar" role="toolbar" aria-label="Primary toolbar">` with classes `h-11 bg-[var(--color-bg-1)] border-b border-[var(--color-line)] px-2 flex items-center gap-2 shrink-0`.
    - Inside, render exactly one `<button type="button" onClick={startRun}>` with accessible name "Optimize all", icon `<Lightning size={13} />`, and classes `h-7 px-3 text-xs font-semibold bg-[var(--color-accent)] text-[var(--color-accent-fg)] border-transparent rounded-[5px] hover:brightness-105 flex items-center gap-1`.
    - Do NOT add split-buttons, segmented control, filter input, theme toggle, or settings popover in this task — those land in Plan 02.

    Create `src/components/shell/StatusBar/StatusBar.tsx`:
    - File header: `// Phase 03 — NAV-03 (minimal slice: worker pip + Idle/Running label). Source: 03-01-PLAN.md`.
    - Import `useStore` from '@nanostores/react'; `runtimeAtom` from '@/stores/runtime'; `cn` from '@/lib/utils'.
    - Export named function `StatusBar()`. Read `const { running } = useStore(runtimeAtom)`.
    - Outer `<div data-testid="statusbar" role="status" aria-live="polite">` with classes `h-[22px] bg-[var(--color-bg-1)] border-t border-[var(--color-line)] px-3 flex items-center gap-3 text-[11px] text-[var(--color-fg-2)] shrink-0`.
    - Inside, render a pip span: tag `<span>` with `data-testid="worker-pip"`, dynamic `aria-label={`Worker status: ${running ? 'Running' : 'Idle'}`}`, and `className={cn('w-2 h-2 rounded-full inline-block', running ? 'bg-[var(--color-info)] motion-safe:animate-pulse' : 'bg-[var(--color-accent)]')}` — followed by a sibling `<span>{running ? 'Running' : 'Idle'}</span>`.
    - Do NOT add SVGO version, codec version, WASM string, file count, or size summary in this task — those land in Plan 02.

    Modify `src/components/shell/AppShell/AppShell.tsx`:
    - Add imports: `Toolbar` from '@/components/shell/Toolbar/Toolbar'; `StatusBar` from '@/components/shell/StatusBar/StatusBar'.
    - Inside the existing outer `<div role="application" ...>`, wrap children so layout is, top-to-bottom: `<Toolbar />`, then the existing `<ResizablePanelGroup ...>` block (unchanged), then `<StatusBar />`.
    - Do NOT touch the `className="dark ..."` string in this task. Do NOT add a `data-theme` effect, keydown listener, TitleBar, or CommandPalette — those land in Plans 02 and 03.
    - Do NOT remove the existing `flex flex-col` on the outer div (required so Toolbar/Panel/StatusBar stack vertically).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="toolbar"' src/components/shell/Toolbar/Toolbar.tsx` returns 1.
    - `grep -c "Optimize all" src/components/shell/Toolbar/Toolbar.tsx` returns 1.
    - `grep -c "onClick={startRun}" src/components/shell/Toolbar/Toolbar.tsx` returns 1.
    - `grep -c 'data-testid="statusbar"' src/components/shell/StatusBar/StatusBar.tsx` returns 1.
    - `grep -c 'data-testid="worker-pip"' src/components/shell/StatusBar/StatusBar.tsx` returns 1.
    - `grep -c "Worker status" src/components/shell/StatusBar/StatusBar.tsx` returns at least 1.
    - `grep -c "<Toolbar" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c "<StatusBar" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c 'className="dark' src/components/shell/AppShell/AppShell.tsx` returns 1 (still present — theme effect deferred to Plan 03).
    - `npm run build` exits 0.
    - `npx playwright test src/tests/navigation.spec.ts --project=chromium` passes the three Plan-01 tests (Toolbar mounts, StatusBar mounts, Optimize click flips pip).
  </acceptance_criteria>
  <done>AppShell renders Toolbar above and StatusBar below the existing 3-pane group; clicking "Optimize all" flips runtimeAtom.running and the worker pip changes color + aria-label live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard/mouse → React event handlers → store actions | All Phase-03 actions are pure store mutations on in-memory nanostores; no network, no eval, no raw HTML injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Toolbar.tsx (button label rendering) | mitigate | Label "Optimize all" is a static string literal in JSX (text node), not interpolated from any store or external input. No raw-HTML React escape hatches anywhere in this plan (grep guard: `grep -rc "dangerouslySet" src/components/shell/ \|\| true` must be 0). |
| T-03-02 | Tampering | StatusBar.tsx (aria-label interpolation) | mitigate | The only interpolated value is the boolean `running` rendered via template literal in aria-label and as text "Idle"/"Running" — both go through React text node escaping. No external string ever reaches this surface. |
| T-03-03 | Information Disclosure | runtime.ts (toast messages) | accept | Toast `msg` and `meta` strings are passed by in-app callers only (no remote source in v1). pushToast is invoked from app-controlled handlers; messages do not include secrets. Re-evaluate in v2 when real worker output feeds toasts. |
| T-03-04 | Denial of Service | runtimeAtom.toasts unbounded growth | accept | UI milestone has no automatic toast pruning; manual `dismissToast` only. Risk is low — only user actions enqueue toasts in this milestone. Mitigation deferred to Polish phase (cap at 5 visible). |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` exits 0.
- `node --experimental-strip-types src/tests/stores.test.ts` exits 0 with all 14 store assertions passing.
- `npx playwright test src/tests/navigation.spec.ts --project=chromium` passes 3/3 Plan-01 smoke tests.
- Manual sanity: `npm run dev`, click "Optimize all" — worker pip visibly switches from green (accent) to blue (info) with motion-safe pulse.
</verification>

<success_criteria>
- ui.ts has zero `@TODO Phase 3` strings; all 10 actions write to uiAtom.
- runtime.ts exports runtimeAtom + 4 actions; barrel re-exports it.
- Toolbar (minimal) + StatusBar (minimal) mount in AppShell in the correct vertical order.
- Clicking "Optimize all" flips runtimeAtom.running AND the StatusBar pip reacts.
- Wave-0 test files exist and the store unit tests pass.
- No file in this plan touches: TitleBar, CommandPalette, commands.ts, $cmdFlat, registerCommands, data-theme effect, or main.tsx (those land in Plan 02/03).
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-shell/03-01-SUMMARY.md` recording: ui.ts actions filled (10), runtime.ts created with 4 actions, Toolbar+StatusBar minimal slice mounted, store unit tests passing, Playwright slice tests passing, AppShell.tsx still uses hardcoded `className="dark"` (theme effect deferred to Plan 03).
</output>
