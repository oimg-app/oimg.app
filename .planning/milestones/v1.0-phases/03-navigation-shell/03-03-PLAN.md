---
phase: 03-navigation-shell
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/stores/ui.ts
  - src/lib/commands.ts
  - src/components/shell/CommandPalette/CommandPalette.tsx
  - src/components/shell/AppShell/AppShell.tsx
  - src/main.tsx
  - src/tests/navigation.spec.ts
  - src/tests/stores.test.ts
autonomous: false
requirements:
  - STORE-03
  - STORE-07
  - SHELL-03
  - NAV-04
must_haves:
  truths:
    - Pressing ⌘K (or Ctrl+K) anywhere opens the CommandPalette modal (uiAtom.cmdkOpen becomes true)
    - Typing in the palette filters the visible command list via $cmdFlat
    - Pressing ↑/↓ moves the selected command (uiAtom.cmdkSel changes); Enter fires the selected command's do() and closes
    - Pressing Escape closes the palette
    - <html data-theme> attribute reflects uiAtom.theme; toggling theme (via TitleBar View menu or Toolbar Sun/Moon button) updates the attribute live
    - ALL_COMMANDS is registered into ui.ts at app start via registerCommands(ALL_COMMANDS.flatMap(g => g.items))
    - AppShell removes the hardcoded className="dark" (since data-theme now drives styling) AND still passes the existing foundation.spec.ts "dark" check by conditionally toggling the class from uiAtom.theme
  artifacts:
    - path: src/lib/commands.ts
      provides: ALL_COMMANDS registry (Actions / View / Palette groups)
      contains: "export const ALL_COMMANDS"
    - path: src/components/shell/CommandPalette/CommandPalette.tsx
      provides: Modal palette with search input, listbox, footer
      contains: "data-testid=\"command-palette\""
    - path: src/main.tsx
      provides: registerCommands wiring at boot
      contains: "registerCommands"
  key_links:
    - from: AppShell useEffect keydown handler
      to: openCmdk
      via: window keydown listener (Meta+K / Ctrl+K) with cleanup
      pattern: "window.addEventListener\\('keydown'"
    - from: AppShell useEffect theme handler
      to: document.documentElement.dataset.theme
      via: subscribe to uiAtom.theme
      pattern: "documentElement.dataset.theme"
    - from: CommandPalette
      to: $cmdFlat
      via: useStore($cmdFlat)
      pattern: "\\$cmdFlat"
    - from: main.tsx
      to: registerCommands
      via: import + call before render
      pattern: "registerCommands\\("
---

## Phase Goal

**As a** developer running the OIMG UI port, **I want to** press ⌘K to open a searchable command palette and toggle themes from the TitleBar that immediately repaint the chrome, **so that** the navigation phase ships with the full keyboard-first + theme-switching experience promised by NAV-04 and SHELL-03.

<objective>
Close the remaining Phase 3 surface area: (1) add `$cmdFlat` computed + `registerCommands` injection in ui.ts (STORE-03 completion); (2) create `src/lib/commands.ts` registry (STORE-07); (3) build the CommandPalette modal (NAV-04); (4) wire `<html data-theme>` and a global ⌘K/Escape keydown listener in AppShell (SHELL-03); (5) call registerCommands from main.tsx at boot; (6) human-verify checkpoint covering all five Phase 3 success criteria.

Purpose: Final vertical slice for Phase 3 — adds the cmdk surface and theme switching, both end-to-end user-visible capabilities that depend on Plans 01 and 02.

Output: New CommandPalette.tsx + commands.ts; extended ui.ts ($cmdFlat + registerCommands); modified AppShell.tsx (theme effect, keydown listener, palette mount, dark-class swap); modified main.tsx (registerCommands call); extended tests; final human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-navigation-shell/03-RESEARCH.md
@.planning/phases/03-navigation-shell/03-PATTERNS.md
@.planning/phases/03-navigation-shell/03-UI-SPEC.md
@.planning/phases/03-navigation-shell/03-VALIDATION.md
@.planning/phases/03-navigation-shell/03-01-PLAN.md
@.planning/phases/03-navigation-shell/03-02-PLAN.md
@src/stores/ui.ts
@src/stores/runtime.ts
@src/components/shell/AppShell/AppShell.tsx
@src/components/shell/TitleBar/TitleBar.tsx
@src/components/shell/Toolbar/Toolbar.tsx
@src/components/ui/dialog.tsx
@src/components/ui/kbd.tsx
@src/main.tsx
@src/index.css
@src/tests/foundation.spec.ts

<interfaces>
<!-- Already shipped in Plans 01 and 02; consume directly. -->

From src/stores/ui.ts (Plan 01 filled actions; Plan 03 adds the two below):
- setOpen, setView, setTheme, openCmdk, closeCmdk, setCmdkQuery, setCmdkSel — already implemented
- uiAtom (read cmdkOpen, cmdkQ, cmdkSel, theme)
- NEW exports this plan adds:
  - export function registerCommands(cmds: CommandItem[]): void
  - export const $cmdFlat: ReadableAtom<CommandItem[]>

From src/stores/runtime.ts:
- startRun (referenced by ALL_COMMANDS)

From src/components/ui/dialog.tsx (existing shadcn):
- Dialog, DialogContent — controlled via `open` + `onOpenChange` props
- DialogContent accepts `showCloseButton`, `className`

From src/components/shell/AppShell/AppShell.tsx (Plans 01+02 already mounted Toolbar/StatusBar/TitleBar):
- Currently uses hardcoded `className="dark"` — this plan removes the static value and replaces with a dynamic one driven by uiAtom.theme

CSS reality check (from src/index.css):
- Theme variables today are driven by `:root` (light) and `.dark { ... }` selectors
- foundation.spec.ts asserts `[role="application"]` has class "dark" by default
- Plan must therefore: (a) set `document.documentElement.dataset.theme = uiAtom.theme` for SHELL-03 attribute contract, AND (b) keep the `.dark` class on the application div when theme === 'dark' so existing CSS and the foundation.spec.ts pass
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ui.ts $cmdFlat + registerCommands; src/lib/commands.ts registry; main.tsx wire-up</name>
  <files>src/stores/ui.ts, src/lib/commands.ts, src/main.tsx, src/tests/stores.test.ts</files>
  <read_first>
    - src/stores/ui.ts (current — append new exports; do not touch existing actions)
    - src/stores/runtime.ts (startRun signature for ALL_COMMANDS)
    - src/main.tsx (current — append registerCommands call BEFORE createRoot.render)
    - src/tests/stores.test.ts (extend with STORE-07 + $cmdFlat tests)
    - .planning/phases/03-navigation-shell/03-PATTERNS.md (commands.ts shape + registerCommands injection)
    - .planning/phases/03-navigation-shell/03-RESEARCH.md (Pitfall 1 circular ESM resolution)
  </read_first>
  <behavior>
    - Test 1: Importing commands.ts followed by importing ui.ts does NOT throw a circular ESM error.
    - Test 2: ALL_COMMANDS is a non-empty array; every nested item has string `label`, string `group`, and function `do`.
    - Test 3: ALL_COMMANDS contains items with labels at minimum: "Add files", "Optimize all", "Batch view", "Compare view", "Report view", "Light theme", "Dark theme", "Open command palette".
    - Test 4: After registerCommands(ALL_COMMANDS.flatMap(g => g.items)), $cmdFlat.get() returns the full flat list (length > 0).
    - Test 5: setCmdkQuery('opt') causes $cmdFlat.get() to return ONLY items whose label.toLowerCase() includes 'opt' (case-insensitive substring match).
    - Test 6: setCmdkQuery('') restores $cmdFlat.get() to the full flat list.
    - Test 7: Calling the "Optimize all" command's do() sets runtimeAtom.running to true.
    - Test 8: Calling the "Light theme" command's do() sets uiAtom.theme to 'light'.
  </behavior>
  <action>
    Modify `src/stores/ui.ts` (append at the end of the file, after setTheme):
    - Add import line at top alongside existing `import { map }`: `import { computed } from 'nanostores'`.
    - Add type-only import: `import type { CommandItem } from '@/lib/commands'`. CRITICAL: this is a TYPE-ONLY import (`import type`) so it does NOT create a runtime ESM cycle — only the type information is referenced. The Vite build erases type-only imports.
    - Add a module-scoped `let _allCommands: CommandItem[] = []`.
    - Export function `registerCommands(cmds: CommandItem[]): void` that assigns `_allCommands = cmds`.
    - Export `$cmdFlat = computed(uiAtom, (s) => s.cmdkQ ? _allCommands.filter(i => i.label.toLowerCase().includes(s.cmdkQ.toLowerCase())) : _allCommands)`.
    - Update the file header comment to also reference 03-03-PLAN.md for the $cmdFlat addition.

    Create `src/lib/commands.ts`:
    - File header: `// Phase 03 — STORE-07: ALL_COMMANDS registry (Actions / View / Palette groups). Source: 03-03-PLAN.md`.
    - Imports (action functions only — never atoms — to satisfy the circular ESM guard): `import { setView, setTheme, openCmdk } from '@/stores/ui'`; `import { startRun } from '@/stores/runtime'`.
    - Export interface `CommandItem { label: string; meta?: string; group: string; do: () => void }`.
    - Export interface `CommandGroup { group: string; items: CommandItem[] }`.
    - Export const `ALL_COMMANDS: CommandGroup[]` containing exactly three groups in this order:
      - Actions: items `{ label: 'Add files', meta: 'A', group: 'Actions', do: () => {} }`, `{ label: 'Optimize all', meta: 'O', group: 'Actions', do: startRun }`.
      - View: items `{ label: 'Batch view', group: 'View', do: () => setView('Batch') }`, `{ label: 'Compare view', group: 'View', do: () => setView('Compare') }`, `{ label: 'Report view', group: 'View', do: () => setView('Report') }`, `{ label: 'Light theme', group: 'View', do: () => setTheme('light') }`, `{ label: 'Dark theme', group: 'View', do: () => setTheme('dark') }`.
      - Palette: items `{ label: 'Open command palette', meta: '⌘K', group: 'Palette', do: openCmdk }`.

    Modify `src/main.tsx`:
    - Add imports BEFORE the createRoot block: `import { registerCommands } from '@/stores/ui'`; `import { ALL_COMMANDS } from '@/lib/commands'`.
    - Add the call `registerCommands(ALL_COMMANDS.flatMap(g => g.items))` BEFORE `createRoot(rootEl).render(...)`. This must run at module load so $cmdFlat returns a populated list on first render.
    - Preserve the existing crossOriginIsolated check and StrictMode wrapper verbatim.

    Extend `src/tests/stores.test.ts` (append, do not remove existing assertions):
    - Add a third assertion section "ALL_COMMANDS + $cmdFlat (STORE-07 + STORE-03 completion)" that:
      - Dynamically imports `../lib/commands.ts` and `../stores/ui.ts`.
      - Calls `ui.registerCommands(commands.ALL_COMMANDS.flatMap(g => g.items))`.
      - Asserts $cmdFlat.get().length > 0.
      - Asserts $cmdFlat returns the full list when cmdkQ === ''.
      - Calls `ui.setCmdkQuery('opt')` and asserts every returned item's label, lowercased, contains 'opt'.
      - Calls `ui.setCmdkQuery('')` and asserts the full list is restored.
      - Finds the "Optimize all" item, calls its do(), then asserts runtimeAtom.running === true.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node --experimental-strip-types src/tests/stores.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import type { CommandItem }" src/stores/ui.ts` returns 1 (TYPE-ONLY import to avoid runtime cycle).
    - `grep -c "export function registerCommands" src/stores/ui.ts` returns 1.
    - `grep -c "export const \\$cmdFlat" src/stores/ui.ts` returns 1.
    - `grep -c "export const ALL_COMMANDS" src/lib/commands.ts` returns 1.
    - `grep -c "Optimize all" src/lib/commands.ts` returns 1.
    - `grep -c "Open command palette" src/lib/commands.ts` returns 1.
    - `grep -c "Light theme" src/lib/commands.ts` returns 1.
    - `grep -c "Dark theme" src/lib/commands.ts` returns 1.
    - `grep -c "registerCommands(ALL_COMMANDS.flatMap" src/main.tsx` returns 1.
    - `node --experimental-strip-types src/tests/stores.test.ts` exits 0 with ALL three sections passing (ui actions, runtime, commands+cmdFlat).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>$cmdFlat + registerCommands live in ui.ts via type-only import (no circular runtime ESM); ALL_COMMANDS exported from commands.ts with the three required groups; main.tsx injects at boot; stores.test.ts now asserts STORE-07 + $cmdFlat filter behavior end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommandPalette modal (NAV-04) + AppShell theme effect, keydown listener, palette mount (SHELL-03 + NAV-04 wire)</name>
  <files>src/components/shell/CommandPalette/CommandPalette.tsx, src/components/shell/AppShell/AppShell.tsx, src/tests/navigation.spec.ts</files>
  <read_first>
    - src/components/shell/AppShell/AppShell.tsx (current Plans 01+02 layout — TitleBar/Toolbar/Panels/StatusBar already mounted; still has hardcoded className="dark")
    - src/components/ui/dialog.tsx (Dialog / DialogContent API — open + onOpenChange + showCloseButton + className)
    - src/components/ui/kbd.tsx (Kbd component for footer hints)
    - src/stores/ui.ts (uiAtom + $cmdFlat + openCmdk + closeCmdk + setCmdkQuery + setCmdkSel + setOpen + setTheme)
    - src/index.css (theme selectors — `.dark` class drives variables; `data-theme` attribute is the SHELL-03 contract)
    - src/tests/foundation.spec.ts (the existing test that asserts `[role="application"]` has class "dark" by default — must continue passing)
    - .planning/phases/03-navigation-shell/03-UI-SPEC.md (CommandPalette dimensions, colors, ARIA)
    - .planning/phases/03-navigation-shell/03-PATTERNS.md (CommandPalette component patterns; AppShell theme effect + keydown listener with mandatory cleanup)
    - .planning/phases/03-navigation-shell/03-RESEARCH.md (Pitfall 3 listener cleanup; Pattern 3 data-theme)
  </read_first>
  <behavior>
    - Test 1: At app load, document.documentElement has dataset.theme equal to 'dark' (matches uiAtom.theme default).
    - Test 2: Calling setTheme('light') updates document.documentElement.dataset.theme to 'light' AND removes the `.dark` class from the application element; setTheme('dark') restores both.
    - Test 3: Pressing ⌘K (Mac) or Ctrl+K (Linux/Win) anywhere on the page opens the CommandPalette modal (data-testid="command-palette" becomes visible).
    - Test 4: Pressing Escape closes the palette.
    - Test 5: Typing "opt" in the palette search input filters the listbox to items whose label includes "opt" (case-insensitive).
    - Test 6: Pressing ArrowDown moves the aria-selected option from index 0 to index 1; ArrowUp moves it back.
    - Test 7: Pressing Enter while "Optimize all" is selected closes the palette AND sets runtimeAtom.running to true (visible via StatusBar pip aria-label changing to "Worker status: Running").
    - Test 8: The keydown listener is removed on AppShell unmount (no leaked listener; cleanup function returned from useEffect).
    - Test 9: foundation.spec.ts existing assertion "dark background applied" still passes (the application element still has class "dark" in dark theme).
  </behavior>
  <action>
    Create `src/components/shell/CommandPalette/CommandPalette.tsx`:
    - File header: `// Phase 03 — NAV-04: CommandPalette modal (Dialog + manual keyboard nav). Source: 03-03-PLAN.md`.
    - Imports: `useStore` from '@nanostores/react'; `MagnifyingGlass` from '@phosphor-icons/react'; `uiAtom, $cmdFlat, closeCmdk, setCmdkQuery, setCmdkSel` from '@/stores/ui'; `Dialog, DialogContent` from '@/components/ui/dialog'; `Kbd` from '@/components/ui/kbd'; `cn` from '@/lib/utils'.
    - Export `CommandPalette()`. Read `const { cmdkOpen, cmdkQ, cmdkSel } = useStore(uiAtom)`; `const cmdFlat = useStore($cmdFlat)`.
    - Render `<Dialog open={cmdkOpen} onOpenChange={(o) => !o && closeCmdk()}>` with `<DialogContent showCloseButton={false} data-testid="command-palette" aria-label="Command palette" className="w-[560px] max-w-[90vw] max-h-[400px] p-0 bg-[var(--color-bg-1)] border-[var(--color-line)] rounded-[8px] overflow-hidden">`.
    - Inside DialogContent, render three regions stacked vertically:
      1. Search row: a flex container with `<MagnifyingGlass size={16} className="text-[var(--color-fg-2)] ml-4 shrink-0" />` and an `<input>` with `autoFocus`, `aria-label="Search commands"`, `placeholder="Search commands…"`, `value={cmdkQ}`, classes `flex-1 h-12 px-4 text-sm text-[var(--color-fg-0)] bg-transparent outline-none placeholder:text-[var(--color-fg-2)]`, the outer flex row has `border-b border-[var(--color-line)]`. The input's `onChange={e => setCmdkQuery(e.target.value)}`. The input's `onKeyDown` MUST: on `ArrowDown` call `setCmdkSel(Math.min(cmdFlat.length - 1, cmdkSel + 1))` and `e.preventDefault()`; on `ArrowUp` call `setCmdkSel(Math.max(0, cmdkSel - 1))` and `e.preventDefault()`; on `Enter` call `cmdFlat[cmdkSel]?.do?.()` then `closeCmdk()`; on `Escape` call `closeCmdk()`.
      2. Listbox: `<ul role="listbox" aria-label="Commands" aria-activedescendant={cmdFlat[cmdkSel] ? "cmd-item-" + cmdkSel : undefined} className="max-h-[320px] overflow-y-auto">`. Render an empty-state `<li className="px-4 py-3 text-[13px] text-[var(--color-fg-2)]">No commands match</li>` when cmdFlat is empty. Otherwise map cmdFlat to `<li id={"cmd-item-"+i} role="option" aria-selected={i === cmdkSel} className={cn('px-4 py-2 text-[13px] text-[var(--color-fg-0)] cursor-default flex justify-between items-center hover:bg-[var(--color-bg-2)]', i === cmdkSel && 'bg-[var(--color-accent-dim)] border-l-2 border-[var(--color-accent)]')} onClick={() => { item.do?.(); closeCmdk() }}><span>{item.label}</span>{item.meta && <span className="font-mono text-[11px] font-semibold text-[var(--color-fg-2)]">{item.meta}</span>}</li>`.
      3. Footer: `<div className="h-8 px-4 flex items-center gap-2 border-t border-[var(--color-line)] bg-[var(--color-bg-0)] text-[11px] text-[var(--color-fg-3)]"><Kbd>↑↓</Kbd>navigate<Kbd>Enter</Kbd>select<Kbd>Esc</Kbd>close</div>`.

    Modify `src/components/shell/AppShell/AppShell.tsx`:
    - Add imports: `useEffect` from 'react'; `useStore` from '@nanostores/react'; `uiAtom, openCmdk, closeCmdk, setOpen` from '@/stores/ui'; `CommandPalette` from '@/components/shell/CommandPalette/CommandPalette'; `cn` from '@/lib/utils'.
    - Inside the component before the return, read `const { theme } = useStore(uiAtom)`.
    - Add data-theme effect: `useEffect(() => { document.documentElement.dataset.theme = theme }, [theme])` — this satisfies SHELL-03.
    - Add global keydown effect with mandatory cleanup (per Pitfall 3): `useEffect(() => { function onKey(e: KeyboardEvent) { if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') { e.preventDefault(); openCmdk(); return } if (e.key === 'Escape') { closeCmdk(); setOpen(null) } } window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey) }, [])`.
    - Update the outer div's className: replace the literal `"dark h-screen w-screen flex flex-col overflow-hidden bg-[var(--color-bg-0)] text-[var(--color-fg-0)]"` with `cn(theme === 'dark' && 'dark', 'h-screen w-screen flex flex-col overflow-hidden bg-[var(--color-bg-0)] text-[var(--color-fg-0)]')`. This keeps the `.dark` class active in dark theme (so legacy `.dark { ... }` CSS still applies and foundation.spec.ts still passes) AND drops it in light theme.
    - Mount `<CommandPalette />` as the LAST child inside the outer div (after `<StatusBar />`).
    - Do NOT remove the existing TitleBar/Toolbar/Panel/StatusBar mounts from Plans 01/02.

    Extend `src/tests/navigation.spec.ts` (append, do not remove prior tests):
    - Test "html data-theme attribute set (SHELL-03)" — page.goto, evaluate `document.documentElement.dataset.theme`, expect it to equal "dark".
    - Test "Toolbar theme toggle swaps html data-theme (SHELL-03)" — page.goto, click the button with aria-label "Toggle theme", evaluate dataset.theme, expect "light"; click again, expect "dark".
    - Test "Meta+K opens CommandPalette (NAV-04)" — press Meta+k, expect getByTestId('command-palette') visible.
    - Test "Escape closes CommandPalette (NAV-04)" — open palette, press Escape, expect getByTestId('command-palette') hidden.
    - Test "Typing filters list (NAV-04)" — open palette, fill search "opt", expect at least one option visible whose text includes "Optimize" (case-insensitive) and expect "Batch view" option NOT visible.
    - Test "Arrow keys move selection (NAV-04)" — open palette, locate the option with aria-selected="true" (index 0), press ArrowDown, assert the next option now has aria-selected="true".
    - Test "Enter on Optimize all sets running (NAV-04 + STORE-04)" — open palette, type "Optimize", press Enter, assert the worker pip aria-label is "Worker status: Running" and the palette is hidden.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -10 && npx playwright test src/tests/navigation.spec.ts src/tests/foundation.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="command-palette"' src/components/shell/CommandPalette/CommandPalette.tsx` returns 1.
    - `grep -c "role=\"listbox\"" src/components/shell/CommandPalette/CommandPalette.tsx` returns 1.
    - `grep -c "aria-selected" src/components/shell/CommandPalette/CommandPalette.tsx` returns at least 1.
    - `grep -c "setCmdkSel" src/components/shell/CommandPalette/CommandPalette.tsx` returns at least 2.
    - `grep -c "closeCmdk" src/components/shell/CommandPalette/CommandPalette.tsx` returns at least 2.
    - `grep -c "Search commands…" src/components/shell/CommandPalette/CommandPalette.tsx` returns 1.
    - `grep -c "useState" src/components/shell/CommandPalette/CommandPalette.tsx` returns 0.
    - `grep -c "documentElement.dataset.theme" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c "window.addEventListener('keydown'" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c "window.removeEventListener('keydown'" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c "<CommandPalette" src/components/shell/AppShell/AppShell.tsx` returns 1.
    - `grep -c "theme === 'dark' && 'dark'" src/components/shell/AppShell/AppShell.tsx` returns 1 (dynamic dark class).
    - `grep -c '"dark h-screen' src/components/shell/AppShell/AppShell.tsx` returns 0 (hardcoded dark class removed).
    - `npm run build` exits 0.
    - `npx playwright test src/tests/navigation.spec.ts src/tests/foundation.spec.ts --project=chromium` passes ALL tests including:
      - foundation.spec.ts "dark background applied" (regression guard)
      - All Plan 01 + Plan 02 + Plan 03 navigation tests
  </acceptance_criteria>
  <done>CommandPalette modal opens on ⌘K and obeys all keyboard contracts; html data-theme reflects uiAtom.theme; AppShell .dark class is dynamic so legacy CSS keeps working AND foundation.spec.ts keeps passing; full Playwright suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 3 success criteria</name>
  <files>(none — manual verification)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 Success Criteria — 5 items)
    - .planning/phases/03-navigation-shell/03-UI-SPEC.md (visual contract)
  </read_first>
  <what-built>
    Plans 01, 02, and 03 of Phase 3 together deliver: filled uiAtom actions + runtimeAtom (STORE-03/04); ALL_COMMANDS registry + $cmdFlat (STORE-07 + STORE-03 completion); html data-theme + dynamic .dark class (SHELL-03); TitleBar with brand/menus/pills/⌘K (NAV-01); full Toolbar with split buttons, segmented control, filter, theme toggle, settings (NAV-02); StatusBar with worker pip + versions + WASM + file count + size totals (NAV-03); CommandPalette modal with keyboard nav (NAV-04).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173.
    2. **SC-1 (NAV-01):** TitleBar shows brand mark "OIMG · image optimizer" + Codec/View/Help menu triggers + "100% local" + "Offline-ready" pills + ⌘K button. Click each of Codec / View / Help — each opens its popover; opening a second one closes the first.
    3. **SC-2 (NAV-02):** Toolbar shows Add files split + Optimize all (accent green) + Export split + Batch/Compare/Report segmented control + Auto split + filter input + Sun/Moon theme button + Settings. Click "Optimize all" — open React DevTools (or watch the StatusBar pip) and confirm runtimeAtom.running flips to true (pip turns blue with pulse, label switches to "Running").
    4. **SC-3 (NAV-03):** StatusBar shows worker pip (green→blue), "SVGO 4.0.1", "@squoosh-kit/core 0.6.0", "WASM ready · 312 KB", live "{n} files" matching stub data count (12 initially), and live "{orig} → {opt}" bytes matching $totals.
    5. **SC-4 (NAV-04):** Press ⌘K (or Ctrl+K on Linux/Win) — CommandPalette modal opens centered with search input focused. Type "opt" — list filters to items including "Optimize all". Press ArrowDown a few times — selection highlight (accent-dim left border) moves down. Press Enter on "Optimize all" — palette closes AND StatusBar pip flips to Running. Reopen palette and press Escape — palette closes.
    6. **SC-5 (SHELL-03):** Open browser devtools and inspect `<html>` — `data-theme="dark"` attribute is present. Toggle the Sun/Moon button in Toolbar (or click "Light theme" in TitleBar View menu) — `<html data-theme>` switches to "light" and the chrome repaints to light tokens. Toggle back to dark and confirm no unstyled flash.
    7. STORE-08 check: open DevTools Components panel, inspect TitleBar/Toolbar/StatusBar/CommandPalette — no local useState hooks visible (only useStore subscriptions).
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass, otherwise describe the failing criterion (cite SC-N).</resume-signal>
  <acceptance_criteria>
    - Developer reports SC-1 through SC-5 all green (typed "approved").
    - No console errors during dev session.
    - foundation.spec.ts continues to pass via `npx playwright test src/tests/foundation.spec.ts --project=chromium`.
  </acceptance_criteria>
  <done>All 5 Phase 3 success criteria visually + functionally confirmed by the developer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → global window keydown listener → uiAtom actions | Single listener attached at AppShell mount with mandatory cleanup; only fires on Meta/Ctrl+K or Escape; no payload from external source. |
| user typing → CommandPalette input → setCmdkQuery → $cmdFlat filter | Substring match on static ALL_COMMANDS labels; no eval, no dynamic command construction. |
| user clicking option → ALL_COMMANDS item.do() → store actions | All do() callbacks are pre-bound functions defined at module load; no dynamic dispatch from user input. |
| theme toggle → document.documentElement.dataset.theme | Single dataset write; value constrained to 'dark' | 'light' by the uiAtom type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering / XSS | CommandPalette listbox rendering | mitigate | Labels and meta strings rendered via JSX text nodes only. ALL_COMMANDS is a static module-level constant — no path from user input to label content in v1. Acceptance guard: `grep -rc "dangerouslySet" src/components/shell/` returns 0. |
| T-03-10 | Tampering | CommandPalette search input → store filter | mitigate | setCmdkQuery writes raw string to uiAtom.cmdkQ; $cmdFlat performs `.toLowerCase().includes()` substring match — no regex compilation from user input, no eval. |
| T-03-11 | Denial of Service | Global keydown listener | mitigate | Single listener attached in useEffect with mandatory removeEventListener cleanup return (per RESEARCH.md Pitfall 3). Acceptance guard: addEventListener AND removeEventListener both present in AppShell.tsx. |
| T-03-12 | Tampering | document.documentElement.dataset.theme write | mitigate | Theme value constrained by TypeScript union type `'dark' | 'light'` on uiAtom.theme; setTheme is the only writer; called only from in-app buttons. |
| T-03-13 | Information Disclosure | Theme switching repaint timing | accept | No flash-of-unstyled-content risk because data-theme is set in useEffect after mount, but `.dark` class is also rendered in JSX on the same tick. Risk is cosmetic only. |
| T-03-14 | Spoofing | registerCommands global injection | accept | registerCommands is exported globally but only called once at boot from main.tsx. UI-only milestone; no plugin extensibility scope. Re-evaluate when v2 adds dynamic commands. |

</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run build` exits 0.
- `node --experimental-strip-types src/tests/stores.test.ts` exits 0 with all three sections passing.
- `npx playwright test src/tests/navigation.spec.ts src/tests/foundation.spec.ts --project=chromium` passes entire suite (Plan 01 + Plan 02 + Plan 03 tests + foundation regression).
- STORE-08 audit: `grep -rc "useState" src/components/shell/` returns 0.
- Circular ESM check: `node -e "import('./src/lib/commands.ts').then(() => import('./src/stores/ui.ts')).then(() => console.log('ok'))"` (run only as a sanity check; the build will catch real cycles).
- Human verification checkpoint passes all 5 Phase 3 ROADMAP success criteria.
</verification>

<success_criteria>
- All five Phase 3 ROADMAP success criteria green (verified by Task 3 checkpoint).
- $cmdFlat computed atom returns filtered list driven by cmdkQ; registerCommands injected at app boot via main.tsx.
- CommandPalette modal mounted as last child of AppShell; opens on ⌘K, closes on Escape, supports arrow + Enter.
- html data-theme attribute live; theme toggle in TitleBar or Toolbar repaints chrome.
- Zero useState in any shell component (STORE-08 holds).
- foundation.spec.ts regression-safe: `.dark` class on application element remains in dark theme.
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-shell/03-03-SUMMARY.md` recording: $cmdFlat + registerCommands added with type-only CommandItem import (no circular ESM); ALL_COMMANDS shipped (8 items across 3 groups); CommandPalette modal with full keyboard nav; AppShell theme effect + keydown listener; dynamic dark class swap preserves foundation.spec.ts; human checkpoint approved.
</output>
